# Bitácora · 2026-09-19 — La guarda de Recepción entra como 0132 · sin release nuevo (`v0.80.0` = producción)

> **Dos documentos en `main` reservaban el mismo número, y los dos lo decían en serio.** El handoff del 17 le
> daba la `0132` a la guarda de Recepción; el plan de Reposición parte 2, mergeado un día después, se la daba
> a su migración aditiva. Ninguna de las dos estaba en `supabase/migrations/`, así que `check-migraciones` no
> tenía nada que ver. Apareció buscando el número en `main` antes de pushear.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** el release `v0.80.0` (mañana del 18) y la guarda de Recepción, de rama local a producción (19)
**Ramas:** [#231](https://github.com/spiraclinicapp/Spira-App/pull/231) (`docs/release-v0.80.0`) y [#234](https://github.com/spiraclinicapp/Spira-App/pull/234) (`fix/recepcion-guarda-borrado-y-renglones`), mergeadas y borradas. Migración **`0132` aplicada en prod** (su marca, en la [#235](https://github.com/spiraclinicapp/Spira-App/pull/235), de otra sesión). **Sin release nuevo**: desde `v0.80.0` no cambió nada que llegue al bundle. **1263 tests**.

Es la sesión de las desviaciones de protocolo (bitácora del 17, anexo), que siguió después de su cierre.

## 1. Resumen de la jornada

| PR | Qué | Cómo arrancó |
|---|---|---|
| [#231](https://github.com/spiraclinicapp/Spira-App/pull/231) | Release `v0.80.0`: versiona la #220, la #222, la #225 y la #226, que estaban en prod encima de `v0.79.0` | «sacá la versión» |
| [#234](https://github.com/spiraclinicapp/Spira-App/pull/234) | La guarda de Recepción, renumerada de `0129` a `0132` y rebaseada sobre `main` | «renumerá la guarda de Recepción a la 0132» |

De otras sesiones, en los mismos dos días: [#232](https://github.com/spiraclinicapp/Spira-App/pull/232) (el cierre del 17 de la sesión de feedback), [#233](https://github.com/spiraclinicapp/Spira-App/pull/233) (el
plan de Reposición parte 2) y [#235](https://github.com/spiraclinicapp/Spira-App/pull/235) (la marca de la `0132` en el índice y en `CLAUDE.md`). Su narrativa les
corresponde a ellas.

## 2. El release `v0.80.0` ([#231](https://github.com/spiraclinicapp/Spira-App/pull/231))

Salió el 18 a la mañana, sobre cuatro features que ya corrían en prod sin versión. Tres cosas del camino:

- **La novedad la frenó un test.** La primera versión del texto tenía 106 caracteres y `version.test.ts` exige
  que cada novedad entre en una línea (tope 100; la más larga que había tenía 92). Quedó en 93: «Una ventana
  vencida se documenta como desviación, y el feedback llega a Ajustes con su lugar.» La bandera de estado no
  entró en el texto.
- **Un commit quedó colgado de una PR ya cerrada.** La nota «salió el release» iba en la #228, el cierre del
  17. El Director la mergeó mientras el commit se pusheaba, y el diff contra `main` lo delató: nueve renglones
  donde la PR tenía casi doscientos. Se movió a la #231 con cherry-pick; antes de borrar la rama vieja se
  comparó el contenido (diferencia cero).
- **La primera sonda contra el bundle llegó cinco segundos antes que el deploy.** Seguía diciendo `0.79.0`. El
  estado del commit en GitHub distinguió «todavía construyendo» de «falló»: Vercel reportó la producción lista
  a las 07:26:38, y a las 07:27 el bundle ya decía `0.80.0`.

## 3. La guarda de Recepción: de `0129` a `0132` ([#234](https://github.com/spiraclinicapp/Spira-App/pull/234))

Estaba escrita y probada desde el 17 en una rama local, esperando a que se aplicara la `0128`. Mientras
esperaba, otras migraciones se llevaron los tres números que tenía adelante: la `0129` (`feedback_lugar`),
la `0130` (desviaciones) y la `0131` (`feedback_visto`).

- **El rebase dejó afuera la mitad del commit original.** Además de la guarda, el commit cambiaba el spec y el
  plan de Reposición para decir que su limpieza era la `0130`. Eso ya era falso, y la sesión de Reposición
  había corregido su spec en `main` («el siguiente número libre»). El spec chocó; **el plan se aplicó sin
  conflicto y estaba igual de mal**. Se revirtieron los dos a `main`: la PR quedó con tres archivos.
- **El número chocó con un plan.** Antes de pushear, un `git grep` de `0132` en `main` encontró que el plan de
  Reposición parte 2 (#233) lo reservaba para su migración aditiva, y la `0133` para la limpieza. **Decisión
  del Director: la `0132` es de la guarda**, que estaba lista, y Reposición corre a `0133` y `0134`. Es el mismo
  criterio que con la `0130`: primero lo que está listo, después lo que depende de un deploy. El plan de
  Reposición no se tocó —es de otra sesión y está en uso—; el cambio quedó en su memoria y en la PR.
- **El orden de despliegue se volvió a probar**, no se copió: el front sigue sólo leyendo `medication_receptions`
  y `reception_items` (dos `select` y un embed).
- **PGlite, 9/9**: aplica entera y es idempotente; con el rol de la app las cuatro escrituras rebotan con el
  mensaje del dominio; como `postgres`, el borrado pasa y la cascada se lleva los renglones.

El Director la aplicó el 19. La marca en el índice, `CLAUDE.md` en `0132` y el borrado de su entrada de
`TODOS.md` los hizo otra sesión en la #235, que dejó escrito que se apoyaba en la confirmación del Director:
un trigger no se puede sondear sin sesión.

## 4. Lo no-obvio

- **`check-migraciones` vigila archivos, no reservas.** Dos documentos en `main` afirmaban la `0132` y el CI no
  podía verlo, porque ninguno de los dos era un `.sql`. Antes de pushear una migración, `git grep` de su número
  en `main` —docs incluidos—, no sólo mirar `supabase/migrations/`.
- **Un rebase sin conflictos no es un rebase correcto.** El plan de Reposición entró limpio y volvía a
  escribir un número que su dueña ya había corregido. Lo que hay que revisar son los hunks, no los conflictos.
- **Una guarda-trigger no se verifica sin sesión, y esta sesión lo había dejado escrito al revés.** La memoria
  decía «un DELETE con el anon key tiene que rebotar». No: sin sesión se corta por privilegio o la RLS deja
  cero filas, y el trigger ni corre. Lo que sí prueba, sin escribir nada: con sesión, un INSERT en
  `reception_items` con un `reception_id` que no existe (el BEFORE ROW corre antes que la FK). O, en el editor,
  `pg_trigger`. La memoria está corregida.
- **El worktree de la sesión se vació DOS veces en el mismo cierre.** La primera vez lo dieron de baja mientras
  la sesión lo tenía abierto: Windows no dejó borrar la carpeta y git empezó a caer en la copia compartida del
  Director. Lo delató un `ENOENT` sobre `.git` en un comando de lectura, antes de cualquier escritura, y se
  volvió a registrar en el mismo directorio con `git worktree add`. La segunda vez, ya registrado, algo borró
  sus 1010 archivos y después los repuso: volvió lo que está en git y **se perdió la bitácora recién escrita**,
  que todavía no lo estaba. **Lo que evitó el daño fue stagear por ruta**: el `git status` de ese momento
  listaba 1010 borrados, y un `git add -A` los habría commiteado. Esta bitácora es la segunda escritura.
- **Un tope en un test documenta una deriva, no un gusto.** Los 100 caracteres de `version.test.ts` existen
  porque alguna vez se escribió una novedad de 601.

## 5. Git — estado de las ramas

`main` en `87227b3` (la #235 encima de la #234). `v0.80.0` = producción, verificado contra el bundle el 18;
desde entonces no cambió nada del front. Las ramas de esta sesión (`docs/release-v0.80.0`,
`fix/recepcion-guarda-borrado-y-renglones` y su respaldo local) se borraron después de confirmar cero
commits fuera de `main`. Queda viva sólo la de este cierre.

Quedan, de otras sesiones y sin tocar:

- `wt-mock-reposicion`, donde una sesión de Reposición está haciendo la revisión de ingeniería del plan de la
  parte 2 (`docs/plan-reposicion-parte-2-eng-review`).
- `docs/cierre-2026-09-17`, local y en `origin`, mergeada (#219).

El worktree `unruffled-dubinsky-186253` queda con `docs/cierre-2026-09-19`. **Se borra desde afuera y en el
acto, directorio incluido**, cuando termine la sesión: dejarlo vacío es lo que provocó el punto 4.

## 6. Pendientes

1. **Reposición parte 2** con `0133` y `0134`: su plan (#233) todavía dice `0132`/`0133`; lo corrige la Task 5.
2. **Los tres QA logueados del feedback** (handoff del 17, anexo 2).
3. **La prueba de punta a punta de la guarda**, opcional: con sesión, el INSERT con un `reception_id` inexistente.
4. **La visita hecha fuera de ventana** (desviaciones, caso 2): `TODOS.md`, P2.
