# Handoff · 2026-08-04 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-08-03.md`](./handoff-2026-08-03.md). Narrativa del día: [`2026-08-04.md`](./2026-08-04.md).

> **Rediseño de "Visitas del día" (Track)** del handoff `handoff_visitas_dia/`, rebindeado al schema real. **Fase 1A** (coordinador por visita, migración **0065**) **MERGEADA** (PR #25) + **aplicada en prod**. **Fase 1B + Fase 2 + pulido** (fila + modal + filtros/agrupar/buscador) en **PR #26 abierta** (`feat/visitas-dia-v2`, HEAD `76787a0`), **pendiente QA logueado + merge**. **Sin release** todavía (la UI está en la #26). Última migración = **0065**.

## Dónde estás

- **`main`** tiene la Fase 1A: migración **0065** aplicada en prod (coordinador por visita — `patient_visits.coordinator_id`/`coordinator_name`, RPC `set_visit_coordinator`) + su capa de datos. **No hay UI nueva en `main` todavía.**
- **La UI del rediseño vive en `feat/visitas-dia-v2` (PR #26), sin mergear:** fila v2 (`DayVisitRowItem`), modal v2 (`VisitDetail`), infra de lista (`MultiFilterMenu` + rewrite de `DayVisitsView`), y el pulido de la nav ↑↓.
- **Working copy:** al cerrar, la rama activa quedó en **`feat/visitas-dia-v2`** (para que el dev server del Director siga mostrando el rediseño para su QA). El commit de esta bitácora/handoff fue a **`main`** y vuelta. Árbol limpio salvo dos carpetas untracked ajenas (`handoff_visitas_dia/`, `Spira — Identidad Visual (6)/`).
- **Migración:** última aplicada = **0065**.

## Lo hecho esta jornada

- **`/plan-eng-review` del handoff** → Step 0 destapó los landmines de datos → 4 decisiones del Director. Plan escrito en `handoff_visitas_dia/PLAN - Visitas dia (Fase 1).md`.
- **Fase 1A** (migración 0065 + datos) → PR #25 mergeada + aplicada en prod.
- **Fase 1B** (fila + modal v2, context-aware; fix del ⋯ clipado) → PR #26.
- **Fase 2** (filtros multi Estado/Protocolo/Médico/Coordinador + agrupar + buscador + "Para ver médico") → PR #26.
- **Pulido** de la nav ↑↓ (guard `defaultPrevented` + `seed` para nav instantánea) → PR #26.

## Decisiones tomadas (no re-discutir)

- **Sin hora en v1.** No existe hora de cita en el schema → la columna izquierda de la fila es el **chip de estado**, y el default de agrupación es **"En el centro / Resto"** (no "hora", que no se puede).
- **Coordinador POR VISITA** (no por protocolo): columna `coordinator_id` nullable elegible entre los coordinadores del protocolo, asignable desde el modal. `coordinator_name` **desnormalizado** (la RLS de `users` + `security_invoker` no dejan joinear; mismo caso que `author_name` en 0048).
- **Nombre del paciente visible SOLO en Visitas del día** (fila + modal). El resto de la app sigue con IVRS + `PrivacyAvatar`. En `VisitDetail`, `showName = context === 'day'`.
- **`VisitDetail` es compartido** (día + ficha + cola médico) → `patient` queda solo-lectura y sin nombre. No romper eso.
- **Procedimientos monocromos** (el catálogo real es texto libre; no se inventan los 7 tonos/letras de la demo).
- **Sin release** hasta que se mergee la #26 (la #25 sola es base de datos sin UI).

## PRÓXIMO PASO (próxima sesión)

1. **QA logueado de la PR #26** (el Director) + **merge**. Checklist en el cuerpo de la #26 y en `handoff_visitas_dia/HANDOFF - Visitas dia.md` §11: alineación de la columna derecha en los 5 estados, el ⋯ abre completo, ↑↓ recorre la lista visible, la ficha/cola siguen sin nombre y read-only, modo oscuro.
2. **Cortar el release al mergear la #26** (`node scripts/release.mjs X.Y.Z "…"` + tag; bundle desde `v0.21.0`).
3. **Fase 2 del proyecto — panel de gerencia** (asignar roles por clicks); hoy por SQL.
4. **⚠️ QA logueada del popup "Atención médica" (PR #20 del 29/07)** — sigue sin confirmar.
5. Heredados: sembrar plantilla de checklist en prod, **Track ve "Medicamento" genérico** (Fase 3 medicación del paciente), tablero logueado como coordinadora, viewer read-only de procedimientos, **rotar la contraseña de QA**.

## Cómo verificar al retomar

`git fetch` · `git status` · `npm run typecheck` (verde) · `npm run build` (verde) · `node scripts/check-migraciones.mjs` (última = **0065**).

- **Estado de la PR:** `git log origin/main..origin/feat/visitas-dia-v2` para ver qué queda sin mergear; si el Director ya mergeó la #26, la rama estará contenida en `main`.
- **El rediseño (con la rama `feat/visitas-dia-v2` o ya mergeada):** Track → **Visitas** → la lista con la fila nueva (nombre de titular, chip de estado, procedimientos, coord+médico, botón médico + CTA + ⋯). Clic en una fila abre el **modal v2** (protocolo de título, ruta + a cargo + paciente | procedimientos + comentarios + dispensación; ↑↓ navega; Esc cierra). En **"A cargo"** se asigna el coordinador (necesita la 0065, ya aplicada). Arriba: filtros multi + "Agrupar por" + buscador. Verificá también que **la ficha del paciente** y **la cola del médico** abran el modal **sin nombre y en solo lectura**.
