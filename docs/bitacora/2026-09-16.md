# Bitácora · 2026-09-16 — El listado del sitio entra a producción, y la tarjeta de Dispensación deja de invitar · release `v0.76.0`

> **Cargar datos reales es lo que hace visibles las reglas que faltaban.** Las 70 entregas del listado
> no entraron por una puerta que existía: obligaron a decidir cómo se registra una entrega que ocurrió
> antes de que Farmacia llevara el circuito en Spira. Y apenas quedaron cargadas, el Director miró una
> visita cerrada y vio la tarjeta ofreciéndole dispensar sobre algo que ya había pasado.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Coordinación y Farmacia · el listado del sitio, la tarjeta de Dispensación y la lista del día
**Ramas:** tres PRs, todas mergeadas: [#195](https://github.com/spiraclinicapp/Spira-App/pull/195), [#204](https://github.com/spiraclinicapp/Spira-App/pull/204) y [#205](https://github.com/spiraclinicapp/Spira-App/pull/205). **Sin migraciones.** Release **`v0.76.0`**. **1147 tests** (1118 → 1134 → 1147).

La jornada arrancó la noche del 15 y siguió de largo; la bitácora del 15 ya tenía dos partes y dos
releases, de otros hilos.

## 1. Resumen de la jornada

| PR | Qué | Cómo arrancó |
|---|---|---|
| [#195](https://github.com/spiraclinicapp/Spira-App/pull/195) | Los scripts que cargaron el listado del sitio: renumeración de LTS17231, las fechas nuevas, **70 entregas** y el cierre de procedimientos de 321 visitas | «hacé un repaso contra lo que se encuentra en producción» |
| [#204](https://github.com/spiraclinicapp/Spira-App/pull/204) | La tarjeta de Dispensación de una visita terminada **muestra qué pasó** en vez de invitar a dispensar, y deja corregirlo | «en el caso de que la visita ya se encuentre como realizada…» |
| [#205](https://github.com/spiraclinicapp/Spira-App/pull/205) | Una visita atendida sale del día en que estaba **citada** | «que se vea en el día únicamente cuando la fecha realizada o programada coincide» |

## 2. El repaso del listado: lo que se pidió y lo que había

El pedido era impactar unas fechas ajustadas y una columna nueva. Comparando el Excel contra el
dataset que se aplicó el 15 —que vive adentro de `out/2-aplicar.sql`, así que el «antes» es exacto—
los cambios de fecha resultaron ser **cinco**. Lo que no estaba en el pedido:

- **LTS17231 quedó renumerado**: seis de ocho inscripciones cambian de IVRS (`…520005`→`…520003` y
  así). La numeración vieja repetía el hueco de ACT18301 (saltaba 003 y 004); la nueva es corrida.
  Aplicarlo de taquito **duplicaba seis pacientes**.
- **Una errata de tipeo**: la V1 de Aguero traía el IVRS de Muñoz Pampillón. Se ve en que esa misma
  fila dice apellido AGUERO y las 19 siguientes dicen `…520003`. Sin corregirla, el script se frena
  por visita repetida y Aguero pierde la única visita que tiene hecha.
- **27 visitas nuevas** de María Ester Guerra, que pasó de «(sin fechas cargadas)» a cronograma completo.

Diferenciar los tres casos exigió comparar **por persona** y no por IVRS: por IVRS, la renumeración
disfrazaba de «81 fechas estimadas distintas» algo que no había cambiado en absoluto.

## 3. Las 70 entregas, sin lotes

El listado trae, visita por visita, qué medicación se entregó. Registrarlas obligó a una decisión, y
conviene tenerla escrita porque **esquiva un candado del propio sistema**:

`apply_dispensation_stock` (0003) corta la entrega sin renglones — *«dejaría stock sin mover»*. Es un
trigger **`after update`**, así que un insert directo ya en `entregada` no lo despierta. El candado
protege el circuito **vivo**, donde hay stock que mover; acá no hay stock que mover porque la entrega
no salió de esta farmacia. Mismo criterio con el que la carga anterior apagó el sello de IP.

**Cada pedido lleva ese porqué escrito en `notes`**, y ese texto es además la marca de idempotencia.
Resultado en prod: **70 pedidos · 86 renglones · 70 entregas · 0 lotes · 0 movimientos de stock**.

El otro candado del mismo circuito sí corre en el insert de los renglones: `check_request_item_protocol`
(0050) exige el medicamento asignado al protocolo **y** habilitado y activo para el paciente. Tres
pacientes de ACT18301 terminaron el estudio y no tienen ninguna activa: se les da de alta y **se vuelve
a apagar** dentro del mismo bloque, que es exactamente lo que pasó (la tuvieron, ya no).

## 4. El cierre de procedimientos

Todas las visitas realizadas quedan con sus procedimientos tildados y sus reportes en `evolucionado`,
**menos las dos últimas de cada inscripción**, que el Director revisa a mano. Por inscripción y no por
persona: los ocho pacientes que están en dos estudios tienen dos recorridos con reportes propios, y
contar dos en total dejaría un estudio entero cerrado sin mirar.

En prod: **321 visitas pasaron a «completa»** y quedaron **129 abiertas**, que son exactamente las dos
últimas de cada inscripción. Se ve de un vistazo en el cronograma de cualquier paciente de Victorion:
V1 a V5 «Completa», V6 y V7 «Visita realizada».

## 5. La tarjeta de Dispensación en una visita cerrada ([#204](https://github.com/spiraclinicapp/Spira-App/pull/204))

La tarjeta distinguía sólo **quién mira** (`readOnly` = permisos) y no si la visita **terminó**. Sobre
una visita cerrada hace un mes seguía ofreciendo «Elegir medicación», y lo entregado quedaba plegado al
pie bajo un rótulo —«Ver historial»— que suena a archivo viejo cuando el pedido puede ser de hoy.

Ahora, con **fin de atención** y sin pedidos abiertos: «Dispensada el DD/MM» con lo entregado, o «En
esta visita no se entregó medicación», y debajo la salida explícita —**«Corregir entrega»** o
**«Registrar entrega»**— con la línea *«La entrega anterior queda registrada. Lo que cargues acá la
corrige.»*. En positivo y no como advertencia: `guard_dispensation_immutable` prohíbe revertir una
entrega, así que corregir sólo puede sumar, pero el encuadre del Director describe el trabajo en vez
del candado.

**El corte es `ready_at` y no `real_date`**: la fecha real se pone al EMPEZAR a atender, y ahí todavía
falta dispensar. Y no alcanza con `ready_at`: **si queda un pedido abierto, la tarjeta sigue operable**,
porque un pedido `lista` es un paquete preparado esperando que lo retiren.

Se ejecutó con subagentes, cuatro tareas con revisión entre cada una y revisión final de rama. **Las
revisiones encontraron cuatro cosas que el plan no tenía**, y las cuatro están en §8.

## 6. Una visita atendida sale del día en que estaba citada ([#205](https://github.com/spiraclinicapp/Spira-App/pull/205))

Una visita citada para el 17 y atendida el 15 aparecía en **los dos días**.

El pedido decía «que no se vea por la fecha estimada», pero **la fecha estimada nunca estuvo en la
consulta del día**: la que se ve en el encabezado es calculada (aleatorización + offset) y no existe
como columna. Quien la metía en el 17 era `estimated_date`, que en pantalla se llama **«fecha
programada»**. Los nombres de la base y los de la pantalla no coinciden en este dominio.

La regla nueva: la citación mete la visita en su día **sólo mientras no se atendió**. Es la misma que
ya gobierna el encabezado —«atendida, la citación deja de estar en pantalla»— aplicada a la lista.

De paso, la cola «Para ver médico» tenía el filtro **copiado literal**: dos definiciones del mismo día,
de las que divergen el día que alguien arregla una sola. Quedó unificada.

## 7. El hook que miraba la rama equivocada

`branch-guard.mjs` preguntaba por la rama de la carpeta del proyecto aunque el comando fuera a
commitear en un **worktree**. Se equivocaba en las dos direcciones: falso positivo (frenó la primera
tarea de la tanda) y falso **negativo**, que es el peor — commitear en `main` desde un worktree pasaba
sin que el hook lo viera. Ahora lee el último `cd` del comando. Probado con siete casos.

## 8. Lo no-obvio

- **Un invariante del servidor no dice nada del estado que vive en el componente.** Se venía razonando
  que «cualquier pedido abierto fuerza `cerrada = false`, luego no queda nada accionable». Cierto para
  los pedidos, **falso para el estado local sin enviar**: elegir medicación, marcar fin de atención sin
  enviar, y el pie «Solicitar dispensación» seguía vivo disparando un RPC real sobre una visita que la
  misma tarjeta describía como cerrada. Tres reportes seguidos lo dieron por bueno.
- **Una tarjeta que todavía no leyó no puede afirmar nada.** `requests` es `reqQ.data ?? []`, así que
  con la visita cerrada la tarjeta decía «En esta visita no se entregó medicación» **mientras cargaba**
  — y como el hook anula `data` ante error, una consulta fallida dejaba esa frase **fija**. Dato
  inventado presentado como real. El repo ya lo resolvía en tres lugares; las dos frases nuevas eran
  las únicas que no seguían la convención.
- **Un modo que se enciende y no se apaga.** `setCorrigiendo` sólo se llamaba a `true`. Venía calcado
  del plan, y el fixer encontró **un segundo camino** que la revisión no había pedido.
- **El estado se arrastraba entre pacientes**: el panel no se remontaba al pasar con ↑↓, así que el modo
  corrección viajaba al siguiente y los pedidos de la visita anterior se pintaban como de ésta. Un
  `key={visit.id}` cerró eso y una fuga preexistente (los renglones sin enviar cruzaban de paciente).
- **Derivar una lista de un subconjunto y usarla como alcance.** La lista de protocolos salía de «los
  que tienen entregas», y Victorion no entrega: quedaba **fuera entero** del cierre de procedimientos,
  justo el que más visitas viejas tenía. Lo encontró el banco de pruebas con PGlite, no la lectura.
- **Probar el SQL generado contra Postgres de verdad paga.** Esquema de juguete con los candados reales
  de la 0003 y la 0050, poblado desde el mismo Excel: encontró el bug de arriba, probó la idempotencia y
  confirmó que el `do` es atómico (un error en el medio no deja nada a medias).
- **El hook matchea el TEXTO del comando, no sólo comandos.** Una nota que mencione `git` y la palabra
  «commit» en la misma línea lo dispara. Para editar archivos con esa prosa, usar la herramienta de
  edición en vez de un heredoc por bash.
- **El `add` de un subagente deja archivos stageados**: el commit del hook se llevó puestos los de la
  tarea. Separado con `reset --soft`. Mirar `git status` antes de cada commit propio.
- **Cada puerto es un origen distinto**, así que servir un worktree en otro puerto pide login de nuevo.
  Reusar el puerto donde ya se entró evita molestar al Director dos veces.
- **El conteo de tests del árbol principal está inflado** (2273) porque vitest levanta los tests de los
  worktrees de `.claude/worktrees/`. El número real —medido en un worktree afuera— es **1147**.

## 9. Git — estado de las ramas

Las tres PRs mergeadas. El worktree `wt-dispensacion` y su rama, borrados después de verificar que sus
commits están en `origin/main`. Sigue vivo `C:/Users/Tutuca/Desktop/Spira/wt-dia`
(`fix/visita-atendida-solo-su-dia`, ya mergeada) — **borrarlo**. Y de otra sesión,
`.claude/worktrees/vigorous-noether-f99a79` en `feat/baja-por-estudio`.

El `launch.json` tuvo dos entradas temporales para servir los worktrees en el preview; las dos
revertidas.

## 10. Pendientes

1. **Las 129 visitas para revisar a mano**: son las dos últimas de cada inscripción. La consulta 5 de
   `supabase/scripts/verificacion-carga-2026-09-15.sql` las lista con cuántos procedimientos y reportes
   le faltan a cada una, y se exporta a CSV desde el editor.
2. **El desenlace del IP quedó a medias.** Una visita cerrada con IP previsto y nunca entregado dice
   «Sin constancia cargada.», que describe un papel faltante y no un desenlace. La spec prometía los
   cuatro estados de `v_visit_ip_status`; se ven dos.
3. **Falta un test que cruce `excluir` con un rechazo vigente** en `historialPlegadoModel`. El código de
   hoy es correcto; el riesgo es que un refactor le pase la lista filtrada a `rechazoVigente` y nada lo
   atrape.
4. **«Cano Angel Daniel»** figura con el apellido primero, mientras el resto va nombre primero. Es el
   nombre que ya tenía de su otro estudio y el script lo conservó a propósito. Cambio a mano si molesta.
5. Siguen los pendientes del 15 (§16 de esa bitácora), en particular el IVRS del estudio madre en
   Farmacia y las consultas 1 a 3 de la sonda de la 0126.

---

# Segunda parte · La baja deja de ser de la persona y pasa a ser del estudio · release `v0.77.0`

> **Un reporte que no era el que parecía.** La jornada arrancó con «los procedimientos de la visita
> siguen sin asociarse al cronograma» y terminó arreglando otra cosa: a mitad de la investigación el
> Director dio de baja a tres pacientes en ACT18301 y los vio dados de baja **también en LTS17231**.
> Eran las mismas personas inscriptas dos veces, y el estado que la app editaba era el de la persona,
> no el del estudio. El reporte original sigue abierto.

**Ramas:** [#206](https://github.com/spiraclinicapp/Spira-App/pull/206) (`feat/baja-por-estudio`), mergeada. Migración **`0127`**, aplicada en prod. Release **`v0.77.0`**. **1159 tests** (1147 → 1159).

## 11. Resumen de la segunda parte

Tres cosas, en este orden:

1. **Se investigó el reporte de los procedimientos** y se descartaron tres causas con evidencia: no es
   caché del front, no es la RLS y no es el guardado del cronograma. Sí apareció, de paso, que las
   **doce últimas visitas de ENDURA (V17–V28) no tienen ningún procedimiento cargado** — el Excel de la
   carga corta en la V16 y esas doce entraron como extras desde el listado de pacientes. El caso que el
   Director tenía en la cabeza era otro (la V17 de ACT18301) y **quedó sin resolver**.
2. **Apareció el bug de la baja** y se arregló de punta a punta: spec, plan de diez tareas, migración
   `0127`, front y QA logueado contra producción.
3. **Se corrigió el dato en producción antes que el código** (Fase 0): los tres pacientes volvieron a
   activos como personas y su inscripción a ACT18301 quedó `completado` — que es lo que pasó de verdad,
   porque no abandonaron: terminaron ACT y pasaron a la extensión.

## 12. El diagnóstico: dos «activo» que se llaman igual

En la base hay dos estados y nadie los sincroniza: `patients.status` es de la **persona** —una sola fila
para todos los estudios— y `enrollments.status` es de la **inscripción**, una por estudio.

«Editar paciente › Estado › Inactivo» escribía el primero, y el punto verde/rojo de todas las listas lo
leía de ahí. Como LTS17231 es la extensión de ACT18301, dar de baja en uno se veía en el otro.

El estado por inscripción ya existía, pero la única puerta que lo movía era el desenlace «fallo de
screening» de una visita, que siempre escribe `discontinuado`. **Faltaba el cierre bueno**: completar un
estudio no es abandonarlo, y en un sistema auditable esa diferencia es el dato.

## 13. Las cinco decisiones del Director

Salieron de cinco preguntas, una por vez, antes de escribir una línea:

1. **El estado de la persona se deduce**, no se guarda: activa si tiene alguna inscripción abierta. Se
   va el campo «Estado» de «Editar paciente», que era la puerta del problema.
2. **Las visitas futuras sin atender se borran** al cerrar. Lo atendido no se toca, y lo que ya está en
   alerta sigue el camino de archivarlo con motivo y autor (decisión del 2026-09-05).
3. **El punto sigue binario.** Se ofreció un tercer color para separar «completó» de «se discontinuó» y
   lo rechazó: el color ya significa otras cosas en esa lista. La diferencia se dice en palabras.
4. **La acción vive en la ficha**, no en el listado.
5. **Se elige un motivo, no un estado.** Siete motivos, sin texto libre; la base deriva si eso es
   `completado` o `discontinuado`. Quien opera sabe si el paciente terminó o abandonó, no cómo se llama
   eso en un enum.

## 14. La implementación y la verificación

Diez tareas, de la corrección de producción al copy del desenlace de la visita. La `0127` agrega el
sello del cierre (`closed_at`, `closed_reason`, `closed_by`, `closed_from_status`) y dos funciones:
`close_enrollment` —que cierra y borra las visitas futuras en la misma transacción— y
`reopen_enrollment`, que devuelve al estado que la inscripción **tenía**, no a `activo` a secas.

**Probado contra Postgres real con PGlite** antes de pasarle el SQL al Director: de cinco visitas de
juguete —atendida, futura, ventana vencida, futura con pedido de farmacia, suelta— borra exactamente
una. Esa regla vive en SQL y ningún test de vitest la puede tocar.

**QA logueado contra producción.** El lado a lado que prueba el arreglo: los tres pacientes salen en
rojo «Completó el estudio» en ACT18301 y en verde «Activo en el estudio» en LTS17231. Y la prueba real
del botón, con el freno acordado: se reabrió y se volvió a cerrar a Ricardo Lucio Aguero desde la app
—el modal avisó «no le quedan visitas futuras», así que el borrado no tenía nada que llevarse— y el
sello quedó completo, con su LTS17231 intacta y sus 26 visitas.

## 15. Lo no-obvio

- **Una sesión caída se ve igual que un bug de permisos.** Las RPC nuevas devolvieron
  `42501 permission denied for function` y el diagnóstico fue «faltan los grants» — **estaba mal**. El
  preview había cerrado sesión por inactividad (30 minutos), así que las llamadas salían como `anon`, y
  `anon` efectivamente no puede ejecutarlas, que es lo que la migración quiere. Lo destapó leer el
  `role` del JWT antes de acusar a la base. Costó hacerle correr dos grants que ya estaban.
- **Probar una RPC sin escribir nada:** llamarla con un id inexistente. Busca la fila, no la encuentra
  y levanta antes de tocar nada — prueba que existe, que el permiso está y que el mensaje llega
  traducido, con cero riesgo sobre datos reales.
- **El número en la confirmación es lo que hizo posible la prueba real.** El modal dice cuántas visitas
  va a borrar *antes* de apretar. Decía cero, y por eso se pudo probar el botón sobre un paciente de
  verdad sin arriesgar nada.
- **Un comentario SQL con backticks adentro de un template literal de JS cierra la cadena.** El error
  que tira es `Unterminated regexp literal` en la línea del `db.exec(`, lejísimo del comentario culpable.
- **El heredoc de bash de esta máquina se come los backslashes**, así que un script con regex escrito
  con `node - << EOF` llega roto. Para cualquier script con expresiones regulares o markdown: escribir
  el archivo y correrlo. Y ojo con los backticks en un `node -e`: el shell los ejecuta como comandos y
  se comió dos fragmentos de una memoria.
- **Los archivos del repo están checkouteados en CRLF**, así que un reemplazo multilínea con `\n` no
  matchea nada. La forma que funciona es armar la regex uniendo las líneas con `\r?\n`.
- **El typecheck encontró un call-site que el plan no tenía mapeado.** `PatientsTable` es la tabla
  cruza-estudios —sólo la ve Farmacia— y ahí la pregunta no es la misma: no hay un estudio en contexto,
  así que lo único honesto es si a la persona le queda alguna participación abierta. Salió un componente
  aparte (`EstadoPersona`), y aparte a propósito: son la misma pinta y dos preguntas distintas.
- **El KPI de Inicio bajó de 64 a 60 y es una corrección**, no una baja nueva: cuatro personas con todas
  sus inscripciones cerradas se mostraban en verde porque el punto leía a la persona.
- **Dos sesiones cerraron la misma jornada otra vez.** La otra ya había tagueado `v0.76.0` cuando ésta
  mergeó la #206, así que `main` quedó por delante del tag. Se resolvió con release propio en vez de
  dejar producción corriendo código sin versión.

## 16. Git — estado de las ramas

`feat/baja-por-estudio` mergeada en la [#206](https://github.com/spiraclinicapp/Spira-App/pull/206) y
borrable. El cierre sale de `docs/cierre-2026-09-16-v0.77.0`. El worktree
`.claude/worktrees/vigorous-noether-f99a79` sigue vivo; el `launch.json` tuvo el puerto 5251 durante el
QA y quedó revertido al 5250.

## 17. Pendientes

1. **El reporte con el que arrancó la jornada sigue abierto: los procedimientos de la V17 de ACT18301.**
   Se descartó que sea caché del front (no hay), RLS (ver la visita y ver sus procedimientos piden lo
   mismo) o el guardado del cronograma. Quedan dos hipótesis y una consulta que las separa —está escrita
   en el chat— : si `ACT18301` tiene **dos** definiciones con código `V17`, editás una y las visitas de
   los pacientes cuelgan de la otra; si tiene una sola, el problema es de render. **Falta correr esa
   consulta y que el Director diga qué ve distinto exactamente** (si faltan procedimientos, si están
   todos en otro orden, o si lo que cambia es la forma).
2. **ENDURA (222714) V17 a V28 no tienen ningún procedimiento**, y es así desde la carga: el Excel
   describe el estudio hasta la V16 y esas doce se crearon desde el listado de pacientes, que da día y
   semana pero no procedimientos. Tampoco tienen marca de entrega de IP. Falta que el Director diga qué
   lleva cada una y sale por SQL o por la UI.
3. **La URL de una ficha usa el número del estudio madre.**
   `/coordinacion/pacientes/LTS17231/032001500005` abre bien y la pantalla muestra el IVRS correcto de
   LTS (`032001520003`), pero la ruta lleva el de ACT. Es anterior a esto y no rompe nada.
4. Siguen los pendientes de la primera parte (§10) y los del 15.

---

# Tercera parte · Las 129 que quedaban para revisar a mano, y por qué el alcance cambió dos veces · sin release

> **Un pedido que se afina dos veces no es un pedido mal hecho: es información que llega.** El cierre
> de Victorion empezó siendo «todo menos las dos últimas», pasó a «todo menos la última» y terminó en
> «todo salvo dos pacientes». Cada paso lo movió un dato que el Director tenía y el repo no.

**Contexto:** Coordinación · las 129 visitas que la primera parte dejó abiertas a propósito
**Rama:** [#207](https://github.com/spiraclinicapp/Spira-App/pull/207), **sin mergear al escribir esto** · **sin migraciones** · **sin release** (son scripts de datos)

## 18. De dónde salen las 129

La carga de la primera parte cerró los procedimientos de las visitas realizadas **menos las dos
últimas de cada inscripción** (§4). Esas 129 se repartían así: **96 de Victorion**, 16 de ACT18301,
12 de ENDURA y 5 de LTS17231.

Mirándolas con la consulta 5 de `verificacion-carga-2026-09-15.sql`, las 96 de Victorion son la misma
visita noventa y seis veces: mismo protocolo, los mismos dos procedimientos, el mismo reporte — y
sobre las **fechas provisorias** que la carga del 15 puso iguales a la estimada. Las otras 33 varían
entre sí y son las que tienen medicación entregada.

De ahí salió la oferta: cerrar Victorion por script y dejarle las 33 que tienen algo que decidir.

## 19. El alcance cambió dos veces, y las dos por un dato de él

1. **«Todo menos las dos últimas»** era lo que había hecho la carga.
2. **«Cerrame las de Victorion menos la última»** — primer recorte. El script quedó en `rn > 1`.
3. **«Perdón pero previamente ya estaban marcadas»** — y tenía razón: el tablero daba **205 completas
   / 89 abiertas** contra las 198 / 96 de unas horas antes. Había cerrado **siete a mano** mientras
   tanto. El script no necesitaba cambios (sus inserts van con `on conflict do nothing` y los reportes
   sólo se mueven si no están en `evolucionado`), pero sí lo necesitaba un comentario del control que
   afirmaba «tiene que quedar en 48».
4. **«Todos salvo 4022001 y 4022016, que son los únicos dos sin los reportes evolucionados»** — el
   dato que cambió todo. Había revisado Victorion contra los registros del sitio: el resto tiene los
   procedimientos hechos y los reportes evolucionados **incluida la última visita**. El alcance pasó a
   ser el protocolo entero salvo dos pacientes.

## 20. Lo que quedó escrito, y por qué ahí

- **El porqué vive en la cabecera del SQL**, no sólo en el mensaje del cambio: *«el Director revisó
  Victorion contra los registros del sitio el 2026-09-16 y confirmó que…»*. En una base auditada, «por
  qué se marcaron 280 visitas como hechas» tiene que poder responderse dentro de seis meses sin
  reconstruir una conversación. El script **transcribe** un hecho; no lo deduce.
- **A los dos excluidos no se les toca nada**, ni los procedimientos —aunque a Garay ya se los había
  tildado él—. Mezclar «lo cerró el script» con «lo cerró él» justo en las dos fichas que SON la
  excepción es donde después nadie sabe qué pasó.
- **Un guard para el IVRS excluido inexistente.** Un número mal tipeado ahí no se nota en el
  resultado: el script cerraría de más y el informe daría una cifra plausible. Se frena antes de
  escribir. Probado: frena y deja cero filas.

## 21. Lo no-obvio

- **El informe tiene que contar lo que CAMBIA, no el alcance.** La primera versión decía «288 visitas
  alcanzadas» cuando las que cambiaban eran 48: las otras 240 ya estaban cerradas y entraban sólo para
  salir por el `on conflict`. Un número que asusta al leerlo hace que nadie lea el resto del informe,
  que es exactamente lo contrario de para qué existe el paso de simular.
- **Un control que afirma un número fijo envejece mal.** «`con_pendientes` tiene que quedar en 48»
  dejó de ser cierto el mismo día, porque el Director venía cerrando a mano. Lo que hay que escribir
  es el invariante —que no quede abierta ninguna visita que no sea la última de su paciente—, no el
  valor de hoy. Misma familia que «un pendiente que describe la solución envejece peor que uno que
  describe el problema».
- **«Ya estaban marcadas» merecía una consulta, no una explicación.** La tentación era defender el
  número propio; lo que lo resolvió en tres segundos fue una consulta de sólo lectura que distinguía
  las dos lecturas posibles. Y el resultado le dio la razón a él.
- **El banco de pruebas tuvo que crecer con el alcance.** Al pasar de «menos la última» a «salvo dos
  pacientes», lo que había que probar dejó de ser un conteo y pasó a ser *de quién* son las que
  quedan: el control agrupa por paciente, y el banco verifica que las cuatro abiertas sean las de los
  dos excluidos.

## 22. Git — estado de las ramas, y un merge que salió antes de tiempo

Dos PRs para un solo cambio, y vale anotar por qué: la [#207](https://github.com/spiraclinicapp/Spira-App/pull/207) **se mergeó con el alcance viejo**
(«todo menos la última de cada paciente») en el momento en que llegaba la información que lo cambiaba.
Durante un rato `main` tuvo un script que hacía otra cosa de la que se quería, y el Director iba a
correrlo desde ahí. La [#208](https://github.com/spiraclinicapp/Spira-App/pull/208) lo corrigió.

**Lo que lo detectó no fue leer el código: fue preguntar dónde estaba el archivo.** Al ir a dar la
ruta apareció que la versión de `main` y la del disco no eran la misma — la carpeta estaba parada en
la rama, con el archivo bueno, y `main` tenía el viejo. Un `grep -c "4022016"` sobre las dos versiones
lo dijo en un segundo.

Es sólo `supabase/scripts/cerrar-victorion/`, sin tocar código de la app, así que no lleva versión.

## 23. El cierre, aplicado

Corrido en producción el **2026-09-16**, desde `main` y después de la #208. Victorion queda con sus
procedimientos y reportes cerrados salvo **Quiroga (`4022001`) y Garay (`4022016`)**, que son los dos
que todavía no tienen los reportes evolucionados.

La lista de revisión a mano queda entonces en las de **ACT18301, ENDURA y LTS17231** —las que varían
entre sí y las que tienen medicación entregada— más esas dos fichas de Victorion.

## 24. Pendientes

1. **Los reportes de Quiroga y Garay** siguen sin evolucionar. Los cierra él cuando los tenga.
2. **Las de ACT18301, ENDURA y LTS17231**, que son las que hay que mirar de verdad. La consulta 5 de
   `verificacion-carga-2026-09-15.sql` las sigue listando con lo que le falta a cada una.
3. Siguen los pendientes de la primera parte (§10) y de la segunda (§17).
