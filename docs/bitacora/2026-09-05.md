# Bitácora · 2026-09-05 — Cuatro PRs para una tarjeta, y las tres las corrigió el Director mirando la pantalla · release `v0.55.0`

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Fundación Scherbovsky · farmacia e investigación clínica · sistema auditable ANMAT/ICH-GCP
**Ramas:** cuatro PRs — [#118](https://github.com/spiraclinicapp/Spira-App/pull/118), [#119](https://github.com/spiraclinicapp/Spira-App/pull/119), [#120](https://github.com/spiraclinicapp/Spira-App/pull/120) y [#121](https://github.com/spiraclinicapp/Spira-App/pull/121), las cuatro mergeadas y borradas. **Sin migraciones.** Release **`v0.55.0`**. 647 tests.

> **La jornada empezó como un cambio de panel y terminó reencuadrando un submódulo.** El pedido fue
> reemplazar "Próximas visitas" por "Por reprogramar"; se hizo, se corrigió, y a mitad de camino el
> Director paró la mano con una idea mejor que dejó la tarjeta original de vuelta en su lugar. De las
> cuatro PRs, **tres nacieron de que él miró la pantalla en producción**, no de que yo leyera el diff.

---

## 1. Resumen de la jornada

Cuatro entregas encadenadas, cada una corrigiendo a la anterior:

| PR | Qué |
|---|---|
| **#118** | El panel del Resumen pasa de "Próximas visitas · 7 días" a **"Por reprogramar"** |
| **#119** | Su regla de ámbito estaba mal, y el KPI vuelve a "Próximas visitas" |
| **#120** | **El reencuadre:** lo atrasado se va a Pendientes, *Alertas* se llama **Pendientes**, y la tarjeta vuelve a mirar adelante — **un día** |
| **#121** | El vocabulario de la pantalla acompaña al renombre |

Lo que vale guardar no es la tarjeta. Son cuatro cosas:

- **Un estado calculado por prioridades no es un conjunto**, y filtrar por el que nombra el caso deja
  afuera el caso peor.
- **Copiar una regla arrastra su premisa**, y la premisa no viaja escrita.
- **Un renombre no falla: se desincroniza.** Cuatro literales sobrevivieron sin un solo error.
- **El reencuadre del Director arregló un defecto que yo había diseñado alrededor** en vez de resolver.

---

## 2. El estado que nombra el caso no lo contiene

El pedido definía "por reprogramar" en dos partes: las visitas **con estado *Por reprogramar*** y las
que **se pasaron de fecha sin atender**. Lo obvio era filtrar por `computed_status = 'por_reprogramar'`.

Lo obvio dejaba afuera el caso peor. El `case` de `v_patient_visits` está **ordenado por gravedad**
(0102, ramas 2 y 3): `ventana_vencida` va **antes** que `por_reprogramar`. Así que una visita marcada
"No vino" cuya ventana *además* venció **pierde el estado que la nombra** — justo la que más necesita
fecha nueva.

La condición terminó escrita sobre **el hecho** y no sobre el estado:

```
real_date is null  AND  estimated_date is not null
AND ( no_show_at is not null  OR  estimated_date < hoy )
AND enrollment_status in ('activo','screening')
```

Un enum calculado por prioridades contesta *"¿cuál es lo más grave de esta fila?"*, no *"¿la fila
tiene X?"*. Son preguntas distintas, y la segunda no se responde con la primera.

Dos cosas más de esa consulta, que no eran obvias:

- **No hizo falta migración.** `v_track_visits` proyecta `no_show_at` **desde la 0067**; sólo faltaba
  declararlo en `TrackVisitRow`. Es la misma ausencia que en su momento hizo creer que la vista no
  traía `coordinator_id`.
- **El filtro por `enrollment_status` es el único de su clase** en las consultas de visitas, y se
  agregó porque acá cambia el resultado: con siete días hacia adelante un paciente discontinuado no
  aparece nunca; en una lista sin límite hacia atrás, sus visitas abiertas encabezan para siempre y
  nada las cierra.

---

## 3. Cómo probar un filtro de PostgREST sin sesión

El `.or()` que arma esa consulta es la parte de la capa de datos que **ningún test puro cubre**: es un
string que arma el front y parsea el servidor. Si está mal escrito, la pantalla muestra el bloque de
error — y hay que estar logueado para verlo.

Se puede verificar sin sesión, porque **PostgREST parsea el filtro antes de chequear permisos**:

| | respuesta |
|---|---|
| filtro válido, sin sesión | **401** `42501 permission denied for view` |
| filtro inválido | **400** `PGRST100 failed to parse logic tree` |

La corrida mandó **un control negativo** en el mismo lote (`or=(no_show_at.noexiste.null)`): dio 400,
la consulta real dio 401 ⇒ el árbol lógico parseó bien. **Sin el control, el 401 no prueba nada** —
podría venir de cualquier cosa.

---

## 4. Dos tarjetas de la misma pantalla discrepando sobre la misma fila

El primer defecto real, y lo encontró el Director mirando prod: una visita aparecía en "Por
reprogramar" y no en "Alertas", sin nada que lo explicara.

La causa fue **copiar la regla de la lista de al lado**. "Por reprogramar" filtraba *"Lo mío"* con
`esDeMisProtocolos` a secas, tomado de "Próximas visitas" — donde **es correcto, porque una visita
futura nunca tiene coordinador asignado**. En la lista nueva esa premisa es falsa: sus visitas sí lo
pueden tener.

| Visita de mi protocolo, asignada a **otra** persona | |
|---|---|
| `esDeMisProtocolos` (la que usaba Por reprogramar) | es mía → **aparece** |
| `esAlertaMia` (la que usa Alertas) | no es mía → **no aparece** |

Una regla de filtrado **viene con una premisa pegada, y la premisa no viaja escrita**. El código
copiado compila, pasa los tests y se ve bien: el defecto no está en ninguna de las dos listas leídas
por separado, está en que **discrepan** — y eso sólo se ve con las dos tarjetas juntas en pantalla.

Al generalizar la regla, `esAlertaMia` pasó a llamarse **`esMiaSinAtender`**: el nombre viejo describía
*una lista*, el nuevo describe *la condición*, que es lo que se reusa. Quedó un test que fija la
divergencia contra `esDeMisProtocolos`, para que el día que alguien vuelva a elegir la regla floja
haya un caso exacto que lo cace.

En la misma PR volvió el KPI a **"Próximas visitas"**, revirtiendo una decisión mía (D6). Mi argumento
era que un número y su lista tienen que contar lo mismo — pero el KPI vive en el mosaico de **arriba**
y la tarjeta en el de **abajo**, así que no se leen como par; y esa cifra no está en ninguna otra
parte de la pantalla.

---

## 5. El reencuadre: la duplicación que yo había asumido en vez de resolver

A mitad del diseño de "suprimir avisos" —reprogramar o ignorar—, el Director frenó:

> *"pará pará estoy pensando y si unificamos todo? Osea que la visita por reprogramar sea una alerta
> más en vez de ocupar un espacio…"*

**Tenía razón, y por una razón que no era la que él daba.** En la #118 yo había documentado como
decisión (D1) que una visita en ventana vencida aparece en **las dos** tarjetas del Resumen, con el
argumento de que piden acciones distintas. Era una excusa. Metidas en una sola lista el problema
desaparece solo: `computed_status` **ya** resuelve la prioridad, así que sale **exactamente una fila
por visita**, con la severidad peor.

Y el encuadre correcto no es "agregar una clase de alerta" sino **ensanchar la que ya existe** a los
grados de un mismo hecho: *visita sin atender fuera de fecha*.

> **Al escribir el plan de la pieza 3, ese mismo día, quedaron DOS grados y no tres.** Acá pensábamos
> sumar también "atrasada con la ventana abierta" — pero `window_end` es `not null`, así que ese caso
> es una visita **dentro de su tolerancia**, y además no tiene estado propio (el `case` lo resuelve
> como `proxima`). Ver `docs/plan-pendientes-clase-de-alerta.md`, D1: sin ese grado no hace falta
> tocar el enum, y la pieza se achica a la mitad.

El plan quedó en **cinco piezas**, de las que se hicieron dos:

| | Pieza | Estado |
|---|---|---|
| 1 | Renombrar el submódulo a **Pendientes** | ✅ #120 |
| 2 | El Resumen: sacar "Por reprogramar", poner **el próximo día** | ✅ #120 |
| 3 | **Ensanchar la clase de alerta** (migración) | ⬜ sin empezar |
| 4 | Preselector + filtros en Pendientes, al estilo de Stock | ⬜ |
| 5 | Tareas pendientes (necesita tabla, RLS y auditoría) | ⬜ |

---

## 6. "Mañana" un viernes es sábado

El pedido para la tarjeta fue *"las visitas del día siguiente únicamente"*. Tomado al pie de la letra,
la tarjeta queda **vacía todos los viernes** —mañana es sábado— y en cada feriado: un renglón muerto
una vez por semana, por diseño.

Quedó como **el próximo día CON visitas**, con el rótulo diciendo cuál es ("Mañana", "Martes 08/09").
Dice lo mismo, nunca miente, y sigue siendo una jornada.

La regla salió del JSX a `views/resumen/proximoDia.ts` con **8 tests**, porque falla en silencio de
tres maneras:

- **`>` vs `>=`** — con `>=` muestra las de **hoy** bajo el rótulo "Hoy" y contesta otra pregunta.
- **Confiar en el orden de la consulta** — la primera versión hacía `find()` asumiendo que la lista
  venía ordenada por fecha. Venía, pero eso es un contrato de **otro archivo**. Ahora busca el mínimo
  explícitamente y el orden de entrada deja de importar.
- **Las visitas sueltas** (`kind <> 'programada'`) no tienen fecha citada: sin guarda, el `null` se
  cuela como día y la tarjeta queda encabezada por una fecha inválida.

Y hay algo que esta versión gana sobre las dos anteriores: **el pie promete exacto.** Va a Visitas del
día **con ese día puesto**, así que el destino muestra literalmente lo que se estaba mirando. La de
siete días prometía una lista que ninguna pantalla junta; la de atrasadas, filas repartidas en semanas.

---

## 7. El renombre no falló: se desincronizó

Cambiar el submódulo `alertas` a **"Pendientes"** costó **dos líneas** del registry. `SUB_SLUG`
(`lib/router.ts`) ya desacopla el nombre visible de la URL —lo hace con `protocolos`→*Pacientes* y
`medicamentos`→*Stock*—, así que `/coordinacion/alertas` siguió resolviendo y ningún link compartido
se rompió. Y los submódulos son front puro: no hay enum de base de por medio.

Lo caro fueron **los literales**. Cada lugar que escribía `"Alertas"` a mano siguió diciéndolo, **sin
un solo error**, y salieron de a uno cada vez que el Director miraba la pantalla:

1. el pie `VerMas nombre="Alertas"` de la tarjeta del Resumen,
2. el enlace de vuelta de la vista, `label: 'Volver a Alertas'`,
3. **el título dentro de `AlertCardHeader`** — la banda teñida de las **dos** pantallas a la vez,
4. el pie de la campana, `"Ver todas las alertas"`,

más el `aria-label` de una fila, que **no se ve mirando** y por eso es el que más fácil queda viejo.

Lo irónico: `destinos.ts` ya advertía por escrito contra el "diccionario paralelo" —el mock del
handoff traía un `DEST_LABEL`— y había cuatro vivos igual. Ahora el nombre sale de `MODULES` vía
`nombreDeDestino()`, con la constante `DESTINO_PENDIENTES`, y los componentes de presentación lo
**reciben por prop** en vez de escribirlo.

**El corolario de género**, que un `grep` del sustantivo no encuentra: cambiar el nombre del contenedor
arrastra la concordancia. *"Ver descartad**as**"* (alertas, femenino) pasó a *"Ver descartad**os**"*
(pendientes, masculino).

**Y dónde se paró:** se renombró el vocabulario del **contenedor** —la banda, "45 de 45 pendientes",
los vacíos, el "Cargando…", la campana, la tarjeta de Coordinación en Inicio— y **no** el del **ítem**
("Descartar esta alerta", el modal de descarte). Lo que se descarta sigue siendo una alerta en la
base: `alert_dismissals`, `dismiss_alert`, los motivos del catálogo y el `audit_log`. Renombrar la UI
sin el modelo es exactamente como se desincroniza el vocabulario — que es el problema de esta sección.

---

## 8. Lo no-obvio

- **Un estado calculado por prioridades no es un conjunto.** El estado que *nombra* un caso no lo
  *contiene*: hay que filtrar por la columna del hecho, no por el enum derivado. Antes de escribir
  `computed_status = 'algo'`, abrir el `case` de la última migración que recrea la vista y mirar **qué
  ramas están arriba** — cada una es un caso que el filtro va a perder.
- **Copiar una regla arrastra su premisa, y la premisa no viaja escrita.** El porqué de
  `esDeMisProtocolos` estaba documentado en `ambito.ts` y decía justamente cuál era la premisa. No
  alcanzó con que estuviera escrito: hay que ir a leerlo.
- **Un renombre no falla, se desincroniza.** Cuatro literales, cero errores, cuatro rondas de "acá
  también dice Alertas". Ante un renombre no alcanza con grepear el nombre viejo: hay que **cortar la
  fuente**. Y el barrido incluye tres lugares que se olvidan — los `aria-label`, los textos del
  **shell** (la campana) y los de **otros módulos** (Inicio).
- **El 400 vs 401 de PostgREST prueba la sintaxis sin sesión**, pero sólo con un control negativo en
  la misma corrida. Sin él, el 401 no significa nada.
- **Documentar una decisión no la vuelve correcta.** La D1 de la #118 explicaba con tres párrafos por
  qué la misma visita aparecía en dos tarjetas. La explicación era buena y la decisión era mala; el
  reencuadre la borró de un plumazo. Un comentario largo justificando una rareza es una señal para
  volver a mirar el problema, no un permiso para dejarla.
- **Un cambio deja falsos los comentarios que lo describen, incluso los de hace media hora.** Al
  retirar la tarjeta "Por reprogramar" quedaron dos comentarios afirmando que la regla la usaban "las
  dos tarjetas". Es el mismo tropiezo que la tanda del 04: al cerrar algo que corrige a lo anterior,
  hay que releer lo que lo anterior afirmó.

---

## 9. Git — estado de las ramas

- **#118** `feat/resumen-por-reprogramar` — mergeada (`4a86a82`), borrada en local y `origin`. Dos
  commits: el plan (`docs/plan-por-reprogramar.md`, ocho decisiones) y la implementación.
- **#119** `fix/resumen-ambito-y-kpi` — mergeada (`e7dc012`), borrada.
- **#120** `feat/pendientes-y-proximo-dia` — mergeada (`635af25`), borrada.
- **#121** `fix/vocabulario-pendientes` — mergeada (`698a370`), borrada.
- **No queda ninguna rama viva.** Sin migraciones en toda la jornada.
- Release **`v0.55.0`**, con una entrada de changelog de 85 caracteres — dentro del tope de 100 que
  fijó el test de la tanda anterior.

Los tests netos: 645 → **647**. Entraron 8 de `proximoDia` y 1 de la divergencia de reglas; salieron
7 de `recorte` (el recorte por grupos de día, sin consumidor tras el rediseño de la tarjeta).

---

## 10. Pendientes

**Lo que abrió esta jornada:**

1. **La pieza 3 — ensanchar la clase de alerta.** Es la que hace que lo atrasado viva en Pendientes.
   ✅ **El plan quedó escrito el mismo día:** `docs/plan-pendientes-clase-de-alerta.md` — ocho
   decisiones cerradas, una abierta (D9, el filtro por `enrollment_status`). Lleva migración (la
   `0107`, aditiva) y tiene **tres trampas identificadas**:
   - `dismiss_alert` **valida el estado** (`if v_status not in ('ventana_vencida','item_vencido') then
     raise`) y rechazaría las clases nuevas;
   - **la huella** no puede ser `window_end`: en una visita atrasada con la ventana todavía abierta
     está **en el futuro** y no describe la condición. Tiene que anclar en `estimated_date`;
   - **la campana**: el panel hace `rows.map(...)` **sin recortar** y ya renderiza 43 ítems.
     *(Corregido el 2026-09-05 al escribir el plan de la pieza 3: acá decía que el **badge** saltaría
     de 43 a ~90, y es falso — `badge = count > 9 ? '9+'` ya está topeado y el ensanchamiento no lo
     mueve. El problema es real y es la LISTA, no el número. Ver `docs/plan-pendientes-clase-de-alerta.md`.)*

   El código retirado (`useVisitsPorReprogramar` + `views/resumen/reprogramar.ts` con sus 13 tests) se
   recupera del commit **`bf0a536`**.

2. **El hueco, mientras tanto:** las visitas por reprogramar **no se ven en la app**. Eran **49** en
   producción. Queda visible sólo el grado rojo (`ventana_vencida`), que ya era alerta.

3. **Las 49, y el arrastre.** La más vieja estaba citada el **22 Oct 2025 — hace 318 días**. Si el
   grueso es arrastre de la carga histórica, se cierra **en la base**: un corte de fecha en la tarjeta
   las escondería sin resolverlas.

4. **Piezas 4 y 5:** el preselector de Pendientes (al estilo del de Stock) y las tareas pendientes,
   que necesitan tabla, RLS y auditoría.

**Los tres arrastrados, sin novedad:**

5. **Revertir la asignación de prueba** de la coordinadora a LTS17231 (conviene seguir postergándolo:
   es la única cuenta que muestra el caso "sin acceso").
6. **El `security_invoker` de `v_protocol_report_status`**, que sigue sin poder distinguirse.
7. **Los dos "Resumen"** de la navegación, que siguen chocando.

---

# Bitácora · 2026-09-05 (segunda tanda) — La pieza 3, y un plan que achicó su propia implementación · release `v0.56.0`

**Ramas:** dos PRs — [#122](https://github.com/spiraclinicapp/Spira-App/pull/122) (el plan) y [#123](https://github.com/spiraclinicapp/Spira-App/pull/123) (código + migración), las dos mergeadas y borradas. **Migración `0107`, aplicada en prod el 2026-09-05** — y esta vez **en el orden correcto**: primero la migración, después el front. Release **`v0.56.0`**. 659 tests.

> Cierra el hueco que la primera tanda dejó abierto a propósito: las visitas por reprogramar ya se ven, ahora **adentro de Pendientes** y como una alerta más.

---

## 11. Escribir el plan cambió lo que había que construir

El plan anterior definía la clase en **tres** grados. Al ir a escribir el de la pieza 3 aparecieron dos hechos que dejaron el tercero afuera, y con él más de la mitad del trabajo:

**`window_start` y `window_end` son `not null`** (`0002_tables.sql:128-129`). Toda visita programada tiene ventana, así que el tercer grado —*pasó la fecha citada, la ventana sigue abierta*— es exactamente **una visita dentro de su tolerancia**. No es un desvío: la ventana existe para eso.

**Y no tiene estado propio:** el `case` lo resuelve como `proxima`. Distinguirlo exigía un valor nuevo en el enum, `ALTER TYPE` en archivo aparte, recrear las dos vistas y **deploy front-primero** — porque los ocho accesos `VISIT_STATES[...].color` **no tienen guarda**.

Con dos grados, nada de eso. La migración quedó en **una función**: `dismiss_alert`.

**La lección de método:** el plan no fue papeleo previo a la implementación, fue *parte* de ella. Los dos hechos estaban a la vista en archivos que ya existían, y ninguno se iba a notar escribiendo código — se notaron preguntándole al schema qué garantizaba.

## 12. La huella: el mismo campo no sirve para las dos clases

El descarte de la 0070 guarda `(status, anchor)` para valer **sólo mientras la condición sea la misma**. Para `ventana_vencida` el ancla es `window_end` y describe la condición.

Para "no vino" **no sirve, y por una razón que se lee al revés**: esa alerta existe justamente porque la ventana **todavía no venció**, así que `window_end` está en el **futuro**. Dos visitas con la misma ventana y citas distintas tendrían la misma huella. Lo que define la condición es **a qué cita no vino el paciente**: `estimated_date`.

El ancla pasó a elegirse **por estado**, escrita igual en el servidor (`dismiss_alert`) y en el front (`anclaDeLaVisita`). Las dos mitades viven lejos y tienen que coincidir: si eligen columnas distintas, ningún descarte matchea nunca — o peor, matchea de más y silencia una alerta que no correspondía.

Es el cambio que más silenciosamente podía fallar de toda la pieza, y por eso lleva **seis tests**: uno de ellos falla explícitamente con la regla vieja.

## 13. Un empate escondido en una función sin consumidores

`priorizarAlertas` decía `ventana_vencida ? 0 : 1`. Con dos clases alcanzaba; con tres dejaba **"no vino" y "reporte vencido" empatados**, contradiciendo a la cabecera de la tarjeta, que sí los distingue por `GRAVEDAD`.

Ahora ordena por `GRAVEDAD`: **un solo ordenamiento**, no dos que se desincronizan sin que nada deje de compilar. Y estrena su **primer consumidor** —la campana— después de existir con test y sin usarse.

## 14. Lo no-obvio (segunda tanda)

- **El plan achicó la implementación, no al revés.** Preguntarle al schema qué garantiza (`not null`) sacó un grado entero del alcance y con él un `ALTER TYPE`, dos vistas recreadas y un deploy front-primero.
- **Una huella no es "una fecha": es la columna que DEFINE esa condición**, y cambia con la clase. Reusar la de al lado silencia de más.
- **Corregí dos afirmaciones mías el mismo día.** Que el badge de la campana saltaría de 43 a ~90 (falso: ya decía "9+"; el defecto era la **lista** sin tope) y que las bandas de las dos severidades ámbar "se ven distintas" (medidas: `243,239,236` contra `247,243,236`, indistinguibles). **Las dos las desmintió una medición, no un razonamiento** — y las dos estaban escritas con seguridad.
- **Una función sin consumidores envejece mal en silencio.** `priorizarAlertas` tenía test y pasaba; lo que no tenía era a alguien que la usara, así que su lógica quedó vieja frente a un cambio que la afectaba de lleno.

## 15. Git y pendientes

- **#122** (`docs/plan-pieza-3`) y **#123** (`feat/alerta-por-reprogramar`) — mergeadas (`7834957` y `b2f87fe`), borradas en local y `origin`. **No queda ninguna rama viva.**
- **Migración `0107` aplicada en prod**, registrada en el índice; CLAUDE.md pasa a apuntar a ella.
- Release **`v0.56.0`** (commit `eb2e25c`, tag sobre `main`).
- Tests: 647 → **659** (+6 de la huella, +4 de la severidad, +2 de la prioridad).

Pendientes:

1. **El QA logueado de la pieza 3**, que es lo único que no se pudo hacer. Sobre todo la huella: reagendar una descartada la saca de la lista; dejar vencer su ventana la **devuelve en rojo**.
2. **Piezas 4 y 5:** el preselector de Pendientes (al estilo del de Stock) y las **tareas**, que necesitan tabla, RLS y auditoría.
3. Los tres arrastrados de siempre (la asignación a LTS17231, el `security_invoker` de `v_protocol_report_status`, los dos "Resumen").

El hueco de la primera tanda —las visitas por reprogramar invisibles— **quedó cerrado el mismo día en que se abrió**.

---

# Bitácora · 2026-09-05 (tercera tanda) — La pieza 4, y el mismo descuido por tercera vez · release `v0.57.0`

**Ramas:** dos PRs — [#124](https://github.com/spiraclinicapp/Spira-App/pull/124) y [#125](https://github.com/spiraclinicapp/Spira-App/pull/125), las dos mergeadas y borradas. **Sin migraciones.** Release **`v0.57.0`**. 669 tests.

---

## 16. Entendí mal el pedido, y la corrección lo simplificó

El pedido original decía: *"Le haría un preselector parecido al de stock **de los protocolos** con los filtros que ya tiene actualmente."* Leí "el preselector de Stock" —el segmentado de apartados (Protocolo · Ambulatoria · Catálogo)— y armé un diseño alrededor de un **eje de clase** nuevo: `Todo · Visitas · Reportes`.

Ese diseño tenía un problema real que llegué a plantear: **el menú Estado ya es un eje de clase** (`REPORTE_PENDIENTE` es una de sus opciones y decide si la lista de reportes entra entera). Dos controles sobre el mismo eje se contradicen: elegís "Visitas" arriba, tildás "Reporte pendiente" abajo, y la lista queda vacía sin que nada lo explique.

La corrección —*"me refiero a los atajos modo card de los protocolos dentro de la tabla"*— **hizo desaparecer el problema en vez de resolverlo**: las tarjetas escriben el `protocolFilter` que **ya existe**, así que no hay eje nuevo y no hay nada que pueda contradecirse.

**Lo reusable:** "de los protocolos" estaba escrito en el pedido original. No pregunté qué parte de Stock, asumí — y el diseño que salió de esa suposición era más grande y peor.

## 17. El mismo descuido, por tercera vez en el día

Al cablear las tarjetas aparecieron **dos defectos que yo mismo había dejado en la pieza 3**, dos horas antes:

- **El menú Estado no conocía "Por reprogramar".** Listaba tres opciones y la clase ya tenía cuatro: las visitas "No vino" **no se podían pedir por filtro**, no aparecían con su conteo, y tildar cualquier otra opción **las escondía sin manera de traerlas de vuelta**.
- **La leyenda del pie no nombraba el terracota.** Su propio comentario dice que *"la leyenda tiene que nombrar el color que se VE"*, y ya había quedado falsa una vez antes por lo mismo.

Es el **mismo patrón que el renombre de la mañana**: cambiar una fuente y no barrer a quienes la leen. Tres veces en una jornada, con tres formas distintas —un literal `"Alertas"`, un `if` de prioridad, una lista de opciones escrita a mano— y siempre el mismo síntoma: **nada falla, todo se dibuja perfecto, y falta algo**.

La cura fue la misma las tres veces: **cortar la fuente**. El menú Estado y la leyenda **se arman ahora desde `GRAVEDAD`**, igual que el desglose de las tarjetas y que `priorizarAlertas`. Cuatro consumidores, una sola lista.

## 18. Una decisión de diseño que no era mía

La primera versión de la tarjeta salió **más baja que la de Stock**: le saqué el nombre del protocolo, el chip de estado y el ícono para ahorrar alto en la pantalla que más se mira. Lo documenté como decisión y lo medí (223 px de bloque).

El Director mandó una captura de Stock: *"quiero que los pendientes se vean de esta forma… que sea el mismo funcionamiento"*. **El funcionamiento ya era el mismo** —las tarjetas escribían el mismo filtro— pero la forma no, y ahí estaba el punto: **si dos pantallas ofrecen el mismo gesto, tienen que verse iguales**. Y el nombre del protocolo no era adorno: es lo que permite elegir sin saberse los códigos de memoria.

El bloque pasó de **223 px a 376 px**. Es un costo real y quedó anotado en la PR para que sea una decisión y no una sorpresa.

## 19. Un botón cuyo efecto pasa fuera de la vista

El segundo pedido de esa tanda: *"que el ver descartados muestre un cajón arriba… si no no se entiende dónde se están viendo esos descartados"*.

El botón vive arriba, en la línea del contador, y **el panel se dibujaba después de la lista entera**. Con cuarenta y pico de pendientes, tildarlo no cambiaba nada de lo que se veía: había que scrollear hasta el fondo para descubrir que sí había hecho algo. **Un control cuyo efecto ocurre fuera de la vista se lee como un botón roto**, y no hay comentario en el código que arregle eso.

De paso subió el aviso de error por encima del cajón: "Restaurar" se aprieta **adentro** del cajón, así que con el aviso abajo el mensaje aparecía fuera de la vista justo cuando la acción falla.

## 20. Lo no-obvio (tercera tanda)

- **Una suposición sobre el pedido cuesta más que una pregunta.** "De los protocolos" estaba ahí; leerlo mal produjo un diseño con un eje de más y un conflicto que había que resolver. La versión correcta no tenía nada que resolver.
- **El tercer caso del mismo descuido es información, no mala suerte.** Un defecto que aparece tres veces en un día no se arregla con más cuidado: se arregla haciendo imposible el error. Por eso las cuatro listas derivan de `GRAVEDAD` en vez de repetirla.
- **Optimizar sin que te lo pidan también es apartarse del pedido.** Achicar la tarjeta fue una decisión mía disfrazada de criterio; el pedido decía "como la de Stock". Documentarla no la volvió correcta — el mismo error que la D1 de la mañana.
- **Los rótulos de un chip no sobreviven a un número adelante.** `VISIT_STATES[...].short` está escrito para estar solo ("Ventana", "No vino"), y detrás de una cifra salía "9 ventana · 2 vencido". La salida **no** fue escribir plurales en la tarjeta —sería un diccionario paralelo de rótulos, justo lo que viene fallando— sino invertirlo: **"Ventana 9"**, un marcador, que no le pide concordancia a nada.
- **Un `fetch` puede correr contra el merge.** Al ir a cerrar, `git fetch` dijo "up to date" y la API decía `merged=true`: el fetch había salido segundos antes del merge. La ref local mentía y la API no. Se resuelve fetcheando de nuevo — pero conviene mirar **las dos** fuentes antes de decirle a alguien que su merge no entró.

## 21. Git y pendientes

- **#124** (`feat/pendientes-cards-de-protocolo`) y **#125** (`feat/pendientes-cards-como-stock`) — mergeadas (`9e47322` y `e7ab1f0`), borradas en local y `origin`. **No queda ninguna rama viva.**
- Release **`v0.57.0`**. Tests: 659 → **669** (+10 del conteo por protocolo).
- **Sin migraciones**: sigue la `0107`.

Pendientes:

1. **El QA logueado de las piezas 3 y 4** — la huella de los descartes, y el cajón reubicado, que es JSX puro y no se pudo ver sin sesión.
2. **Los 376 px** de las tarjetas, si al usarlas resultan demasiados.
3. **Pieza 5: las tareas**, que necesitan tabla, RLS y auditoría.
4. Los tres arrastrados de siempre.
