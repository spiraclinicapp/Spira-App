# Handoff · 2026-08-02 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-29.md`](./handoff-2026-07-29.md). Narrativa del día: [`2026-08-02.md`](./2026-08-02.md).

> Dos features de Pharma/Track **mergeadas a main**: **Medicamentos editar/eliminar** (PR #19) y **legibilidad del stepper de Visitas + ROADMAP al día** (PR #22). Sin migración; **release `v0.20.0`** cortado al cierre. `main` limpio y en sync.

## Dónde estás

- **Medicamentos editar/eliminar SHIPEADO:** PR #19 **mergeada a main** (`e506a85`). En el Catálogo de Pharma, kebab por fila → **Editar** (modal con parity de campos, incluye Código de barras + Laboratorio) y **Eliminar del catálogo** (confirmación por tipeo `ELIMINAR`, borrado FÍSICO). Gateado `canManage` (pharma leader+). **Sin migración.** QA logueado del Director OK.
- **Stepper de Visitas más legible SHIPEADO:** PR #22 **mergeada a main** (`7908c5d`). Puntos pendientes del stepper con contraste 3.4:1 (antes 1.4:1). **El centrado ya venía de `16c791c`** (Director, 29/07); esto solo mejoró la claridad. QA logueado del Director OK.
- **ROADMAP al día:** `docs/#4- ROADMAP.md` reescrito como arco estratégico (en la misma PR #22).
- **Sin migración** en toda la jornada → **última aplicada = 0064**. **Release `v0.20.0`** cortado al cierre (bump + tag `v0.20.0`, pusheados a origin).
- **`_cleanup_beta_patients.sql` eliminado** (residuo ya corrido; el Director lo pidió).
- **Working copy:** rama activa **`main`**, con ambos merges, **en sync con origin**, árbol **limpio**. Sin cambios ajenos colgados.

## Lo hecho esta jornada

- **Puesta al día** sobre el hilo `0064` del Director (procedimientos = checklist de la visita): confirmado que resuelve la pregunta abierta del 22/07 y que su QA dio OK; `main` local puesto al día.
- **Medicamentos (PR #19):** `/plan-eng-review` → `deleteMedication` (espejo de `deleteDrug`), `DeleteMedicationModal.tsx` (nuevo), `NewMedicationForm.tsx` (parity Código+Laboratorio + zona de eliminación), `MedicamentosView.tsx` (kebab en `CatalogoRow` + estado editing/deleting/toast + `KebabItem` danger), `medications.ts` (`laboratorio_id` en lectura+update).
- **Stepper + ROADMAP (PR #22):** `/impeccable layout` → `VisitStepper.tsx` (legibilidad) + `docs/#4- ROADMAP.md` (reescrito).
- **Resumen de Spira** para el Director (leyendo README/PRODUCT/ROADMAP/supabase — sin código).

## Decisiones tomadas (no re-discutir)

- **Borrado de medicamento = FÍSICO, no baja lógica.** El grafo de FKs a `medications` es todo `ON DELETE restrict` salvo `medication_codes` (cascade) → la base ya garantiza integridad; solo se borra un alta nunca-usada. (Decisión del `/plan-eng-review`, Opción B: ceremonia por tipeo del mock.)
- **Copy honesto** del modal de borrado (no promete "se conserva el historial" — falso bajo borrado físico).
- **Parity de campos en edición:** Código de barras + Laboratorio visibles en el modal de edición; el código se reconcilia aparte (`linkCode`/`updateCode`), vaciarlo NO lo borra.
- **Stepper:** el centrado (grid `1fr·auto·1fr`) es del Director (16c791c); lo nuevo fue la claridad de los puntos.
- **ROADMAP como arco estratégico:** delega el detalle fino a las fuentes vivas (índice de migraciones, tags, bitácora) para no volver a desfasarse.

## PRÓXIMO PASO (próxima sesión)

1. **Release `v0.20.0`** ✅ — cortado al cierre de esta jornada (bump + tag + push). Nada pendiente acá.
2. **⚠️ QA logueada del popup "Atención médica" (PR #20 del 29/07)** — sigue sin confirmar si se corrió (checklist en el cuerpo de la #20).
3. **Panel de gerencia** (asignar roles por clicks) — único sub-ítem abierto del Paso 2.
4. Heredados (sin confirmar estado): sembrar plantilla de checklist en prod, **Track ve "Medicamento" genérico** (Fase 3 de la medicación del paciente), tablero logueado como coordinadora, viewer read-only de procedimientos, **rotar la contraseña de QA**.

## Cómo verificar al retomar

`git fetch` · `git status` (main, limpio) · `npm run typecheck` (verde) · `npm run build` (verde) · `node scripts/check-migraciones.mjs` (sin migración nueva; última = 0064).

- **Medicamentos:** Pharma → Medicamentos → **Catálogo** → kebab de una fila → **Editar** (aparecen Código + Laboratorio) / **Eliminar del catálogo** (pide tipear `ELIMINAR`; un medicamento en uso da "está en uso" y no se borra).
- **Stepper:** Track → **Visitas del día** → las pelotitas del medio se ven claras (puntos pendientes con aro visible) y centradas.
