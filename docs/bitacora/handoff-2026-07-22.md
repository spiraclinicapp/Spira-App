# Handoff · 2026-07-22 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-21.md`](./handoff-2026-07-21.md). Narrativa
del día: [`2026-07-22.md`](./2026-07-22.md).

> Hilo de esta entrada: **carga de visitas históricas** (estimada + real, migración 0062, PR #17).
> El hilo de procedimientos (PR #16) cerró en el handoff del 21.

## Dónde estás

- **Histórico de visitas de 4 protocolos cargado en PROD.** 21 personas, **29 inscripciones**,
  cronograma de los 4 protocolos, **262 visitas con estimada + real**, todas marcadas terminadas.
  El front ahora muestra estimada + real + desvío (antes coalescía y la real quedaba invisible).
- **Migración `0062`** (`enrollments.ivrs_code`) **aplicada en prod** y registrada en
  `supabase/README.md`. **Última aplicada = 0062.**
- **PR #17 creada y pendiente de merge del Director** (no hay self-merge). Rama
  `feat/carga-visitas-historicas`, 17 commits, rebaseada sobre el main actual, verde.
  https://github.com/spiraclinicapp/Spira-App/pull/17
- **El SQL de carga se corrió en prod** (dry-run → commit por el Director). Vive en `out/` **local
  y gitignored** (tiene PII); al repo fue solo la lógica del generador.
- **Working copy compartido:** la rama activa al cerrar es `feat/carga-visitas-historicas`. Los
  untracked son ajenos (hilo de checklist + `_cleanup_beta_patients.sql`).
- Sin release/bump: `package.json` sigue en `0.19.0`.

## Lo hecho esta jornada

- **Diseño → spec → plan → ejecución inline** (skills brainstorming / writing-plans / executing-plans).
  Spec y plan en `docs/carga-visitas-historicas/`. Bitácora §1–§3.
- **Migración 0062** + **generador Node** en `supabase/scripts/carga-visitas-historicas/`
  (`parse-xlsx.mjs`, `mapeo.mjs`, `correcciones.mjs`, `generar.mjs`) + **front** (Fase C:
  `PdFullSchedule.tsx`, `VisitDetail.tsx`, `ProtocolDetailView.tsx`, helpers en `lib/visits.ts`).
- **Carga en prod iterativa**: dry-run + assert + diagnóstico atajaron 6 gotchas del schema sin
  cargar nada mal (Bitácora §4). Front verificado en vivo por el Director.

## Decisiones tomadas (no re-discutir)

- **Estimada = la genera Spira** (híbrido: divergencias al informe, no se fuerzan).
- **Doble IVRS → `enrollments.ivrs_code` (0062):** persona única (`patients`), N° de sujeto por
  inscripción. 21 personas / 29 inscripciones.
- **Offsets DERIVADOS del Excel** (mediana estimada−ancla). Anclas: CEREN-2 → V2 (RANDOMIZACIÓN),
  ACT18301/THESEUS → V3 (INICIO), LTS → V1. Preinclusión = screening.
- **Códigos de protocolo:** Excel→prod = CEREN-2→**EFC18419**, THESEUS→**EFC18244**, ACT18301→ACT18301,
  LTS 17231→**LTS17231** (el Director creó LTS en prod hoy). En `MAPEO_CODIGO_PROTOCOLO`.
- **Ventanas ±3** (mías); **ACT usa ±7** (su cronograma pre-existente).
- **Screen-failure → enrollment `'discontinuado'`** (el enum no tiene `'inactivo'`).
- **ACT tenía V1/V2/V3 `date_mode='libre'`** → no las autogenera el trigger; se insertan directo
  (paso 3b) **sin tocar los defs**.
- **PII gitignored** (Excel/SQL/informe en `out/`); al repo solo la lógica.
- **Visitas históricas terminadas** (`arrived/ready/left = real_date`).

## PRÓXIMO PASO (próxima sesión)

1. **Mergear la PR #17** (lo hace el Director). CI: build + `check-migraciones`.
2. **Limpiar las 2 inscripciones de prueba de ACT** si eran de test (suman 14 reales al control). Su
   decisión.
3. **Follow-ups de la Fase C (opcionales):** "Adherencia" window-aware (hoy es conteo, no % en
   ventana); enum `visit_type` sin `'domicilio'` (nota `b` de LTS, visita al centro vs domicilio).
4. **Regenerar/re-correr** el generador si aparece un ajuste de datos: copiar el Excel a
   `supabase/scripts/carga-visitas-historicas/`, `node generar.mjs "<xlsx>" --build ./out`, y correr
   `out/carga-visitas-historicas.sql` en dry-run. Las correcciones viven en `correcciones.mjs`.
5. Pendientes heredados (handoff 07-21): tablero logueado como coordinadora, Track no ve el nombre del
   medicamento (Fase 3), rotar la contraseña de QA.

## Cómo verificar al retomar

`git fetch` · `git status` · `npm run typecheck` (verde) · `node scripts/check-migraciones.mjs`
(62 migraciones, al día). La 0062 ya está en prod.

Para ver la feature: **Track → Pacientes → abrir un protocolo (CEREN-2/ACT18301/THESEUS/LTS17231) →
ficha de un paciente**. En el cronograma y el detalle de visita, las atendidas muestran **Estimada +
Real + desvío** (ej. `Est 12/01 · Real 13/01 (+1 d)`) y marca de fuera-de-ventana. El generador es
reproducible en `supabase/scripts/carga-visitas-historicas/` (README con el paso a paso); el Excel y
la salida son **locales/gitignored**.

---

# Hilo paralelo — Checklist con reporte + edición en el modal (migración 0063, PR #18 MERGEADA)

> Segundo hilo del 22. Narrativa: [`2026-07-22.md`](./2026-07-22.md) (sección "Hilo paralelo").
> **Termina con una pregunta de alcance SIN RESOLVER — leer el PRÓXIMO PASO 1.**

## Dónde estás

- **Feature de reporte en el checklist SHIPEADA:** PR #18 **mergeada a main** (`0ca08ef`). Migración
  **0063 aplicada en prod (2026-07-22)**, registrada en README. **Última aplicada = 0063.** Sin bump
  (`0.19.0`).
- Qué hace: ítem de checklist con `has_report` + ETA → estado "reporte listo" **aparte del tilde**
  (`checklist_report_ready`) → **alerta persistente** (`v_report_alerts`) + edición inline
  **override-por-visita**. Configuración del tipo en Plantillas (Configuración → Protocolos y plantillas).
- **Verificado en preview:** el schema de la 0063 calza exacto con el código (consultado con el cliente
  autenticado, sin tocar datos); la app corre limpia.
- **PERO: el checklist está SIN SEMBRAR en prod** — **0 plantillas / 0 ítems / 0 materializados** → la
  feature no se ve en ninguna visita hasta que gerencia cargue una plantilla de checklist.

## ⚠️ PRÓXIMO PASO (lo PRIMERO de la próxima sesión)

1. **RESOLVER: ¿superficie correcta?** Al cerrar, el Director mostró el modal de **Procedimientos**
   (Cronograma, feature 0061) y preguntó cómo editarlo + marcar "requiere reporte". Su pedido original
   —"editar para no eliminar"— **calza con Procedimientos, NO con el Checklist** donde se implementó.
   **Preguntar al Director:** ¿el reporte + edición va en **Procedimientos**, en el **Checklist** (lo
   shipeado), o en **ambos**?
   - Si **Procedimientos:** llevar `has_report`/`report_eta` + edición inline al `VisitProceduresModal`
     y al modelo `procedures`/`protocol_activities` (feature 0061). La 0063 (checklist) queda como base
     reutilizable, o se deja inerte.
   - Si **Checklist:** sembrar una plantilla de checklist en prod (decisión de gerencia) para que se vea.
2. **Rotar la contraseña de QA** (apareció en el transcript al leer el archivo de creds).

## Cómo verificar al retomar

`git fetch` · `git status` · `npm run typecheck` · `node scripts/check-migraciones.mjs` (**63
migraciones, última = 0063**). Para ver el estado del checklist en prod: en la consola del preview,
`import('/src/lib/supabase.ts').then(m => m.supabase.from('checklist_templates').select('id'))` → hoy
devuelve **0 filas** (sin plantilla). El worktree `feat+checklist-reportes` sigue en `.claude/worktrees/`
(branch ya mergeado → se puede limpiar con `git worktree remove`).
