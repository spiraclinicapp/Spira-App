# Handoff · 2026-07-12 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-10.md`](./handoff-2026-07-10.md). Narrativa
del día: [`2026-07-12.md`](./2026-07-12.md).

## Dónde estás

- **`main` en prod incluye la PR #6** (comentarios de visita, migración **0048** aplicada).
- **Migraciones en prod: 0048 y 0049**, ambas aplicadas por el Director hoy. **0049 falta el
  merge de su PR** (código ya escrito, verificado y pusheado — ver más abajo).
- **PR del rediseño de "Para ver médico" pendiente de abrir/mergear** — el código está commiteado
  en una rama nueva off `origin/main` (no la vieja `feat/menu-colapsar-nav`, que ya está mergeada).
- Sin trackear / de actividad concurrente (no míos, no tocados hoy): `CLAUDE.md` (contenido
  sustancial sin commitear), `.gitignore`, `src/lib/auth.tsx`, `.claude/settings.json`,
  `.github/workflows/ci.yml`, `.claude/hooks/`, `.claude/skills/`, `scripts/*.mjs`, las carpetas de
  handoffs/informe borradas.

## Lo hecho esta jornada

**Comentarios de visita (0048):** shipeado y en prod (PR #6). Drawer deslizable + hilo de
comentarios, reusado en la cola del médico y en `VisitDetail`. Detalle en §2 de la bitácora.

**Rediseño "Para ver médico" (0049):** idéntico a las fotos de referencia del Director, con dato
HONESTO en vez de inventado (reloj de espera real vía `wants_doctor_at`, procedencia vía
`doctor_marked_by`, demográficos reales). Migración con revisión adversarial (cazó un blocker real:
`v_patient_visits` sin `revoke` de escritura, agujero heredado desde 0023, cerrado en el SQL).
Frontend reescrito completo (`DoctorQueueView` + 4 componentes nuevos). QA en vivo encontró y
arregló un bug real de CSS (`var(--x)NN` inválido en tintes de fondo). Polish de espaciado extendió
el shell (`ViewHeader.content`, aditivo) para que el filtro+fecha compartan fila con el título,
igual que la referencia. Detalle completo en §3-4 de la bitácora.

## Decisiones tomadas (no re-discutir)

- **Autor de comentarios: desnormalizado** (`author_name`/`author_role` snapshot en la fila, NO
  join a `users`) — la RLS de `users` solo expone la fila propia; un join ocultaría comentarios
  ajenos en silencio. Mismo patrón para `doctor_marked_by` en la 0049.
- **`wants_doctor_at` se estampa SOLO en la transición false→true**, en los DOS RPC de marcado
  (`mark_wants_doctor` y `toggle_wants_doctor`) — un re-marcado no reinicia el reloj de espera.
- **Umbrales del `WaitBadge` recalibrados a horas** (<1h/1-3h/≥3h), no a los 30/60min del prototipo
  original — las esperas reales de esta cola son de horas, no minutos.
- **Nombre del paciente VISIBLE como texto en "Para ver médico"** — desvío deliberado de
  `PrivacyAvatar` (que lo oculta en toda la demás Track), decisión explícita del Director, acotada
  a esta pantalla.
- **`ViewHeader.content`** (nuevo campo opcional, `src/views/types.ts`): escape hatch para que una
  vista ponga contenido rico (no solo botones simples) en la fila del título del shell. Usado hoy
  solo por `DoctorQueueView`; el resto de las vistas sigue con `actions` sin cambios.
- **Filtro por médico tratante: sacado** de "Para ver médico" (no está en la referencia).

## PRÓXIMO PASO (próxima sesión)

1. **Mergear la PR del rediseño PVM** (link abajo, cuando el Director la revise).
2. **QA cross-user de la 0048:** confirmar que un segundo coordinador ve comentarios ajenos —
   sigue siendo el test de regresión pendiente del bug de RLS que motivó la desnormalización.
3. **`CLAUDE.md` tiene contenido sustancial sin commitear** (no de esta sesión, actividad
   concurrente) — incluye, entre otras cosas, "la última aplicada va por la 0048"; hay que llevarlo
   a "0049" cuando se commitee ese archivo (quien sea que lo esté trabajando).
4. **Épicas heredadas** (siguen abiertas): Dispensación real en el detalle de visita; informe de
   facturación al sponsor; a11y de inertizar el fondo con overlay abierto; portar el foco
   atrapado/inicial/retorno del `Drawer` al `Modal` genérico.

## Cómo verificar al retomar

`git fetch` · `npm run build` (verde). En vivo (login del Director, nunca credenciales por chat):
**Track → Para ver médico** — el filtro y la fecha deben estar en la MISMA fila que el título; una
fila con `wants_doctor_at` real muestra el reloj con color por umbral (no "—"); "Marcar visto" es
sólido en el teal del módulo. **Track → cualquier visita → Abrir** — el panel "Comentarios" del
detalle muestra el hilo real (no "en construcción"). Gotchas del preview (doc oculto bloquea
también los clicks, no solo los screenshots; bypass documentado) en la memoria `browse-bloqueado-wdac`.
