# Handoff · 2026-06-29 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-06-28.md`](./handoff-2026-06-28.md). Narrativa del día: [`2026-06-29.md`](./2026-06-29.md).

## Dónde estás

- Rama **`feat/pharma-recepcion-wizard`**, HEAD `cf81885`. **`typecheck` verde total.**
- **NO está mergeada** a `main`: se eligió "dejar la rama como está" hasta que el Director **verifique
  en el navegador** (detrás del login). Tras verificar → merge a `main` (o PR) → taggear.
- Migraciones en prod: **hasta la 0036** (0035 + 0036 aplicadas a mano hoy).
- `main` tiene el setup de impeccable (`ca2c8a0`); release vigente `v0.8.0`.

## Lo hecho esta jornada

- **Setup de diseño impeccable** (en `main`, `ca2c8a0`): `PRODUCT.md` + `DESIGN.md` "Sereno" + sidecar
  + live config + puntero en `CLAUDE.md`. gstack proactivo on.
- **Recepción tipada + wizard** (en rama): wizard de pantalla propia de 4 pasos, dos ámbitos
  (**Farmacia Protocolo** + **Farmacia Ambulatoria**), tipo y stock segregados por ámbito. Construido
  **task-por-task con subagentes** (10 tareas + reviews + fixes), **review final de rama** (cazó el
  silent-drop por salto de stepper, corregido), y **pulido** de tu verificación (foco suave sin borde
  verde + cerrar el desplegable al clickear afuera).

## PRÓXIMO PASO (próxima sesión)

1. **Verificación funcional en navegador** (pharma-leader, `TEST-*`), ambas ramas:
   - **Protocolo:** ámbito Protocolo + elegir protocolo → wizard → escanear/contar (un código conocido
     suma 1; `TEST-9999` desconocido → panel ámbar → asociar; "agregar a mano" typeahead) → lotes
     (`TEST-LOTE`, probar "dividir" y que el resto cierre) → crear → **Verificar** → el stock del
     protocolo sube. **Probar el path del review:** entrar al Paso 2, volver al 1, escanear un
     medicamento NUEVO, saltar con el stepper al 3 → NO se pierde.
   - **Ambulatoria:** ámbito Ambulatoria (sin protocolo) → recibir cualquier medicamento → crear →
     verificar → confirmar en la base que el lote quedó con **`protocol_id = null`** (y aparece en
     `v_medication_stock` con `tipo='ambulatoria'`).
   - Borrar los `TEST-*` creados.
2. Si OK → **merge a `main`** (o PR) + taggear (¿`v0.9.0`?).
3. **Tajadas de Pharma pendientes** (orden sugerido):
   - **Producto de Investigación (IP)** — fundacional, **bloqueado: cerrar dominio con Pablo** (número
     de kit, dispensación por kit, código único/repetido, nombre cegado, lote por unidad). Ver
     [`2026-06-28-pharma-modelo-ip-vs-base-design.md`](../superpowers/specs/2026-06-28-pharma-modelo-ip-vs-base-design.md).
   - **Tajada 1b** — medicación de protocolo / **DataMatrix GS1**.
   - **Tajada 2** — **dispensación** (+ DataMatrix del sponsor).
   - **Follow-up** — vista de **stock de ambulatoria** (la data ya queda; falta UI).
4. **Post-merge (anotado):** `pharmaErrorMessage` mapea `23514` a texto genérico y descarta el mensaje
   claro del DB (pre-existente 0032, helper compartido) → passthrough.
5. **Heredados:** deploy a Vercel (con Pablo), QA de Track.

## Artefactos

- Spec: `docs/superpowers/specs/2026-06-29-pharma-recepcion-tipos-design.md` (datos + flujo + review de diseño).
- Plan: `docs/superpowers/plans/2026-06-29-pharma-recepcion-tipos.md` (10 tareas).
- Ledger SDD: `.superpowers/sdd/progress.md` (gitignored; estado por tarea + minors p/futuro).
- Migraciones: `supabase/migrations/0035_pharma_recepcion_tipos.sql`, `0036_pharma_v_stock_fix.sql`.
- Código nuevo: `src/views/pharma/ReceptionWizard.tsx` + `wizard/Step0Setup..Step3Summary.tsx`,
  `src/views/pharma/MedicationPicker.tsx`, `src/components/Stepper.tsx`, `SegmentedControl.tsx`.
  Modificados: `src/data/pharma/receptions.ts`, `src/views/pharma/RecepcionView.tsx`. Borrado: `NewReceptionModal.tsx`.
- Contexto de diseño (en main): `PRODUCT.md`, `DESIGN.md`, `.impeccable/`.

## Cómo verificar al retomar

`git checkout feat/pharma-recepcion-wizard` · `git log --oneline main..HEAD | cat` · `npm run typecheck`
(verde) · leer este handoff + `2026-06-29.md`. Las 0035/0036 ya están en prod. Para ver el wizard:
`npm run dev`, login como pharma-leader, Pharma → Recepción → elegir ámbito → "Nueva recepción".
