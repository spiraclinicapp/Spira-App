# Handoff · 2026-08-24 (fin de jornada)

> Handoff anterior: [`handoff-2026-08-23.md`](handoff-2026-08-23.md) · Narrativa del día: [`2026-08-24.md`](2026-08-24.md)

> ✅ **TODO EN `main` (`981798a`), releases `v0.42.0` y `v0.43.0` tagueados y pusheados.** El repo quedó con **`main` y nada más**. El día tuvo **dos sesiones**: las URLs a la mañana (`v0.42.0`, § 1-9 de la bitácora) y la tarjeta de paciente a la tarde (`v0.43.0`, § 10-12). Este handoff cubre las dos; lo de la mañana está tal como se escribió, y lo de la tarde va en su propia sección más abajo.

> ✅ **Sin migraciones.** No hay nada que aplicar en la base.

> 📋 **Historia de cómo se mergeó, que dejó una lección.** Las cinco PRs se mergearon **en cadena, cada una contra su base**, y sólo `feat/urls-andamiaje` llegó a `main` (#69 y #71). Como las #72, #73 y #74 se mergearon **después** de que andamiaje ya hubiera subido, ese contenido quedó estancado en las ramas intermedias: `main` tenía la Fase A completa y la B a medias. **Y la #74 ya estaba cerrada, así que no se podía reapuntar** — una PR cerrada no se reabre. Se resolvió con una PR nueva de la punta contra `main` (la [#75](https://github.com/spiraclinicapp/Spira-App/pull/75), 27 commits), que trajo todo de una porque las ramas se habían apilado linealmente.

## Dónde estás

`main` en `981798a`, versión **`v0.43.0`**, última migración **`0092`** (sin cambios en todo el día).
`npm run build` verde: **388 tests** (307 al empezar el día).

El proyecto **"URLs de navegación" está COMPLETO y en `main`**, las nueve vistas migradas:

- **La URL es la fuente de verdad de dónde estás parado.** F5 te deja donde estabas, el botón atrás
  del navegador camina entre pantallas, y cualquier pantalla se puede compartir por link.
- **`/coordinacion/pacientes/ACT18301/03200070001`** abre la ficha de ese paciente, en Coordinación y
  en Farmacia.
- **Los códigos no distinguen mayúsculas** (`/act18301` funciona) y el código canónico se reescribe
  solo en la primera navegación.
- **El lugar va en la dirección, los filtros después del `?`**: `/farmacia/stock/catalogo?estado=vencido`,
  `/farmacia/recepcion/nueva`.
- **El botón atrás no descarta una recepción a medio cargar**: pregunta con el mismo cartel que el
  botón Cancelar.
- **El nombre del paciente no va a la URL ni al título de la pestaña.** El IVRS sí (decisión
  ratificada); `?buscar=` puede llevar un apellido, también ratificado.

## Lo hecho esta jornada

| Fase | PR | Qué |
|---|---|---|
| A | [#69](https://github.com/spiraclinicapp/Spira-App/pull/69) | `router.ts` (puro, con tests) + `useUrlState`. Andamiaje, invisible. Incluye el ajuste de copy de Alertas ("Pendientes y por vencer"). |
| B | [#70](https://github.com/spiraclinicapp/Spira-App/pull/70) | El shell deriva módulo y submódulo de la URL. Pantalla serena para ruta inexistente o sin acceso. Título de pestaña sin PII. |
| C | [#72](https://github.com/spiraclinicapp/Spira-App/pull/72) | Pacientes: protocolo, ficha, filtros. El pedido original del Director. |
| D | [#73](https://github.com/spiraclinicapp/Spira-App/pull/73) | Visitas del día, Para ver médico, Alertas. |
| E | [#74](https://github.com/spiraclinicapp/Spira-App/pull/74) | Dispensaciones, Stock, Recepción, Estadísticas. Más los cuatro pedidos del final. |

Y en la **segunda sesión**, ya con `v0.42.0` en la calle:

| | PR | Qué |
|---|---|---|
| — | [#76](https://github.com/spiraclinicapp/Spira-App/pull/76) | **Pacientes: se invierten los dos gestos de la tarjeta.** Pinchar la tarjeta abre la ficha; el recorrido de visitas pasó a un botón **"Resumen"** que lo despliega sin sacarte de la lista. Release `v0.43.0`. |

## Segunda sesión · La tarjeta de paciente cambia de gesto

Un solo archivo (`src/views/track/PdPatientRow.tsx`, +36/−19), que es la fila reusada por las **dos**
listas de pacientes: el tablero de un protocolo y "Todos los pacientes".

| | Antes | Ahora |
|---|---|---|
| Click en la tarjeta | desplegaba el recorrido | **abre la ficha del paciente** |
| Botón de la derecha | `Abrir ficha →` | **`Resumen ⌄`** → despliega el recorrido |
| Chevron suelto al lado del botón | sí | se mudó **adentro** del botón, y rota al abrir |

Dos cosas que no se ven en la pantalla y conviene no revertir sin querer:

- **El nombre del paciente es un `.spira-textlink`.** No es decoración: al mudar el gesto de un
  `<button>` a la tarjeta (un `<div>` con `onClick`), la ficha se quedaba sin camino de **teclado**.
  La fila no se convirtió en `<button>` porque adentro vive el de "Resumen" y anidar interactivos es
  inválido. Es el mismo patrón del encabezado de visita.
- **Sin visitas, "Resumen" no se dibuja** (guarda `expandable`, la misma que antes escondía el
  chevron) y la tarjeta igual abre la ficha. **Ese caso no se pudo probar en vivo**: no hay ningún
  paciente sin visitas en los datos actuales.

## Decisiones tomadas (no re-discutir)

1. **Capa propia sobre la History API, no react-router.** Revierte el spec del 2026-06-17. El trabajo
   real era decidir qué estado es direccionable; eso ningún router lo resuelve.
2. **El IVRS va en la URL, el nombre no.** También revierte junio: aquella premisa se apoyaba en el
   `PrivacyAvatar`, eliminado el 2026-08-04.
3. **`?buscar=` puede llevar un apellido** y se decidió dejarlo. Es la única vía por la que un nombre
   llega a la barra, y no es el identificador de la pantalla sino texto que el usuario tipeó.
4. **Los códigos no distinguen mayúsculas**, con match exacto primero y flexible **sólo si es único**
   (el `unique` de la base sí distingue caja).
5. **El lugar va en el path, los filtros en el query.**
6. **Los formularios a medio llenar NO se restauran.** `/farmacia/recepcion/nueva` dice que el wizard
   está abierto; si recargás, se abre **vacío**. Una dirección que promete devolverte una recepción a
   medio cargar y te entrega un formulario vacío miente.
7. **Agenda no lleva ruta.** Su entrada está comentada en `MODULES` desde que salió del menú; el día
   que se descomente, la ruta se habilita sola.
8. **Cerrar una entidad reemplaza, no apila** — si no, el atrás la reabre.
9. *(segunda sesión)* **En la tarjeta de paciente, el gesto grande lleva a la ficha.** El destino de
   una tarjeta que muestra un paciente es el paciente; el recorrido de visitas es lo que se pide
   aparte, con el botón "Resumen". Pedido explícito del Director.

## QA hecho al cierre

**El logout, probado en vivo desde el menú real y sobre el peor caso** — la ficha de una paciente, con
protocolo e IVRS en la barra:

| | Antes | Después |
|---|---|---|
| URL | `/coordinacion/pacientes/ACT18301/03200070001` | `/` |
| Título de la pestaña | `Spira — Pacientes` | `Spira` |
| Pantalla | la ficha | Login |

Los tres puntos que importaban:

- **La URL salta a `/` en el instante del click**, antes de que termine el `signOut()`. Si esperara al
  round-trip de red, la barra seguiría mostrando el IVRS todo ese rato.
- **El atrás no vuelve a la ficha**: sigue en `/` y en Login. El `replaceUrl` no dejó entrada en el
  historial.
- **No quedó nada del paciente**: ni el IVRS en la barra, ni el nombre en pantalla, ni el submódulo en
  el título. En una máquina compartida de clínica, eso era el punto.

**Deep link deslogueado:** `/farmacia/stock/catalogo?estado=vencido` sin sesión muestra el Login
**conservando la dirección completa, con el filtro incluido**. La otra mitad —que al entrar te lleve
ahí— se ve al loguearse: la URL ya queda esperando en su lugar.

> ⚠️ **La sesión del preview quedó cerrada** por este QA. ~~Hay que loguearse de nuevo antes del
> próximo.~~ **Hecho en la segunda sesión**: el Director se logueó a mano en el preview y quedó
> abierta. Si al retomar volvió a caducar, es lo mismo — el agente no ingresa contraseñas.

## PRÓXIMO PASO (próxima sesión)

1. **Confirmar el deploy en Vercel** y hacer una recorrida en prod (no sólo en el preview). Son
   **dos** deploys para mirar: el de las URLs y el de la tarjeta de paciente.
2. **La otra mitad del deep link deslogueado**: loguearse teniendo una URL profunda pendiente y
   confirmar que entra ahí y no en Inicio.
3. Lo que quedó del 23: el **QA del tablero de Reportes pendientes** —el tilde de "realizado" nunca se
   probó, porque escribe sobre visitas reales— y el **resumen de Coordinación**
   (`docs/design_handoff_resumen_track/`), que es el próximo trabajo grande y viene postergado
   desde el 19.

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main   # 981798a, tags v0.42.0 y v0.43.0
npm run build                                    # 388 tests
```

En el navegador (el preview corre en el 5250). **Ojo: la sesión quedó cerrada por el QA del logout**,
así que el primer paso es loguearse:

1. `/coordinacion/pacientes/act18301/03200070001` → abre la ficha; las migas muestran `ACT18301`.
2. `/coordinacion/pacientes/efc18244/03200070001` → **"Esa dirección no existe"** (el paciente no es
   de ese protocolo). Ese es el Critical clínico del día; si abre la ficha, algo se revirtió.
3. Filtrá Visitas y apretá **atrás**: sale de la pantalla en un paso, no deshace filtro por filtro.
4. `/lab/muestras` → "No tenés acceso", **con el marco y sin listar los submódulos de Lab**.
5. Abrí una recepción nueva, elegí tipo y coordinador, y apretá **atrás**: tiene que preguntar
   "¿Descartar la recepción en curso?" y, al cancelar, dejarte con los datos intactos.
6. *(segunda sesión)* En `/coordinacion/pacientes/todos`, **pinchá una tarjeta en cualquier lado que
   no sea el botón**: tiene que abrir la ficha de ese paciente. Después apretá **"Resumen"**: el
   recorrido se despliega, el chevron rota y **la URL no se mueve**. Y tabulá hasta el nombre de un
   paciente: se subraya y abre su ficha.

> **Ojo al verificar en el preview:** `requestAnimationFrame` no corre (documento oculto) y cuelga la
> herramienta 30 s. Y la cuenta de QA tiene los cinco módulos, así que **no reproduce las fallas de
> permisos** — para eso usá un módulo `proximamente` (`/lab/...`), que `isAllowed` rechaza sin
> importar el rol.
