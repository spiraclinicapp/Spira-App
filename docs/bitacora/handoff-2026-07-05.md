# Handoff · 2026-07-05 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-04.md`](./handoff-2026-07-04.md). Narrativa del día: [`2026-07-05.md`](./2026-07-05.md).

## Dónde estás

- **`main` = `v0.13.0`** (bump + tag), **pusheado a origin** (github.com/spiraclinicapp/Spira-App). `npm run typecheck` verde.
- **Migraciones en prod: hasta la `0043`** (0040 de ayer + 0041/0042/0043 de hoy, aplicadas a mano y verificadas por el Director). Índice al día en `supabase/README.md`. CLAUDE.md → "última 0043".
- **Falta el QA humano logueado** del flujo completo: yo verifiqué typecheck + boot, pero **no el render logueado** (preview = sesión aparte, sin credenciales).
- Sin trackear (a propósito, decisión del Director): `Mejora visual Medicamentos/`, `Mejora visual formulario Medicamentos/`, `Mejora visual Buscador Core/` (handoffs de diseño, **fuera del repo**).

## Lo hecho esta jornada (release v0.13.0)

Rediseño completo de la **vista Medicamentos** de Pharma + **formulario "Registrar medicación"**, vía `/plan-eng-review` + `/plan-design-review` (alcance **Medium**), review adversarial del diff, e iteración fina. Migraciones **0041** (vista por-lote `v_medication_lots_detail`), **0042** (índice único anti-duplicado), **0043** (columna `clase`). Componentes nuevos: `SearchableSelect` (desplegable con buscador + crear/eliminar con confirmación), `CodigoModal`; `Modal` enriquecido (icon/accent). Ver la bitácora para el detalle.

## Decisiones de dominio tomadas (no re-discutir)

- **Alcance Medium:** menú de apartado (Protocolo/Ambulatoria/Catálogo) + filas **por lote**; el **IP se mantiene como tarjeta macro** por protocolo (NO se reabre el modelo IP-vs-base diferido).
- **Método = la Presentación (`unit`) relabeleada** (mismo campo; vacío → `'Sin especificar'` por el NOT NULL). **Monodroga opcional**. **Clase/Indicación** = columna nueva (0043).
- **Anti-duplicado del catálogo:** clave `(nombre normalizado, presentación)` → índice único 0042. Front avisa "ya existe" + Editar el existente.
- **Stock por LOTE** (no total del medicamento), umbral fijo 5. **Estado de vencimiento = ícono de forma + color** (WCAG 1.4.1, nunca color-solo). **Desktop-first** (reflow de tablet diferido).
- **Foco de controles = sombra suave, NUNCA outline/borde verde** (reafirmado hoy por el Director).
- **"Agregar nuevo" abre un panel con confirmación; eliminar con confirmación** (catálogos con registro real: monodroga, laboratorio).

## PRÓXIMO PASO (próxima sesión)

1. **QA humano logueado end-to-end** de Pharma → Medicamentos (los 3 apartados, filas por lote, kebab, stock, filtro de protocolos, búsqueda por código) y del formulario "Registrar medicación" (crear/eliminar con confirmación, anti-duplicado, editar). Probar con `TEST-*` y borrar.
2. **Diferidos del rediseño:** reflow responsive de tablet (D2); "Agregar variante comercial" operativa (hoy kebab deshabilitado); confirmar que la **alerta de stock bajo** viva en Reportes/Resumen (se sacó de Medicamentos); barcode SVG para etiquetas.
3. **Follow-ups del review adversarial:** unicidad normalizada (`citext`) en `drugs`/`laboratorios` (evitar duplicados por acento); `updateCode` apuntando a la fila puntual del código.
4. **Épicas heredadas:** informe de facturación al sponsor (bloqueado por la dispensación de Track), retoques de Track, **deploy del front (Vercel)**.

## Cómo verificar al retomar

`git checkout main` · `git log --oneline -6 | cat` (arriba el commit de v0.13.0) · `git tag` (aparece `v0.13.0`) · `npm run typecheck` (verde). Flujo nuevo: `npm run dev`, login pharma-leader, **Pharma → Medicamentos** (menú de apartado; entrá a Protocolo y Ambulatoria: filas por lote con EAN/lote/vencimiento/stock, kebab, filtro de protocolos; Catálogo global) y **"Agregar medicamento"** (dropdowns con buscador, "Agregar nuevo" con confirmación, tacho para eliminar monodroga/laboratorio, anti-duplicado).
