# Handoff · 2026-09-17 (fin de jornada)

> Handoff anterior: [`handoff-2026-09-16.md`](handoff-2026-09-16.md) · Narrativa del día: [`2026-09-17.md`](2026-09-17.md)

> 🔄 **ÚLTIMA ACTUALIZACIÓN — cierre de la sesión de bandera y feedback, mañana del 18. MANDA SOBRE TODO LO
> DE ABAJO:**
>
> - **`v0.80.0` = `main` (`4928c03`) = producción**, verificado contra el bundle de Vercel (`app: 0.80.0`). La
>   [#231](https://github.com/spiraclinicapp/Spira-App/pull/231) y la [#229](https://github.com/spiraclinicapp/Spira-App/pull/229) están mergeadas: el PRÓXIMO PASO 1 del anexo de desviaciones **ya está hecho**.
> - **Última aplicada: `0131`**, marcada en el índice y en `CLAUDE.md`. **1263 tests**; `check-migraciones` ✓ 131.
> - **Lo único abierto del día:** tres QA logueados (anexo 2, al final) y la guarda de Recepción a `0132`.

> 🔄 **ACTUALIZACIÓN — cierre de la sesión de desviaciones, madrugada del 18. MANDA SOBRE LOS AVISOS DE
> ABAJO, que se escribieron antes y quedaron viejos:**
>
> - **De la `0128` a la `0131`, todas APLICADAS en prod** — la 0131 verificada sin sesión el 18
>   (`mark_feedback_seen` existe). Las marcas del índice: la 0128 y la 0129 en la [#224](https://github.com/spiraclinicapp/Spira-App/pull/224), la 0130 en la
>   [#226](https://github.com/spiraclinicapp/Spira-App/pull/226), la 0131 en la #229 (`docs/0131-aplicada`, que también pone `CLAUDE.md` en 0131). **La
>   última aplicada es la `0131`.** El PRÓXIMO PASO de abajo («aplicar la `0128`») **ya está hecho**.
> - **Versión ≠ producción.** Encima de `v0.79.0` entraron a prod **cuatro** features: la [#220](https://github.com/spiraclinicapp/Spira-App/pull/220) (bandera de
>   estado del paciente), la [#222](https://github.com/spiraclinicapp/Spira-App/pull/222) (feedback con lugar), la [#225](https://github.com/spiraclinicapp/Spira-App/pull/225) (la bandeja de feedback) y la [#226](https://github.com/spiraclinicapp/Spira-App/pull/226)
>   (desviaciones de protocolo). **Las versiona la `v0.80.0`, en la [#231](https://github.com/spiraclinicapp/Spira-App/pull/231)** (tag ya pusheado); hasta que se
>   mergee, el bundle de Vercel sigue diciendo `0.79.0`.
> - **El worktree `unruffled-dubinsky-186253` ya no trabaja en la guarda de Recepción**: hizo las desviaciones. La
>   rama de la guarda sigue sólo local y **tiene que renumerarse a `0132`** antes de pushear. Detalle en el
>   anexo, al final.

> ⚠️ **LA `0128` ESTÁ EN `main`, DESPLEGADA Y SIN APLICAR.** Entró con la [#216](https://github.com/spiraclinicapp/Spira-App/pull/216) (reposición de corte a
> corte, parte 1: modelo, datos y migración, de otra sesión). Sondeada sin sesión el 17: `reposicion_del_periodo`
> da `PGRST202` y `pedidos_medicacion` da `PGRST205`. **Hoy no rompe nada**, porque ninguna pantalla usa esa
> capa de datos y la recepción sólo manda `p_pedido_id` cuando hay pedido. **Tiene que aplicarse ANTES de la
> parte 2** (la pantalla). La última aplicada sigue siendo la **`0127`**.

> 🟢 **EL CRONOGRAMA ESTÁ EN LA FICHA DEL ESTUDIO** ([#217](https://github.com/spiraclinicapp/Spira-App/pull/217)). Ya no es pestaña de la lista de pacientes:
> es el botón «Cronograma y procedimientos», debajo del título del estudio y antes de Sponsor, y abre un
> modal con Visitas y Procedimientos del estudio.

> ✅ **VERSIÓN = PRODUCCIÓN.** `v0.79.0` ([#218](https://github.com/spiraclinicapp/Spira-App/pull/218)) versiona la #216 y la #217. `main` en `1fde281`, verificado
> contra el bundle de Vercel.

> ⚠️ **HAY OTRA SESIÓN TRABAJANDO**: el worktree `unruffled-dubinsky-186253` tiene
> `fix/recepcion-guarda-borrado-y-renglones` sin pushear. Si cierra después de este handoff, anexa acá y a la
> bitácora del 17; no pisa.

## Dónde estás

Release **`v0.79.0`**, última migración aplicada **`0127`** (la `0128` en `main` sin aplicar, ver arriba).
`npm run build` verde: **1223 tests** (`npx vitest run --exclude ".claude/worktrees/**"` da el número
real; con los worktrees de otras sesiones el conteo se dispara).

**Todo lo que el handoff del 16 dice de los DATOS sigue valiendo** y no se repite acá: las 70 entregas sin
lotes, las visitas que quedaron abiertas a propósito, LTS17231 renumerado, la baja por estudio y los dos
«activo». Ver [`handoff-2026-09-16.md`](handoff-2026-09-16.md) § Dónde estás.

**Lo nuevo que hay que saber:**

- **El cronograma se abre desde la ficha del estudio** (`ProtocolDetailView`, estado `cronogramaAbierto`).
  El contenido es el mismo `CronogramaTab`. Lo ve sólo `canManageSchedule` (gerencia o admin de
  Coordinación), igual que la pestaña que reemplazó. La columna derecha tiene «Pacientes» y «Reportes
  pendientes», y `NavTarget.protocolTab` ya no acepta `'cronograma'`.
- **`Modal` cierra con Esc sólo el de arriba** (pila de módulo en `components/Modal.tsx`). Afecta a toda la
  app, pero sólo cambia algo con dos modales abiertos a la vez. Si escribís un modal nuevo que se abre
  desde otro, ya no hace falta cuidarlo a mano.
- **La `0128` no está aplicada.** Cualquier pantalla nueva que llame a `reposicion_del_periodo`,
  `emitir_pedido_medicacion` o a las tablas de pedidos va a fallar en producción hasta que se aplique.

## Lo hecho esta jornada

| PR | Qué |
|---|---|
| [#217](https://github.com/spiraclinicapp/Spira-App/pull/217) | «Cronograma y procedimientos» pasa de pestaña a modal en la ficha del estudio; `Modal` cierra con Esc sólo el de arriba; el vacío del tablero de reportes apunta al lugar nuevo |
| [#218](https://github.com/spiraclinicapp/Spira-App/pull/218) | Release `v0.79.0` (la #216 y la #217) y el aviso de la `0128` en el handoff del 16 |

De otra sesión, mergeada el mismo día: [#216](https://github.com/spiraclinicapp/Spira-App/pull/216) (reposición de corte a corte, parte 1).

## Decisiones tomadas (no re-discutir)

1. **El botón va entre la identidad del estudio y sus datos**, con el mismo estilo que «Exportar reporte» y
   «Editar protocolo». Es lo que pidió el Director, con la ubicación exacta.
2. **Se reusa `CronogramaTab` tal cual adentro de un `Modal`**, sin rediseñar el cronograma.
3. **Un `?tab=cronograma` viejo cae a «Pacientes»**, no abre el modal. Nadie navegaba con ese valor.
4. **Esc se arregló en el `Modal` compartido**, no sólo para el cronograma: es un defecto del contenedor,
   que aparece con cualquier par de modales.
5. **El release no marca la `0128` como aplicada.** La marca en el índice y en `CLAUDE.md` va sólo con la
   confirmación del Director.

## PRÓXIMO PASO (próxima sesión)

**Aplicar la `0128`** antes de que entre la pantalla de reposición (parte 2), coordinando con la sesión de
la #216, y ahí marcarla en `supabase/README.md` y en `CLAUDE.md`. Para confirmar que quedó aplicada sin
sesión: la misma sonda tiene que **dejar de dar `PGRST202`** (según los permisos de la función, va a dar
`42501` o datos; cualquiera de los dos prueba que existe).

Después, **mirar en pantalla la `v0.77.1`**, que sigue sin QA logueado desde el 16: abrir una visita
atendida y confirmar que «Fecha realizada» queda sola y que «Fin de atención» sigue con su hora.

Y lo que ya estaba, por orden de valor (detalle en el handoff del 16):

1. **El desenlace del IP en una visita cerrada** dice «Sin constancia cargada.»: describe un papel, no un
   desenlace.
2. **ENDURA (222714) V17 a V28 no tienen procedimientos**: falta que el Director diga qué lleva cada una.
3. **El IVRS del estudio madre en Farmacia** (cajón, kanban, habilitación, comprobante).
4. **Los reportes de Quiroga (`4022001`) y Garay (`4022016`)** en Victorion.
5. **La revisión a mano de ACT18301, ENDURA y LTS17231** (consulta 5 de
   `supabase/scripts/verificacion-carga-2026-09-15.sql`).
6. **Un test que cruce `excluir` con un rechazo vigente** en `historialPlegadoModel`.
7. **La píldora de reportes ROJA con un dato real**: todavía no hay reportes vencidos.
8. **Esc con TRES modales** (cronograma › «Procedimientos · V3» › «Editar procedimiento»): cubierto por
   construcción, no mirado.

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -3          # v0.79.0 en main (1fde281) o el cierre del 17 encima
npx vitest run --exclude ".claude/worktrees/**"   # 1223
```

**El cronograma, en pantalla:** abrir **ACT18301** en «Estudios y pacientes». Debajo de «AIRLYMPUS», el
botón «Cronograma y procedimientos» abre el modal. Adentro, «Procedimientos de la visita» de la V1 abre un
segundo modal encima: el primer Esc cierra sólo ése y el segundo cierra el cronograma. La columna derecha
tiene sólo «Pacientes» y «Reportes pendientes».

**La `0128`, sin sesión:** `POST /rest/v1/rpc/reposicion_del_periodo` con `p_desde`, `p_hasta` y
`p_protocol_id`. Hoy da `PGRST202`; aplicada deja de darlo (`42501` o datos, según los permisos).

**Limpieza:** `.claude/worktrees/cronograma-en-el-estudio` tiene la rama `docs/cierre-2026-09-17` y es donde
corrió esta sesión. **Se borra después de cerrar la sesión**, nunca desde adentro: desde adentro deja una
carpeta vacía que la próxima sesión confunde con la copia compartida. `wt-reposicion` y
`unruffled-dubinsky-186253` son de otras sesiones.

---

## Anexo · Desviaciones de protocolo ([#226](https://github.com/spiraclinicapp/Spira-App/pull/226))

> Narrativa: [`2026-09-17.md`](2026-09-17.md), § Anexo. Spec: [`docs/plan-desviaciones-de-protocolo.md`](../plan-desviaciones-de-protocolo.md).

### Dónde estás (lo que cambia respecto de arriba)

La #226 está en producción, verificado contra el bundle de Vercel. Después entraron la #227 y la #225
(`main` en `4f91ba1` al cerrar). Última migración aplicada: **`0131`** (la de la #225). **1249 tests** con la
#226 (`npx vitest run --exclude ".claude/worktrees/**"`); la #225 suma los suyos.

- **Una ventana vencida ahora tiene salida:** «Documentar desviación» en su ítem de Pendientes, con motivo de
  catálogo y explicación obligatoria. La visita no cambia de estado; sale de la lista porque ya está
  explicada. Se lee en el panel «Desviaciones» (gemelo de «Descartados») y en una pastilla del encabezado
  de la visita. Tabla `protocol_deviations`, RPC `record_protocol_deviation` (0130).
- **Las vencidas de inscripciones cerradas ya no piden acción.** La 0127 las conserva al cerrar (son
  evidencia); ahora quedan en el panel como «sin documentar». El filtro está en `data/activeAlertsFilter.ts`.
- **La señal «lo archivado cambió» vive en `data/alertSignal.ts`** y la comparten descartes y desviaciones.
  Si algo nuevo archiva alertas, **tiene que usar ésa**: con un contador propio, la campana se queda con el
  número viejo.
- **Hoy hay cero ventanas vencidas en prod**, así que en pantalla el botón no aparece hasta que se venza una.

### Lo hecho

| PR | Qué |
|---|---|
| [#226](https://github.com/spiraclinicapp/Spira-App/pull/226) | Documentar la desviación de protocolo de una ventana vencida: migración `0130`, capa de datos con tests, modal, botón, panel y marca en la visita; las vencidas de inscripciones cerradas salen de la lista activa |

De otras sesiones, el mismo día y ya en `main`: #220, #221-#223 (feedback con lugar y su plan), #224 (marcas de
0128 y 0129), #225 (la bandeja de feedback, con la 0131), #227 (el índice de migraciones en su propio job de
CI). Abierta al cierre: #229 (la marca de la 0131 y `CLAUDE.md`).

### Decisiones tomadas (no re-discutir)

1. **La ventana vencida ES un pendiente; lo que le faltaba era una salida.** No se muda a la campana: la
   campana ya muestra lo mismo, y mudarla habría mudado el sedimento intacto.
2. **Documentar ≠ descartar, y conviven.** Descartar dice «no correspondía»; documentar dice «ocurrió, y acá
   está el porqué».
3. **Tabla propia**, no un motivo más de `alert_dismissals`: el listado de desviaciones es lo que pide un monitor.
4. **Seis motivos, explicación obligatoria SIEMPRE.** Se descartó «el paciente salió del estudio»: no explica
   por qué se venció la visita.
5. **Inscripción cerrada ⇒ sus vencidas salen de la lista activa**, sin borrarse. El filtro va por
   EXCLUSIÓN de `completado`/`discontinuado`: `screening` es una inscripción abierta.
6. **La visita hecha FUERA de ventana queda fuera de alcance** (misma desviación, otro flujo): `TODOS.md`, P2.
7. **Numeración:** 0129 = `feedback_lugar`, 0130 = desviaciones, 0131 = `feedback_visto`
   (`feat/feedback-bandeja`). La limpieza de Reposición toma el número libre cuando se escriba.

### PRÓXIMO PASO

1. **Mergear la [#231](https://github.com/spiraclinicapp/Spira-App/pull/231)** (release `v0.80.0`, que versiona la #220, la #222, la #225 y la #226). El tag
   ya está en `origin`. Después, confirmar que el bundle de `spira-app.vercel.app` diga `0.80.0`.
2. **La guarda de Recepción** (`fix/recepcion-guarda-borrado-y-renglones`, sólo local, `282ec8f`): renumerar
   su `0129_recepcion_guarda_borrado_y_renglones.sql` a **`0132`**, rebasear sobre `main` y abrir la PR.
   Nada más cambió en ella.

### Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main              # 4f91ba1 o algo encima
npx vitest run --exclude ".claude/worktrees/**"            # en verde (1249 con la #226, más los de la #225)
node scripts/check-migraciones.mjs                          # ✓ 131 migraciones, índice al día
```

**La tabla, por la API:** con sesión, `supabase.from('protocol_deviations').select('id')` tiene que contestar
filas o vacío, no `PGRST205`. Si alguna vez contesta `PGRST205` con la migración aplicada, es el cache de
PostgREST: `notify pgrst, 'reload schema';` en el editor.

**Limpieza:** el worktree `unruffled-dubinsky-186253` queda con la rama del cierre
(`docs/cierre-2026-09-17-desviaciones`). Se borra después de cerrar esta sesión, no desde adentro. Los datos
`TEST-DESV` que se crearon para verificar contra prod **ya se borraron por id**; no queda ninguno.

---

## Anexo 2 · Bandera de estado y feedback con lugar ([#220](https://github.com/spiraclinicapp/Spira-App/pull/220) a [#229](https://github.com/spiraclinicapp/Spira-App/pull/229))

> Narrativa: [`2026-09-17.md`](2026-09-17.md), § Anexo 2. Spec: [`docs/superpowers/specs/2026-09-17-feedback-con-lugar-design.md`](../superpowers/specs/2026-09-17-feedback-con-lugar-design.md).

### Dónde estás (lo que cambia respecto de arriba)

`main` en `4928c03` = `v0.80.0` = producción, verificado contra el bundle. Última aplicada **`0131`**, con su
marca. **1263 tests** (`npx vitest run --exclude ".claude/worktrees/**"`); `check-migraciones` ✓ 131.

- **El estado del paciente es una bandera con la palabra** en la esquina de la tarjeta y de la ficha
  (`EstadoPaciente`, `forma="bandera"` por defecto; la palabra sale de `PALABRA_ESTADO` en
  `lib/inscripcion.ts`). **«Resumen» es un enlace** al pie de la columna (`.spira-disclosure-link`).
- **Cada pantalla puede publicar su lugar** con `useLugar` (`src/lib/lugar.ts`). Lo hacen la ficha, el
  tablero del estudio, el detalle de visita y el wizard de Recepción. **Si sumás una pantalla que muestra UNA
  entidad, publicá ahí su lugar**: si no, el feedback cae a las migas del encabezado y no ofrece el salto.
- **`Ctrl/⌘ ⇧ F` abre el feedback aun con un modal abierto**; la tecla se ve en el popover «Acerca de».
- **Ajustes › Feedback recibido** existe, sólo con el módulo gerencia.
- **El CI son dos jobs**: «Índice de migraciones» y «Typecheck + build». Un índice roto ya no saltea el build.

### Lo hecho

| PR | Qué |
|---|---|
| [#220](https://github.com/spiraclinicapp/Spira-App/pull/220) | Bandera de estado con la palabra y «Resumen» como enlace al pie (tarjeta y ficha) |
| [#221](https://github.com/spiraclinicapp/Spira-App/pull/221) · [#223](https://github.com/spiraclinicapp/Spira-App/pull/223) | Spec del feedback con lugar y los planes de sus dos entregas |
| [#222](https://github.com/spiraclinicapp/Spira-App/pull/222) | Entrega 1: el feedback guarda desde dónde se reportó (`0129`), el atajo y cuatro pantallas que publican |
| [#224](https://github.com/spiraclinicapp/Spira-App/pull/224) · [#229](https://github.com/spiraclinicapp/Spira-App/pull/229) | Las marcas «Aplicada en prod» de la 0128, la 0129 y la 0131 |
| [#225](https://github.com/spiraclinicapp/Spira-App/pull/225) | Entrega 2: la bandeja en Ajustes (`0131`) |
| [#227](https://github.com/spiraclinicapp/Spira-App/pull/227) | El índice de migraciones en su propio job de CI |

### Decisiones tomadas (no re-discutir)

1. **La bandera dice el estado real en una palabra** (Screening / Activo / Completado / Discontinuado); el
   color sigue binario. En la ficha va arriba y el nombre debajo.
2. **La tinta de la bandera es `--spira-acc-deep-*`**; el tinte del fondo va en la regla, no en un token.
3. **El lugar es la migaja + la entidad abierta, con el id aparte**, publicado en una pila y no en un Context.
   **El `target` se hereda hacia abajo.**
4. **El atajo no se bloquea con un modal abierto y no togglea.**
5. **`RegisterVisitFlow` no publica**: no tiene el paciente a mano y la ficha que lo abre dice más.
6. **«Marcar como visto» va por RPC idempotente**, no por policy de UPDATE: la RLS no limita columnas.
7. **El embed de `users` en la bandeja va por columna** (`autor:users!user_id(...)`).
8. **«Ir al lugar» sólo aparece si el salto se puede dar** (`alcanzable`, la misma regla que el shell).
9. **Numeración:** 0129 `feedback_lugar`, 0130 desviaciones, 0131 `feedback_visto`; la guarda pasa a **0132**.

### PRÓXIMO PASO

**Tres QA logueados**, los tres con tu sesión de gerencia:

1. **Ajustes › Feedback recibido**: marcar uno como visto → se atenúa, dice «visto» y deja de estar en
   «Pendientes».
2. **La bandera en la ficha real**: abrir un paciente → la bandera se apoya en la esquina y el nombre arranca
   debajo, sin superponerse.
3. **El atajo con una visita abierta**: en el detalle de una visita, `Ctrl ⇧ F` abre el feedback encima y la
   caja de contexto dice «… › Visita V8 · <paciente>».

La guarda de Recepción (`0132`) sigue siendo de la sesión que la escribió.

### Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main     # 4928c03 o algo encima
npx vitest run --exclude ".claude/worktrees/**"   # 1263
node scripts/check-migraciones.mjs                 # ✓ 131 migraciones, índice al día
```

**La 0131, sin sesión:** `POST /rest/v1/rpc/mark_feedback_seen` con `{ "p_id": "00000000-0000-0000-0000-000000000000" }`
tiene que contestar `28000` («Tu sesión venció.»): la función existe y aborta en su primera guarda sin tocar
nada. `PGRST202` querría decir que no está.

**Limpieza:** el worktree `vigorous-noether-f99a79` queda con la rama de este cierre
(`docs/cierre-2026-09-17-feedback`). Se borra después de cerrar la sesión, no desde adentro.
