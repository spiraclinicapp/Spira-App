# Bitácora · 2026-09-15 — Arranca la prueba real: cronograma, pacientes y la limpieza de la fase alpha · release `v0.74.0`

> **Spira pasa a usarse en serio.** El Director y Agustín tienen asignados cuatro protocolos
> (LTS17231, ACT18301, 222714 = ENDURA y CKJX839D12302 = Victorion). En un día entraron el cronograma de
> los cuatro, 72 inscripciones con sus visitas y su medicación, y —decisión del Director— **se borró todo
> lo anterior**: la fase alpha no tuvo usuarios y sus datos no servían ni como auditoría. Producción
> quedó con esos cuatro estudios, la configuración y el catálogo. Nada se mergeó: las dos PRs del día
> están abiertas.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Fundación Scherbovsky · farmacia e investigación clínica · sistema auditable ANMAT/ICH-GCP
**Ramas:** [#191](https://github.com/spiraclinicapp/Spira-App/pull/191) (`feat/carga-prueba-real`: los tres scripts de datos) y [#192](https://github.com/spiraclinicapp/Spira-App/pull/192) (`feat/pacientes-mis-estudios`), **las dos abiertas, sin mergear**. **Sin release.** **Sin migraciones**: sigue la `0125`. **2226 tests.**

---

## 1. Resumen de la jornada

| Hilo | Qué | Dónde |
|---|---|---|
| Cronograma | Los 11 procedimientos unificados en los 4 estudios, el cuadro de visitas (semana/día, ventana ±3, marca de IP) y los procedimientos de cada visita | `supabase/scripts/cronograma-prueba-real/` · corrido en prod |
| Pacientes | 72 inscripciones / 64 personas, 1008 visitas y 27 medicaciones de base, desde el listado del sitio | `supabase/scripts/pacientes-prueba-real/` · corrido en prod |
| Limpieza | Todo lo anterior a la carga borrado, salvo configuración y catálogo, incluida su auditoría | `supabase/scripts/limpieza-prueba-real/` · corrido en prod |
| Accesos | Los 4 estudios asignados a la cuenta institucional; quitado THESEUS | prod, por `set_protocol_access` |
| App | Coordinación › Pacientes abre en «Mis estudios», con «Todos» a un clic | [#192](https://github.com/spiraclinicapp/Spira-App/pull/192) |

Los tres scripts salen de un generador que lee el Excel del sitio: nada se transcribió a mano. Los tres
se probaron **en un Postgres de verdad** antes de ir a producción (ver §7).

## 2. El cronograma

El Excel del Director traía, por estudio, la visita, la semana, el día y los procedimientos ya
unificados (11 nombres para las decenas del protocolo). El generador arma dos SQL: una **sonda** de sólo
lectura y un **aplicar** atómico e idempotente.

- **El nombre de la visita y su etapa salen del formulario del cronograma**, no de una convención nueva:
  screening y randomización se agendan a mano (`libre`), el tratamiento se genera al randomizar
  (`automatica`) y su nombre es la semana (`W4`).
- **Las visitas sin fecha fija** —ETD, WS, FdE, la inclusión de ACT y la preinclusión de ENDURA— quedan
  como manuales: no se generan solas.
- **La marca de «Entrega producto en investigación»** se sacó de la hoja CRUDA, la única que todavía
  distingue «Administración del IMP» de «IVRS».
- **Los procedimientos viejos de esos estudios se borraron con sus reportes**, a pedido del Director.

## 3. Los pacientes

El segundo Excel traía 72 inscripciones con nombre, IVRS, estado, nacimiento, visitas realizadas y
medicación. El script **simula** (hace la carga entera y la deshace, con el informe como mensaje) y
después **aplica**. Decisiones que lo fijan, todas del Director:

- **El Excel manda** sobre lo que ya había en Spira, pero **nunca borra una fecha real que sólo tenga
  Spira**: se conserva y se informa.
- **Victorion, V2 a V7 sin registro** (246 visitas de 2024 a 2026): se cargan **realizadas con fecha real
  igual a la estimada**, cada una con una nota que lo dice. Lo que se quería evitar eran las alertas, no
  las visitas (§7).
- **Una inscripción que no sigue no deja visitas pendientes**: las alertas no filtran por estado de
  inscripción (0107), así que alertarían para siempre.
- **El sello de IP se apaga durante la carga**: una visita histórica queda con `lleva_ip` en null, que es
  exactamente lo que ese campo dice de las visitas fechadas fuera de Spira.

El control después de aplicar: ENDURA 19 realizadas / 101 pendientes / 4 vencidas · ACT18301 133 / 24 ·
Victorion 294 (246 provisorias) / 370 · LTS17231 8 / 75.

## 4. La limpieza

> «Si bien tenés razón, estábamos en una fase alpha, sin contacto con los usuarios. La información es
> basura y no la puedo dejar como auditable porque no sirve.»

Se borró todo lo anterior a la carga salvo configuración y catálogo: los protocolos THESEUS, CEREN-2,
PROT-A y PROT-B con lo suyo, las inscripciones y pacientes que no son de la carga, todo lo operativo de
Farmacia (el stock quedó en cero y los comprobantes vuelven a 1), todo lo operativo de Coordinación, la
configuración de reposición inventada del QA y **la auditoría anterior al corte**. Los 72 de la carga
volvieron al estado del Excel: sin sueltas, sellos de atención, médico, ausencias ni notas viejas.

El borrado **no deja rastro**: los triggers de usuario se apagan dentro del bloque y se vuelven a prender
en la misma transacción; los de FK son internos de Postgres y siguen haciendo las cascadas. El script se
frena solo si la carga no da 8/8/48/8 inscripciones y 64 personas, antes y después.

Quedó pendiente un paso a mano: el único archivo del bucket `ip-docs`.

## 5. «Mis estudios» en Pacientes ([#192](https://github.com/spiraclinicapp/Spira-App/pull/192))

Un administrador de Coordinación ve **todos** los protocolos (0028) pero sólo los pacientes de sus
estudios asignados (0006). La grilla le mostraba tarjetas ajenas en «0 pacientes». Ahora abre en los
asignados, con el alternador **Mis estudios / Todos** —mismo `SegmentedControl` y mismo `?ambito=` que el
Resumen—, y quien no coordina nada no lo ve. Al crear un protocolo pasa a «Todos», porque nace sin
asignar y no aparecería. `SegmentedControl` sumó `size="barra"`: suelto mide 44px y al lado de los
botones de 38 quedaba más alto.

## 6. Los accesos

«0 pacientes» en tres estudios no era la carga: **hay dos cuentas a nombre de Lautaro Molina**. La
institucional (`l.molina@…`) tenía ACT18301 y THESEUS; los cuatro estudios estaban asignados a la de
Gmail. Se corrigió por `set_protocol_access`, la misma función que usa Ajustes, y quedó en el historial
de accesos. **a.bazzani todavía no existe en Spira.**

## 7. Lo no-obvio

- **El listado de pacientes corrigió el cronograma.** Sus «fecha estimada» son la verdad operativa del
  sitio, y no coincidían con lo que yo había deducido del cuadro de procedimientos: ENDURA cuenta
  **Día − 1** y llega hasta la V28; Victorion usa el **Día** tal cual. Con la primera versión, todas las
  fechas quedaban corridas un día. **El guard que lo detecta es de mayoría**, no exacto: en LTS17231 a
  quien todavía no pasó de ACT el sitio le estimó todo desde la V1 **estimada**, un día después de la
  real — frenar por eso habría sido un falso positivo.
- **«No quiero que se creen» no era «no quiero que existan».** Entendí que las 254 visitas sin registro de
  Victorion no debían crearse y llegué a escribir una migración (`0126`, visitas omitidas) con tabla,
  trigger de generación y sincronización. El Director aclaró: *«pensé que hablábamos de alertas»*. Con las
  visitas creadas, nada las recrea y **la migración se descartó antes de aplicarse**. Preguntar por el
  efecto, no por el mecanismo.
- **Una fecha provisoria en un sistema auditable necesita marca.** Como la real sale igual a la estimada y
  en pantalla no se distingue de una registrada, cada visita lleva una nota que lo dice. Es la forma de
  encontrarlas cuando aparezcan las fechas verdaderas.
- **Advertí una inundación que no existía.** Dije que al configurar reportes cada visita histórica
  aparecería como tarjeta pendiente. Falso: el tablero filtra por `visita_iniciada`, pero la **tarjeta**
  la decide `esTarjeta` = procedimiento **tildado** (`reportes/estados.ts:105`). Con 16 definiciones y 674
  cruces posibles, el tablero dice **0 reportes en curso** (verificado en prod). Leí el hook y no la regla.
- **Un `git checkout` puede desproteger un archivo con datos personales.** El `.gitignore` que excluye el
  SQL generado vivía en la rama; al volver a `main`, la carpeta `out/` quedó sin ignorar. Se tapó con
  `.git/info/exclude`, que es local y no se commitea.
- **PGlite como banco de pruebas de SQL.** Los tres scripts se corrieron contra un Postgres real en WASM,
  sobre un esquema de juguete con las FKs, triggers y funciones copiadas de las migraciones: aplicar,
  segunda corrida sin cambios ni filas nuevas en `audit_log`, y freno sin escribir ante precondiciones
  rotas. Ver [[probar-sql-con-pglite]].

## 8. Git — estado de las ramas

Dos ramas pusheadas por el Director (a mí el `git push` me lo bloquea el clasificador) y **dos PRs
abiertas por API**: [#191](https://github.com/spiraclinicapp/Spira-App/pull/191) (scripts, no toca la app) y [#192](https://github.com/spiraclinicapp/Spira-App/pull/192) (Mis estudios; al mergear sale a producción). Ninguna
mergeada. Este cierre va en `docs/cierre-2026-09-15`. **Sin release**: nada entró a `main`.

## 9. Pendientes

1. **La corrección de fechas que el Director va a pasar.** Con eso se termina de decidir el resto.
2. **Las 4 vencidas de ENDURA** (707401 V3, 707402 V4 y V5, 707405 V3): hoy alertan. Puede dejarse así o
   cargarlas como realizadas con fecha provisoria, como Victorion.
3. **Storage:** borrar el único archivo del bucket `ip-docs`.
4. **Cuentas:** crear `a.bazzani` (dashboard, Auto Confirm + módulo + estudios) y dar de baja la cuenta de
   Gmail del Director.
5. **Mergear [#191](https://github.com/spiraclinicapp/Spira-App/pull/191) y [#192](https://github.com/spiraclinicapp/Spira-App/pull/192)**, y sacar versión con la #192 (es cambio visible).
6. **Las 246 fechas provisorias de Victorion**, cuando aparezcan las reales.
7. **Borrar del disco** `supabase/scripts/pacientes-prueba-real/out/`: tiene nombres y fechas de nacimiento.

## 10. Coda · las tres PRs mergeadas y `v0.74.0`

Después de escribir el cierre, el Director mergeó las tres: [#191](https://github.com/spiraclinicapp/Spira-App/pull/191) (scripts), [#192](https://github.com/spiraclinicapp/Spira-App/pull/192) («Mis
estudios») y [#193](https://github.com/spiraclinicapp/Spira-App/pull/193) (este cierre). Las tres ramas se borraron en local y en `origin` después de verificar
con `merge-base --is-ancestor` que cada commit está en `origin/main`.

Como la #192 es cambio visible y ya corría en producción, salió **`v0.74.0`** con una línea en el
changelog: *«Pacientes abre en tus estudios asignados, con «Todos» a un clic.»* Los scripts y la
documentación no suman renglón. `scripts/release.mjs 0.74.0`, commit directo en `main` (autorizado),
tag `v0.74.0` y `push --follow-tags`. **2226 tests**, sin migraciones. Antes se verificó que el tag no
existiera en `origin`.

---

# Segunda parte · Lo que la prueba real destapó: el IVRS de otro estudio, y el encabezado en tres vueltas · release `v0.75.0`

> **Con datos de verdad, los defectos aparecen solos.** Ninguna de las siete correcciones de la noche
> salió de leer código: todas salieron de que el Director MIRÓ producción con los cuatro estudios
> cargados. La más grave —el número de sujeto de otro estudio en toda la app— llevaba meses esperando
> a que hubiera un paciente en dos estudios para volverse visible.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Coordinación · la ficha del paciente, sus visitas y el número de sujeto
**Ramas:** siete PRs, todas mergeadas y con su rama borrada en `origin` y en local: [#194](https://github.com/spiraclinicapp/Spira-App/pull/194), [#196](https://github.com/spiraclinicapp/Spira-App/pull/196), [#197](https://github.com/spiraclinicapp/Spira-App/pull/197), [#198](https://github.com/spiraclinicapp/Spira-App/pull/198) + [#199](https://github.com/spiraclinicapp/Spira-App/pull/199), [#200](https://github.com/spiraclinicapp/Spira-App/pull/200), [#201](https://github.com/spiraclinicapp/Spira-App/pull/201) y [#202](https://github.com/spiraclinicapp/Spira-App/pull/202). **Migración `0126` aplicada.** Release **`v0.75.0`**. **1126 tests** (1118 → 1122 → 1126).

## 11. Resumen de la segunda parte

| PR | Qué | Cómo se encontró |
|---|---|---|
| [#194](https://github.com/spiraclinicapp/Spira-App/pull/194) | «Ver en Visitas» desde la visita de la ficha, y el calendario del navegador de fecha con mes y año | «no puedo ver la misma visita en el submódulo de Visitas» |
| [#196](https://github.com/spiraclinicapp/Spira-App/pull/196) | El nombre de la visita no repite la semana donde la semana ya está en pantalla | «queda V6 - W16 W16» |
| [#197](https://github.com/spiraclinicapp/Spira-App/pull/197) | En «Próxima visita», la visita manda sobre la semana | «la visita es más importante que la semana» |
| [#198](https://github.com/spiraclinicapp/Spira-App/pull/198) + [#199](https://github.com/spiraclinicapp/Spira-App/pull/199) | **El IVRS que se muestra es el del estudio de la fila** (migración `0126`) | «se está usando el mismo IVRS en LTS que en ACT» |
| [#200](https://github.com/spiraclinicapp/Spira-App/pull/200) | Encabezado equilibrado y «faltan N d» en vez de «ventana N d» | «el día lo veo muy tirado para abajo» |
| [#201](https://github.com/spiraclinicapp/Spira-App/pull/201) | El reporte se pone al día al marcar, y los procedimientos al volver a la pestaña | «sigue diciendo como si no lo hubieses marcado» |
| [#202](https://github.com/spiraclinicapp/Spira-App/pull/202) | Los dos bloques del encabezado, centrados entre sí | «sigue desalineado en altura» |

## 12. El IVRS de otro estudio ([#198](https://github.com/spiraclinicapp/Spira-App/pull/198), `0126`)

**El síntoma:** el listado de pacientes de LTS17231 mostraba los números de ACT18301 (`032001500001`…).
Son las mismas personas en los dos estudios, y en investigación clínica **el número de sujeto es por
estudio**.

**La causa no era el dato.** La columna correcta existe desde la **0062** (`enrollments.ivrs_code`, con
índice único por protocolo) y el script de carga de la mañana la escribe. Lo que faltaba era leerla:
**ninguna** vista la proyectaba —las seis que devuelven `patient_code` daban `patients.code`, el del
estudio madre— y en el front no había una sola aparición de `ivrs_code`.

- **La 0126** recrea las seis vistas con `coalesce(e.ivrs_code, pa.code)`: copias **verbatim** con una
  línea cambiada, generadas por un script que extrae la definición vigente de cada una (retipear seis
  vistas a mano es la forma de equivocarse). `create or replace`: mismas columnas, permisos intactos.
  Las seis ya joineaban `enrollments`.
- **El `coalesce` cubre tres casos reales:** inscripciones anteriores a la 0062, la pre-randomización
  (el número se asigna al randomizar) y las salidas **ambulatorias**, que entran por `left join` porque
  no tienen enrolamiento. Sin él, esas filas pasarían de un número equivocado a ninguno.
- **El front**, donde el código no sale de esas vistas: `usePatients` trae `ivrs_code` y la regla
  `ivrsDelEstudio` (`lib/ivrs.ts`, con tests) elige el de la inscripción del protocolo en contexto. La
  usan la ficha y la fila del listado, que recibe `protocolId`.
- **Alcance:** 31 archivos del front leen ese `patient_code` — Visitas del día, Para ver médico,
  Pendientes, la campana, los reportes de Farmacia, el CSV y el **comprobante impreso** de dispensación.

## 13. El encabezado de la visita, en tres vueltas ([#197](https://github.com/spiraclinicapp/Spira-App/pull/197), [#200](https://github.com/spiraclinicapp/Spira-App/pull/200), [#202](https://github.com/spiraclinicapp/Spira-App/pull/202))

El mismo renglón —fecha a la izquierda, visita y semana a la derecha— necesitó tres pasadas, y las tres
las disparó el Director mirando la pantalla:

1. **La jerarquía estaba al revés** ([#197](https://github.com/spiraclinicapp/Spira-App/pull/197)): la semana en 25px tinta contra los 19px de la visita, y las
   dos como columnas gemelas del mismo molde. Se fusionaron en un bloque: la visita como ancla, la
   semana como su pie.
2. **La fecha quedaba hundida** ([#200](https://github.com/spiraclinicapp/Spira-App/pull/200)): con `flex-end` mandaba el bloque más alto. Se alineó arriba, la
   visita subió a 25px (igual que la fecha) y la semana a 14. Y «ventana 112 d» pasó a **«faltan 112 d»**,
   con dos frases aparte para lo que no entra en ésa: «vence hoy» y «ventana vencida».
3. **Seguía desalineado** ([#202](https://github.com/spiraclinicapp/Spira-App/pull/202)): alineados arriba quedaba un hueco DEBAJO de la fecha. Con bloques de
   distinta cantidad de renglones —dos contra tres— ninguna alineación por los extremos empareja:
   `center`.

## 14. Lo no-obvio

- **La columna correcta puede existir hace meses y el dato seguir mal en pantalla.** `ivrs_code` estaba
  desde la 0062 y **nada** lo leía. Buscar antes de construir vale también al revés: antes de dar por
  ausente una capacidad, grepear quién la consume — que no la consuma nadie ES el bug.
- **Un defecto de dos estudios no existe hasta que hay dos estudios.** Con el padrón viejo (un paciente,
  un estudio) el IVRS del estudio madre era siempre el correcto. La carga de la mañana lo volvió visible.
- **react-day-picker no arma la lista de años sin rango**: `getYearOptions` devuelve `undefined` sin
  `navStart`/`navEnd`, así que el calendario del navegador de fecha se quedaba con las flechas de mes y
  nadie veía un error — sólo doce clics para retroceder un año.
- **Un banco de pruebas tiene que reproducir la GEOMETRÍA, no sólo los datos** (lección del 14 que volvió
  a pagar): las filas sin visitas daban una columna más ancha y escondían el choque del punto con el
  nombre.
- **Una marca optimista puede tapar que al lado hay otra consulta.** El tilde del procedimiento se veía
  al instante; el bloque de reportes —`v_protocol_report_status`, otra consulta— se quedaba diciendo «se
  habilita al marcar».
- **Un `cherry-pick` que rescata un commit cambia su SHA**, así que `merge-base --is-ancestor` dice que
  no está en `main` y frena el borrado de la rama. Quien decide ahí es `git cherry`, que compara por
  contenido: marcó el commit como ya aplicado.
- **Me pisé el archivo de la sonda con el de la migración** mientras lo pasaba al editor. No había nada
  que lamentar porque los dos estaban commiteados: `git checkout --` y listo. Se detectó comparando
  tamaños (los dos en 14.736 bytes).

## 15. Git — estado de las ramas

Las siete PRs mergeadas y **sus ramas borradas** en `origin` y en local, cada una después de verificar
que su commit está en `origin/main`. La de la #198 necesitó la verificación por contenido (§14).
**Ojo:** el commit del registro de la 0126 quedó fuera del merge de la #198 —se pusheó minutos después—
y volvió en la #199. Este cierre va en `docs/cierre-2026-09-15-v0.75.0` con su PR, y el tag `v0.75.0`
sobre el commit del release.

Queda viva, de otra sesión, `feat/dispensacion-visita-cerrada` (worktree `C:/Users/Tutuca/Desktop/Spira/wt-dispensacion`).

## 16. Pendientes

1. **Farmacia sigue mostrando el IVRS del estudio madre** en dispensaciones (el cajón, el kanban, la
   sección de habilitación y el **comprobante impreso**): traen el código por su cuenta, con un embed
   que ya pasa por la inscripción, así que es sumar `ivrs_code` y preferirlo. Es la PR que sigue.
2. **Las consultas 1 a 3 de la sonda de la 0126** no se corrieron: la 4 confirmó que las seis vistas
   quedaron con su comentario, pero **que el número sea el correcto lo dice la 2**.
3. **El contador «N/M» de la lista del día** no se refresca al volver a la pestaña (es otra consulta).
4. **«Le sigue faltando algo al encabezado»** (Director): falta definir QUÉ dato suma — el estado de la
   visita, el desvío contra el cronograma o el médico a cargo.
5. **Sin QA logueado** de nada de la noche.
6. Siguen los siete pendientes de la primera parte (§9).
