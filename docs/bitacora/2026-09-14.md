# Bitácora · 2026-09-14 — La Tanda 3 de la dispensación entera: el panel, las partes y el saldo, y «Otro medicamento» · release `v0.71.0`

> **El plan de dispensación de base y de IMP, terminado.** La Tanda 3 salió en las tres entregas que fijó
> la revisión de arquitectura (3a, 3b, 3c), cada una con su QA logueado de sólo lectura y, las dos que
> tocaban la base, con la migración primero. El Director eligió no crear datos de prueba en producción:
> lo que sólo se ve escribiendo quedó cubierto por tests. La 3a arrancó la noche del 13.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Fundación Scherbovsky · farmacia e investigación clínica · sistema auditable ANMAT/ICH-GCP
**Ramas:** cinco PRs, todas mergeadas y borradas en local: [#170](https://github.com/spiraclinicapp/Spira-App/pull/170) (3a, `e467fe7`), [#171](https://github.com/spiraclinicapp/Spira-App/pull/171) (0123, `8ad381d`), [#172](https://github.com/spiraclinicapp/Spira-App/pull/172) (3b, `33d3949`), [#173](https://github.com/spiraclinicapp/Spira-App/pull/173) (0124, `72a9b04`) y [#174](https://github.com/spiraclinicapp/Spira-App/pull/174) (3c, `8aea598`). **Migraciones `0123` y `0124` aplicadas.** Release **`v0.71.0`**. **1036 tests** (980 → 998 → 1025 → 1036).

---

## 1. Resumen de la jornada

| Entrega | Qué | PR | Migración |
|---|---|---|---|
| 3a | El historial plegado en una línea y la sección del IP siempre presente | [#170](https://github.com/spiraclinicapp/Spira-App/pull/170) | — |
| 3b | Entregar en partes, el saldo con «Pedir el saldo» y el aviso rojo por droga | [#171](https://github.com/spiraclinicapp/Spira-App/pull/171) + [#172](https://github.com/spiraclinicapp/Spira-App/pull/172) | `0123` |
| 3c | «Otro medicamento» con receta y la habilitación en Farmacia | [#173](https://github.com/spiraclinicapp/Spira-App/pull/173) + [#174](https://github.com/spiraclinicapp/Spira-App/pull/174) | `0124` |

Las tres se verificaron **logueado contra prod y sin escribir datos**. El Director eligió cubrir con tests
lo que sólo se ve creando pedidos de prueba (el saldo de punta a punta, habilitar y no habilitar).

## 2. 3a · El panel

- **Historial plegado (D17):** «2 pedidos cerrados · el último, entregado el 21/07» y «Ver historial», con
  un renglón por pedido. Abre desplegado **sólo con un rechazo vigente** (el pedido más nuevo que no se
  canceló es el rechazado): en la ficha de un paciente de meses, un rechazo viejo lo dejaría abierto para
  siempre. Cuenta todos los cerrados, también el entregado con IP, que antes se excluía y dejaba sus
  renglones de base sin aparecer en ningún lado.
- **Sección del IP siempre presente (D18):** «El cronograma no lo pide en esta visita.» con «Pedir fuera de
  cronograma» adentro; el motivo y la constancia van en la misma sección, con el rótulo en ámbar. Con un
  cierre de la `0119` lo dice y no ofrece nada (lee `v_visit_ip_status`, lo mismo que Procedimientos).
- **QA:** encontró que a 286px de tarjeta el texto del estado vacío caía a 38px de ancho, una palabra por
  renglón. Se arregló con `wrap` antes de la PR.
- Las reglas puras se llaman `*Model.ts` porque en Windows `historialPlegado.ts` y `HistorialPlegado.tsx`
  son el mismo archivo para `tsc` (TS1149).

## 3. 3b · Partes, saldo y aviso rojo

- **`0123`:** `quantity_indicated` y `saldo_de_item_id` en el renglón; la alta de un renglón en una función
  interna revocada (`alta_renglon_pedido`) que valida el saldo en el servidor; `contexto_dispensacion` con
  filas crudas (entrega / abierto / indicación / IP).
- **Un renglón por medicamento va como guard, no índice.** El conteo en prod (después, con sesión) dio **cero
  duplicados**: el índice habría entrado, pero el guard no depende de eso.
- **Front:** «Entregar en partes» en el selector; cajas de saldo con «Pedir el saldo»; aviso rojo por droga
  y entre protocolos al elegir, con una caja y una línea por droga; aviso de IP reciente adentro de la
  excepción; «1 u. (de 2 indicados)» en el comprobante y el cajón. Se borraron `AvisoReciente` y
  `useUltimaDispensacion`.
- **Pedidos del Director al verlo:** la cantidad arranca en 1; «En partes · entregar … de [2] envases» no
  se entendía y pasó a «Entregar en partes» con un ⓘ y una sola línea («Total indicado [2] envases ·
  queda 1 de saldo»); y «no lo prevé» pasó a «no lo pide».
- **QA:** el rojo salió con datos reales (Alvetide en la V5 de Susana, pedido abierto en su V6).

## 4. 3c · «Otro medicamento»

- **`0124`:** tabla `dispensation_habilitaciones` (receta en `ip-docs/{protocolo}/habilitaciones/`, decisión
  de Farmacia con motivo de lista); `patient_medications.habilitacion_id`, la marca de «habilitado para una
  entrega» que un trigger desactiva al terminar el pedido; `candidatos_otro`, `solicitar_habilitacion`,
  `habilitar_medicamento_pedido`, `no_habilitar_medicamento_pedido`; `alta_pedido_interna` revocada y
  compartida con `create_dispensation_request`; `mark_dispensation_ready` espera la habilitación; se revoca
  `resolve_dispensation`.
- **Sumado al plan:** `quitar_habilitacion` (el mock tiene una ✕ en «Por habilitar» y no había función) y
  una acción al pie en `SearchableSelect` («Otro medicamento» no es `onCreate`, que en la casa es dar de alta).
- **Front Coordinación:** «Otro» al pie del desplegable; formulario con candidatos con stock, receta
  obligatoria y vista previa; filas «Por habilitar» y «No habilitado» con «Pedir de nuevo»; el saldo de un
  «Otro» se pide como habilitación con la misma receta.
- **Front Farmacia:** señal «Pide habilitar X» en el tablero; sección arriba del cajón con «Habilitar y sumar
  al pedido» o «No habilitar»; la habilitación pendiente es el primer requisito y bloquea «Marcar lista»
  (regresión con test); la trazabilidad la cuenta en castellano.
- **QA:** el riesgo que había quedado abierto —si la función puede leer `storage.objects`— se probó **sin
  escribir**: con una receta inexistente contesta «No se encontró la receta subida», así que lee Storage; con
  una ruta de otra forma corta antes, por el protocolo. En pantalla, sobre EFC18419: candidatos reales,
  «Otro» al pie, el formulario con una receta elegida en el navegador y la fila «Sin solicitar · Con receta»,
  quitada sin mandar. La sección del cajón y el modal, en un banco de pruebas.

## 5. Lo no-obvio

- **La primera vez que el Director «corrió» la `0123`, no estaba en su carpeta.** El working copy había
  quedado en la rama del front, que salió de `main` antes del merge de la migración. La sonda lo detectó
  (columnas `42703`, igual que el control inventado). Antes de pedir que se corra un SQL, el local va en
  `main` con el archivo en disco, y se abre en el editor.
- **`sed -i` de Git Bash reescribe los archivos aunque no cambie nada**, y los pasa de CRLF a LF. Sobre
  `*.ts` dejó 21 archivos ajenos marcados como modificados sin diff de contenido. Se restauraron con
  `git checkout --`.
- **El hook de ramas mira la rama actual al ver `git commit` en el comando.** `checkout rama && commit` en un
  solo comando, parado en `main`, se bloquea entero: van separados.
- **Una FK de la tabla de habilitaciones a los renglones la volvía tabla puente** entre pedido y renglón, y
  podía dejar ambiguo `items:dispensation_request_items` (la lección de la 0076). Se resolvió sin FK, por
  (pedido, medicamento).
- **El trigger de «una entrega» no desactiva** si otro pedido abierto del paciente lleva el mismo
  medicamento: la 0050 exige la habilitación también al entregar, y ese pedido quedaría trabado.
- **La regex de la ruta de la receta cierra con `\Z` y no con `$`**: el editor de Supabase rastrea los signos
  peso sin entender el SQL (0071).
- **No hay ninguna entrega en los últimos 31 días en prod.** El aviso rojo por entrega reciente no se puede ver
  con datos reales hoy; el de pedido abierto sí.

## 6. Git — estado de las ramas

Todo en `main`: [#170](https://github.com/spiraclinicapp/Spira-App/pull/170)-[#174](https://github.com/spiraclinicapp/Spira-App/pull/174) mergeadas. **Sin ramas vivas**: las cinco se borraron en local y en `origin` después de verificar que cada una estaba contenida en `main`. Release `v0.71.0` con tag, commit directo en `main` (autorizado por el Director).

## 7. Pendientes

1. **QA con datos reales** de lo que se cubrió con tests: el saldo de punta a punta, habilitar, no habilitar,
   quitar un «Otro», el cierre de «una entrega» y el saldo de un «Otro». Se va a ver con el primer uso real.
2. **Tildar «Entrega producto en investigación»** en los cronogramas reales (sigue del 13): sin eso la Tanda 1 no
   se ve.
3. De `TODOS.md`: el `status` del pedido todavía modificable por Farmacia y gerencia, las alertas al día sin
   recargar y los huérfanos de `ip-docs` (ahora también las recetas).

---

# Segunda parte · Compras del mes que viene: del pedido a producción en una tarde · release `v0.72.0`

> **Un pedido nuevo del Director, entero en el día:** «en base a la medicación asignada a cada paciente,
> una cuenta del stock mínimo al comenzar el mes, y en Estadísticas una card con el faltante por estudio».
> Salió plan (`/plan-eng-review` + `/plan-design-review`), mock, cuenta con tests, migración, card y QA
> logueado contra prod. **Dos veces el Director corrigió la lectura del pedido y una vez rechazó el diseño
> por complejo**; las tres correcciones mejoraron lo que salió.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Farmacia de investigación · reposición de medicación de base por estudio
**Ramas:** [#175](https://github.com/spiraclinicapp/Spira-App/pull/175) (plan + cuenta + `0125`, `971a640`), [#176](https://github.com/spiraclinicapp/Spira-App/pull/176) (card, «Ver pedido», excepción, `6ae90e8`) y [#178](https://github.com/spiraclinicapp/Spira-App/pull/178) (copy, `1d6ccaa`), mergeadas y borradas. La [#177](https://github.com/spiraclinicapp/Spira-App/pull/177) (realce de «Editar medicación», `226624f`) salió de una tarea sugerida y la hizo otra sesión. **Migración `0125` aplicada.** Release **`v0.72.0`**. **1087 tests** (1036 → 1081 → 1087).

## 8. Resumen de la segunda parte

| Paso | Qué | Dónde |
|---|---|---|
| Plan | 32 decisiones de ingeniería + 16 de diseño, voz externa incluida | `docs/plan-reposicion-stock-minimo.md` |
| Mock | Lienzo con la versión final y las formas descartadas | `docs/design_handoff_reposicion/` |
| Cuenta | `reposicionModel.ts`, pura, 46 tests | [#175](https://github.com/spiraclinicapp/Spira-App/pull/175) |
| Base | `0125`: cómo se repone, excepción, demora, pedidos, `insumos_de_reposicion` | [#175](https://github.com/spiraclinicapp/Spira-App/pull/175) |
| Pantalla | Card «Compras para octubre», «Ver pedido», excepción por paciente | [#176](https://github.com/spiraclinicapp/Spira-App/pull/176) + [#178](https://github.com/spiraclinicapp/Spira-App/pull/178) |

## 9. Cómo se leyó el pedido

- **La primera lectura estaba mal y la corrigió el Director con casos concretos.** Se había planteado
  «cantidad por visita que entrega» y «estimación por lo gastado». Él aclaró que los pacientes con visitas
  cada tres meses vienen igual todos los meses a una VNP de entrega (la unidad es el **mes**, y el tilde
  «entrega medicación» quedó para el IP), y que la compra es **«para todos»**: «si el mes pasado no vino
  nadie no vas a comprar 15 menos». Lo gastado quedó como referencia.
- Las dos correcciones salieron de **mostrar la cuenta con números inventados** (una tabla de Seretide), no
  de preguntar en abstracto.
- **La cuenta final:** necesidad del mes siguiente (pacientes que siguen × cantidad mensual) + lo que falta
  entregar este mes − lo que queda al 1° (FEFO) − lo que está en camino. A demanda: tener N − estante −
  en camino. La demora no cambia el número: da la fecha límite.

## 10. El diseño: tres formas y una simplificación

- La revisión de diseño comparó tres formas (inventario por estudio, card por estudio con sólo lo que se
  compra, lista de compras) y el Director eligió la segunda.
- **Después la rechazó:** «se ha complejizado mucho más de lo que es. Yo necesito sacar con un botón cuál es
  el pedido mensual». Quedó: **la card plegada con sólo el número**, desplegable a **todos** los
  medicamentos en renglones simples, y **«Ver pedido»** ordenable por estudio / medicamento (sumando entre
  estudios) / cantidad, con **«Imprimir» y «Ya lo pedí»** (marca todo el pedido en camino, con «Deshacer»).
- La cuenta no cambió con la simplificación: los casos borde (vencimientos, cronograma terminado, sin
  retiros, dos presentaciones) viven adentro del renglón abierto.

## 11. La implementación

- **Orden (D29): modelo primero, migración después.** La forma del JSON de `insumos_de_reposicion` quedó fija
  con los tests antes de escribir la `0125`, que es inmutable.
- **`0125`:** columnas en `protocol_medications` (`reposicion_modo`, `envases_por_mes`, `stock_fijo`) y
  `patient_medications.envases_por_mes`; `farmacia_ajustes` (una fila, la demora nace vacía);
  `reposicion_pedidos`; `configurar_reposicion` (operator escribe sólo esas columnas, D12);
  `registrar_pedido_reposicion` / `anular_pedido_reposicion`; `insumos_de_reposicion` **SECURITY DEFINER**
  porque Farmacia no tiene select sobre `patient_visits`.
- **Front:** `ComprasDelMes.tsx` va arriba de Estadísticas y fuera de sus cortes (carga, error, ventana
  angosta, «No hubo movimientos»); `VerPedido.tsx` con la hoja impresa del membrete de siempre;
  `excepcionReposicionModel.ts` decide la línea y el control en «Editar medicación». `chip`, `chipActivo` y
  el aviso pasaron a `estilos.ts`.
- **Verificación sin sesión y sin la migración:** dos bancos de prueba temporales (Supabase simulado, auth
  falsa con `AuthContext` exportado sólo mientras duró). Encontraron un error visible: el formulario de un
  renglón sin cargar decía «0 pacientes siguen», porque el modelo sólo contaba pacientes con modo mensual.

## 12. QA contra prod

- **Sondas sin sesión** antes y después de aplicar la `0125`: las ocho piezas pasaron de «no existe» a
  401/42501, con controles inventados en la misma corrida.
- **Logueado:** la primera llamada real a `insumos_de_reposicion` anduvo y la forma del JSON coincidió
  clave por clave con el modelo.
- **El hallazgo del QA: en prod hay 4 asignaciones de medicación que suman y 21 pacientes activos sin
  ninguna habilitada** (ACT18301 9, EFC18244 7, EFC18419 4, PROT-A 1). La cuenta sólo suma lo habilitado,
  así que el número plegado se veía completo y quedaba corto. El Director eligió **aviso visible en ámbar**
  en la card y en «Ver pedido», y que Farmacia habilite; la card nunca dice «cubierto» con pacientes afuera.
- **Carga con valores inventados, a pedido del Director** (va a limpiar para una prueba real): demora 20
  días y el modo de los 12 medicamentos. Resultado: 3 envases a comprar, «ya es tarde: llega el 04/10».
  **Cuadre** de Symbicort en ACT18301 contra la base: 1 paciente, lote de 9 y 1 envase pendiente de
  septiembre → quedan 8 → alcanza. «Ya lo pedí» escribió 2 filas con el mismo grupo; «Deshacer» las borró.

## 13. Lo no-obvio

- **Una review larga empuja a mostrar cada decisión en pantalla.** Cada caso borde resuelto terminó con su
  propio elemento visible, y la card pasó a explicar la cuenta en vez de servir para comprar. El mock se
  arranca por la acción que el usuario viene a hacer.
- **Las sondas sin sesión no ejercitan el cuerpo de una función con chequeo de auth:** cortan en «No
  autenticado» antes de la consulta. Un error de plpgsql aparece recién en la primera llamada logueada.
- **El modelo se movió de `views/` a `data/pharma/`** antes de mergear la #175: el barril de la capa de datos
  declara que depende sólo del Core, y el hook necesitaba los tipos del JSON.
- **La #176 se mergeó antes del último push**, así que el arreglo de copy fue en una PR aparte (#178): una PR
  mergeada no toma commits nuevos.
- **Con un worktree de otra sesión en `.claude/worktrees/`, vitest corre también sus tests** (el conteo pasó a
  2174). No afecta a CI; confunde al leer el build local.
- **El panel del preview mide ~700px**, así que Estadísticas entra por la rama angosta: para verla como en la
  notebook hay que emular 1536×864 y disparar un `resize`.

## 14. Git — estado de las ramas

Todo en `main`: #175, #176 y #178 mergeadas y sus ramas borradas en local. Deploy verificado en el bundle de
`spira-app.vercel.app` (`index-Cjwn0RiU.js`: card, «Ya lo pedí», aviso y el copy). Release `v0.72.0` con tag,
commit directo en `main` (autorizado por el Director).

## 15. Pendientes

1. **La limpieza que pidió el Director («act y lts») para arrancar una prueba real**, en otra sesión.
   Precisar primero qué es «lts» y qué se borra; lista en sólo lectura antes de tocar nada.
2. **La configuración inventada del QA sigue en prod** (demora 20 días y el modo de los 12 medicamentos):
   vaciarla o pisarla con los valores reales.
3. **Farmacia tiene que habilitar la medicación de los 21 pacientes activos** para que entren en la cuenta.
4. **La sonda `supabase/_sonda_reposicion.sql`**: el QA ya dio dos de sus números (4 asignaciones, 21 sin
   medicación); faltan los de sin retiros, dos presentaciones y la integridad de pedidos y movimientos.
