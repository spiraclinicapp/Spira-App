# Handoff · 2026-09-01 (fin de jornada)

> Handoff anterior: [`handoff-2026-08-31.md`](handoff-2026-08-31.md) · Narrativa del día: [`2026-09-01.md`](2026-09-01.md)

> ✅ **TODO EN `main`, release `v0.50.0`.** Tres PRs ([#104](https://github.com/spiraclinicapp/Spira-App/pull/104) · [#105](https://github.com/spiraclinicapp/Spira-App/pull/105) · [#106](https://github.com/spiraclinicapp/Spira-App/pull/106)) mergeadas y sus ramas borradas local y en `origin`. No queda ninguna rama viva.

> ✅ **MIGRACIÓN 0103 APLICADA** en prod el 2026-09-01, **antes** del front (es aditiva). No queda nada por correr en el dashboard. La última aplicada pasa a ser la **0103**.

> ⚠️ **EL QA LOGUEADO ES EL PENDIENTE REAL DE ESTA JORNADA.** Casi todo se verificó con un banco de pruebas de datos falsos, porque el Resumen exige sesión y el preview es un navegador aparte. Lo que el Director sí miró en pantalla está marcado abajo; lo demás, no.

## Dónde estás

`main` en `6953ca1`, release **`v0.50.0`**, última migración **`0103`**.
`npm run build` verde: **552 tests** (+46 en la jornada).
Working copy limpio, sin ramas vivas.

Dos pantallas cambiaron de forma: el **Resumen de Coordinación** (mosaico 2×2, tres renglones por tarjeta, KPIs que navegan, tarjeta nueva de dispensaciones) y **Alertas** (barra de filtros completa, cabecera teñida por severidad, filas de reporte que ya no salen con un recuadro negro).

## Lo hecho esta jornada

- **#104** — La capa de interacción del handoff `design_handoff_resumen_tareas_enfoque`: hovers, links y el revelado de destino. Token `--spira-shadow-hover` (global, con variante para tema oscuro), clase `.spira-dest`, `.spira-link-arrow` con deslizamiento, y `.spira-row-link` resaltando también con teclado.
- **#105** — El mosaico 2×2 con "Reportes pendientes", tres renglones por tarjeta con dos formas de pie ("Ver más →" navega, "Ver más ⌄" despliega), los destinos de fila, y la barra de filtros de Alertas con la migración **0103**.
- **#106** — La fila de dispensación abre el modal de su visita en vez de mandarte a la lista de hoy.

Bajo el capó: cuatro tarjetas del Resumen extraídas a componentes con nombre, el buscador y el "Limpiar" extraídos a `components/FilterBar` (y Visitas usándolos), y la pestaña del detalle de protocolo mudada a la URL.

## Decisiones tomadas (no re-discutir)

1. **"Tareas personales" NO se implementó, y es correcto.** Es el sujeto de las cuatro variantes del handoff y no tiene tabla, RLS ni capa de datos. Queda en `TODOS.md` (P2) con su casa reservada (`inicio/tareas`). **Antes de la migración va un `/office-hours` para fijar el modelo**: el handoff dibuja la card, no el modelo.
2. **El mosaico es 2×2**: izquierda lo que hay que cerrar (Reportes, Alertas), derecha lo que estás esperando (Dispensaciones, Visitas). Cuando entren las Tareas van arriba a la derecha y Dispensaciones baja a la izquierda.
3. **El pie tiene dos formas y es a propósito.** "Ver más (n) →" con nombre de submódulo **navega** (Alertas, Visitas); "Ver más (n) ⌄" con chevron **despliega en el lugar** (Reportes, Dispensaciones, que no tienen pantalla a la que mandar). Ninguna fila queda escondida.
4. **La cabecera de Alertas se tiñe por la PEOR alerta presente, nunca fija en rojo** como el mock: una cabecera siempre roja exagera un dato clínico todos los días y gasta la señal.
5. **Las filas se aplanan sólo en el Resumen.** La vista de Alertas conserva sus superficies teñidas: a veinte ítems de tipos mezclados, el bloque de color es cómo se encuentra la grave sin leer.
6. **El reporte pendiente va en azul** (`--spira-acc-deep-blue`) y no en el petróleo de marca: el petróleo es el acento del módulo y una fila teñida con él se lee como "seleccionada".
7. **La fila de dispensación abre el modal, no navega.** El pedido se resuelve EN la visita, y además `track/visitas` no podía abrirla si era de otra fecha.
8. **Los tartamudeos colapsan sólo con cadenas idénticas, nunca "si una contiene a la otra".** En la misma visita hay dos reportes del mismo procedimiento ("ECG" y "Electrocardiograma (ECG)"): colapsar por "contiene" los dejaría indistinguibles.

## Lo que quedó verificado al cierre

**Verificado por el Director en pantalla, con datos reales:**
- El mosaico 2×2 y el recorte a tres renglones.
- Las filas de reporte con su superficie azul (ya no el recuadro negro).
- La cabecera de Alertas sin asomar por las esquinas y con aire suficiente.
- La fila de dispensación abriendo el modal de su visita.

**Verificado por medición, con datos falsos (banco de pruebas, ya borrado):**
- Reposo, `:hover` y `:focus-visible` de KPI, fila y pie.
- El revelado disparando con Tab.
- Las reglas de `prefers-reduced-motion`.
- Contraste sobre fondo compuesto en los dos temas: claro 5,04–7,51 · oscuro 6,01–11,95, todo AA.
- El presupuesto de ancho de la fila de visitas: columna 586px, texto 442px, vocabulario 340px, nombre de 30 caracteres sin truncar.

**NO verificado:** todo lo que necesita sesión (ver abajo).

## PRÓXIMO PASO (próxima sesión)

**QA logueado, en este orden.** Es lo único abierto de la jornada:

1. **Tildar un médico en Alertas** y confirmar que los reportes pendientes reaccionan igual que las alertas de visita. Es exactamente lo que la 0103 vino a habilitar y no se probó con datos reales.
2. **El deep-link de Reportes** desde el Resumen: que la fila caiga en el protocolo correcto con la pestaña ya abierta. Es lo que más plomería tocó (`NavTarget` → `ProtocolsView` → `ProtocolDetailView`).
3. **Con una cuenta sólo de Coordinación**, el scopeo de la RLS. Afecta a tres tarjetas y a los filtros. **La cuenta de QA tiene los cinco módulos y no lo reproduce**: te va a mostrar todo y vas a creer que anda.
4. **El barrido de `.spira-link-arrow`** con su deslizamiento nuevo (~15 pantallas: Visitas del día, la cola del médico, Alertas) y **la sombra de hover, que ahora es global** — mirá Inicio y las tarjetas de módulo.

Después de eso, lo más grande en la mesa es **Tareas personales**, y arranca por `/office-hours`, no por la migración.

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -1        # 6953ca1, el merge de la #106
npm run build                             # verde: 552 tests
node scripts/check-migraciones.mjs        # ✓ 103 migraciones, índice al día
```

En la app: **Coordinación › Resumen** tiene que abrir con cuatro tarjetas parejas y KPIs que se elevan al apuntarlos revelando el nombre de su destino. **Coordinación › Alertas** tiene que mostrar la barra con Estado, Protocolo, Médico, Coordinador y buscador, y **ninguna fila con borde oscuro**: si volvés a ver un recuadro casi negro, alguien reintrodujo la concatenación de hex en `alertItemStyle` (ver §5 de la narrativa).
