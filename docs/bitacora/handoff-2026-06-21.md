# Handoff · 2026-06-21 (fin de jornada)

Estado para retomar en una sesión nueva (post `/clear`). Narrativa completa: [`2026-06-21.md`](./2026-06-21.md).

## Dónde estás
- **Rama:** `feat/cronograma-protocolo`, **sincronizada con `origin`** (todo pusheado, `a4c71ce`).
- **El árbol ya incluye el login nuevo**: se mergeó `origin/main` (PR #1: rediseño de login + Google OAuth + reset de contraseña) dentro de esta rama, **sin conflictos**. `npm run dev` desde el directorio principal muestra **cronograma + login nuevo** juntos.
- `npm run build` **verde**.

## Migraciones (todas APLICADAS en prod)
0026 (RLS+RPCs cronograma) · 0027 (`count_deletable_visits`) · 0028 (track-admin ve protocolos) · **0029** (cuadro completo Fase 1: `visit_definitions.role`, `date_mode`, vistas + generación/sync/KPIs solo `automatica`). **La próxima migración nueva sería la 0030 (Fase 2).**

## Lo hecho esta jornada
1. **Cronograma del protocolo** (cierra "Próximas visitas: 0"): editor por protocolo (pestaña "Cronograma" en Detalle de Protocolo) + generar/sincronizar. Capa `src/data/visitDefinitions.ts`, UI `src/views/track/Schedule*`.
2. **Evolución a cuadro de actividades completo — Fase 1** (IMPLEMENTADA): `role` (screening/randomizacion/comun) + `date_mode` (libre/automatica); el editor usa **un select "Etapa"**; display **"código - nombre"** (helpers `visitTitle`/`visitCode` en `lib/visits`; labels puros en `lib/visitLabels`). Generación/sync/KPIs solo automáticas.
3. **Login** mergeado a esta rama (venía de `origin/main`).
- Todo revisado con `/autoplan` y **verificación adversarial** (workflows); fixes aplicados (mark_ready idempotente, preview==apply en sync, etc.).

## PENDIENTE — Fase 2 (flujo operativo), NO implementada
Spec/plan: `docs/superpowers/specs|plans/2026-06-21-cronograma-cuadro-completo*` (plan en 2 fases; Fase 1 hecha). Fase 2 = **migración 0030 + UI**:
- `schedule_protocol_visit` (agendar las pre-rando desde el cuadro) y "Agendar visita" elige de las definiciones libres.
- `mark_ready_with_outcome`: al pasar a **"Listo para irse"**, alerta de **screening** (¿IVRS? → `patients.code`) / **randomización** (¿randomizó? → fija `randomization_date` y genera tratamiento; **idempotente, no pisa**) con **recitar** o **marcar fallo** (`discontinue_enrollment`).
- **Cerrar el camino viejo** de sueltas (screening/firma/rando) cuando el protocolo tiene cuadro.
- Salvaguarda: alerta de "randomización atendida sin confirmar".
- Para retomar: leer el plan y ejecutar la sección "FASE 2" tarea por tarea.

## Decisiones tomadas (gate /autoplan)
- **Data actual es de beta → borrón y cuenta nueva** (no migrar legacy). Script `supabase/scripts/2026-06-21-reset-beta.sql` (lo corre el usuario; review-first).
- Camino viejo de sueltas se cierra cuando hay cuadro · un solo select "Etapa" · "No randomizó" → recitar o fallo.

## Pendientes del usuario / notas
- **Backfill** (sobre data nueva): por protocolo con randomizados, definir el cuadro → "Generar / actualizar".
- **QA visual** queda del lado del usuario: en esta máquina **WDAC bloquea el headless** de gstack (`/browse`), así que la verificación visual es a ojo con `npm run dev`.
- **Merge de `feat/cronograma-protocolo` → `main`**: pendiente (cuando se cierre Fase 2). No taggeé versión (Fase 1 es checkpoint).
- **`stash@{0}`** = copia redundante del login (ya está commiteado vía el merge) → **se puede dropear** (`git stash drop stash@{0}`). No lo dropeé por las dudas.
- Untracked sin commitear: `.claude/worktrees/` (worktree del login, `e4e612d`) y `.gstack/` (estado local de gstack) — convendría agregarlos al `.gitignore` (no se tocó porque el `.gitignore` tenía cambios en el stash).
- Deuda menor anotada en el plan/review: flip de una def `automatica`→`libre` deja sus visitas viejas inertes (raro; lo cubre el reset); `coalesce(date_mode)` en KPIs es defensa redundante.

## Cómo verificar al retomar
`git status -sb` + `git log --oneline -10` · `npm run build` · leer este handoff + `2026-06-21.md`. Confirmar que las migraciones 0026–0029 siguen aplicadas antes de prometer comportamiento en vivo.
