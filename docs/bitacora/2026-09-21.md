# Bitácora · 2026-09-21 — La dispensación se lee como comprobante · release `v0.87.0`

> **Un «ya la mergeé» no dice QUÉ se mergeó.** El arreglo de `Esc` se pusheó a la rama de la #277
> minutos después de que el Director la mergeara, y quedó afuera sin que nada lo avisara: la PR
> figuraba cerrada, el commit estaba en la rama y la descripción decía «arreglado en esta PR». Se vio
> recién en el cierre, preguntándole a git si el commit era ancestro de `main`. Es la segunda vez en
> dos días (la #261 el 20): antes de dar por hecho un merge, se verifica el contenido, no el estado.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** el handoff de diseño «Dispensación — estado de la entrega en el modal de visita» aplicado de
punta a punta —spec, implementación, QA logueada contra prod— y el release que versiona todo lo que entró
después de la `v0.86.1`
**Ramas:** [#277](https://github.com/spiraclinicapp/Spira-App/pull/277) (`feat/dispensacion-estado-entrega`) y [#281](https://github.com/spiraclinicapp/Spira-App/pull/281) (`fix/esc-popover-visita`), mergeadas; este cierre en
`docs/cierre-2026-09-21`. **Sin migraciones propias.** Release **`v0.87.0`** (el tag sale después del
merge de este cierre, con `tag-release.mjs`). Último tag antes de hoy: `v0.86.1` · prod = `f490855` al
escribir esto (leído del bundle de `spira-app.vercel.app`).

Sesión `Comprobante dispensacion`, en su propio worktree (`.claude/worktrees/dispensacion-estado`).

## 1. Resumen de la jornada

| PR | Qué | Cómo arrancó |
|---|---|---|
| [#277](https://github.com/spiraclinicapp/Spira-App/pull/277) | La entrega de la visita se lee como **comprobante** (ticket con sello, N°, fecha y quién entregó) y las entregas de las otras visitas van en el chip **«Historial»** de la banda | «¿Podés aplicar este handoff?» → «en las preguntas elegí siempre la recomendada, hacé el spec y ejecutalo» |
| [#281](https://github.com/spiraclinicapp/Spira-App/pull/281) | **Esc con un popover abierto ya no cierra la visita** | lo destapó la QA de la #277; su commit quedó afuera de ese merge |
| este cierre | Bitácora, handoff y release `v0.87.0` | «ya la mergeé, hacé el cierre de jornada» |

De otras sesiones, el mismo día: **Estudios en Farmacia** en cuatro PRs —[#276](https://github.com/spiraclinicapp/Spira-App/pull/276) (el modelo, la
consola en Ajustes y el recorte de estudios y pacientes; `0139`, aplicada y marcada en la [#278](https://github.com/spiraclinicapp/Spira-App/pull/278)),
[#280](https://github.com/spiraclinicapp/Spira-App/pull/280) (`0140`, stock y recepción), [#282](https://github.com/spiraclinicapp/Spira-App/pull/282) (`0141`, dispensaciones) y [#283](https://github.com/spiraclinicapp/Spira-App/pull/283) (`0142`, reposición y el bucket
`ip-docs`)—, más la coda del cierre del 20 ([#274](https://github.com/spiraclinicapp/Spira-App/pull/274), [#275](https://github.com/spiraclinicapp/Spira-App/pull/275)) y su anexo E.10 ([#284](https://github.com/spiraclinicapp/Spira-App/pull/284), abierta al escribir
esto). **La `0140`, la `0141` y la `0142` quedaron aplicadas en prod el mismo 21, al tercer intento** —las dos
primeras corridas la sonda seguía dando `false`—; según esa sesión, la sonda del editor da `true` en las tres
y sus funciones pasaron de `PGRST202` a `200` por la API; su marca en el índice entró en la [#285](https://github.com/spiraclinicapp/Spira-App/pull/285). **Después
el Director miró la consulta del bucket `ip-docs` de la `0142` y dio bien** (dos filas, las dos recortadas):
**el recorte por estudio de Farmacia quedó completo en prod y la regla de «no acotar a nadie» se levantó**. Esa
marca va en la [#287](https://github.com/spiraclinicapp/Spira-App/pull/287), abierta al escribir esto.

## 2. El handoff: qué se eligió y por qué

El bundle llegó por `Downloads/Spira — Identidad Visual (6)/design_handoff_dispensacion_estado/` y se
copió a [`docs/design_handoff_dispensacion_estado/`](../design_handoff_dispensacion_estado/) antes de tocar código. Traía **tres variantes** del
comprobante (A, B, C) y **tres accesos** al historial (H1, H2, H3) como alternativas a decidir. Las dos
capturas del bundle eran del Director y mostraban **C (ticket) + H1 (chip «Historial»)**; el README
marcaba H1 como preferido. Eso se tomó como la elección.

El pedido fue explícito: *«en las preguntas elegí siempre la recomendada, hacé el spec y ejecutalo»*. Así
que las 16 decisiones quedaron escritas en el spec
([`2026-09-21-dispensacion-estado-de-entrega-design.md`](../superpowers/specs/2026-09-21-dispensacion-estado-de-entrega-design.md)), cada una con su porqué, en vez de
preguntarse una por una. Las que se apartan del mock, a propósito:

- **Color y tipografía, de los tokens vivos.** El mock traía la paleta vieja (`--spira-muted #7C8C87`,
  Hanken + IBM Plex Mono, banda `#2E7D74`). La geometría sí sale del mock.
- **El sello va sobre el color PROFUNDO del estado.** El mock dice «texto blanco sobre color pleno» como
  garantía de accesibilidad, y blanco sobre `#5C8A5A` da 4,0:1 y sobre `#B0823F` 3,4:1: fallan AA.
- **La palabra del estado es la de la casa** (Solicitada / Preparando / Lista para retirar / Entregada),
  no «En preparación» para todo: eso es falso para una solicitud que Farmacia todavía no tomó.
- **Sin N° hasta que se emite.** El correlativo de una preparación cancelada queda reservado.
- **La constancia dice «cargada», no «firmada»**, y no dice quién la subió: la base no sabe si está
  firmada, y el nombre de quien la subió pide una migración (queda pendiente).

## 3. La implementación, sin migración

- **Las reglas, puras y con test.** `comprobanteModel.ts` decide qué pedido es ticket, el N°, la línea de
  contexto en hora argentina, el enlace del pie, qué rechazo avisar, cuándo la sección del IP va debajo
  de los tickets y qué frase dice «Sin entrega». `historialEntregasModel.ts` arma el historial: ordenado
  por la ENTREGA, «Sin entrega» por parte y la constancia vigente. **28 tests nuevos**, que pasan también
  con `TZ=UTC` (el CI corre en UTC).
- **Quién entregó ya estaba en la base**: `dispensations.delivered_by_name` es de la `0119`. Alcanzó con
  pedirlo en `REQUEST_COLS`. El historial es una lectura nueva, `useEntregasDelEnrolamiento`, con el
  mismo patrón `!inner` de `useEntregasIpDelEnrolamiento`.
- **Se retiró el historial plegado** del pie (`HistorialPlegado.tsx` y su modelo). Lo que otros seguían
  usando —`estaCerrado` y `rechazoVigente`— se mudó a `pedidosCerradosModel.ts`. El rechazo vigente,
  que antes abría el historial desplegado, ahora es un aviso en el cuerpo.
- **El formulario de carga no cambió de lógica**: avisos, selector, «Otro», «En partes», excepción fuera
  de cronograma y «Solicitar» quedaron igual. Aparece si no hay ticket, si se abre con «Nueva
  dispensación» / «Corregir esta entrega», o si hay algo sin enviar.

## 4. La QA logueada contra prod

A 1536×864 (la notebook de referencia), sólo mirando en los estudios reales y escribiendo sólo en
`TEST-QA`:

- **Entregada, el caso exacto del mock**: Azcurra, 222714, V3, N° 86, «16 Sep 2026 · 10:53 · entregó
  Spira Clinic», con la constancia «#25 - UMBRIEL - V18.pdf» y «142 KB · cargada 16 Sep 2026 10:47 ·
  1 kit».
- **El recorrido de un pedido en TEST-001**: pedir → «Solicitada · Sin número todavía» → cambiar la
  cantidad desde el ticket → Farmacia lo toma → «Preparando · lo tiene Spira Clinic», sin lápiz ni
  «Cancelar» → Farmacia cancela la preparación → «Cancelar solicitud» desde el ticket. TEST-001 quedó
  con un pedido cancelado más en su historia.
- **Sin entrega** (Azcurra V1), **Corregir esta entrega**, y el **chip** con los seis pedidos de Hugo
  Sarmiento (ACT18301).
- **No se vieron con datos reales**: el N° reservado —tomar el pedido NO crea la fila de dispensación,
  así que el caso no se da cancelando la preparación— y «Lista para retirar», que pide escanear. Los
  dos están cubiertos por el test y se miraron en un banco de pruebas temporal.

## 5. El Esc que cerraba la visita, y el commit que quedó afuera

Con el popover del historial abierto, **un `Esc` cerraba el popover y también la visita entera**.
`VisitDetail` escucha Escape en `document` y se registra al montar el modal, o sea ANTES que cualquier
popover; corre primero y cierra. El `stopPropagation` de `usePopover` no frena a otro listener del mismo
nodo. **No era del chip**: valía para cualquier popover sin campo de texto adentro del modal.

La cura fue `hayPopoverAbierto()` en `usePopover.ts` —el registro `abiertos` sólo guarda popovers
montados, y todos los de la casa se montan únicamente abiertos— y que `VisitDetail` no cierre mientras
devuelva `true`. Verificado en el navegador: Esc cierra el popover y el foco vuelve al chip; sin popover,
Esc sigue cerrando la visita.

El commit se pusheó a la rama de la #277 **después** de que el Director la mergeara (el merge entró
hasta `389eb1b`). Se detectó en el cierre con `git merge-base --is-ancestor`, se cherry-pickeó sobre
`main` y salió en la #281. La descripción de la #277 se corrigió para no afirmar que lo incluía.
`components/Modal.tsx` tiene el mismo patrón y **no se tocó**: quedó como tarea aparte.

## 6. El release, y quién lo saca

Después de la `v0.86.1` entraron nueve PRs de tres sesiones. «Color verde visita» había propuesto que la
versión la sacara la sesión de Estudios en Farmacia —la que más PRs tenía— para que no salieran dos.
Pero el Director eligió en este chat «`v0.87.0` ahora». Se resolvió por mensaje entre las tres sesiones:
**la saca esta**, y Estudios en Farmacia mandó su mitad de la línea.

La línea es **«La dispensación se lee como comprobante con su historial, y Ajustes suma Estudios en
Farmacia.»** (94 de los 100 caracteres que permite `version.test.ts`). La mitad de Farmacia nombra
**sólo lo que el usuario ve hoy** —la tarjeta de la #276 en Ajustes, con la `0139` aplicada—: la #280, la
#282 y la #283 son SQL que, cuando se escribió la línea, todavía no estaba aplicado. Prometer «Farmacia se
acota por estudio» habría sido anunciar algo que en ese momento prod no tenía. La regla se levantó un rato
después (§1); la línea quedó como estaba, porque lo que el usuario ve sigue siendo la tarjeta de Ajustes.

## 7. Lo no-obvio

- **Un listener de `document` registrado antes corre antes.** `stopPropagation` corta la subida a
  `window`, no a los otros listeners del mismo nodo. Un contenedor que cierra con Escape desde
  `document` tiene que PREGUNTAR si hay algo abierto adentro; no alcanza con que lo de adentro «frene»
  el evento.
- **`--spira-primary` y `--spira-track` no cambian en oscuro** (2,4:1 y 3,1:1 sobre la card oscura). Un
  texto o un hover «petróleo» que tenga que leerse en los dos temas va en `--spira-acc-deep-teal`. Y para
  un sello sólido con texto: fondo `--spira-acc-deep-*` y tinta `--spira-white`, que se invierten solos
  entre temas.
- **Tomar un pedido en Farmacia no crea la fila de `dispensations`**: nace recién al marcarlo listo. El
  «número reservado por una preparación cancelada» no se reproduce cancelando la preparación desde el
  cajón; es un caso de filas viejas o de otro camino.
- **Un handoff con alternativas A/B/C no dice cuál se eligió en el texto.** Lo dijeron las capturas: eran
  del Director y mostraban una sola combinación.
- **La emulación de 1536×864 en el preview deja las capturas recortadas o chicas**: el marco de
  coordenadas es de 800×450. Para verificar, DOM y estilos computados; la captura sólo para mirar.

## 8. Git — estado de las ramas

- `feat/dispensacion-estado-entrega` → mergeada en la #277. Tiene un commit más (`d113e17`) que NO está en
  `main` con ese SHA: entró cherry-pickeado como `d14a0ea` por la #281. La rama se puede borrar.
- `fix/esc-popover-visita` → mergeada en la #281.
- `docs/cierre-2026-09-21` → este cierre (bitácora, handoff y bump a `0.87.0`).
- El worktree `.claude/worktrees/dispensacion-estado` queda con esta rama; se borra **desde afuera y con el
  directorio en el mismo paso**, cuando la sesión esté cerrada.
- La copia del Director quedó en `main` al día hasta `f490855`; el merge de este cierre la deja un commit
  atrás.

## 9. Pendientes

1. **El tag `v0.87.0`**, después de mergear este cierre: `node scripts/tag-release.mjs --dry` y después sin
   `--dry`.
2. **Mergear la #287** (Estudios en Farmacia): la marca de la consulta del bucket y de la regla levantada. El
   recorte por estudio ya está completo en prod; lo que falta probar es una cuenta acotada que no sea gerencia.
3. **Quién subió la constancia**: pide `dispensation_ip_documents.uploaded_by_name` (snapshot + trigger +
   backfill). Es aditiva → la migración va ANTES del front.
4. **`Modal.tsx` tiene el mismo Esc que `VisitDetail`**: quedó como tarea aparte, para reproducirlo antes de
   tocarlo.
5. **Lo que el propio handoff dejó sin dibujar**: el IP que no corresponde adentro del ticket, la entrega
   parcial / saldo, «Otro medicamento» con receta y el ticket en modo corrección. Hoy se resuelven con lo
   que ya existía.
6. **La constancia de Azcurra V3 («UMBRIEL - V18»)** —pendiente del 20— ahora se lee en grande en el ticket.
   Que el Director mire si es el papel de otro estudio.

---

# Anexo A — Estudios en Farmacia: el recorte por estudio, de punta a punta

> Escrito por la sesión `feat-estudios-en-farmacia`, que arrancó el 20 a la noche con un pedido de una
> línea y terminó el 21 con cuatro migraciones en prod, el único archivo del schema que nadie miraba
> cerrado, y un chequeo en el CI para que no se vuelva a abrir. **Lo que más costó no fue el SQL: fue
> saber si estaba aplicado.**

## A.1. Resumen

| PR | Qué |
|---|---|
| [#276](https://github.com/spiraclinicapp/Spira-App/pull/276) | **PR 1** — el modelo (`ve_todos_los_estudios`, `pharma_protocol_access`), las funciones `pharma_alcanza_*`, la tarjeta «Estudios en Farmacia» en Ajustes › Equipo y accesos, y el recorte de estudios, inscripciones y pacientes. **`0139`** |
| [#280](https://github.com/spiraclinicapp/Spira-App/pull/280) | **PR 2** — stock y recepción, y la regla D6. **`0140`** |
| [#282](https://github.com/spiraclinicapp/Spira-App/pull/282) | **PR 3** — dispensaciones, con los RPC mixtos. **`0141`** |
| [#283](https://github.com/spiraclinicapp/Spira-App/pull/283) | **PR 4** — reposición, el bucket `ip-docs`, el barrido final y `scripts/check-alcance-farmacia.mjs`. **`0142`** |
| [#285](https://github.com/spiraclinicapp/Spira-App/pull/285) | La `0140`, la `0141` y la `0142` marcadas como aplicadas |
| [#287](https://github.com/spiraclinicapp/Spira-App/pull/287) | La consulta del bucket mirada y la regla de «no acotar a nadie» levantada (el commit que quedó afuera de la #285) |
| [#289](https://github.com/spiraclinicapp/Spira-App/pull/289) | El chequeo de alcance en el CI, como job propio |

La `0139` la marcó otra sesión en la [#278](https://github.com/spiraclinicapp/Spira-App/pull/278); la #279 de ésta quedó duplicada y se cerró. **Las cuatro
migraciones están aplicadas en prod (2026-09-21)** y la regla se levantó el mismo día. Las PRs de código
entraron en la `v0.87.0` con la mitad de su línea, «Ajustes suma Estudios en Farmacia» (§6 de arriba); la
#285, la #287 y la #289 son docs y CI y **no piden versión**.

## A.2. El pedido y las seis decisiones

*«Quiero que apliquemos la lógica a farmacia de asignar protocolos, predeterminado viene con todos el rol,
pero se le puede quitar la vista a ciertos estudios.»* Coordinación ya se aísla por protocolo; Farmacia
era central —veía todo— y el caso que lo disparó es alguien nuevo o externo que arranca acotado.

Las decisiones las tomó el Director una por una y están en el plan
([`plan-estudios-en-farmacia.md`](../plan-estudios-en-farmacia.md)):

- **D1 · RLS de verdad**, no un filtro de pantalla: ni por URL, ni por la API, ni en un reporte.
- **D2 · Dos estados por persona**: «ve todos» (el default) o una lista cerrada. Un estudio nuevo **no** se
  le abre solo a quien está acotado.
- **D3 · Todo Farmacia**, los seis submódulos.
- **D4 · El recorte es del MÓDULO, no de la persona.** Alguien con Coordinación del estudio X y Farmacia
  sin X ve X como coordinadora, no como farmacéutica.
- **D5 · Una tarjeta propia** en Ajustes, hermana de «Estudios en Coordinación».
- **D6 · Lo que no es de ningún estudio lo ve todo Farmacia** (el stock ambulatorio, `protocol_id` null).
  Se decidió a mitad de la PR 2, cuando apareció el caso.

Cuatro PRs, cada una a prod, con los números de migración sin fijar en el plan: el siguiente libre al
pushear (la regla D18 de antes). Hizo falta: otra sesión tomó la `0138` mientras ésta escribía la suya.

## A.3. Cómo se construyó

- **Funciones de alcance que no chequean el módulo ni el nivel.** `pharma_alcanza_protocolo`, `_paciente`,
  `_lote`, `_recepcion`, `_solicitud`, `_dispensacion`, `_pedido`, `_visita`, `_inscripcion` y
  `_archivo_ip` contestan una sola cosa —¿este estudio le toca?— y se **SUMAN con `and`** a la cláusula de
  Farmacia de cada policy. Nunca la reemplazan: 11 de las 42 policies comprueban NIVEL, y un reemplazo
  habría subido a cualquier visor a operador. Las cláusulas de gerencia, contable y Coordinación quedan
  letra por letra, afuera del `and`.
- **La RLS no alcanza a los RPC `security definer`**, que la saltean. Cada uno lleva una **guarda** después
  del `begin`, clasificada por **su propio chequeo de permiso**, no por la pantalla que lo llama:
  - **de Farmacia** (el cuerpo sólo autoriza a Farmacia): la guarda es el alcance, sin excepciones, ni
    siquiera para gerencia;
  - **mixto** (autoriza también por gerencia o Coordinación): la guarda deja pasar ese camino, copiado del
    propio cuerpo, y sólo corta a quien entra únicamente por Farmacia. Es lo que hace cumplir D4.
- **Un RPC que devuelve VARIOS estudios no se arregla con una guarda**: se le suma un **filtro**, una sola
  condición en el CTE del que cuelga todo el resultado (`pedidos_por_recibir`, `reposicion_del_periodo`).
- **Los cuerpos se copiaron por script, no a mano.** Cada RPC se extrajo de su migración VIVA (la última
  que lo define) y un comparador verificó que el cuerpo nuevo es **idéntico salvo una edición**. Con eso
  apareció que tres cuerpos vivos decían `create function` a secas: copiados así, en prod habrían cortado
  con *«already exists»*.
- **Probado en PGlite** con las migraciones reales encima de un esquema de juguete (30, 46, ~160
  verificaciones y todo verde en la PR 4), con `set role` a un rol sin privilegios: el superusuario de
  PGlite saltea la RLS aunque esté forzada, y sin eso el recorte no se prueba.

## A.4. El agujero que ningún barrido veía: el bucket `ip-docs`

Tres PRs barrieron todas las policies, vistas y RPC de `public` y dieron cero pendientes. El hueco más
grande estaba afuera: las dos policies del bucket de constancias de IP y recetas (`storage.objects`, `0071`)
autorizaban a Farmacia **por el módulo solo**. Con un `list()` se veían todas las rutas y se bajaba la
constancia de un estudio oculto. Lo encontró leer qué hace el front con los archivos, no el barrido.

La `0142` las rehace con `pharma_alcanza_archivo_ip`, que **no aplica D6**: el protocolo sale del principio
de la ruta, y una ruta sin protocolo no es un archivo «de nadie» sino un archivo roto, porque en ese
bucket todo es de un estudio. Por la misma razón, `pharma_alcanza_solicitud` resuelve el estudio de un
pedido viejo (anterior a la `0071`, con `protocol_id` null) por su visita: un pedido siempre es de alguien.

Como el editor puede no tener permiso sobre `storage.objects` y el «Success» no lo prueba, la `0142`
termina con una consulta sobre `pg_policies` que tiene que dar **exactamente dos filas recortadas**: una
tercera sería una policy hecha a mano que, por el OR, seguiría abriendo todo.

## A.5. Aplicar costó tres intentos

El Director corrió la `0140`, la `0141` y la `0142` y avisó que estaban. La API de prod decía que no: las
funciones nuevas daban `PGRST202`, igual que una inventada, mientras la de la `0139` respondía. Las volvió a
correr, y lo mismo.

Una sonda por la API **no distingue** «no aplicada» de «aplicada con la caché de PostgREST vieja». La que
zanjó fue una **de catálogo**, de sólo lectura, para el editor: `to_regprocedure(...)` por cada función
nueva y, para la `0140` —que no crea funciones, sólo redefine—, su **huella**: el comentario de
`pharma_alcanza_protocolo`, que pasó a decir «D6, 0140». Probada antes en PGlite en los dos sentidos. Dio
`false` en las tres: no era la caché.

Al tercer intento, **cada archivo en una pestaña nueva y sin texto seleccionado**, quedaron: catálogo en
`true`, API en `200`. **La causa de las dos primeras no se supo.** Ninguna tenía `create table` (el diálogo de
RLS que ya se tragó una migración) y la `0140` no tiene ni `drop` ni `delete`.

## A.6. La #285 entró incompleta, y esta vez fue por un push propio

Con la #285 ya avisada al Director, esta sesión le sumó un segundo commit a la misma rama (el registro
de la consulta del bucket) y actualizó la descripción contándolo. El Director mergeó entre medio: entró el
primero solo. Se rescató en la #287 y se corrigió la descripción. **Una PR que ya se le avisó al Director
queda cerrada para la sesión: lo que venga después va en una PR nueva.**

## A.7. El chequeo en el CI

`scripts/check-alcance-farmacia.mjs` resuelve cada policy, vista y función por su **definición viva** (el
último evento en orden de aplicación: una redefinida o dropeada después manda, y una tabla borrada no
cuenta) y falla si algo de Farmacia queda sin recorte. Sin la `0142` lista los 13 huecos que esa migración
cierra; con ella da **44 policies recortadas, 17 de catálogo global, 19 vistas `security_invoker` y 44 RPC
con guarda o filtro**. Desde la #289 corre en cada PR como job propio, «Alcance de Farmacia», por la misma
razón que ya separaba el índice del build: siendo un paso del job del índice, un hueco de numeración
legítimo lo dejaría sin correr.

## A.8. Lo no-obvio

- **Un barrido de RLS sobre `public` no ve `storage`.** Un archivo es un dato del estudio igual que una fila.
- **«Lo de nadie lo ve todo el módulo» no vale donde todo es de alguien.** D6 abre el stock ambulatorio; en
  el bucket y en los pedidos, un null es un dato roto o viejo, no un «de nadie».
- **`security definer` saltea la RLS**: recortar las policies no alcanza a un RPC. Y la guarda se clasifica
  por el chequeo del propio cuerpo, porque un mismo archivo del front llama RPC de los dos tipos.
- **Una migración que sólo redefine funciones no se ve por existencia**: conviene que cambie un comentario
  para dejar una huella que se pueda sondear.
- **La sonda de la API mide la caché de PostgREST, no Postgres.** Para «¿está aplicada?», catálogo.
- **`String.replace` con un reemplazo de texto que contiene `` $` `` inserta el prefijo**: triplicó el plan de
  la PR 1. Para reemplazar texto con texto, `slice` e `indexOf`.

## A.9. Git

- `feat/estudios-en-farmacia`, `-2`, `-3` y `-4`, `chore/0140-0142-aplicadas`, `docs/regla-farmacia-levantada`
  y `ci/check-alcance-farmacia`: mergeadas, se pueden borrar.
- `docs/cierre-2026-09-21-farmacia`: este anexo.
- El worktree `.claude/worktrees/feat-estudios-en-farmacia` es de esta sesión: se borra desde afuera cuando
  esté cerrada.
- La copia del Director quedó en `main` al día hasta `330b555` (la #289), verificada archivo por archivo.

## A.10. Pendientes de este hilo

1. **Probar el recorte con una cuenta acotada que no sea gerencia.** La del Director y la de QA tienen los
   cinco módulos y tapan cualquier agujero. La primera vez que se acote a alguien, mirar con esa cuenta que
   el estudio oculto desaparezca de Stock, Dispensaciones y Reposición.
2. **Una tabla o un RPC nuevo de Farmacia** ahora deja la PR en rojo: se le suma el recorte o, si de verdad
   no cuelga de ningún estudio, va a las excepciones del script con el motivo.
3. De la lista de §9 de arriba, **el tag `v0.87.0` ya está puesto** y **la #287 ya entró**.

---

# Anexo B — El Resumen de Inicio muestra sólo los módulos asignados

> Escrito por la sesión `unruffled-dubinsky-186253`, que arrancó con un pedido de una línea y una captura
> del Resumen. **Este cambio falla en silencio justo para quien más mira la pantalla:** el Director y el
> usuario de QA tienen todos los módulos, así que un filtro al revés no les cambia nada. Lo nota recién la
> farmacéutica, y lo nota como números raros, no como un error.

## B.1. Resumen

| PR | Qué |
|---|---|
| [#291](https://github.com/spiraclinicapp/Spira-App/pull/291) | En Inicio › Resumen, las **cards de módulo**, las **cifras de la banda de saludo** y los **números de clínica de la card de la Fundación** aparecen sólo si tenés un módulo que los calcule. Dos commits: `6f93230` (cards y banda) y `7146ff6` (la Fundación, pedida después de ver la PR). Sin migraciones |
| [#288](https://github.com/spiraclinicapp/Spira-App/pull/288) | *De más temprano, en el mismo worktree:* el E.10 del handoff del 20 deja de copiar el estado vivo de un hilo ajeno. Sólo docs del 20, donde está su narrativa. Entró por **cherry-pick**: ver §B.5 |

Este worktree ya había trabajado durante el 21 cerrando el 20 —la coda ([#274](https://github.com/spiraclinicapp/Spira-App/pull/274), [#275](https://github.com/spiraclinicapp/Spira-App/pull/275)), la marca de la `0139`
([#278](https://github.com/spiraclinicapp/Spira-App/pull/278)) y el E.10 ([#284](https://github.com/spiraclinicapp/Spira-App/pull/284))—, todo ya nombrado arriba. La única que faltaba acá era la #288.

Entró como `69a456d`, y **prod = `69a456d`** al escribir esto (leído del bundle de `spira-app.vercel.app`,
que ya sella el commit). Toca `src/`, así que **pide versión**. Lo mismo vale para la
[#292](https://github.com/spiraclinicapp/Spira-App/pull/292) de otra sesión (el cronograma del protocolo pasa a
franja teñida), mergeada detrás. Ver §B.7.

## B.2. El pedido y las decisiones

*«Quiero que en la vista resumen veas el widget únicamente de los módulos que tengas asignados.»* Las
decisiones las tomó el Director por pregunta, una por una:

- **El alcance: cards y banda.** Las tres cifras de la banda son de un módulo cada una («visitas hoy» y
  «ventanas vencidas», Coordinación; «dispensaciones pendientes», Farmacia). Ya con la PR abierta se sumó
  **«filtrá también los números de la Fundación»**, que entró como segundo commit en la misma PR.
- **Con un solo módulo, la card va a todo el ancho**: a media fila dejaba un hueco a la derecha y el Resumen
  dejaba de cerrar como un rectángulo (la regla del 2026-09-09).
- **La regla es la del riel, `moduloHabilitado`**, no una propia. Una card es una puerta al módulo: si el
  Resumen la dibujara con un criterio y el riel dejara entrar con otro, habría cards que llevan a un «no
  tenés acceso». Consecuencia buscada: **gerencia sola no ve nada operativo**, aunque la RLS le deje leer el
  centro entero, porque tampoco puede abrir esos módulos.
- **La identidad de la Fundación (logo y credenciales) queda para todos**: es la institución, no un dato de
  ningún módulo.

## B.3. Un número es de los módulos que lo pueden calcular

La card de la Fundación no tenía un módulo «dueño», así que la pertenencia se decidió por la **RLS vigente**
(la `0139` incluida), no por la pantalla:

| Número | Tabla | Coordinación | Farmacia |
|---|---|---|---|
| pacientes en seguimiento | `patients` + `enrollments` | ✓ sus protocolos | ✓ su alcance |
| protocolos activos | `protocols` | ✓ | ✓ su alcance |
| visitas realizadas | `patient_visits` | ✓ | — cero filas |
| % dentro de ventana | `patient_visits` | ✓ | — |

Por eso cada pieza declara una **lista** de módulos y aparece si tenés alguno (`deMisModulos`, con `some`).
La regla vive en `src/views/inicio/alcance.ts` y se prueba en `alcance.test.ts` **contra el registro real**,
no contra un catálogo de juguete: la regla de acceso ya está probada en `home.test.ts`, y lo que puede
romperse acá es el ensamble (que `track` y `pharma` sigan siendo las claves y ninguno quede `proximamente`).
Se comprobó que el test cae con la regla invertida y con `some` cambiado por `every`.

**De paso se fueron números falsos.** Farmacia no lee `patient_visits` —la RLS devuelve cero filas en
silencio—, así que a quien tenía sólo Farmacia el Resumen le mostraba «0 visitas hoy» y «0 visitas
realizadas», con cara de dato.

## B.4. Cómo se verificó

`npm run build` en verde (1496 tests). **No se probó logueado con una cuenta acotada**: no hay ninguna a
mano. Para ver los casos que ninguna cuenta disponible muestra, un banco de pruebas temporal montó la
**vista real** con un contexto de auth fabricado por caso (`AuthContext` exportado un rato y revertido, verificado
con grep), a 1185px:

| Caso | Banda | Números de la Fundación | Cards | Novedades = izquierda |
|---|---|---|---|---|
| los dos módulos | 3 cifras | los 4 | 2, de 392px | 499 = 499 |
| sólo Farmacia | dispensaciones | pacientes + protocolos | Farmacia, 797px | 499 = 499 |
| sólo Coordinación | visitas + ventanas | los 4 | Coordinación, 797px | 499 = 499 |
| sólo gerencia | sin bloque ni raya | sin bloque ni raya | ninguna | **340 ≠ 250** |

En **gerencia sola** la columna izquierda termina a los 250px y Novedades se queda en su piso de 340 (el
`minHeight` que la celda ya tenía para no aplastar el panel): quedan 90px de hueco al pie. Se dejó así a
propósito, porque ese piso ya estaba previsto; está en los pendientes.

## B.5. Lo no-obvio

- **Un filtro por módulo no se puede verificar con una cuenta que tiene todos.** Ni mirando ni con la QA
  logueada: hace falta el test contra el registro real, o un banco con cuentas fabricadas.
- **La pertenencia de un número la decide la RLS, no la pantalla donde está.** «Pacientes en seguimiento»
  parece de Coordinación y Farmacia lo calcula entero sobre su alcance; las visitas, no.
- **`git checkout -- <archivo>` no restaura un archivo NUEVO sin trackear**, y no avisa. Al invertir la regla
  para ver caer el test, la mutación quedó viva; la delató el aviso de «cambió en disco», no git. Se arregló
  antes de commitear. Ahora: commit WIP **antes** de mutar.
- **Las consultas de los módulos que no tenés igual salen**, y vuelven vacías o con error; simplemente no se
  dibujan. `useSupabaseQuery` no tiene `enabled`, y agregárselo no valía para esto.
- **Tercer commit del día que queda afuera de su merge.** El del E.10 se pusheó a la rama de la #284 **48
  segundos después** de que el Director la mergeara (`fad54d6` a las 19:05:03, el commit a las 19:05:51) y se
  rescató con cherry-pick en la #288. Es el mismo patrón que el Esc de la #277 (§5) y la #285 (A.6): con la
  PR ya avisada, lo que venga después va en una PR nueva.
- **Un contexto nuevo no hereda lo que el anterior acordó por mensaje.** La sesión del Anexo A le avisó a
  ésta «podés anexar tu B» porque el contexto anterior lo había pedido; el actual lo leyó como un mensaje mal
  dirigido hasta que la memoria y el reflog del worktree mostraron que no. Lo que se reserva con otra sesión
  conviene dejarlo escrito en el repo, no sólo en el chat entre sesiones.

## B.6. Git

- `feat/resumen-modulos-asignados`: mergeada (#291), se puede borrar.
- `docs/cierre-2026-09-21-resumen-modulos`: este anexo.
- El worktree `.claude/worktrees/unruffled-dubinsky-186253` es de esta sesión: se borra desde afuera cuando
  esté cerrada.
- La copia del Director estaba en `main` en `9451f04`, **atrasada**; desde el worktree no se puede tocar. Se
  actualiza con `git -C "C:\Users\Tutuca\Desktop\Spira\Spira App" pull --ff-only`.

## B.7. Pendientes de este hilo

1. **La versión.** La #291 y la #292 tocan `src/` y ya se despliegan, así que la `v0.87.0` quedó atrás de
   lo que corre. **Este anexo no saca versión** (no se pidió): queda para que el Director decida si sale y
   quién la saca, coordinando con la sesión de la #292.
2. **Probar con una cuenta de un solo módulo**: que a una farmacéutica el Resumen le muestre sólo la card de
   Farmacia a todo el ancho, «dispensaciones pendientes» en la banda y pacientes y protocolos en la
   Fundación.
3. **Gerencia sola: el hueco de 90px al pie.** Si esa cuenta existe y molesta, se baja el piso de Novedades
   o se le da otro contenido a la izquierda. Decisión del Director.
