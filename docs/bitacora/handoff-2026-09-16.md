# Handoff · 2026-09-16 (fin de jornada)

> Handoff anterior: [`handoff-2026-09-15.md`](handoff-2026-09-15.md) · Narrativa del día: [`2026-09-16.md`](2026-09-16.md)

> 🟢 **EL LISTADO DEL SITIO ESTÁ EN PRODUCCIÓN.** Las fechas ajustadas, **70 entregas de medicación** y
> el cierre de procedimientos de **321 visitas**. LTS17231 quedó **renumerado** (`…520001` a `…520008`).

> ⚠️ **CAMBIÓ UNA VERDAD DEL HANDOFF ANTERIOR.** Decía que «las visitas históricas nunca van a decir
> *Completa*». **Ya no es cierto:** 321 de ellas están en «Completa». Las que siguen en «Visita
> realizada» son **las dos últimas de cada inscripción, a propósito** — el Director las revisa a mano.

> ✅ **SIN SQL ESPERANDO.** La última migración sigue siendo la **`0126`**. Esta jornada **no trajo
> ninguna**: todo salió de datos que ya existían.

> ✅ **TODO MERGEADO Y VERSIÓN = PRODUCCIÓN.** Tres PRs ([#195](https://github.com/spiraclinicapp/Spira-App/pull/195), [#204](https://github.com/spiraclinicapp/Spira-App/pull/204), [#205](https://github.com/spiraclinicapp/Spira-App/pull/205)), todas mergeadas. Salió **`v0.76.0`**.

## Dónde estás

Release **`v0.76.0`**, última migración **`0126`**. `npm run build` verde: **1147 tests**
(`npx vitest run --exclude ".claude/worktrees/**"` da el número real; con los worktrees de otras
sesiones el conteo se dispara a 2273).

**Lo que hay que saber para tocar estos datos:**

- **Las 70 entregas se cargaron SIN LOTES y sin movimiento de stock**, a propósito: ocurrieron antes de
  que Farmacia llevara el circuito en Spira y nadie anotó el lote. Cada pedido lo dice en `notes`, y ese
  texto —`Carga histórica de entregas (listado del sitio, 2026-09-15)`— es por el que se las encuentra
  y lo que hace idempotente al script. **Farmacia sigue en cero**: estas entregas no movieron stock.
- **321 visitas quedaron «completa»; 129 siguen abiertas** y son exactamente las dos últimas de cada
  inscripción. Si aparece una tercera abierta, algo se rompió.
- **LTS17231 se renumeró**: seis inscripciones cambiaron de IVRS. Cualquier apunte viejo con
  `…520009`/`…520010` está desactualizado.
- **Las 246 visitas provisorias de Victorion siguen siéndolo** (fecha real igual a la estimada, con su
  nota). El cierre de procedimientos las alcanzó igual, que fue la decisión del Director.
- **Una visita atendida ya no aparece en el día en que estaba citada**, ni en Visitas ni en la cola del
  médico. Sólo en su día real.

## Lo hecho esta jornada

| PR | Qué |
|---|---|
| [#195](https://github.com/spiraclinicapp/Spira-App/pull/195) | Los tres generadores de la carga + las cinco consultas de verificación |
| [#204](https://github.com/spiraclinicapp/Spira-App/pull/204) | La tarjeta de Dispensación de una visita cerrada muestra qué pasó y deja corregirlo (+ el arreglo de `branch-guard`) |
| [#205](https://github.com/spiraclinicapp/Spira-App/pull/205) | Una visita atendida sale del día en que estaba citada; la cola del médico deja de tener el filtro duplicado |

## Decisiones tomadas (no re-discutir)

1. **Las entregas van sin lotes.** Inventar un lote sería meter inventario falso en una base auditada.
   Esto **esquiva** `apply_dispensation_stock`, que es `after update` y no se despierta en un insert: el
   candado protege el circuito vivo, donde hay stock que mover.
2. **La corrección sólo suma.** `guard_dispensation_immutable` prohíbe revertir una entrega. El copy lo
   dice en positivo: «la entrega anterior queda registrada, lo nuevo la corrige».
3. **Se descartó declarar «no correspondía entregar»** (el equivalente al «No corresponde» del IP): la
   medicación de base no tiene alerta que apagar, así que sería ceremonia. Si algún día la tiene, se revisa.
4. **El corte de la tarjeta es `ready_at`** (fin de atención), nunca `real_date` — ésa se pone al empezar
   a atender. Y con un pedido **abierto** la tarjeta sigue operable, porque un `lista` es un paquete
   esperando que lo retiren.
5. **Desde la ficha del paciente también se ve «Corregir entrega».** `readOnly` sale del ROL, no de la
   pantalla. Se decidió dejarlo: es coherente con «leer, con una salida explícita».
6. **Las dos últimas visitas de cada inscripción** quedaron sin cerrar, por inscripción y no por persona.

## PRÓXIMO PASO (próxima sesión)

**Las 129 visitas para revisar a mano.** Correr la **consulta 5** de
`supabase/scripts/verificacion-carga-2026-09-15.sql` (sólo lee) y exportarla a CSV: lista paciente,
visita, fecha y cuántos procedimientos y reportes le faltan a cada una. Victorion son 96 iguales, de 2
procedimientos y 1 reporte.

Después, por orden de valor:

1. **El desenlace del IP en una visita cerrada.** Hoy dice «Sin constancia cargada.», que describe un
   papel faltante y no un desenlace. La spec prometía los cuatro estados de `v_visit_ip_status`; se ven
   dos. Y si lo que hay que corregir es el IP, la única puerta es un botón que vive en la subsección de
   medicación concomitante.
2. **El IVRS del estudio madre en Farmacia** (pendiente del 15): el cajón, el kanban, la habilitación y
   el comprobante impreso traen el código por su cuenta.
3. **Un test que cruce `excluir` con un rechazo vigente** en `historialPlegadoModel`.

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -3          # v0.76.0 en main
npx vitest run --exclude ".claude/worktrees/**"   # 1147
```

En prod, las cinco consultas de `supabase/scripts/verificacion-carga-2026-09-15.sql` (sólo leen):

- **la 3** → `con_pendientes` tiene que sumar **129**;
- **la 4** → `70 · 86 · 70 · 0 · 0 · 0`. Los tres ceros del final son la prueba de que no se inventó
  ni un lote ni un movimiento de stock.

**Limpieza pendiente:** borrar el worktree `C:/Users/Tutuca/Desktop/Spira/wt-dia` (su rama ya está
mergeada) y el preview que quedó corriendo en el 5251.
