# Handoff · 2026-07-21 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-18.md`](./handoff-2026-07-18.md). Narrativa
del día: [`2026-07-21.md`](./2026-07-21.md).

> Hilo de esta entrada: **procedimientos por visita** (PR #16). Hoy hubo trabajo en paralelo en otras
> ramas (limpieza de datos de beta + UI, PR #15; plan de carga de visitas históricas, 0062). Ver la
> memoria del proyecto para esos.

## Dónde estás

- **Procedimientos por visita (SoA) implementado, QA logueado OK y en PR #16.** Cada visita del
  cronograma declara qué procedimientos lleva, desde un catálogo global reutilizable. Modelo híbrido:
  tabla nueva `procedures` + `protocol_activities` (latente desde 0002) revivida como el join. UI:
  píldora de conteo por fila + modal de chips (SearchableSelect + ↑/↓), sin drag&drop.
- **Migración `0061` aplicada en prod hoy** por el Director y registrada en `supabase/README.md`.
  **Última aplicada = 0061.**
- **La PR #16 está creada y pendiente de merge del Director** (no hay self-merge). Rama
  `feat/cronograma-procedimientos`, un commit (`9717b21`), rebaseada sobre el main actual.
  https://github.com/spiraclinicapp/Spira-App/pull/16
- **Working copy compartido:** al cerrar, el árbol quedó en la rama del Director
  (`feat/carga-visitas-historicas`). Los untracked son suyos, no míos.
- Sin release/bump: `package.json` sigue en `0.19.0`.

## Lo hecho esta jornada

- **Tres revisiones encadenadas** (`/plan-ceo-review` → `/plan-eng-review` → `/plan-design-review`),
  todas CLEARED, con voz externa que corrigió un error de hecho de la propia revisión. Plan/reporte
  durable en `~/.gstack/projects/spiraclinicapp-Spira-App/ceo-plans/2026-07-21-procedimientos-por-visita.md`.
  Bitácora §2–§3.
- **Implementación:** migración 0061 + `src/data/procedures.ts` + `ScheduleEditor.tsx` (columna +
  píldora) + `VisitProceduresModal.tsx` + `Icon.tsx` (+clip). Bitácora §4.
- **QA logueado end-to-end** en PROT-A demo (round-trip verificado, cero errores de consola, data de
  prueba limpiada). Bitácora §5.
- **Polish de la tabla** (`/impeccable polish`): destapó que Nombre había colapsado a 10px (columna
  `acciones` en `auto`); se arregló a ancho fijo + rebalanceo + píldora compacta + header "Proced.".
  Bitácora §6.

## Decisiones tomadas (no re-discutir)

- **Modelo B (híbrido):** catálogo global `procedures` + `protocol_activities` revivida como join
  (NO una tabla-join nueva; NO cuarto concepto sobre checklists/dispenses).
- **UI de chips, NO transfer-list** (se descartó el prototipo con drag&drop/FLIP): WCAG AA por
  construcción + menos código bespoke.
- **Guardado atómico** por RPC `set_visit_procedures` (reemplaza el set en una transacción).
- **Auditado** (`audit_row` en procedures y protocol_activities). El SoA es config regulatoria.
- **Procedimientos = metadata interna, no registro regulado** → hard-delete OK; el catálogo se
  gestiona (crear/eliminar) inline en el selector.
- **Orden en `suggested_order`** (columna que ya existía; no se agregó `sort_order`).
- **est_minutes fuera de v1**; requires_dispensation en el catálogo, con sugerencia no destructiva de
  marcar `dispenses` (no auto-flip, que Pharma lo lee en vivo).
- **Layout:** columna `acciones` a ancho fijo (con `auto`, Nombre colapsaba y header/filas no
  alineaban).

## PRÓXIMO PASO (próxima sesión)

1. **Mergear la PR #16** (lo hace el Director). El CI corre solo en la PR (build + `check-migraciones`).
2. **Coordinar 0061 ↔ 0062:** el plan de carga de visitas históricas reserva la 0062 y también toca
   `visit_definitions`. La 0061 solo la lee; coordinar el orden de aplicación al aterrizar ambas.
3. **Diferidos de la feature** (a TODOS.md si se quiere): superficie de procedimientos en la ejecución
   de la visita + registro en `patient_timeline.activity_id`; grilla SoA (visitas × procedimientos);
   herencia/copiar procedimientos entre visitas.
4. **Follow-up chico:** viewer read-only de procedimientos (hoy la píldora solo abre con `canEdit`).
5. Pendientes heredados (handoff 07-18): tablero logueado como coordinadora, Track no ve el nombre del
   medicamento (Fase 3), rotar la contraseña de QA.

## Cómo verificar al retomar

`git fetch` · `git status` · `npm run typecheck` (verde) · `node scripts/check-migraciones.mjs`
(61 migraciones, al día). La 0061 ya está en prod.

Para ver la feature: **Track → Pacientes → abrir un protocolo → pestaña Cronograma**. Cada fila de
visita tiene la píldora "Procedimientos": clic → modal de asignación. El catálogo viene sembrado (16
procedimientos estándar). El QA se hizo en **PROT-A (Protocolo A demo)**; no tocar el cronograma de
protocolos reales para probar (regla de datos reales — usar el demo). El plan/reporte completo está en
`~/.gstack/projects/spiraclinicapp-Spira-App/ceo-plans/2026-07-21-procedimientos-por-visita.md`.
