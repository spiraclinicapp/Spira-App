# Handoff · 2026-09-19 (fin de jornada)

> Handoff anterior: [`handoff-2026-09-17.md`](handoff-2026-09-17.md) · Narrativa del día: [`2026-09-19.md`](2026-09-19.md)

> 🆕 **ANEXO AL FINAL (tarde del 19 y madrugada del 20): el desenlace del IP ([#239](https://github.com/spiraclinicapp/Spira-App/pull/239)), el IVRS de Farmacia ([#242](https://github.com/spiraclinicapp/Spira-App/pull/242)) y los
> releases `v0.81.0` y `v0.82.0`.** En lo que se pisa, manda el anexo.

> ✅ **VERSIÓN = PRODUCCIÓN.** `v0.80.0` ([#231](https://github.com/spiraclinicapp/Spira-App/pull/231)), verificada contra el bundle de Vercel el 18. Desde entonces
> entraron la #232 a la #235, que son sólo documentos y SQL: el front no cambió, así que no hay release nuevo.

> 🟢 **LA GUARDA DE RECEPCIÓN ESTÁ EN PROD como `0132`** ([#234](https://github.com/spiraclinicapp/Spira-App/pull/234) y [#235](https://github.com/spiraclinicapp/Spira-App/pull/235)). Una recepción ya no se borra
> por fuera de la app, y sus renglones no se escriben por fuera de `create_reception`.

> ⚠️ **REPOSICIÓN PARTE 2 CORRE UN NÚMERO: `0133` (la aditiva) y `0134` (la limpieza).** Su plan en `main`
> ([#233](https://github.com/spiraclinicapp/Spira-App/pull/233)) todavía dice `0132`/`0133`: la `0132` ya es de la guarda. Lo corrige su Task 5, que verifica el número.

## Dónde estás

**Este handoff consolida el del 17**, que quedó como una pila de actualizaciones y anexos que se pisan: lo que
dice acá manda. Para el detalle de cada feature, el del 17 sigue valiendo (sus anexos de desviaciones y de
feedback), y el del 16 para lo de los datos.

`main` en `87227b3`. Release **`v0.80.0`** = producción. Última migración aplicada: **`0132`**, marcada en el
índice y en `CLAUDE.md`. **1263 tests** (sin cambios de código desde `v0.80.0`);
`check-migraciones` ✓ 132. **Nada a medio hacer** de las sesiones que cerraron: ninguna rama de trabajo viva.

**Lo nuevo que hay que saber:**

- **Una recepción no se borra, y sus renglones no se escriben directo** (`0132`: `trg_guard_reception_delete` y
  `trg_guard_reception_items_write`). Rechazan con `42501` a cualquiera que no sea `postgres`, **sin mirar el
  estado**. Si alguna vez hace falta corregir una recepción, se anula desde Recepción. Si escribís una función
  que toque esas tablas, tiene que ser del dueño (SECURITY DEFINER) o va a rebotar.
- **La numeración de migraciones hoy:** 0129 `feedback_lugar`, 0130 desviaciones, 0131 `feedback_visto`, 0132
  la guarda. **Lo próximo libre es la `0133`**, y está prometida a Reposición parte 2.
- **Antes de pushear una migración, `git grep` de su número en `main`, docs incluidos.** `check-migraciones`
  sólo ve archivos `.sql`: dos documentos pueden reservar el mismo número y el CI no se entera.

## Lo hecho esta jornada

| PR | Qué |
|---|---|
| [#231](https://github.com/spiraclinicapp/Spira-App/pull/231) | Release `v0.80.0` (la #220, la #222, la #225 y la #226), y la nota del release en la bitácora del 17 |
| [#234](https://github.com/spiraclinicapp/Spira-App/pull/234) | La guarda de Recepción, renumerada a `0132` y rebaseada; sin los cambios viejos a los docs de Reposición |

De otras sesiones: [#232](https://github.com/spiraclinicapp/Spira-App/pull/232) (cierre del 17, feedback), [#233](https://github.com/spiraclinicapp/Spira-App/pull/233) (plan de Reposición parte 2), [#235](https://github.com/spiraclinicapp/Spira-App/pull/235) (la
marca de la `0132`).

## Decisiones tomadas (no re-discutir)

1. **La `0132` es de la guarda de Recepción; Reposición parte 2 pasa a `0133` y `0134`.** Decisión del Director
   del 19.
2. **Con dos migraciones compitiendo por un número, va primero la que está lista**, y después la que depende de
   un deploy. Es el mismo criterio de la `0130` (desviaciones antes que la limpieza de Reposición).
3. **La guarda rechaza siempre, sin mirar el estado.** Una recepción pendiente cargada por error también se
   anula, no se borra (decisión del 17).
4. **Sin release desde `v0.80.0`:** nada de lo que entró llega al bundle.
5. **Un trigger no se da por verificado con una sonda sin sesión.** Se prueba con sesión (un INSERT con un
   `reception_id` inexistente: rebota la guarda o la FK, y no se escribe nada) o mirando `pg_trigger`.

## PRÓXIMO PASO (próxima sesión)

**Reposición parte 2**: la `/plan-eng-review` del plan de la #233 y después ejecutarlo, **con `0133` y `0134`**.
Es lo que más valor pone en uso: el submódulo existe en la base desde la `0128` y ninguna pantalla lo usa.

Después, por orden de valor:

1. **Los tres QA logueados del feedback** (handoff del 17, anexo 2, PRÓXIMO PASO): marcar uno como visto en
   «Feedback recibido», la bandera en la ficha real, y `Ctrl ⇧ F` con una visita abierta.
2. **La prueba de punta a punta de la guarda**, opcional: con sesión de Farmacia, el INSERT con un
   `reception_id` inexistente tiene que rebotar con «Los renglones de una recepción no se modifican por
   separado…».
3. **Mirar en pantalla la `v0.77.1`**, que sigue sin QA logueado desde el 16: «Fecha realizada» sola y «Fin de
   atención» con su hora.
4. **El desenlace del IP en una visita cerrada** dice «Sin constancia cargada.»: describe un papel, no un
   desenlace.
5. **ENDURA (222714) V17 a V28 no tienen procedimientos**: falta que el Director diga qué lleva cada una.
6. **El IVRS del estudio madre en Farmacia** (cajón, kanban, habilitación, comprobante).
7. **Los reportes de Quiroga (`4022001`) y Garay (`4022016`)** en Victorion.
8. **La revisión a mano de ACT18301, ENDURA y LTS17231** (consulta 5 de
   `supabase/scripts/verificacion-carga-2026-09-15.sql`).
9. **La visita hecha fuera de ventana** (desviaciones, caso 2): `TODOS.md`, P2. Su disparador es que un monitor
   pida el listado y falten las que sí se hicieron, tarde.
10. Sin apuro: un test que cruce `excluir` con un rechazo vigente en `historialPlegadoModel`; la píldora de
    reportes roja con un dato real; Esc con tres modales abiertos.

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main              # 87227b3 o algo encima
npx vitest run --exclude ".claude/worktrees/**"            # 1263
node scripts/check-migraciones.mjs                          # ✓ 132 migraciones, índice al día
```

**La `0132`, en el editor** (sólo lectura): `select tgname from pg_trigger where tgname like 'trg_guard_reception%';`
tiene que devolver las dos. Sin sesión no se puede sondear: las funciones trigger no se exponen por la API.

**La versión:** el bundle de `spira-app.vercel.app` tiene que decir `0.80.0`.

**Limpieza:**

- **`wt-mock-reposicion` está EN USO**: al cerrar el 19, una sesión de Reposición trabajaba ahí en la revisión
  de ingeniería del plan de la parte 2 (`docs/plan-reposicion-parte-2-eng-review`). No se toca.
- **`unruffled-dubinsky-186253`** queda con la rama de este cierre (`docs/cierre-2026-09-19`). Se borra
  **desde afuera y con el directorio incluido, en el mismo paso**. El 19 se vació dos veces con la sesión
  abierta: Windows no deja borrar la carpeta de una sesión viva, y una carpeta vacía hace que git caiga en la
  copia compartida.

---

## Anexo · Tarde del 19 y madrugada del 20: el desenlace del IP, el IVRS de Farmacia y dos releases

> Narrativa: [`2026-09-19.md`](2026-09-19.md), § Anexo. Spec:
> [`2026-09-19-desenlace-ip-visita-cerrada-design.md`](../superpowers/specs/2026-09-19-desenlace-ip-visita-cerrada-design.md).

### Dónde estás

- **`v0.81.0` fue a producción** el 19 (#240, bundle verificado). **Y de madrugada salió `v0.82.0`**, que
  versiona el IVRS de Farmacia (#242, ver § A.7 de la narrativa) y «lleva sangre» (#241, de otra sesión).
- Última aplicada: **`0134`**. Entraron la `0133` (Reposición parte 2A, #243) y la `0134` («lleva sangre»),
  las dos de otras sesiones. Ninguna es de la #239 ni de la #242: no tienen SQL.
- **1384 tests** en total (la #242 sumó 7; el resto es de las otras sesiones).

**Lo nuevo que hay que saber:**

- **La sección «Producto en investigación» dice qué pasó** con la misma frase que la fila de Procedimientos
  (`desenlaceIp`, en `src/views/track/ipEstado.ts`). Un test exige que la fila empiece con ella.
- **Visitas anteriores a la 0119:** dicen «Visita anterior al registro del IP en Spira.». La marca `lleva_ip` se
  lee con `useMarcaIp`, porque `v_track_visits` no la trae.
- **«Registrar la entrega»** de la sección y «Registrar entrega» de concomitante comparten condición y gesto
  (`puedeCorregir` / `abrirCorreccion`). Si se toca una, se tocan las dos.

### Lo hecho

| PR | Qué |
|---|---|
| [#239](https://github.com/spiraclinicapp/Spira-App/pull/239) | El desenlace del IP en una visita cerrada: spec, plan y código, sin SQL |
| [#240](https://github.com/spiraclinicapp/Spira-App/pull/240) | Release `v0.81.0` y este anexo |
| [#242](https://github.com/spiraclinicapp/Spira-App/pull/242) | El IVRS de cada estudio en Farmacia (cajón, kanban, comprobante, búsqueda e historial), sin SQL |
| `docs/release-v0.82.0` | Release `v0.82.0`, que versiona la #242 y la #241 |

Además, sin PR:
- el QA logueado del feedback: la bandera y el atajo pasan; «Marcar como visto» espera un feedback real;
- la guarda de la 0132 probada de punta a punta;
- la `v0.77.1` mirada en pantalla.

### Decisiones tomadas (no re-discutir)

1. **E1-E5 del spec del IP:**
   - una sola voz;
   - las históricas, honestas y sin acción;
   - la puerta propia;
   - sin el aviso sobre una entrega hecha;
   - sin SQL.
2. **«Marcar como visto» se prueba con el primer feedback real**, no con uno de prueba.
3. **La eng review de Reposición parte 2 es de la sesión de `wt-mock-reposicion`** (#237). Esta sesión le pasó
   sus cuatro decisiones y se bajó.

### PRÓXIMO PASO (próxima sesión)

1. **Confirmar el bundle:** después del merge del release de `v0.82.0`, `spira-app.vercel.app` tiene que
   decir `0.82.0`. (La `v0.81.0` ya se verificó el 19.)
2. **Lo que sigue en la lista**, que necesita algo del Director:
   - qué llevan las visitas V17 a V28 de ENDURA;
   - los reportes de Quiroga y Garay;
   - la consulta 5.
3. **El IVRS del historial de Farmacia, en la vista** (`TODOS.md`, P2): la `0135` que pasa la regla a
   `v_pharma_history` y arregla también su buscador, apenas se pueda pushear.
4. De paso, anterior a la #239:
   - el copy «La entrega anterior queda registrada…» en el modo corrección de una visita sin entrega;
   - el foco que se pierde al tocar las puertas de corrección.

Reposición parte 2 **está en ejecución en otra sesión** (`wt-reposicion-2`): no se toca.

### Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main              # el merge del release de v0.82.0
npx vitest run --exclude ".claude/worktrees/**"            # 1384
node scripts/check-migraciones.mjs                          # ✓ 134 migraciones, índice al día
```

**En pantalla** (sólo lectura, con sesión de Coordinación): la V1 de Fontana Toledo (LTS17231) dice «Sin
entregar: no se pidió a Farmacia.» con «Registrar la entrega», y la V4 de Calderon dice «Visita anterior al
registro del IP en Spira.».
