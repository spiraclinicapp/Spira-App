# Handoff · 2026-06-20 — estado para continuar (post v0.5.0)

Resumen para retomar Spira en una sesión nueva (después de `/clear`). Detalle narrado en
[`2026-06-19.md`](./2026-06-19.md) + [`2026-06-20.md`](./2026-06-20.md); visión en [`../#4- ROADMAP.md`](../#4-%20ROADMAP.md).

## Dónde estamos

- **Rama:** `feat/track-visitas-del-dia`, en `origin`, **taggeada `v0.5.0`** (la punta). Árbol limpio salvo
  WIP del usuario sin commitear (PDFs/HTML de `docs/`, `2026-06-17-ruteo-design.md`).
- **Build:** `npm run build` (= `tsc --noEmit && vite build`) verde. Preview: `npm run dev` (puerto en `.claude/launch.json`).
- **Migraciones aplicadas en Supabase prod:** hasta **0025**. (El usuario las aplica a mano en el SQL Editor;
  no tengo acceso SQL directo a prod.)

## Qué trae v0.5.0 (todo en esta rama)

1. **Visitas del día** (Track, 0023): etapas operativas `por_llegar→en_el_sitio→atendido→listo→fuera`
   (`arrived_at/ready_at/left_at` + RPCs gateadas), cola "Para ver médico", `track_dispensations`, vista
   Alertas. Archivos: `src/data/dayVisits.ts`, `src/views/DayVisitsView.tsx`, `DoctorQueueView.tsx`,
   `TrackAlertsView.tsx`, `src/views/track/{VisitStepper,DayVisitRowItem,DispenseModal,VisitChecklist}.tsx`.
2. **Eliminar paciente** (0024): RPC `delete_patient` (cascada, gateado leader+, auditado, guarda Pharma).
   UI: "Zona de peligro" en `src/views/EditPatientForm.tsx`. Data: `deletePatient`/`patientFootprint` en
   `src/data/patients.ts`. **Verificado e2e.**
3. **Ciclo de vida de la visita** (0025): registrar = agendar (`register_visit_event` setea `estimated_date`).
   Las pelotitas del tracker reflejan el recorrido: **gris** (sin atender) → **contorno verde marca** (atendida,
   abierta) → **relleno verde marca** (cerrada: `left_at` + sin checklist pendiente). Cada pelotita muestra el
   **número de visita** (conteo de todas). Lógica: `dotVisual`/`visitStateLabel`/`visitIndex` en
   `src/lib/visits.ts`; `dotColor` en `src/views/visitStates.tsx`; `src/views/track/VisitDot.tsx`;
   `PdVisitFlow.tsx`/`PdFullSchedule.tsx`. La ficha **solo agenda** (`RegisterVisitFlow.tsx`).
4. **Fix transversal:** `useSupabaseQuery` stale-while-revalidate (sin flash de "Cargando" al refetch).

## Pendientes (prioridad alta → baja)

1. **Merge a `main`** de `feat/track-visitas-del-dia` (v0.5.0). Siguen sin mergear también
   `feat/tablero-protocolo-ficha` y `feat/protocolos-unifica-pacientes` — decidir el orden.
2. **Task V completa** de Visitas del día con el runbook SQL
   (`docs/superpowers/verification/2026-06-19-track-visitas-del-dia-verificacion.sql`): gating con 2 roles,
   aislamiento RLS, audit_log. Opcional.
3. **Relleno en visitas históricas:** una visita con `real_date` pero sin pasar por el cierre operativo nuevo
   (sin "Fuera del sitio") se ve "Atendido" (contorno), no rellena, aunque su checklist esté completo. Si
   molesta, exponer en `v_track_visits` si la visita tiene checklist (migración chica) y rellenar por
   "checklist completo" en vez de por "cerrada".
4. **UX:** confirmación al avanzar etapa en Visitas del día (es fácil tocar la fila equivocada).
5. **Pharma** (siguiente módulo grande) y panel de gerencia (roles con clicks) — del roadmap.

## Constraints / lecciones (en memoria, ver MEMORY.md)

- **Solo tocar `TEST-*`** en la base demo (tiene datos reales; hubo un incidente de borrado recuperado del
  audit_log). Las escrituras a prod las hace el **usuario** (la preview que controlo es una sesión aparte).
- Migraciones: el usuario las aplica a mano en Supabase. Numeradas, no se renumeran.
- `public.dispensations` es de Pharma; Track usa `track_dispensations`.
- Tag = hito leíble contra la bitácora; `1.0.0` recién en producción con pacientes reales (Fase 3).
