# Handoff · 2026-06-20 — estado para continuar (post-merge v0.5.0 · cronograma WIP)

Resumen para retomar Spira en una sesión nueva (después de `/clear`). Detalle narrado en
[`2026-06-20.md`](./2026-06-20.md) (sección 5) + [`2026-06-19.md`](./2026-06-19.md); visión en
[`../#4- ROADMAP.md`](../#4-%20ROADMAP.md).

## Dónde estamos

- **`main`**: tiene **v0.5.0 mergeado** (las 3 ramas viejas fueron mergeadas/superadas y borradas) **+ el fix
  del Bug A (timezone)**. `main = origin/main`. `npm run typecheck` verde.
- **Rama activa (WIP):** `feat/cronograma-protocolo` (en `origin`) — la feature del cronograma del protocolo,
  **pausada** tras la migración 0026. **Estás parado en una u otra según dónde dejaste; para seguir la feature: `git checkout feat/cronograma-protocolo`.**
- **Migraciones aplicadas en Supabase prod:** hasta **0025**. La **0026 está en la rama, SIN aplicar** (el
  usuario las aplica a mano en el SQL Editor; no hay acceso SQL directo a prod).
- **gstack** instalado (`~/.claude/skills/gstack`): usar `/browse` para QA del browser; nunca
  `mcp__claude-in-chrome__*` (regla en `~/.claude/CLAUDE.md` global).

## Qué se hizo esta sesión

1. **Merge de v0.5.0 a `main`** (fast-forward) + limpieza de las 3 ramas feature.
2. **`CLAUDE.md`** del proyecto (gate `typecheck`, reglas duras, convenciones) + sección gstack en el global.
3. **Bug A "visitas pegadas" (timezone) — ARREGLADO en `main`** (commit `b84270f`): `useVisitsForDay`/
   `useDoctorQueue` anclaban mal la ventana del día; ahora usan offset `-03:00`. Verificado (7 → 6 visitas hoy).
4. **Bug B "Próximas visitas: 0" — diagnosticado**: los protocolos no tienen cronograma cargado (solo 2
   `visit_definitions`, sin UI). Spec + plan en `main`; implementación arrancada (abajo).

## WIP: feature "cronograma del protocolo" (cierra el Bug B)

- **Spec:** [`../superpowers/specs/2026-06-20-cronograma-protocolo-design.md`](../superpowers/specs/2026-06-20-cronograma-protocolo-design.md)
- **Plan:** [`../superpowers/plans/2026-06-20-cronograma-protocolo.md`](../superpowers/plans/2026-06-20-cronograma-protocolo.md)
- **Hecho (rama `feat/cronograma-protocolo`):** **Tarea 1-3** = migración `0026_protocol_schedule.sql` (RLS de
  edición **gerencia/track-admin**, cierra el borrado directo, +read track-admin; RPCs `sync_protocol_schedule`
  (preview/apply, no toca atendidas) y `delete_visit_definition` (bloquea si hay atendidas)).
  El doble-review cazó y corrigió una **regresión de seguridad de RLS** del plan.
- **Para retomar:** seguir el plan desde la **Tarea 4** (capa `src/data/visitDefinitions.ts` → UI editor/preview
  en Detalle de Protocolo → verificación) con subagent-driven-development.
- **Para que funcione en vivo:** aplicar **0026 en Supabase** y correr "Generar/actualizar cronograma" por
  protocolo (backfillea los 12 pacientes randomizados).
- **Decisión tomada:** editar el cronograma = **gerencia + track-admin**.

## Pendientes (prioridad alta → baja)

1. **Terminar la feature del cronograma** (tareas 4-8 del plan) + aplicar 0026 + backfill.
2. **Deuda del Bug B** (fuera del alcance de la feature, documentada): sueltas que nunca vencen
   (sin `window_end`); validar `real_date` futuro; regla de "cierre/arrastre" en Visitas del día
   (visitas cerradas que siguen en la lista del día).
3. **Pharma** (siguiente módulo grande) y panel de gerencia (roles con clicks) — del roadmap.
4. **Task V** de Visitas del día (runbook SQL, 2 roles) — opcional.

## Constraints / lecciones (en memoria, ver MEMORY.md)

- **Solo tocar `TEST-*`** en la base demo (tiene datos reales; hubo un borrado recuperado del audit_log). Las
  escrituras a prod las hace el **usuario**; la preview/`/browse` que controlo son sesiones aparte.
- Migraciones: el usuario las aplica a mano en Supabase. Numeradas, **no se renumeran**, **no se editan** una vez
  aplicadas (cada cambio = archivo nuevo).
- **No hay suite de tests** → gate = `npm run typecheck` + QA en browser.
- **QA logueado de Spira:** loguear por el **formulario** (sesión Supabase en `localStorage`, no en cookies →
  `setup-browser-cookies` no sirve).
- `public.dispensations` es de Pharma; Track usa `track_dispensations`.
- Tag = hito leíble contra la bitácora; `1.0.0` recién en producción con pacientes reales (Fase 3).
