# Handoff · 2026-09-22 (cierre corto)

> Handoff anterior: [`handoff-2026-09-21.md`](handoff-2026-09-21.md) (con sus anexos A a E, éste último con
> la coda `v0.88.1` marcada) · Narrativa del día: [`2026-09-22.md`](2026-09-22.md)

> 📦 **ÚLTIMO TAG `v0.88.1` · prod = `ab75b3f`** (sin cambios desde el cierre del 21 — este cierre no sacó
> versión). Nada nuevo que versionar: sin código.

## Dónde estás

`main` en `d3ec800` (la #303, coda `v0.88.1` y su Anexo E.10). Última migración **aplicada**: **`0142`**;
lo próximo libre es la **`0143`**. **1496 tests** (89 archivos); `check-migraciones` ✓ 142.
`check-alcance-farmacia` ✓ (sin cambios de schema desde el 21).

**Lo nuevo que hay que saber:**

- **El self-merge NO está bloqueado técnicamente.** `CLAUDE.md` sigue diciendo lo contrario A PROPÓSITO —es
  la costumbre del equipo, no un límite real—. Ver §2 de la narrativa del día y
  [[ci-y-pr-operativa]] en la memoria. No usarlo sin que el Director lo pida cada vez.
- Sin novedades de código: el 22 fue sólo confirmar la coda de otra sesión y mantener la carpeta del
  Director al día.

## Lo hecho esta jornada

| Qué | Detalle |
|---|---|
| Se probó y confirmó que el self-merge funciona vía API | A pedido explícito, sobre la PR #300. El Director decidió dejar `CLAUDE.md` como está y avisar al equipo |
| Se confirmó la coda `v0.88.1` (de `Layout impecable`) | PR #302, `ab75b3f`, tag sobre `d8b43fe` — verificado contra `origin/main`, no repetido de memoria |
| Carpeta del Director actualizada dos veces | `git pull --ff-only`, verificando árbol limpio antes |
| Este cierre | Bitácora y handoff del 22, sin release |

## Decisiones tomadas (no re-discutir)

1. **`CLAUDE.md` queda con el texto "el self-merge está bloqueado"**, aunque se comprobó que no lo está.
   Describe una política, no una traba técnica. Cualquier sesión que lo lea debe seguir pidiendo el merge
   al Director, no asumir que puede hacerlo sola.
2. **El hallazgo se avisó a las sesiones activas** (`Layout impecable`, `chirolita-ec`) para que ninguna
   cambie su comportamiento por su cuenta.

## PRÓXIMO PASO (próxima sesión)

Nada propio de este hilo. De los hilos anteriores, sin resolver:

1. **Probar el Resumen de Inicio con una cuenta de un solo módulo** (Anexo B) — la del Director y la de QA
   tienen todos los módulos.
2. **Confirmar en un navegador real el cambio en vivo del libro de Reposición** y recorrer las pantallas
   que faltan con el encabezado corrido (Anexo E, E.9).
3. **Probar el recorte de Farmacia por estudio con una cuenta acotada que no sea gerencia** (Anexo A).

## Cómo verificar al retomar

```bash
git fetch && git log --oneline -3 origin/main   # d3ec800 (la #303) o algo encima
git tag --sort=-creatordate | head -1           # v0.88.1
npx vitest run --exclude ".claude/worktrees/**" # 1496
node scripts/check-migraciones.mjs              # ✓ 142 migraciones, índice al día
```

**La versión:** el popover «Acerca de» de prod dice `0.88.1` y, debajo, el commit `ab75b3f`.

**Limpieza:** ninguna rama de código propia de este cierre. El worktree `unruffled-dubinsky-186253` sigue
siendo de esta sesión.
