# Handoff · 2026-06-25 (fin de jornada)

Estado para retomar en una sesión nueva. Handoff anterior: [`handoff-2026-06-24.md`](./handoff-2026-06-24.md). Narrativa del día: [`2026-06-25.md`](./2026-06-25.md).

## Dónde estás
- Rama `feat/pharma-1a`. **Migración `0032` APLICADA en prod y smoke-testeada** (commit `8b83f21`). La base de Pharma 1a está viva.
- **Última migración aplicada: `0032`** (ya no la 0031).
- `typecheck`/`build` siguen verdes (no se tocó TS esta jornada).

## Lo hecho esta jornada
- **Cambio de modelo (catálogo global):** `medications` sin `protocol_id` (+ `drug_id`→`drugs`); protocolo en el stock (`medication_lots.protocol_id`) + asignación explícita `protocol_medications`. Coherencia reubicada a la asignación + al lote. Spec actualizada (`b6fcd03`).
- **Sembrado en 3 capas** definido (Excel curado + top 100-200 + on-demand).
- **Plan de la 0032** (`9c3c111`).
- **0032 escrita, idempotente, aplicada + smoke-test OK** (`8b83f21`). Cazó y corrigió un **bug latente del 0003** (`apply_reception_stock`: `excluded.quantity` → `excluded.quantity_on_hand`).

## PRÓXIMO PASO (mañana)
1. Plan + implementación de **`src/data/pharma*.ts`**: hooks `useXxx` sobre `v_medication_stock` / `medications`+`drugs` / `medication_lots` / `protocol_medications` / recepciones / `medication_codes`; mutaciones por `supabase.rpc(...)` a las 6 RPCs (`create_drug`/`create_medication`/`assign_medication_to_protocol`/`create_reception`/`verify_reception`/`adjust_stock`); tipos a mano citando la 0032; errores serenos. Gate: `npm run typecheck`.
2. Después: vistas `pharma/medicamentos` + `pharma/recepcion` (espejo de Visitas) en `views/registry.tsx`.
3. Después: sembrado (script Node idempotente desde el Excel + top 100-200).
- **Al cablear:** probar los RPC **desde la app con usuario pharma-leader** (no como postgres). Y **smoke-test de cada trigger/RPC** que se toque (hay bugs latentes en 0002-0009; ver [[pharma-port-plan]]).

## Artefactos
- Spec: [`docs/superpowers/specs/2026-06-24-pharma-1a-medicacion-farmacia-design.md`](../superpowers/specs/2026-06-24-pharma-1a-medicacion-farmacia-design.md)
- Plan: [`docs/superpowers/plans/2026-06-25-pharma-1a-0032-migracion.md`](../superpowers/plans/2026-06-25-pharma-1a-0032-migracion.md)
- Migración: `supabase/migrations/0032_pharma_catalogo_global.sql`

## Cómo verificar al retomar
`git log --oneline -6 feat/pharma-1a` · `git status` · leer este handoff + `2026-06-25.md`. La 0032 **ya está aplicada en prod** (no re-aplicar; igual es idempotente). Próximo: capa de datos.
