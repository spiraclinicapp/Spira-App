# Handoff · 2026-08-06 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-08-04.md`](./handoff-2026-08-04.md). Narrativa del día: [`2026-08-06.md`](./2026-08-06.md) — **el día tuvo dos sesiones** y la bitácora las lleva las dos (§1-11 la primera, §12-20 la segunda).

> **Sesión 1 · Rediseño de los estados de la visita.** Dos ejes en paralelo: el **operativo** pasa de 5 etapas a **4** (Por llegar → Concurrió al centro → Inicio de atención → Fin de atención; "Fuera del sitio" sale del recorrido) y el **clínico** a **7 estados**, sumando "Siendo atendido" y "Por reprogramar". **PR #33** y **#34 MERGEADAS**, migraciones **0066/0067/0068** aplicadas, release **v0.27.0** sacado.
>
> **Sesión 2 · La card de Procedimientos y el retiro del checklist clínico.** El cuadro de procedimientos (0061/0064) terminó de reemplazar al checklist: se borró del front (`VisitChecklist`, `TemplatesView`, `useReportAlerts`) y salió del circuito en la base con la **0069**. Además, el polish de la fila del procedimiento. **PR #35** y **#36 MERGEADAS**, **0069 aplicada**, release **v0.28.0**.

## Dónde estás

- **`main`** tiene todo lo del día y está **desplegado**. Working copy limpio, en `main`.
- **Migración:** última aplicada = **0069**. El índice de `supabase/README.md` tiene marcadas la 0068 (2026-08-05) y la 0069 (2026-08-06); `CLAUDE.md` dice 0069.
- **Versión: `v0.28.0`** — al día con lo desplegado (el bump y el tag salieron en este cierre).
- **El checklist clínico ya no existe en la app.** Las cinco tablas `checklist_*` siguen en la base, intactas y sin uso, como histórico auditable.

## Lo hecho esta jornada

**Sesión 1** — brainstorming → spec → plan de 12 tareas → ejecución subagent-driven; las revisiones encontraron **7 defectos que el typecheck no veía** (§5). Migraciones aplicadas con un tropiezo (la 0068 sin la 0066) y una ventana de producción rota. QA logueado → PR #34. Release v0.27.0.

**Sesión 2** — un reporte de "no se ve nada" en la card de Procedimientos que **no era el bug que parecía** (§13), pero destapó un vacío mudo dentro de un `Panel`. `/impeccable polish` de la fila: **fuera el tachado** (en esta app significa "esto se borra"), **fin del parpadeo del tilde** (el optimismo se soltaba antes de que llegara el dato) y borde del tilde vacío a `muted` por contraste. Después, el retiro del checklist: front (**−663 líneas**, bundle de 990.18 a 974.73 kB) y base (**0069**). PR #35 y #36. Release v0.28.0.

## Decisiones tomadas (no re-discutir)

De la sesión 1 siguen firmes todas las del modelo de estados: los dos ejes son paralelos, "Fin de atención" es terminal y la marca el clínico, `real_date` se queda en "Inicio de atención", "Ventana vencida" le gana a "Por reprogramar", "Siendo atendido" vale solo para el día en curso, `mark_arrived` limpia `no_show_at`, el recorrido operativo describe **el día** de la visita (para una pasada manda el eje clínico), "Pendiente" fusiona `futura` y `proxima`, sin backfill, e `item_vencido` conserva su valor en la base.

De la sesión 2:

- **El cuadro de procedimientos reemplaza al checklist clínico, punto.** No conviven. El checklist se retiró del front y del circuito de la base; lo que quedó son las tablas como histórico.
- **Las tablas `checklist_*` NO se borran.** Registro regulado: se retiran del circuito, no se eliminan. Mismo criterio que `track_dispensations` en 0050.
- **El tachado está reservado para "esto se borra".** No se usa para "realizado" ni para ningún otro estado.
- **Un bloque que puede quedar vacío dentro de una card que no controla dice su propio vacío**, como hace Dispensación. No devuelve `null`.
- **El optimismo se suelta cuando el dato fresco confirma**, no al volver el RPC: `refetch()` solo bumpea un nonce.
- **`item_vencido` ahora solo puede venir de reportes de procedimiento**, y los rótulos del front lo dicen así ("Reporte de procedimiento fuera de plazo", KPI "Pendientes vencidos").

## PRÓXIMO PASO (próxima sesión)

1. **Contraste — tanda propia, ya son dos frentes.** (a) Los **siete** colores de estado en tema oscuro (2.58-4.18 contra 4.5:1). (b) `muted`/`faint` como texto secundario (~3.5:1 sobre blanco) y las píldoras de estado a 10px bold (~3.4:1). Ambos son decisiones de paleta: medir todos los pares color/fondo, proponer el diff de `tokens.css` y **que lo apruebe el Director** antes de aplicar.
2. **QA de lo que escribe datos** (heredado y sin hacer): recorrer las 4 etapas, el chip "Siendo atendido", el flujo completo de "No vino" (marcar → deshacer → reprogramar), el cierre clínico de screening/randomización, y ahora también **tildar un procedimiento y marcar su reporte descargado** con la fila nueva. Ojo: la llegada y la atención **no se pueden deshacer desde la UI** → hacerlo con una visita que de verdad esté pasando o con un paciente `TEST-*`.
3. **Borrar la rama remota `chore/indice-0068-aplicada`** si sigue viva (botón "Delete branch" en la PR #36). El `push --delete` quedó sin confirmar porque el credential manager de Windows se colgó pidiendo re-autenticación; se destraba con un `git fetch` desde una terminal interactiva.
4. **Fase 2 del proyecto — panel de gerencia** (asignar roles por clicks); hoy por SQL.
5. Heredados: **QA logueada del popup "Atención médica" (PR #20)**, Track ve "Medicamento" genérico, tablero logueado como coordinadora, viewer read-only de procedimientos, **rotar la contraseña de QA**, y decidir qué pasa con **`AgendaView`** (registrada sin enlace en la navegación). **Baja:** "sembrar plantilla de checklist en prod" ya no aplica.

## Cómo verificar al retomar

`git fetch` · `git status` · `npm run typecheck` (verde) · `npm run build` (verde) · `node scripts/check-migraciones.mjs` (**69 migraciones, índice al día**; última aplicada = **0069**).

- **La card de Procedimientos:** abrir una visita **cuya definición tenga procedimientos** (ojo: se asignan por definición de visita en el cronograma del protocolo, así que la V3 de un paciente no muestra lo que se le cargó a la V13). Un procedimiento realizado se ve con el tilde relleno y el nombre **en tinta, sin tachar**; al tildar/destildar **no debe parpadear**. Sin procedimientos, la card **explica el vacío** en vez de quedar en blanco.
- **La campana y Alertas:** tienen que listar alertas o decir "Estás al día" — **nunca** "No pudimos cargar las notificaciones". Si aparece ese error, el front desplegado está pidiendo `v_report_alerts`, que ya no existe (o sea: se desplegó una versión anterior a la #35).
- **El checklist no debe aparecer en ningún lado**: ni pantalla de Plantillas, ni rótulos que digan "Ítem de checklist". Si alguien navega a `track/plantillas` cae al `Placeholder`, que es lo esperado.
- **QA logueado:** el preview corre contra la base real y **el login lo tiene que hacer el Director** (Claude no tipea contraseñas). La forma que funcionó: el Director se loguea en el panel del preview y el agente maneja la navegación desde ahí leyendo el DOM.
- **Si algo se ve con etapas raras** (chips que caen a "Por llegar", los 4 pasos en "Pendiente", sin botón de avanzar): es la firma de que la vista devuelve **nombres de etapa viejos** → revisar que la 0068/0069 estén aplicadas (`select distinct operational_stage from public.v_track_visits;`).
