# Handoff · 2026-06-23 (fin de jornada)

Estado para retomar en una sesión nueva. Handoff anterior: [`handoff-2026-06-22.md`](./handoff-2026-06-22.md). Narrativa del día: [`2026-06-23.md`](./2026-06-23.md).

## Dónde estás
- **Rama `feat/inicio-home` MERGEADA a `main`** · **tag `v0.7.0`** · `package.json` 0.7.0. Verificá con `git log --oneline -5 main` y `git tag`.
- `npm run typecheck` y `npm run build` **verdes**.
- **Sin cambios de base** esta jornada (última migración aplicada: **0031**; 0026–0031 en prod).
- Jornada: **Home del módulo Inicio** (nueva, el landing del shell) + revisión de diseño + ajustes de UX.

## Lo hecho esta jornada
- **`InicioResumenView.tsx` (nuevo)** registrado en `views/registry.tsx` como `inicio/resumen` (antes caía a `Placeholder`). Reusa datos reales de Track (`useVisitsForDay`, `useVisitAlerts`, `MODULES`, etc.), sin fabricar datos que el schema no tiene. Estructura: **Tus módulos** (cards calmas: Track con dato real, Lab/Pharma "Próximamente", sin-acceso bloqueada) + **Tu día** | **Lo prioritario** (críticas primero).
- **`AppShell.tsx`:** (1) **logo "Spira" = hub** → `inicio/resumen` desde cualquier módulo; (2) **barra de submódulos oculta** cuando el módulo es `inicio` (su única vista es la home); (3) `HIDE_ACTION` += `inicio/*` (sin botón "Nuevo" espurio).
- **Diseño:** las tarjetas de módulo pasaron de tiles de color sólido (slop + fallo AA) a **cards blancas con chip de ícono de acento**; alertas priorizan ventana_vencida; "Solicitar acceso" deshabilitado real.

## PENDIENTE
- **Deploy a Vercel — A DEFINIR CON PABLO (mañana).** Técnicamente trivial (SPA Vite + Supabase). Receta: importar `spiraclinicapp/Spira-App` en Vercel (autodetecta Vite, build `npm run build`, output `dist`) → cargar `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` (Vite las incrusta en build → antes de deployar) → **agregar el dominio Vercel a Supabase → Authentication → URL Configuration** (Site URL + Redirect URLs) o se rompen Google OAuth + reset de contraseña (usan `window.location.origin`). Recrear el `vercel.json` (fallback SPA) que se había preparado y se quitó al diferir.
- **QA en navegador** de la Home (acá WDAC bloquea el headless de `/browse`; se revisó con `npm run dev`).
- **Backfill** del cuadro por protocolo (de Fase 2): los randomizados sin cronograma.

## Cómo verificar al retomar
`git log --oneline -5 main` + `git tag` (debe estar `v0.7.0`) · `npm run build` · leer este handoff y `2026-06-23.md`. La rama está en `main`; no hay migraciones nuevas que aplicar.
