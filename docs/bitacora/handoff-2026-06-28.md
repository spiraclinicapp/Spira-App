# Handoff · 2026-06-28 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-06-27.md`](./handoff-2026-06-27.md). Narrativa del día: [`2026-06-28.md`](./2026-06-28.md).

## Dónde estás
- Rama `feat/pharma-1a`. La **Tajada 1a de medicación de base está CERRADA y verificada de punta a punta**: catálogo sembrado, recepción con `linkCode`, laboratorio con autodetección, dosis y presentación estructuradas.
- Última migración aplicada: **0034** (hoy se aplicaron `0033` y `0034`).
- Catálogo en prod: **24 drogas + "Otros" · 48 medicamentos** (sembrados del Excel curado del centro).
- `typecheck` **verde**. La verificación detrás del login la hace el usuario **pharma-leader** (`lautaro.molina.scherbovsky` ya creado; ver memoria `spira-crear-usuario-y-rol`).

## Lo hecho esta jornada
- **`linkCode`** (`5d80a39`): panel ámbar de asociación on-demand de códigos en Recepción.
- **Sembrado** (`727f558` + `2247c75`): script Node idempotente `scripts/seed/seed-medicacion.mjs` desde el Excel curado → 48 medicamentos + "Otros".
- **Laboratorio con autodetección** (migración `0033`): prefijo GS1 del GTIN → laboratorio, aprendido on-demand (espejo del `linkCode`).
- **Presentación + dosis estructuradas** (migración `0034`): formatos de presentación + campo `dosis` con desplegable que aprende.
- **Investigación** verificada de fuentes AR (no hay GTIN gratis en bloque → metadatos curados + GTIN on-demand).

## PRÓXIMO PASO (próxima sesión)
1. **Elegir el próximo bloque grande:**
   - **Wizard de recepción** — diseñado con mockups (pantalla propia, 4 pasos: protocolo+tipo → escaneo con conteo → lotes/vencimientos → resumen). Reemplaza `NewReceptionModal`. Implementable para medicación de base.
   - **Modelo IP** — rediseño fundacional (producto de investigación: código único/repetido, nombre cegado/diferido, unidad rastreable). Requiere **cerrar preguntas de dominio** primero (número de kit, dispensación por kit) antes de tocar schema. Ver spec del modelo.
2. **Limpieza menor:** borrar los `TEST-*` de la verificación (recepción de prueba + código `TEST-9999` + lote `TEST-LOTE`).
3. Más adelante: **Tajada 2** (dispensación) + DataMatrix del sponsor.

## Artefactos
- Specs (todos del 2026-06-28): `docs/superpowers/specs/2026-06-28-pharma-modelo-ip-vs-base-design.md` (modelo IP, **clave**), `2026-06-28-pharma-laboratorios-autodeteccion-design.md`, `2026-06-28-pharma-1a-linkcode-design.md`.
- Migraciones: `supabase/migrations/0033_pharma_laboratorios.sql`, `0034_pharma_dosis.sql`.
- Sembrado: `scripts/seed/seed-medicacion.mjs` + `medicacion-base.json`.
- Código: `src/data/pharma/laboratorios.ts` (nuevo) · `medications.ts` · `src/views/pharma/NewMedicationForm.tsx` · `NewReceptionModal.tsx`.

## Cómo verificar al retomar
`git log --oneline -8 feat/pharma-1a` · `git status` · `npm run typecheck` (verde) · leer este handoff + `2026-06-28.md`. Las migraciones 0033 y 0034 ya están en prod. Para ver las vistas: `npm run dev`, login como pharma-leader, Pharma → Medicamentos (alta con laboratorio/dosis autodetectados) / Recepción (escáner + `linkCode`).
