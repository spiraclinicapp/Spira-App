# Handoff · 2026-07-02 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-01.md`](./handoff-2026-07-01.md). Narrativa del día: [`2026-07-02.md`](./2026-07-02.md).

## Dónde estás

- **`main` en `v0.12.0`** (fast-forward + tags anotados `v0.11.0` y `v0.12.0`). **PUSHEADO a origin**
  (github.com/spiraclinicapp/Spira-App) — ya NO es todo local. `typecheck` + `build` verdes.
- Migraciones en prod: **hasta la 0038** (aplicada por el Director hoy). **La 0039 queda por aplicar.**
- Las ramas `feat/recepcion-reskin` y `feat/pharma-ip-macro` quedaron mergeadas (== ancestros de main).

## Lo hecho esta jornada

- **v0.11.0 — Re-piel de Recepción** (Sereno): lista transversal, wizard con stepper del handoff +
  barra fija + submit lifteado, escaneo 2a, cards de tipo, filtros como chips; + **foco suave de
  inputs app-wide** (sin outline verde), wizard centrado y barra al ras. Review de rama limpio.
- **v0.12.0 — PIVOTE del IP a ingreso MACRO por cantidad**: el IP se recibe macro por cargamento
  (cantidad total), no kit por kit (la trazabilidad por kit la lleva el sponsor/IRT). Wizard de 4
  pasos (Setup+coordinador → Control y Carga General con temperatura que bloquea excursión → Doble
  Check docs+IRT → Ubicación y Cierre). Migraciones **0038** (aplicada) + **0039** (pendiente). Stock
  de IP por cantidad (`v_ip_stock`). Review adversarial limpio.

## PRÓXIMO PASO (próxima sesión)

1. **Aplicar la migración `0039`** a mano en Supabase (unifica almacenamiento a heladera|ambiente).
   La 0038 ya está aplicada.
2. **Verificación en navegador del flujo IP macro** (pharma-leader, `TEST-*`):
   - Recepción → "Producto Investigación" → **Setup** (protocolo + coordinador + farmacéutica). ⚠️ Un
     protocolo **sin coordinador asignado en Track** bloquea acá (el coordinador es obligatorio) —
     asigná uno antes de probar.
   - **Control y Carga General**: marcá **Excursión** → debe BLOQUEAR con el mensaje; volvé a **OK** +
     cargá la cantidad total; rango opcional.
   - **Doble Check**: "Siguiente" deshabilitado hasta tildar documentación + IRT.
   - **Ubicación y Cierre**: heladera / temperatura ambiente + **Confirmar** → aparece en la cola
     (verificada) y el **stock de IP del protocolo sube por la cantidad** en Medicamentos → Producto
     Investigación. Borrá los `TEST-*`.
   - Verificá que la recepción de **base** (Protocolo/Ambulatoria: escaneo + lotes) sigue intacta.
3. **Decisión de producto abierta:** el modelo macro **no lleva vencimiento**. Si se quiere una fecha
   de "vto del cargamento" por recepción, se suma al Paso 2 (Control y Carga General) — decisión del Director.
4. **Limpieza opcional:** borrar las recepciones IP viejas (modelo por-unidad, TEST-*, con `total_kits`
   NULL) + sus `ip_units`. La tabla `ip_units` quedó dormida; si se confirma vacía, se puede dropear en
   una migración futura.
5. **Tajadas de Pharma que quedan:** Tajada 2 (dispensación de IP — ahora por cantidad, restará del
   `v_ip_stock`), UI de stock de ambulatoria.

## Artefactos

- Migraciones: `supabase/migrations/0038_pharma_ip_macro.sql` (aplicada) · `0039_pharma_ip_storage_unificar.sql` (pendiente).
- Plan del pivote macro: `~/.claude/plans/restricci-n-principal-cambio-misty-sutherland.md`.
- Plan de la re-piel: `docs/superpowers/plans/2026-07-01-recepcion-reskin.md`.
- Código nuevo IP macro: `src/data/pharma/{ipStock,coordinators}.ts`, `src/views/pharma/wizard/Step{1ControlCarga,2DobleCheck,3Cierre}Ip.tsx`.

## Cómo verificar al retomar

`git checkout main` · `git log --oneline -6 | cat` · `git tag -l --sort=-version:refname | head` (v0.12.0
arriba) · `npm run typecheck` (verde). Para el flujo IP: aplicá la 0039, `npm run dev`, login pharma-leader,
Pharma → Recepción → "Producto Investigación". `origin/main` ya está al día con el push de hoy.
