# Handoff · 2026-07-04 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-07-02.md`](./handoff-2026-07-02.md). Narrativa del día: [`2026-07-04.md`](./2026-07-04.md).

## Dónde estás

- **`main` = `44029ef`**, **pusheado a origin** (github.com/spiraclinicapp/Spira-App), `origin/main` al día (0/0). `npm run typecheck` verde.
- **Migraciones en prod: hasta la `0040`** (aplicada a mano y verificada por el Director). Índice al día en `supabase/README.md`.
- **Sin bump/tag esta jornada:** `package.json` sigue en `v0.12.0`; main tiene commits de feature por encima del tag `v0.12.0`. Si se quiere, un release ritual (v0.13.0) los taggea.
- Sin trackear (a propósito, decisión del Director): carpeta `Mejora visual Medicamentos/` (handoff de diseño, **fuera del repo**).

## Lo hecho esta jornada (commits `6ba892f`..`44029ef`)

- **Informe visual del primer mes** (`informe-primer-mes/`): PDF + 13 capturas automatizadas (Playwright + Edge del sistema) + mail. Listo para enviar. Commits `18ae885` (+ `e7f74a1` handoffs/launch).
- **Ocultar Lab/Contable** (flag `proximamente`) con candado + priorizar operativos + tarjeta Pharma real en la home. Commit `6ba892f`.
- **Docs al día v0.12.0** (índices migraciones 0033–0039, tabla de versiones). Commits `0950e37` + merge `e25502c`.
- **Pharma: catálogo global + asignación al recibir + código de barras en recepción** (migración `0040` + front). Commit `44029ef`.

## Decisiones de dominio tomadas (no re-discutir)

- **Catálogo de medicamentos = GLOBAL**; la asignación medicamento↔protocolo es **consecuencia de recibir** (la crea `create_reception`/`apply_reception_stock` vía upsert; no hay gate manual). Stock sigue por protocolo.
- **1 código de barras ↔ 1 medicamento** (enforce en UI: el "asociar código desconocido" solo ofrece meds sin código). Follow-up posible: `unique(medication_id)` en `medication_codes` por migración (chequear antes que ningún med tenga 2 códigos).
- Módulos no construidos (Lab, Contable) van bloqueados con candado para todos, hasta que existan.

## PRÓXIMO PASO (próxima sesión)

1. **Informe del primer mes:** el Director le da una lectura a ojo al `informe-primer-mes.pdf` (sobre todo la nota de TrialMetrica) y lo envía.
2. **Reskin de Medicamentos** (épica aparte): aplicar `design_handoff_medicamentos/` (prevista Protocolo/Ambulatoria, lista agrupada con lote y vencimiento, filtros por vencimiento) sobre el comportamiento ya cambiado.
3. **Informe de facturación al sponsor** (épica aparte, bloqueada): primero **enriquecer la dispensación** de Track (`track_dispensations`, 0023 — hoy solo `kit_code` de texto libre) para registrar med + lote + cantidad por visita; recién ahí el reporte "por protocolo: visitas, medicación entregada, paciente".
4. **Retoques de Track** (heredados: QA del cuadro de actividades / cola del médico) y **deploy del front** (Vercel + 2 env vars + dominio en Supabase Auth).

## Cómo verificar al retomar

`git checkout main` · `git log --oneline -8 | cat` (arriba `44029ef`) · `npm run typecheck` (verde) · `origin/main` al día. Para el flujo nuevo: `npm run dev`, login pharma-leader, Pharma → **Medicamentos** (catálogo global sin elegir protocolo) y **Recepción → Nueva recepción** (código de barras por fila, aviso al dar Siguiente, "Buscar a mano" que muestra todos). Probar con registros `TEST-*` y borrarlos.
