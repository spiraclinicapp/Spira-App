# Bitácora · 2026-09-17 — El cronograma pasa a la ficha del estudio · release `v0.79.0`

> **Mover una pantalla adentro de un modal cambió el comportamiento de TODOS los modales.** El pedido era
> de lugar: sacar el cronograma de la lista de pacientes y ponerlo en lo que es del estudio. Pero el
> cronograma abre sus propios modales, y el `Modal` compartido cerraba con cualquier Esc: por primera vez
> iba a haber dos abiertos a la vez, y un solo Esc se los llevaba a los dos.

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Coordinación · la ficha del estudio en «Estudios y pacientes», y el release del día
**Ramas:** [#217](https://github.com/spiraclinicapp/Spira-App/pull/217) (`feat/cronograma-en-el-estudio`) y [#218](https://github.com/spiraclinicapp/Spira-App/pull/218) (`docs/release-v0.79.0`), mergeadas y borradas. **Sin migraciones propias** (la `0128` es de la [#216](https://github.com/spiraclinicapp/Spira-App/pull/216), de otra sesión, y **no está aplicada**). Release **`v0.79.0`**. **1223 tests** (1159 → 1223, los 64 nuevos son de la #216).

La sesión venía de la noche del 16 (la cuarta parte de esa bitácora y las codas `v0.78.x`) y se reinició
en el medio: el cronograma arrancó con una sesión y se commiteó con otra, sobre el mismo worktree.

## 1. Resumen de la jornada

| PR | Qué | Cómo arrancó |
|---|---|---|
| [#217](https://github.com/spiraclinicapp/Spira-App/pull/217) | «Cronograma y procedimientos» deja de ser pestaña de la lista de pacientes y se abre como modal desde la ficha del estudio. `Modal` cierra con Esc sólo el de arriba | «quiero que lo saquemos del panel de pacientes y que quede en lo que es del estudio» |
| [#218](https://github.com/spiraclinicapp/Spira-App/pull/218) | `v0.79.0`, que versiona la #217 y la #216 | «sacá la versión con las dos» |

La [#216](https://github.com/spiraclinicapp/Spira-App/pull/216) (reposición de corte a corte, parte 1: modelo, datos y la `0128`) es de otra sesión y se
mergeó diez segundos antes que la #217. Su narrativa le corresponde a esa sesión; acá sólo aparece por lo
que tocó al release (§3).

## 2. El cronograma, en la ficha del estudio ([#217](https://github.com/spiraclinicapp/Spira-App/pull/217))

El pedido fue preciso: un botón «Cronograma y procedimientos» **abajo del título del estudio y antes de la
línea que está por encima de Sponsor**, que abra un modal con el cronograma y los procedimientos.

- **El contenido no cambió.** `CronogramaTab` ya era autocontenido —las solapas Visitas / Procedimientos
  del estudio y sus modales propios—, así que mudarlo fue cambiar dónde se monta. El modal mide 960px y
  se titula con el código del estudio.
- **El botón es el mismo que «Exportar reporte» y «Editar protocolo»**, y lo ve sólo quien veía la pestaña
  (`canManageSchedule`). De paso, `actBtn` dejó de decidir su acción por el ícono
  (`icon === 'settings' ? onEdit : handleExport`) y la recibe por parámetro.
- **La columna derecha queda con «Pacientes» y «Reportes pendientes».** Un `?tab=cronograma` viejo cae
  a «Pacientes» en silencio (`oneOf`), y `NavTarget.protocolTab` perdió `'cronograma'`, que nadie usaba.
- **El vacío del tablero de reportes** decía «Se definen en Cronograma › Procedimientos del estudio», que
  dejó de existir. Ahora dice dónde está.

### Esc cierra UNA capa

El `Modal` compartido no usa portal y cada instancia escucha Esc en `document`. Nunca se había notado
porque nunca había dos abiertos: con el cronograma en un modal, Esc en «Procedimientos · V1» o en «Editar
procedimiento» cerraba también el cronograma, con lo que estuvieras mirando.

La cura es una pila de módulo: cada modal entra una vez al montarse y sólo el de arriba atiende el Esc.
**El detalle que la hace funcionar** es que el efecto NO depende de `onClose`: casi siempre llega como
flecha nueva en cada render, y un efecto con esa dependencia sacaría al modal de abajo y lo volvería a
meter arriba de todo con cualquier re-render, dejando el próximo Esc apuntando al equivocado. Se lee por
ref.

El clic afuera ya andaba bien: el fondo del modal de adentro vive dentro de la tarjeta del de afuera, que
frena la propagación.

**QA logueado contra producción, sin escrituras**, en ACT18301 a 1536×864: botón entre «AIRLYMPUS» (y=201-219)
y la línea de Sponsor (y=289), 40px como «Editar protocolo»; ficha lateral sin scroll (735/735); modal
960×816 con scroll vertical y sin horizontal en las dos solapas; con «Procedimientos · V1» encima, el
primer Esc y el primer clic afuera cierran sólo ése y el segundo cierra el cronograma; `?tab=cronograma`
abre en «Pacientes»; consola limpia.

## 3. El release, y la `0128` que no está aplicada ([#218](https://github.com/spiraclinicapp/Spira-App/pull/218))

Con la #216 y la #217 en producción y `package.json` en 0.78.1, salió **`v0.79.0`** con una sola línea de
novedades («El cronograma y los procedimientos se abren desde la ficha del estudio.»): la #216 todavía no
tiene nada visible.

Al preparar el release apareció lo importante del día: el índice dice que la `0128` es **ADITIVA y va antes
del front**, no figura como aplicada, y el front ya estaba desplegado. Se sondeó sin sesión contra PostgREST:

| Qué | Respuesta | Lectura |
|---|---|---|
| `rpc/reposicion_del_periodo` | `404 PGRST202` | la función no existe |
| `pedidos_medicacion` | `404 PGRST205` | la tabla no existe |
| `rpc/funcion_que_no_existe_sonda` (control) | `404 PGRST202` | el mismo error que la de arriba |

**No rompe nada hoy**, y se verificó antes de afirmarlo: ninguna vista ni el shell importan la capa de datos
de reposición, y `create_reception` sólo manda `p_pedido_id` cuando hay pedido
(`...(input.pedido_id ? { p_pedido_id } : {})`). El índice de migraciones y `CLAUDE.md` **no se tocaron**:
la marca de aplicada va con la confirmación del Director.

## 4. Lo no-obvio

- **«Mismo contenido en otro contenedor» no es un cambio de lugar.** Mover `CronogramaTab` a un `Modal`
  convirtió a sus modales internos en modales anidados, un caso que el componente compartido nunca había
  tenido. Lo encontró leer `Modal.tsx` ANTES de mudar, no el QA: con dos modales abiertos, el síntoma
  («se cierra todo») parece un Esc de más y no un defecto del contenedor.
- **Un efecto que depende de un callback reordena una pila.** Con `[onClose]` de dependencia la pila de Esc
  habría andado en la prueba corta y fallado con cualquier refetch del modal de abajo. Es el tipo de
  defecto que un QA de dos Esc seguidos no ve.
- **Una migración aditiva que entra DETRÁS de su front no rompe mientras nadie la llame.** La regla de
  `CLAUDE.md` —aditiva, migración primero— protege al front nuevo; acá el front nuevo es una capa de datos
  sin pantalla, así que el orden invertido quedó latente. Deja de ser inocuo el día que entre la parte 2
  (la pantalla): **la `0128` tiene que estar aplicada antes**.
- **«Ya la mergeé» se verificó cada vez, y cuatro veces git no alcanzaba.** Dos veces faltaba el «Confirm
  merge» (la #212 y la #214, el 16) y dos veces el `git fetch` corrió segundos antes de que GitHub la
  marcara (la segunda vuelta de la #214 y la #218). El cache de la app también decía «abierta». Lo que
  distingue los dos casos es la API de GitHub (`state` y `merged_at`), no git.
- **Reiniciar la sesión cambia DÓNDE miran las herramientas, no dónde está el trabajo.** Después del reinicio
  la sesión quedó aislada en el worktree, pero la «carpeta del proyecto» pasó a ser la copia compartida: el
  hook `branch-guard` frenó un commit legítimo porque miró `main` ahí (se resolvió con `cd <worktree> &&`,
  que el hook ya contempla, sin usar `SPIRA_ALLOW_MAIN`), el harness rechazó git encadenado o con `-C`
  hacia afuera, y `preview_start` siguió buscando `launch.json` en la carpeta vieja ya borrada. Recrearla
  ahí habría rearmado la trampa del worktree vacío del 16; se levantó Vite a mano en el 5251 y se abrió
  el navegador por URL.

## 5. Git — estado de las ramas

`main` en `1fde281` = `v0.79.0` = producción (verificado buscando el texto en el bundle de Vercel).
`feat/cronograma-en-el-estudio` y `docs/release-v0.79.0` se borraron en `origin` y en local después de
confirmar cero commits fuera de `main`; el tag sigue.

Quedan, de otras sesiones y sin tocar:

- `feat/reposicion-periodo-base` en `origin` y en el worktree `wt-reposicion` (mergeada: es la #216).
- `fix/recepcion-guarda-borrado-y-renglones` en el worktree `unruffled-dubinsky-186253`, **sin pushear**:
  hay una sesión trabajando. Si cierra la jornada después de ésta, anexa a esta bitácora.

El worktree de esta sesión (`.claude/worktrees/cronograma-en-el-estudio`) queda con la rama del cierre.
**Se borra después de cerrar la sesión**, no desde adentro (ver `gotcha-worktree-de-sesion-vacio`).

## 6. Pendientes

1. **Aplicar la `0128`** antes de la parte 2 de reposición, y recién ahí marcarla en `supabase/README.md` y
   en `CLAUDE.md`. El orden lo sabe la sesión de la #216.
2. **Mirar en pantalla la `v0.77.1`** (la fecha realizada sin la hora): sigue sin QA logueado desde el 16.
3. **El Esc con TRES capas** (cronograma › «Procedimientos · V3» › «Editar procedimiento») no se probó;
   la pila lo cubre por construcción, pero no se miró.
4. Siguen los pendientes del 16 (handoff del 16, PRÓXIMO PASO).

---

# Anexo · La ventana vencida gana una salida: documentar la desviación ([#226](https://github.com/spiraclinicapp/Spira-App/pull/226))

> **El pedido era de lugar y el problema era de salida.** El Director quería sacar las ventanas vencidas de
> Pendientes —«son avisos, no pendientes»— y mandarlas a notificaciones. Medido, una ventana vencida sí es
> trabajo: lo que no tenía era **cómo terminarse**. Mudarla de pantalla habría mudado el sedimento intacto.

**Contexto:** Coordinación · Pendientes, la campana y el encabezado de la visita
**Ramas:** [#226](https://github.com/spiraclinicapp/Spira-App/pull/226) (`feat/desviaciones-de-protocolo`), mergeada y borrada. Migración **`0130`, aplicada en prod**. **1249 tests**. Sin release (ver Pendientes).

Es la sesión que la sección 5 de arriba daba por «trabajando en la guarda de Recepción» en el worktree
`unruffled-dubinsky-186253`: arrancó ahí, sobre esa rama, pero lo que hizo fue esto. La guarda quedó como
estaba.

## A. Del «es un aviso» al «no tiene salida»

Tres hechos, todos medidos contra prod, cambiaron el planteo:

1. **El panel de notificaciones ya existía.** La campana del topbar muestra las mismas cinco clases que
   Pendientes, ventana vencida incluida. Mandarla ahí era *sacarla* de Pendientes, a un desplegable de diez
   renglones sin filtros cuyo pie remite… a Pendientes.
2. **Hoy hay cero ventanas vencidas.** «Un panel lleno» era cómo se iba a poner, no cómo estaba — lo confirmó
   el Director. Lo que viene: 16 visitas cierran ventana por mes en los cuatro protocolos, 231 en el año.
3. **Ninguna se va sola.** La vista la marca vencida con `real_date is null and current_date > window_end`,
   para siempre. Y las dos únicas salidas eran cargar la visita o *descartar* la alerta con un motivo que
   dice «no correspondía». Nadie silencia un desvío clínico, así que se acumula: **sedimento, no caudal**.

La respuesta fue darle la salida honesta: **documentar la desviación de protocolo**. Spira no tenía el
concepto (cero apariciones en 129 migraciones), aunque ya calculaba el hecho: `fueraDeVentana()` pinta la
pastilla roja y alimenta la adherencia del Inicio.

## B. Lo que entró

Tabla `protocol_deviations` y RPC `record_protocol_deviation` (0130), calcados de los descartes de la 0070
en todo lo que comparten, con tres diferencias a propósito: tabla propia (el listado es lo que pide un
monitor), explicación obligatoria siempre, y sólo para ventana vencida. El botón «Documentar desviación» va
**con nombre** en el ítem —un segundo ícono mudo al lado de la X diría que hacen lo mismo—; el panel es
gemelo de «Descartados»; la visita lleva una pastilla ámbar con el motivo.

En el camino apareció un agujero que no se buscaba: **la 0127 conserva las ventanas vencidas al cerrar una
inscripción** (bien: son evidencia), **pero nadie filtraba por inscripción**, así que iban a reclamar para
siempre sobre pacientes que ya no están. Ahora salen de la lista activa y quedan en el panel como «sin
documentar». Hoy no había ningún caso (7 inscripciones cerradas, 59 visitas, todas cerradas).

Fuera de alcance, por decisión del Director y anotado en `TODOS.md` (P2): la visita que **sí se hizo fuera de
ventana**, que es la misma desviación y hoy se escapa sin registro.

## C. La `0130`: tres números y dos intentos

- **El número se movió dos veces.** Nació `0130`, pasó a `0131` porque la `0130` estaba reservada para la
  limpieza de Reposición, y el Director la devolvió a `0130`: esa limpieza espera un deploy de Farmacia y no
  tenía sentido que frenara esto. Mientras tanto la `0129` dejó de ser la guarda de Recepción y pasó a ser
  `feedback_lugar` (#222). El comentario de cabecera del archivo todavía llama «guarda de Recepción» a la
  0129: no se toca, porque la migración ya corrió.
- **Aplicarla costó dos intentos, por dos trampas del editor de Supabase.** Frena en un aviso de «tabla sin
  RLS» y, si se cancela, no corre nada — el «Success» que queda en pantalla es de otra ejecución. Y ya
  aplicada, la API siguió contestando `PGRST205` hasta un `notify pgrst, 'reload schema';`. Una sonda de
  sólo lectura (`to_regclass`, `to_regprocedure`, políticas y permisos) separó los casos en una corrida.
- **Probada antes con PGlite**: 17 verificaciones, incluida la de que el trigger de auditoría no reviente en
  `old.id` (la 0111).

## D. La verificación contra producción

Con la 0130 aplicada se creó un protocolo, un paciente y una visita `TEST-DESV` con la ventana cerrada, y se
documentó la desviación **desde la pantalla**: el ítem salió de la lista y **la campana bajó en el mismo
gesto** (1 → 0 sin recargar); el panel mostró motivo, explicación, autor y «recién»; la visita, la pastilla.
Borrarla desde el panel devolvió todo. El `audit_log` guardó el `INSERT` y el `DELETE`. Los datos se
borraron por id y no queda ninguno. Antes de eso, con la tabla todavía ausente, se había verificado lo otro:
que la consulta falla con `PGRST205` y las tres pantallas de alertas siguen enteras.

## E. Lo no-obvio

- **El reencuadre del pedido vino de medir, no de opinar.** «Mandalo a notificaciones» sonaba razonable
  hasta ver que la campana ya existía y que el problema era de salida. Preguntar el caso concreto (¿cuántas
  hay hoy?) dio cero, y eso cambió la conversación de «qué hay» a «cómo se va a poner».
- **Un renumerado no falla: se desincroniza.** `\b0131\b` no matchea `0131_desviacion_de_protocolo` —el
  guion bajo es carácter de palabra— y el primer renumerado dejó vivos los nombres de archivo adentro del
  plan. Y un reemplazo ciego dejó una frase diciendo «es la `0130` y no la `0130`».
- **Un script que dice «listo» no es evidencia; el diff sí.** Una expresión regular armada con un template
  perdió las barras invertidas en el camino, matcheó cualquier renglón y el script reportó dos marcas que
  nunca escribió. Lo agarró `check-migraciones`, no la salida del script.
- **Dos PRs que tocan el mismo renglón no chocan si lo dejan idéntico.** Las marcas de 0128/0129 de esta rama
  se copiaron byte a byte de la #224: al rebasear, el commit desapareció solo.
- **La campana no se entera de lo que no pasa por la app.** Con la visita de prueba creada por la API, la
  lista (montada después) mostraba 1 y la campana (montada antes) 0. Es la limitación que ya está en
  `TODOS.md`; para medir «el mismo gesto» hubo que recargar primero.

## F. Git — estado de las ramas

La #226 está en producción: el bundle de `spira-app.vercel.app` contiene «Documentar desviación» (y seguía
diciendo `0.79.0`). Después entraron la #227 y la #225 (la bandeja de feedback, con la `0131`, ya aplicada):
`main` en `4f91ba1` al cerrar. `feat/desviaciones-de-protocolo` se borró en `origin` y en local después de
confirmar cero commits fuera de `main`.

Quedan, de otras sesiones:

- **`fix/recepcion-guarda-borrado-y-renglones`**, sólo local (`282ec8f`), con la migración todavía numerada
  `0129`: **hay que renumerarla a `0132`** antes de pushear.
- **`docs/0131-aplicada`** (#229), abierta: la marca de la 0131 en el índice y `CLAUDE.md` en 0131. Por eso
  este cierre **no toca** `CLAUDE.md`: habría chocado con ella en el mismo renglón.

El worktree `unruffled-dubinsky-186253` queda con la rama de este cierre. Se borra después de cerrar la
sesión, no desde adentro.

## G. Pendientes

1. **El release.** `v0.79.0` está atrás de producción por cuatro features: la #220, la #222, la #225 y la #226.
2. **La guarda de Recepción**: renumerar a `0132`, rebasear y abrir la PR.
3. **La visita hecha fuera de ventana** (`TODOS.md`, P2): su disparador es la primera vez que un monitor pida
   el listado de desviaciones y falten las que sí se hicieron, tarde.
