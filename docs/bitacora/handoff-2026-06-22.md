# Handoff · 2026-06-22 (fin de jornada)

Estado para retomar en una sesión nueva. Handoff anterior: [`handoff-2026-06-21.md`](./handoff-2026-06-21.md).

## Dónde estás
- **Rama `feat/cronograma-protocolo` MERGEADA a `main`** (fast-forward) · **tag `v0.6.0`** · `package.json` 0.6.0. Verificá con `git log --oneline -5 main` y `git tag`.
- `npm run typecheck` y `npm run build` **verdes**.
- **Migraciones 0030 y 0031 aplicadas en prod ✅** (0026–0031 al día).
- Esta jornada: (1) **3 fixes** media de auditoría del branch, (2) **Fase 2 del cuadro completo** (Tasks 5–9) + su revisión adversarial, (3) **"Atendido por el médico"** (cola persistente por día, 0031) + ajustes de **"Visita actual"** en la ficha.

## Lo hecho esta jornada
1. **Fixes media (auditoría del branch):**
   - Validación de día/ventanas en el editor del cuadro (`ScheduleDefinitionForm.tsx`): offset entero, ventanas enteras ≥ 0 con hints; mapeo `22P02` en `definitionErrorMessage`.
   - `recovering` pegado del login (`auth.tsx`/`App.tsx`/`Login.tsx`): se limpia al perder sesión (`SIGNED_OUT`); lee el error del hash del link de reset vencido → aviso sereno (`authNotice`).
   - Poster de fallback del video (`CoverVideo.tsx`/`AuthLayout.tsx`/`tokens.css`): si el iframe de YouTube no carga, se ve la miniatura.
2. **Fase 2 — flujo operativo (randomización/screening al cerrar):**
   - **Migración `0030_flujo_randomizacion.sql`** (CREADA, **pendiente de aplicar a mano**): constraint permite `programada` libre sin ventanas; `schedule_protocol_visit` (agendar libre del cuadro, con guard anti-duplicado pendiente); `mark_ready_with_outcome` (cierre clínico **idempotente**: IVRS de screening sin pisar, randomización no pisa fecha previa, exige visita atendida); `discontinue_enrollment`; `register_visit_event` reescrito con **cutover** (cierra sueltas firma/screening/rando cuando el protocolo tiene cuadro); recrea `v_track_visits` con `enrollment_randomization_date`.
   - **Capa de datos**: `useSchedulableDefinitions`/`scheduleProtocolVisit` (visitDefinitions), `markReadyWithOutcome`/`discontinueEnrollment` (dayVisits), `availableEventKinds(...protocolHasCuadro)` (visitEvents), `useRandoAttendedWithoutDate` (visits, salvaguarda).
   - **UI**: `RegisterVisitFlow` consciente del cuadro (agenda defs libres o sueltas; loading/empty; `preselectDefId` para recitar); `ReadyOutcomeModal` (IVRS / ¿randomizó? con recitar o marcar fallo); `DayVisitsView` intercepta "Listo" de screening/rando → modal, + banner de salvaguarda "randomización atendida sin confirmar".
   - **Scripts**: `supabase/scripts/2026-06-21-cuadro-verificacion.sql` (verificación Fase 2) y `2026-06-21-reset-beta.sql` (reset de data de prueba, review-first, lo corre el usuario).
3. **Revisión adversarial de Fase 2** (workflow): 7 hallazgos confirmados, todos resueltos. La alta: `RegisterVisitFlow` congelaba la elección durante el loading (rompía cuadro/recitar) → ahora `choice` se deriva de las opciones cargadas. Medias: IVRS de screening ahora idempotente (no pisa); `ReadyOutcomeModal` no se cierra con RPC en vuelo. Bajas: guard anti-duplicado en `schedule_protocol_visit`, `COMMENT ON VIEW` repuesto, `reorderDefinitions` respeta "0 filas = sin permiso".
4. **"Atendido por el médico" — cola persistente (0031, aplicada ✅):**
   - **0031**: `patient_visits.doctor_seen_at` + RPC `mark_doctor_seen`; recrea `v_track_visits` **y `v_patient_visits`** (el `pv.*` de la vista intermedia se congela al crearla → hay que recrearla para exponer la columna nueva; mismo patrón que 0022/0023). El primer intento falló con `42703` por saltarse ese paso.
   - **Cola** (`DoctorQueueView`): una sola lista **por día** (navegable ◀/Hoy/▶), orden por llegada; el paciente **no se va al retirarse** (se quitó el filtro `left_at`); atendido queda en su lugar y se resalta el siguiente; filtros Todos/Faltan atender/Atendidos + selector de médico tratante. `useDoctorQueue(date)`.
   - **Ficha** (`PatientFichaView`): el cronograma resalta la "Visita actual" (no la próxima); la "Visita actual" reconoce sueltas (VNP/screening); muestra código+nombre en vez de "V# de N".

## Decisión de criterio (desvío consciente del plan)
El plan decía "quitar el anclaje de randomización de `register_visit_event`". Eso rompería el anclaje en protocolos **sin cuadro**. Como el cutover ya cierra el camino de sueltas cuando hay cuadro, **conservé el anclaje** (solo alcanzable sin cuadro) → preserva el comportamiento legacy sin doble-anclaje. Está comentado en 0030. Si preferís el corte total, avisá.

## PENDIENTE — pasos manuales del usuario (no los hace el agente)
- ~~Aplicar `0030`~~ ✅ y ~~`0031`~~ ✅ aplicadas en prod (2026-06-22).
- ~~Merge a `main` / taggear~~ ✅ mergeada (fast-forward) + **tag `v0.6.0`**.
- **QA en navegador** (acá WDAC bloquea el headless de `/browse`): Fase 2 — definir cuadro (V1 Screening, V2 Randomización, V3+ tratamiento) → agendar V1 → "Listo" → IVRS; agendar V2 → "Listo" → "Sí" → ver V3+ y "Próximas visitas">0; "No" → recitar / marcar fallo; verificar que "Agendar" ya no ofrece sueltas con cuadro. **Cola del médico** — atender (queda + resalta el siguiente), deshacer, navegar días, filtros y selector de médico; verificar que al retirarse **no** desaparece.
- **Reset de beta** (`2026-06-21-reset-beta.sql`, review-first) si querés arrancar limpio, y **backfill** del cuadro por protocolo.

## Cómo verificar al retomar
`git log --oneline -5 main` + `git tag` (debe estar `v0.6.0`) · `npm run build` · leer este handoff y `2026-06-22.md`. 0026–0031 ya están aplicadas; la rama está en `main`.
