# Handoff · 2026-06-30 (fin de jornada)

Estado para retomar. Handoff anterior: [`handoff-2026-06-29.md`](./handoff-2026-06-29.md). Narrativa del día: [`2026-06-30.md`](./2026-06-30.md).

## Dónde estás

- **`main` en `v0.9.0`** (merge fast-forward + tag local, `cade913`). **NO pusheado** a origin (deploy
  a Vercel diferido con Pablo).
- Rama **`feat/pharma-ip`**, HEAD `614b01e`, **20 commits sobre `main`**, working tree limpio.
  **NO mergeada, NO pusheada.** `typecheck` + `build` de producción **verdes**.
- Migraciones en prod: **hasta la 0037** (el Director aplicó la 0037 hoy).

## Lo hecho esta jornada

- **Fix del `23514`** (commit `4383965`, en `main`): `pharmaErrorMessage` deja pasar el mensaje claro
  del DB salvo CHECK crudo de tabla.
- **Release `v0.9.0`** (recepción tipada Protocolo/Ambulatoria + wizard) mergeado y taggeado en `main`.
- **Producto de Investigación (IP)** construido de punta a punta en `feat/pharma-ip`: modelo por
  unidad (`ip_units`), recepción (3er ámbito del wizard, escaneo por unidad + droga por fila), stock
  por unidad, parser GS1, capa de datos, migración 0037. Ciclo brainstorm → spec → `/autoplan` →
  plan → ejecución subagent-driven (review por tarea + review de rama). Anda bien en verificación
  preliminar.

## PRÓXIMO PASO (próxima sesión)

1. 🔴 **Confirmar el AI del N° de kit con un escaneo REAL del sponsor** (el Director lo trae). Pasar el
   string crudo del DataMatrix → verificar si el N° de kit viene en el serial `(21)` (el supuesto
   actual) o en otro AI → ajuste de 1 línea en [`src/lib/gs1.ts`](../../src/lib/gs1.ts) si hace falta.
   **No recibir kits REALES hasta esto** (el `unique(protocol_id, kit_number)` ya está vivo sobre el
   supuesto; re-mapear con data real puede chocar). Verificar con `TEST-*` es seguro.
2. **Verificación funcional en navegador** (pharma-leader, `TEST-*`):
   - Pharma → Recepción → ámbito **"Producto Investigación"** + protocolo → Nueva recepción.
   - Paso 1: escanear/tipear códigos (un DataMatrix GS1 de prueba, ej.
     `010759912345678817251231<GS>10TEST-LOTE<GS>21TEST-KIT-001`) → una fila por beep arriba; re-escaneo
     dedup; no-GS1 → fila "manual".
   - Paso 2: corregir lote/vto; seleccionar filas → aplicar droga; dejar otras "Cegado".
   - Paso 3: resumen (total / con droga / cegadas) → Crear → **Verificar** en la cola → el stock IP sube.
   - Pharma → Medicamentos → ámbito "Producto Investigación" + protocolo → unidades como cards.
   - Rebote: re-recibir un `TEST-KIT-*` ya cargado → mensaje que **lista** el kit.
   - Borrar los `TEST-*`.
3. Si OK → **merge `feat/pharma-ip` a `main` + tag** (¿`v0.10.0`?).
4. **Tajada 2 (dispensación, con Pablo):** trazabilidad kit→paciente (Drug Accountability Log ANMAT),
   reconciliar el `kitCode` de texto libre de Track ([`DispenseModal.tsx`](../../src/views/track/DispenseModal.tsx))
   con `ip_units.kit_number`, unblinding de emergencia, y el **libro de movimientos unificado**
   (generalizar `stock_movements` o `ip_unit_movements`).
5. **Heredados:** deploy a Vercel (con Pablo), QA de Track.

## Artefactos

- Spec: `docs/superpowers/specs/2026-06-30-pharma-ip-recepcion-design.md` (dominio + modelo + revisión /autoplan).
- Plan: `docs/superpowers/plans/2026-06-30-pharma-ip-recepcion.md` (12 tareas).
- Ledger SDD: `.superpowers/sdd/progress.md` (gitignored; estado por tarea + minors diferidos).
- Migración: `supabase/migrations/0037_pharma_ip_units.sql` (**aplicada**).
- Código nuevo: `src/lib/gs1.ts`, `src/data/pharma/ipUnits.ts`, `src/views/pharma/DrugPicker.tsx`,
  `src/views/pharma/wizard/Step{1ScanIp,2ReviewIp,3SummaryIp}.tsx`. Modificados: `ReceptionWizard.tsx`,
  `Step0Setup.tsx`, `RecepcionView.tsx`, `MedicamentosView.tsx`, `data/pharma/index.ts`.

## Minors diferidos (no bloquean el merge)

Índice compuesto `(reception_id, status)` en `ip_units`; `aria-live` no re-anuncia el 2° duplicado
idéntico; `DrugPicker` unifica `onMouseDown` (touch marginal); `protocolId` no se limpia al pasar
investigación→protocolo (heredado); `useIpUnits` usa `select('*')`; `Step2ReviewIp` no permite quitar
una unidad (solo editar). Detalle en el ledger.

## Cómo verificar al retomar

`git checkout feat/pharma-ip` · `git log --oneline main..HEAD | cat` · `npm run typecheck` (verde) ·
leer este handoff + `2026-06-30.md`. La 0037 ya está en prod. Para ver el flujo IP: `npm run dev`,
login como pharma-leader, Pharma → Recepción → ámbito "Producto Investigación".
