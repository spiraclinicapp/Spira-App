# Bitácora · 2026-09-13 — Las barras de filtros en un renglón, Recepción filtrando en la base, y la dispensación de base y de IMP · codas `v0.69.2` a `v0.69.6` + release `v0.70.0`

> **Una jornada de presentación, medida con regla.** El Director mandó dos capturas: las barras de
> filtros de Visitas y Pendientes partían en dos renglones "por falta de espacio". No se resolvió a
> ojo: se midió cada control con los componentes reales, en tres anchos y cinco estados, y lo que más
> pesaba no era lo que se veía en la captura sino un botón que **todavía no estaba**: «Limpiar N».

**Proyecto:** Spira (plataforma modular de investigación clínica)
**Autor:** Lautaro Molina (con Claude)
**Contexto:** Fundación Scherbovsky · farmacia e investigación clínica · sistema auditable ANMAT/ICH-GCP
**Ramas:** dos PRs, [#158](https://github.com/spiraclinicapp/Spira-App/pull/158) (`fix/filtros-un-renglon`, mergeada en `0d97c96`) y [#159](https://github.com/spiraclinicapp/Spira-App/pull/159) (`fix/estadisticas-copy-angosto`, mergeada en `fd78fe8`), las dos borradas en local y en `origin`; después la [#160](https://github.com/spiraclinicapp/Spira-App/pull/160) (`b62aaf2`), ver §10, la [#161](https://github.com/spiraclinicapp/Spira-App/pull/161) (`a7d6894`), ver §11, la [#162](https://github.com/spiraclinicapp/Spira-App/pull/162) (`bda8143`), ver §12, y la [#163](https://github.com/spiraclinicapp/Spira-App/pull/163) (`0e5f69c`), ver §13. Codas **`v0.69.2`** a **`v0.69.6`**, sin changelog. **941 tests** (932 → 941: siete de §11 y dos de §13). **Segunda parte (§14-§20):** la dispensación de base y de IMP, con [#164](https://github.com/spiraclinicapp/Spira-App/pull/164) a [#169](https://github.com/spiraclinicapp/Spira-App/pull/169), **migraciones `0119` a `0122` aplicadas**, release **`v0.70.0`** y **980 tests**. **Tercera parte (§21-§27):** el mock de la Tanda 3 y sus dos revisiones (diseño y arquitectura), **sin código, sin PR y sin migraciones**; commit de docs directo en `main`.

---

## 1. Resumen de la jornada

| Hilo | Qué | PR |
|---|---|---|
| Barras de filtros | Visitas y Pendientes entran en un renglón a ~1010px de contenido | [#158](https://github.com/spiraclinicapp/Spira-App/pull/158) |
| Aviso de Estadísticas | «El informe necesita más espacio / Agrandá la ventana para verlo.» | [#159](https://github.com/spiraclinicapp/Spira-App/pull/159) |

Las dos se verificaron **logueado contra prod**, con la sesión que el Director abrió en el preview.

## 2. Las barras: primero el ancho real

La captura medía **1358px**. Se dedujo sin preguntar, con el truco de siempre: el buscador mide 240px
fijos en el código y medía 239 en la imagen, así que la escala era 1:1. Descontando riel, panel y
padding quedan **1024px de contenido** en Visitas (vacía, sin barra de scroll) y **1009px** en
Pendientes (con scroll). Es **menos** que la notebook de referencia (1185px).

Con los componentes reales montados en un banco de pruebas temporal:

| Barra | Pedía | Tenía |
|---|---|---|
| Visitas | 1092px | 1024 |
| Pendientes | 1156px | 1009 |

Y en la notebook de referencia las dos entraban **vacías** pero partían **con un solo filtro puesto**.

## 3. El control que más pesaba no estaba en la captura

«Limpiar N» aparece recién cuando hay algo puesto —un filtro tildado o **una letra en el buscador**—
y suma ~100px. O sea: la barra partía **en el momento en que la tocabas**, y la lista entera bajaba
46px bajo el cursor. Achicar lo visible para que entre vacía habría repetido lo de Recepción (la
variante barata que alcanza en vacío y se rompe al primer filtro).

Se le preguntó al Director dónde ponerlo, con las tres opciones medidas, y eligió **junto al
recuento**:

- **Pendientes:** «12 de 52 pendientes · × Limpiar 2 · Ver descartados (1)», en la línea que ya era
  el resultado de la barra.
- **Visitas:** al final de «Viernes 11/09 · 3 visitas · 3 por llegar».

`ClearFilters` pasó de botón de 38px a forma de texto (12.5px, padding con margen negativo para que el
área de click no agrande el renglón).

## 4. Lo demás que cambió en la barra

- **Pendientes — «Antigüedad» corto.** `FilterDropdown` tiene una prop nueva, `neutralLabel`: quieto,
  el disparador nombra la **dimensión**, como «Estado» o «Protocolo» a su lado; con un umbral
  elegido, muestra el umbral («Últimos 14 días»). **307px → 149px.** En el menú la opción neutra
  conserva «Cualquier antigüedad». No se combina con `prefix`.
- **Visitas — «Ordenar por: En el centro» se queda como está**, pedido explícito del Director.
- **El buscador es la pieza elástica**: piso 160, techo 240, pegado a la derecha. Por debajo del
  techo el placeholder se corta con puntos suspensivos; por debajo del piso, recién ahí, baja de
  renglón.
- **Disparadores de `MultiFilterMenu` y `FilterDropdown`: padding 13→12 y gap 9→7.** Son ~30px en la
  barra de Visitas, que sin eso no entraba a 1009. **Toca a todas las pantallas que usan esos menús**
  (Stock, Recepción, Medicamentos, Dispensaciones, Reportes, Cola del médico): ahí sólo quedan más
  angostos. El prefijo de «Ordenar por» pasó de margen -4 a -2 para conservar sus 5px contra el valor.

## 5. Cómo quedó (medido)

Banco de pruebas, a 1009px:

| Estado | Visitas | Pendientes |
|---|---|---|
| Sin filtros | 1 renglón | 1 renglón |
| Con búsqueda escrita | 1 renglón | 1 renglón |
| 1 filtro | 1 renglón | 1 renglón |
| 2 filtros + orden cambiado | **2 renglones** | 1 renglón |
| 4 filtros + búsqueda | 2 renglones | 2 renglones |

A 1185px (la notebook de referencia) entran **todos** los casos.

Y logueado, a 1358px de ventana: Visitas del viernes 11/09 en un renglón sin filtros (buscador
200px) y con «Estado = Por llegar» (175px); «Limpiar 1» aparece sin mover la línea y limpia.
Pendientes en un renglón sin filtros (buscador 240) y con «Últimos 14 días + Ventana vencida» (226).
Los filtros se limpiaron al terminar; no se tocó ningún dato.

## 6. El aviso de Estadísticas

Al pasar por Farmacia › Estadísticas en el preview angosto, el Director marcó el texto: *"no hace
falta tanta explicación… no es para monitores"*.

| | Antes | Ahora |
|---|---|---|
| Título | El informe necesita más ancho | El informe necesita más espacio |
| Texto | Esta pantalla se diseñó para monitores de 1024px o más. Podés descargar el detalle del período y abrirlo desde acá. | Agrandá la ventana para verlo. |

El texto viejo venía **tal cual del mock** (`docs/handoffs/pharma-reportes/mock-revisado.html`). La
nota de detalle cortado de ayer (`notaDeDetalleCortado`) se sigue sumando cuando corresponde.

## 7. Lo no-obvio

- **El control que parte la fila puede no estar en la captura.** «Limpiar N» no se veía y era el que
  más pesaba. Medir el estado vacío es medir la mitad: lo que aparece al interactuar es justo lo que
  hace saltar la pantalla bajo el cursor.
- **Un envoltorio le roba la elasticidad al hijo.** El buscador vivía adentro de un `<div
  style="margin-left: auto">`: el ítem flex era el div, que no sabe encogerse. Hubo que sacar el
  envoltorio para que el buscador cediera.
- **En `flex-wrap`, la base es el piso y no el techo.** El corte se decide con el tamaño hipotético,
  así que `flex: 1 1 160px` + `max-width: 240` reserva 160 para decidir y después crece. Con base 240 y
  `flex-shrink` habría partido igual (ya estaba en la memoria de la notebook; se confirmó).
- **Un `span` común en un renglón de fuente mono crece 1.5px** cuando adentro entra un botón
  `inline-flex`: el renglón de 15px pasaba a 16.5 al aparecer «Limpiar», y la barra de abajo saltaba.
  Se arregló con `display: flex` en el envoltorio. Sólo se vio **midiendo alturas**, no mirando.
- **Medí después de `document.fonts.ready`.** La primera lectura en la app real dio disparadores 3px
  más angostos que el banco de pruebas (114 vs 117) y parecía una discrepancia; la segunda, con la
  fuente ya cargada, coincidió.
- **"Ya la mergeé" no es un hecho hasta verlo en el remoto.** Con la #159, el primer aviso llegó con
  la PR todavía abierta: `origin/main` sin el commit, la API diciendo `merged: false`. No se hizo el
  `checkout main`, que le habría sacado el cambio de la carpeta al Director. El segundo aviso sí
  estaba.
- **El copy de un mock no es sagrado.** El aviso de Estadísticas se implementó literal del mock y era
  lo que el Director menos quería leer: píxeles y "monitores" para decir "falta lugar".

## 8. Git — estado de las ramas

- [#158](https://github.com/spiraclinicapp/Spira-App/pull/158), un commit (`16fbd6e`), mergeada en
  `0d97c96`.
- [#159](https://github.com/spiraclinicapp/Spira-App/pull/159), un commit (`4bd8149`), mergeada en
  `fd78fe8`.

Las dos **verificadas en `origin/main` antes del `checkout main`**, y las ramas borradas en local y
en `origin`. **No queda ninguna rama viva.** Sin apilar. Sin migraciones: la última aplicada sigue siendo la **`0118`**.

Coda **`v0.69.2`** (`a44c896`): bumpea `package.json` y los dos lugares del lock, **sin changelog**.
Son ajustes de presentación, sin comportamiento nuevo que contar en «Acerca de».

**Esta jornada no pasó por el ciclo completo.** Fue `/impeccable layout` con una pregunta de diseño al
Director, banco de pruebas medido y QA logueado; no hubo spec, ni plan, ni subagentes, ni review de
rama. Queda dicho para que el registro no sugiera un proceso que no ocurrió.

## 9. Pendientes

De esta jornada:

- **Visitas con dos filtros a 1009px todavía parte.** ~~La vía más limpia es acortar el placeholder
  («Paciente o protocolo…»).~~ **Esto estaba mal**: el corte lo decide el piso del buscador (160px),
  no el largo del placeholder. Se resolvió al final del día en la PR
  [#160](https://github.com/spiraclinicapp/Spira-App/pull/160) — ver §10.

Y lo de antes, sin cambios:

- ~~**El aviso de Recepción manda a un filtro que no viaja a la base** (el §0 del handoff del 12).~~
  **Cerrado al final del día** — ver §11.
- El caso **negativo de permisos** con una cuenta acotada.
- Las URLs de IQVIA, LabCorp y Roche 4G — las busca el Director, **no se inventan**.
- Los dos caminos defensivos de `truncamiento.ts`, los dos del reskin de Equipo y accesos, y los
  P2/P3 viejos de `TODOS.md`.

---

## 10. Coda: el placeholder corto, y la cuenta que estaba mal · `v0.69.3`

El Director aprobó acortar el placeholder a «Paciente o protocolo…» porque este mismo §9 decía que
eso hacía entrar la barra con dos filtros. **Medido logueado, no alcanzaba**: con dos filtros y el
orden cambiado quedaban 143px a 1022 de contenido, y el buscador no baja de su piso de 160. El largo
del texto de ayuda sólo decide si se lee entero o con puntos suspensivos; **si la fila parte lo
decide `min-width`**. La recomendación salió de confundir las dos cosas.

Se le planteó así al Director, con los números, y eligió **las dos cosas juntas**: placeholder corto
y piso **130px**.

| Ancho de contenido | Dos filtros + «Ordenar por: Coordinador» |
|---|---|
| 1022 (sin barra de scroll) | 1 renglón · buscador 144px · «Calderon» escrito entra entero con la X |
| 1007 (con barra de scroll) | 2 renglones · faltan **2px** (128 contra 130) |

Con «Ordenar por: En el centro» (7px menos) entra también a 1007. Bajar el piso a 125 cerraría el
último caso, pero un apellido escrito quedaría casi sin margen: no se hizo. La búsqueda **sigue**
encontrando por N° (IVRS) en las dos pantallas — verificado en `DayVisitsView` y
`alertFilters.coincideBusqueda` —; el placeholder sólo dejó de anunciarlo.

**Lo no-obvio:** una recomendación que da un número ("lo haría entrar") se mide antes de escribirla
en un handoff. Esta se razonó sobre el ancho del placeholder y el control real era otro.

**Git.** Una PR, [#160](https://github.com/spiraclinicapp/Spira-App/pull/160), dos commits (código
`0146a2e` y esta corrección de docs `7a8e3dc`), mergeada en `b62aaf2` y verificada en `origin/main`
antes del `checkout main`. Rama borrada en local y en `origin`: **no queda ninguna rama viva**. Sin
migraciones. Coda **`v0.69.3`** (`858c444`), sin changelog, pedida por el Director para que la versión
coincida con lo desplegado: el merge de la #160 salió a producción después del tag `v0.69.2`.

---

## 11. Recepción: la fecha y el protocolo a la base · `v0.69.4`

El Director pidió meterse con el §0 del handoff del 12: el aviso de corte de **Farmacia › Recepción**
mandaba a *"acotá por fecha o por ámbito"*, y la fecha filtraba **en memoria** sobre las 500
recepciones ya traídas.

### Lo que la entrada de `TODOS.md` no decía

Leyendo el código aparecieron **dos defectos más** del mismo tema:

- **El protocolo también filtraba en memoria.** `useReceptions(fTipos, null)` recibía siempre `null`
  como protocolo. De los cinco controles, **sólo «Tipo»** hacía que la base trajera otras filas. Y
  el aviso decía «ámbito», que no es el nombre de ningún control.
- **El detector comparaba contra el techo propio** (`total > TECHO_RECEPCIONES`) y no contra lo que
  llegó. Es **el mismo defecto que se cerró en Estadísticas el 2026-09-12**; acá seguía vivo.

### La decisión

Se le planteó al Director como copy o transporte, y eligió **transporte**: tipo, protocolo y fecha
filtran en la base; estado, medicamento y búsqueda siguen en memoria. La regla quedó en una función
pura, `consultaDeRecepciones` (`src/data/pharma/receptionsModel.ts`), con 7 tests. Se escribieron
después del código, así que se comprobaron **por mutación**: romper la regla de «ambulatoria» y
quitar el orden de los protocolos hace caer cada uno su test.

El aviso pasó a decir **cuántas llegaron de cuántas hay** («Se muestran las 500 recepciones más
recientes de 1.234…») en vez de «hay más de 500»: si el `max-rows` del proyecto corta antes, el número
del techo sería falso. Y la lista vacía con corte dice «Ninguna de las N más recientes coincide» en
vez de «Ninguna recepción coincide», que podía ser mentira.

### Una sutileza que casi se pierde

`useSupabaseQuery` deja las filas viejas visibles mientras llega la respuesta nueva. Con la fecha
filtrando **sólo** en la base, al elegir «7 días» se veían por un momento recepciones de cualquier
fecha. Por eso la vista **repite** fecha y protocolo en memoria: durante la recarga achica lo que ya
había; con la respuesta nueva no quita nada. Está comentado para que nadie lo borre como duplicado.

### El QA

Primero, **la sintaxis sin sesión**: los filtros reales dan 401 (se entienden, falta el permiso) y un
filtro roto de control da 400.

Después, **logueado contra prod**. Producción tiene **11 recepciones** (7 de protocolo, 3 de
investigación, 1 ambulatoria, del 29/06 al 26/08, ninguna pendiente): el techo de 500 está lejos y
hoy el aviso no aparece. Se instaló un **espía de `fetch`** para ver qué salía en cada consulta:

| Filtro | Consulta | Resultado |
|---|---|---|
| 01/07 → 07/07 | `reception_date=gte…&lte…` | 6 |
| + ACT18301 desde el menú | `protocol_id=in.(…)` + rango | 4 — el conteo directo en la base da 4 |
| Tipo ambulatoria | `tipo=in.(ambulatoria)&protocol_id=is.null` | 1 |

Y con **el techo bajado a 3** (revertido antes del commit y confirmado fuera del diff): «3 de 11» sin
filtros, «3 de 7» con tipo protocolo, y **el rango 01/07 → 02/07 trae sus 3 recepciones sin aviso** —
el caso que antes mostraba «Nada con esos filtros». Inicio › Resumen siguió mostrando «0 por
verificar» con la firma nueva del hook.

### Lo no-obvio

- **Una entrada de `TODOS.md` describe lo que alguien vio, no todo lo que hay.** Hablaba de la fecha;
  el protocolo tenía el mismo defecto y el detector, el de ayer. Se encontró leyendo la llamada, no
  la entrada.
- **Un defecto cerrado en una pantalla sigue vivo en las que copiaron el patrón.** El detector contra
  el techo propio se arregló ayer en Estadísticas y nadie barrió las demás listas con techo.
- **Con stale-while-revalidate, mover un filtro a la base cambia lo que se ve durante la recarga.**
  No es un error de la consulta: es el hueco entre dos respuestas.
- **Quedaba uno de la misma forma:** Inicio contaba "por verificar" sobre las 500 más recientes.
  Se cerró el mismo día — ver §12.

### Git

Una PR, [#161](https://github.com/spiraclinicapp/Spira-App/pull/161), un commit (`e8ff0a0`), mergeada
en `a7d6894` y verificada en `origin/main` antes del `checkout main`. Rama borrada en local y en
`origin`: **no queda ninguna rama viva**. Sin migraciones. **939 tests** (932 → 939). Coda
**`v0.69.4`** (`7a3514f`), sin changelog: con 11 recepciones hoy no cambia nada visible.

---

## 12. Inicio: «por verificar» contado en la base · `v0.69.5`

Lo último que dejó abierto el §11. La cifra **«por verificar»** de la card de Farmacia en Inicio ›
Resumen contaba las pendientes **en memoria** sobre `useReceptions()`, o sea sobre las 500
recepciones más recientes: una pendiente más vieja no entraba en la cuenta.

Era lo **único** que Inicio usaba de esa consulta. Así que en vez de pedir la lista y filtrarla, la
cifra pasó a un conteo en la base, `usePendingReceptionsCount` (`head: true`, `count: 'exact'`,
`status = pendiente`): no trae filas y no tiene techo. **Y de paso Inicio dejó de descargar 500
recepciones con sus renglones embebidos para mostrar un número.** Mientras carga o si falla, la cifra
muestra «—» y nunca 0, que diría que no hay ninguna.

No lleva test: no hay regla pura, es una consulta. Se verificó en el navegador.

**QA logueado contra prod**, con el espía de `fetch` instalado **antes** de entrar a Inicio (se navegó
por la SPA para no perderlo en una recarga): Inicio pidió sólo
`HEAD select=id&status=eq.pendiente` con `Prefer: count=exact`, **sin ningún GET de la lista**. La card
dice «0 por verificar», y el conteo directo da 0 pendientes, 11 verificadas y 0 anuladas. Los pedidos
salieron duplicados: es el `StrictMode` de desarrollo montando dos veces, no el código. El caso que
arregla —una pendiente más allá de las 500— no existe en producción, y no se crearon recepciones de
prueba para forzarlo porque quedarían registradas.

**Git.** Una PR, [#162](https://github.com/spiraclinicapp/Spira-App/pull/162), un commit (`49c0544`),
mergeada en `bda8143` y verificada en `origin/main` antes del `checkout main`. Rama borrada en local y
en `origin`. Sin migraciones, **939 tests**. Coda **`v0.69.5`** (`662d94f`), sin changelog.

---

## 13. Las fuentes del aviso de corte, como unión discriminada · `v0.69.6`

Otro pendiente de `TODOS.md`, que venía del review de la tanda del techo de filas (2026-09-12): **dos
caminos defensivos de `truncamiento.ts`** que ningún test ejercía y que hoy no se alcanzan con las cinco
fuentes reales, pero que **compilaban**.

1. **Un control en `false` sin `motivo`.** El consejo caía en `motivo ?? 'no responde a ese control'` y
   quedaba circular: «Filtrar por protocolo no achica X: no responde a ese control».
2. **Los dos controles en `false`.** Caía en la rama mixta, que pide acotar el rango **y** filtrar por
   protocolo cuando ninguno la achica: impresión bloqueada, sin ninguna acción posible.

La entrada ya traía la solución, y se hizo esa: `FuenteDeDatos` pasó a `{ que, total, truncado } &
AlcanceDeFuente`, una **unión discriminada** con tres formas —los dos controles, o uno en `false` con
**`motivo` obligatorio**—. El consejo recibe el `motivo` tipado como `string` por guardas de tipo, así
que el fallback desapareció, y la fuente con los dos en `false` ya no se puede escribir. `ReportesView`
compiló sin tocarse.

El costo que anticipaba la entrada era real: la fábrica del test, `fuente(over: Partial<FuenteDeDatos>)`,
dejaba de compilar —y peor, un `Partial` de la unión habría dejado armar desde el test justo las
fuentes prohibidas—. Se rehízo con el alcance en su propio parámetro.

### El candado, y la trampa de ponerlo

Los dos casos se fijaron con `@ts-expect-error`, que chequea `tsc` dentro de `npm run build`. La trampa:
con `noUnusedLocals`, una `const` sin usar **ya da error por sí sola**, y ese error deja la directiva
satisfecha aunque la unión se afloje. El candado quedaría puesto sin cerrar nada. Por eso los casos van
como **llamadas en una sola línea**, cuyo único error posible es el del tipo.

Se verificó por mutación: hacer `motivo` opcional, o sumar la forma con los dos en `false`, hace caer
`tsc` con `Unused '@ts-expect-error' directive` (la primera además rompe el propio consejo). **El primer
intento de mutación no mutó nada**: el archivo está en CRLF y el patrón buscaba `\n`, así que los dos
reemplazos fallaron y `tsc` dio limpio. Se detectó porque el script lo dijo ("no encontré la rama");
un script que no lo chequeara habría reportado el candado como probado.

### Lo no-obvio

- **Un `@ts-expect-error` sobre algo que ya tiene otro error no prueba nada.** La directiva no dice
  QUÉ error espera. Poné el caso donde el único error posible sea el que querés fijar.
- **Una mutación que no se aplicó da el mismo verde que un test que no muerde.** Hacé que el script
  verifique que el reemplazo ocurrió antes de correr nada.
- **En este repo los archivos del working copy suelen estar en CRLF**: todo patrón sobre texto crudo va
  con `\r?\n`.

**Git.** Una PR, [#163](https://github.com/spiraclinicapp/Spira-App/pull/163), un commit (`7da62c9`),
mergeada en `0e5f69c` y verificada en `origin/main` antes del `checkout main`. Rama borrada en local y
en `origin`. Sin migraciones, **941 tests** (939 → 941). Coda **`v0.69.6`** (`5bab0b1`), sin changelog:
es un cambio de tipos y la app se comporta igual.

---

# Segunda parte · La dispensación de base y de IMP · release `v0.70.0`

## 14. El pedido, y la revisión que lo convirtió en plan

El Director pidió ajustar la dispensación en dos frentes. **Base:** independiente del cronograma y
siempre editable, un aviso rojo si la medicación se entregó en los últimos 30 días, stock visible, una
opción «Otro» con pedido adjunto y entregas en partes (la Fenisona: 60 dosis en dos entregas de 30).
**IMP:** atado al cronograma, que genere su procedimiento en el panel de la visita, que se marque
realizado sólo cuando Farmacia confirma, y alerta al coordinador a los dos días sin confirmación.

Se pasó por `/plan-eng-review`. Salió **`docs/plan-dispensacion-base-e-imp.md`**: **16 decisiones** en
**tres tandas = tres PRs**. La Tanda 3 («Otro», partes, aviso rojo) pide mock antes de implementarse.
Hoy salieron las Tandas 1 y 2.

Las decisiones que más cambian el diseño respecto del pedido literal:

- **D1 · La fila del IP es CALCULADA, no un procedimiento guardado.** Guardarlo en
  `protocol_activities` chocaba en ocho lugares: `set_visit_procedures` lo borraba al guardar
  (`0091:84-86`), la RLS dejaba tildarlo a mano (`0064:59-63`) y el estado de las visitas viejas
  cambiaba de golpe.
- **D10 · Una sola regla del IP, en SQL** (`v_visit_ip_status`), que leen la fila, la alerta y el
  estado de la visita. "Esta visita lleva IP" se **sella** al fecharse la visita; nunca se mira el
  cronograma en vivo.
- **D3 · La alerta es en la app, a las 48 h, y no se descarta.** No hay SMTP ni nada que corra con
  horario. La apagan la entrega, "No corresponde" o "Entregado en otra visita" (D2, D11).
- **D4/D9 · La base es libre, con un sello propio** (`base_sin_cronograma`). Reusar `off_schedule` sin
  motivo revienta su check (`0071:98`) y le abre el IP a cualquier pedido (`0071:333`).
- **D6 · El stock muestra lo que Farmacia puede ARMAR**, no el total del estante: el FEFO arma desde un
  solo lote (`0075:499-510`).
- **D8 · Las partes van por envases, con saldo.** El Director eligió "dosis" por error en la pregunta
  y lo corrigió en el acto.

**La revisión encontró un bug que ya estaba en prod:** `create_dispensation_request` copiaba
`dispenses_ip` en CADA pedido (`0071:495`). El segundo pedido de una visita con IP salía "con IP"
aunque fuera de pura base, y el mostrador le pedía constancia impresa y kits que no existen. Se
arregló en la 0119.

## 15. Tanda 1 · IMP · 0119, 0120, #164/#165/#166

- **0119 (aditiva):**
  - `dispensations.delivered_by`/`_name`, sellados por trigger y congelados por el candado de la 0073;
  - `patient_visits.lleva_ip`, la marca de IP que se escribe al fecharse la visita;
  - `visit_ip_closures` con `close_visit_ip`/`reopen_visit_ip`;
  - las vistas `v_visit_ip_status` y `v_ip_delivery_alerts`;
  - el arreglo del bug de `includes_ip`.
- **0120 (después del deploy):** `computed_status` espera la entrega del IP.
- **Front:**
  - la fila «Entrega de producto en investigación», primera en Procedimientos, que aparece aunque la
    visita no tenga otros;
  - las dos salidas con «Deshacer»;
  - la clase de alerta «IP sin entregar» en la campana (primera y sin tacho) y en Pendientes (filtro,
    tarjetas, médico, coordinador, recuento);
  - `bumpIpEstado`, para que el panel de Dispensación, la fila y las alertas se enteren entre sí.

**El corte no es una fecha literal**, como decía el plan: es el propio sello. `lleva_ip` en NULL quiere
decir "fechada antes de la 0119". Mismo efecto, sin la trampa del huso.

**QA logueado contra prod** sobre PROT-A (demo), con una definición y un paciente `TEST-QA IP`
creados y borrados:
- la fila en «Sin pedir»;
- «No corresponde» y «Deshacer», con INSERT/DELETE en `audit_log`;
- el sello se escribe al registrar la visita, y apagarlo desde el cliente no tiene efecto;
- el ancla cae en la medianoche AR;
- la alerta aparece en Pendientes y en la campana.

## 16. Tanda 2 · Base · 0121, 0122, #167/#168/#169

- **0121 (aditiva):**
  - `base_sin_cronograma` y `prepared_by_name` (trigger);
  - create/add sin candado de cronograma para la base;
  - `update_dispensation_item_quantity` / `remove_dispensation_item`, con lock y guard;
  - auditoría de `dispensation_request_items`;
  - `dispensation_audit_trail` que encuentra renglones borrados;
  - cierre de las tres policies de escritura directa de Track;
  - `stock_de_la_visita`.
- **0122:** guard de columnas selladas del pedido.
- **Front:**
  - la base en cualquier visita sin motivo, con el sello en el comprobante, el cajón, la tarjeta y el
    historial;
  - lápiz y cruz sobre el pedido solicitado, y «Lo está preparando <nombre>» cuando Farmacia lo tomó;
  - stock en el desplegable y aviso de lote;
  - la tarjeta se relee ante un rechazo del servidor.

**QA logueado** sobre PROT-A (demo) con una VNP:
- «2 en stock»; con 3 avisa «hasta 2 de una vez»;
- pedir sin motivo deja el sello;
- editar a 2 queda guardado y el último renglón no se quita (tarjeta y servidor);
- el historial registra el renglón;
- insert directo de renglón y de pedido rechazados;
- cancelar deja el stock intacto.

**Residuo PERMANENTE autorizado por el Director:** `TEST-QA base`, su VNP y un pedido cancelado
(`delete_patient` no borra pacientes con pedidos).

## 17. Lo que el QA destapó

- **Ningún cronograma de producción tiene `dispenses_ip`.** Cero definiciones, y un solo pedido de IP en
  toda la base, cancelado. Hasta que se tilde «Entrega producto en investigación» en los cronogramas
  reales, la Tanda 1 no se ve.
- **Farmacia y gerencia podían hacer PATCH de cualquier columna del pedido** («pharma atiende
  solicitud», 0009:161-163). Se probó (`includes_ip` pasó a true y se revirtió) y se cerró con la
  **0122**: un trigger que distingue por `current_user`. Dentro de una función SECURITY DEFINER es el
  dueño; una escritura directa llega como `authenticated`. Verificado: el PATCH de `includes_ip` y el de
  `protocol_id` son rechazados y `notes` pasa. El `status` quedó en `TODOS.md`.
- **`dispensation_request_items` nunca tuvo auditoría**, aunque el historial de Farmacia estaba escrito
  esperándola. Los eventos de renglón nunca aparecieron.

## 18. Lo no-obvio

- **Una migración que va segunda no puede estar en la carpeta, ni suelta ni commiteada.** La 0120 estaba
  sin commitear en `supabase/migrations/`. Le dije al Director «la 0119 está en la rama
  `feat/imp-0119`», pero la rama activa era otra. Abrió la más nueva que veía y corrió la 0120.
  Falló en la sentencia 3 y **no dejó commiteados los dos `drop view` anteriores**: la sonda dio
  `42501` sobre las vistas. Desde ahí el SQL se pasa **como archivo**.
- **"Aplicada" se verifica con la sonda, no se da por hecho.** La primera vez que se avisó la 0121 no
  estaba: columnas `42703` y funciones `PGRST202`. La segunda sí (`42501`). Una columna inventada de
  control distingue "no existe" de "sin permiso".
- **Un trigger con `tg_op = 'INSERT' or old.x` en UNA expresión revienta en el INSERT**: se planifica
  entera, la misma trampa de `audit_row`. Van dos ramas de plpgsql.
- **El workflow multiagente se cortó a mitad** por consumo de tokens, a pedido del Director. Los
  hallazgos de SQL se verificaron a mano. Ver la memoria `workflows-solo-si-el-tamano-lo-justifica`:
  el Director no usa ultracode.
- **El tope del changelog es de 100 caracteres** (`version.test.ts`). El primer texto de la 0.70 tenía
  172 y el build lo frenó.

## 19. Git — estado de las ramas

- **Tanda 1:** [#164](https://github.com/spiraclinicapp/Spira-App/pull/164) (0119, `83c09e7`),
  [#165](https://github.com/spiraclinicapp/Spira-App/pull/165) (front, `46511eb`) y
  [#166](https://github.com/spiraclinicapp/Spira-App/pull/166) (0120, `76cd40c`).
- **Tanda 2:** [#167](https://github.com/spiraclinicapp/Spira-App/pull/167) (0121 + 0122, `8986df3`) y
  [#168](https://github.com/spiraclinicapp/Spira-App/pull/168) (front, `3295bcc`).
- Todas mergeadas, verificadas en `origin/main` y con el deploy confirmado en el bundle. Ramas borradas
  en local y en `origin`.
- [#169](https://github.com/spiraclinicapp/Spira-App/pull/169) (`docs/0122-aplicada`, sólo la fila
  «Aplicada» de la 0122): mergeada después del cierre, en `4e4c3f6`. Rama borrada en local y en `origin`:
  **no queda ninguna rama viva**.
- **Release `v0.70.0`** (`83077b3`), con entrada en el changelog. CLAUDE.md dice ahora que la última
  aplicada es la `0122`.

## 20. Pendientes

1. **Tildar «Entrega producto en investigación»** en las visitas con IP de los cronogramas reales.
   Configuración, no código.
2. ~~**Tanda 3** («Otro» con receta, entregas en partes, aviso rojo por droga entre protocolos): mock en
   el repo y `/plan-design-review` antes de la primera línea.~~ **Hecho en la tercera parte** — ver §21-§27.
3. `TODOS.md`: alertas al día sin recargar, y el `status` del pedido todavía modificable por Farmacia y
   gerencia.
4. Sin probar: «Entregado por <farmacéutica>» (hace falta una entrega real), quitar con más de un
   renglón, «Lo está preparando», y los permisos con una cuenta que sea sólo coordinadora.

---

# Tercera parte · El mock de la Tanda 3 y sus dos revisiones

## 21. Resumen

| Hilo | Qué salió | Dónde |
|---|---|---|
| Mock | 15 pantallas en tres páginas (Coordinación, Farmacia, Historial) | `docs/design_handoff_dispensacion_t3/` + [lienzo](https://claude.ai/code/artifact/e110068f-a952-4880-a78c-76c3cb308780) |
| `/plan-design-review` | 6/10 → 9/10, decisiones **D17-D28**, el Historial simplificado | plan, «Revisión de diseño · Tanda 3» |
| `/plan-eng-review` | 22 hallazgos, correcciones **R1-R13**, la Tanda 3 partida en 3a/3b/3c | plan, «Revisión de arquitectura · Tanda 3» |

Sin código, sin PR, sin migraciones. La última aplicada sigue siendo la **`0122`**.

## 22. El mock, sacado del código

No había mock de la Tanda 3 (en Descargas sólo había fotos ajenas al tema). El Director eligió que lo armara
yo. Se hizo con la skill `design`, un lienzo editable publicado como Artifact. **Los artboards no se
dibujaron a mano**: un script arma el HTML con los valores
literales de `tokens.css`, `Panel.tsx`, `VisitDispensationPanel.tsx`, `ConstanciaIp.tsx` y el cajón
(`DispensacionDrawer`, `PanelPreparando`, `ItemRow`, `RailProceso`, `KanbanCard`). Quedó en el repo como
`docs/design_handoff_dispensacion_t3/generar-artboards.mjs`. Cada pantalla se abrió en
el Browser pane para medir su alto antes de fijar el tamaño del marco.

- **Coordinación:** el aviso rojo al elegir, «Otro» en el desplegable y con la receta, el pedido «Por
  habilitar» y «No habilitado», las entregas en partes, el saldo en la visita siguiente, y saldo + rojo
  juntos.
- **Farmacia:** la tarjeta del tablero con «Pide habilitar», el cajón con la habilitación arriba, y el
  modal «No habilitar».
- **Historial:** la tarjeta de hoy, las opciones A y B, y la B con un pedido rechazado.

Una segunda pasada con un subagente de sólo lectura encontró dos inconsistencias entre pantallas: el
pedido de Farmacia tenía Fenisona y el de Coordinación no, y las fechas del saldo chocaban con el cajón.
Se corrigieron.

## 23. `/plan-design-review` · el Historial del Director

Con la revisión, el Director pidió **simplificar el Historial**: le chocaba el historial de medicamentos
contra el botón «Pedir producto en investigación fuera de cronograma». Leído en el código
(`VisitDispensationPanel.tsx:1218-1267`), el botón era otra caja blanca del mismo ancho y el mismo borde,
pegada a «Ver N más»: **se leía como un pedido más del historial**, y era una salida del IP lejos de la
sección del IP.

Se dibujaron la tarjeta de hoy y dos opciones, y el Director eligió la **B**:

- **D17 · El historial se pliega en una línea al pie.** Con un pedido rechazado, la línea lo nombra y se
  abre desplegado.
- **D18 · La sección «Producto en investigación» existe siempre.** Si el cronograma no lo prevé, su estado
  vacío ofrece «Pedir fuera de cronograma», y el motivo vive adentro. Se va la subsección aparte.
- **D19 · Orden fijo:** avisos · concomitante · IP · solicitar · pie del pedido · historial.

Además se resolvieron las cinco preguntas que había dejado el mock:
- D24: pedir más que el saldo va en rojo (después reescrita por R2).
- D25: varias drogas en rojo van en una sola caja.
- D26: un «Otro» solo abre su propio pedido.
- D27: «1 (de 2 indicados)» en el comprobante.
- D23: «Pedir de nuevo» en la fila rechazada.

Y dos supuestos míos que se confirmaron:
- D22: «Marcar lista» espera la habilitación.
- D28: los motivos de «No habilitar» van en lista.

Sin voz externa: el Director eligió no sumarla.

## 24. `/plan-eng-review` · lo que el plan no había visto

Se leyó el SQL vivo antes de opinar: 0121, 0075, 0076, 0071 y 0050. Tres cosas que el plan daba por
resueltas no lo estaban:

- **D26 choca con la base:** un pedido sin renglones ni IP lo rechaza `create_dispensation_request`
  (`0121:146`), y también `mark_dispensation_ready` (`0075:449`).
- **Coordinación no puede leer lo entregado:** `dispensation_items` es sólo de Farmacia, contable y
  gerencia (0006). El saldo y el aviso tienen que calcularse del lado del servidor.
- **Cancelar, rechazar y quitar renglones no saben nada de habilitaciones.**

Correcciones principales:

- **R1:** la Tanda 3 se parte en **3a** (panel, sólo front), **3b** (partes, saldo, aviso rojo) y **3c**
  («Otro», habilitación).
- **R2:** el saldo es un vínculo al renglón original (`saldo_de_item_id`), resta lo que está en camino y se
  valida en SQL.
- **R3:** un «Otro» nace en una sola transacción.
- **R4:** la habilitación anulada se deduce del estado del pedido.
- **R5:** una sola tabla nueva, con la receta adentro.
- **R7:** `contexto_dispensacion` recibe la VISITA y no el paciente, para no abrir un canal a la historia
  de pacientes ajenos.

## 25. La voz externa, y la decisión contra la recomendación

La voz externa (subagente, porque codex no está instalado) trajo 15 hallazgos. Los que cambiaban decisiones
se verificaron contra el código antes de preguntarlos.

- **Se mantuvo D12** (la habilitación viaja con el pedido), frente a la propuesta de un trámite aparte: con
  el paciente en la visita, serían dos viajes.
- **R6 · «Otro» habilita UNA sola entrega.** Recomendé «tratamiento» (queda habilitado hasta que Farmacia
  lo deshabilite) y el Director eligió **una entrega**. Se diseñó así:
  - un trigger desactiva `patient_medications` cuando el pedido termina, sólo si la marca apunta a esa
    habilitación;
  - el saldo de un «Otro» pide una habilitación nueva con la misma receta.

  El copy del mock se ajustó: «sólo para esta entrega».
- **La cuenta del saldo y la ventana de 30 días pasaron a reglas puras con test**, sobre filas crudas de la
  base, para que el test cubra la regla que falla en silencio.

## 26. Lo no-obvio

- **`0007_realtime_grants.sql:30` le da EXECUTE a toda función nueva a `authenticated`.** El `revoke all
  … from public` del repo no lo quita. Una función "interna" nace llamable por `/rpc` salvo que se revoque
  de `authenticated` a propósito. Sin precedente en el repo.
- **El bucket `ip-docs` no tiene policy de borrado**, a propósito. El «borrado best-effort» de
  `uploadIpDocument` no borra nada: Storage devuelve vacío sin error. Los huérfanos quedan, y la limpieza
  fue a `TODOS.md`.
- **«Preparando» no quiere decir «sin comprobante».** Con la dispensación en `lista`, el pedido sigue en
  `preparando` (`0071:339-342`). Cualquier función que sume algo al pedido tiene que mirar la dispensación,
  no sólo el estado.
- **`resolve_dispensation` sigue viva y ejecutable** (`0050:342`) aunque la 0054 la declaró deprecada.
  Saltea escaneo, constancia y cualquier guard nuevo. Se revoca en la 3c.
- **Un botón que se lee como parte de otra sección no se arregla con color.** Se arregla moviéndolo a la
  sección de la que es: el estado vacío de la sección del IP.
- **Una sola revisión no alcanzaba.** La de diseño aprobó D26 como algo de pantalla; recién la de
  arquitectura vio que la base lo rechazaba en dos funciones.

## 27. Git y pendientes

**Git.** Un commit de docs en `main` con el mock, el plan (D17-D28, R1-R13, tareas por entrega), la
entrada nueva de `TODOS.md` (archivos huérfanos de `ip-docs`), esta bitácora y el handoff. Sin ramas.

**Pendientes:**

1. **Implementar la 3a** (historial plegado y sección del IP, sólo front).
2. Después **3b** y **3c**, las dos con la migración primero.
3. Siguen del día: tildar el IP en los cronogramas reales, y el `status` del pedido en `TODOS.md`.
