# Dependencias
node_modules/

# Build
dist/
dist-ssr/
*.local

# Variables de entorno / secretos (NUNCA commitear claves de Supabase)
.env
.env.*
!.env.example

# Supabase (estado local del CLI)
supabase/.branches/
supabase/.temp/
supabase/.env

# Brainstorm companion (mockups efímeros)
.superpowers/

# gstack: worktrees locales y estado local (no van al repo)
.claude/worktrees/
.gstack/

# impeccable (live mode): la config (.impeccable/live/config.json) SÍ se versiona;
# el journal de sesiones es estado local de recuperación y no va al repo.
.impeccable/live/sessions/

# Editores / SO
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log

# Vercel (estado local del CLI + token de deploy — NUNCA commitear)
.vercel
.vercel.token
.env*

# Credenciales de QA locales para el preview (ver CLAUDE.md → Verificación en el preview)
.claude/qa-creds.local.md
