# Spira · Base de datos (Supabase / PostgreSQL)

La **fuente de verdad del schema son las migraciones** en `migrations/`, numeradas y
aplicadas en orden. No hay un `schema.sql` monolítico a propósito (para no tener dos
copias que diverjan). Para una vista combinada: `cat migrations/*.sql` o `supabase db dump`.

## Orden de migraciones

| # | Archivo | Contenido |
|---|---------|-----------|
| 0001 | `extensions_enums.sql` | extensiones (`uuid-ossp`, `pgcrypto`) + todos los enums |
| 0002 | `tables.sql` | 26 tablas: identidad/RBAC, Track, Pharma, auditoría |
| 0003 | `functions_triggers.sql` | `updated_at`, generación de visitas, materialización de checklist, stock (recepción/dispensación), auditoría genérica, validaciones e inmutabilidad |
| 0004 | `views.sql` | `v_patient_visits` (estado calculado), `v_medication_stock`, `v_billing_dispensations` |
| 0005 | `indexes.sql` | índices de uso frecuente |
| 0006 | `rls_policies.sql` | `enable RLS` + helpers `auth.*` + todas las policies |
| 0007 | `realtime_grants.sql` | publicación realtime + `replica identity` + revoke de `anon` |
| 0008 | `handle_new_user.sql` | trigger `auth.users → public.users` (perfil automático al registrarse, sin roles) |
| 0009 | `role_levels.sql` | niveles de rol estrictos (viewer<operator<leader<admin) + `role_rank` / `has_min_role` |
| 0010 | `pharma_read_enrollments.sql` | pharma puede LEER enrollments (read-only) para la vista protocolo→pacientes |
| 0011 | `protocols_description.sql` | columna `description` (libre, nullable) en `protocols` + seed demo — pista corta para la card del selector |
| 0012 | `create_patient_with_enrollment.sql` | RPC de alta atómica paciente+enrolamiento (SECURITY DEFINER con authz a mano, actor server-side) |
| 0013 | `v_track_visits.sql` | vista plana visita+definición+protocolo+paciente (security_invoker) para Resumen/Agenda de Track |
| 0014 | `checklist_templates_scoping.sql` | scoping de plantillas: global → track admin/gerencia; por protocolo → coordinadora asignada (operator+); + cierra la lectura de ítems sin scopear |
| 0015 | `track_rpcs.sql` | RPCs de Track: alta de paciente con bypass gerencia/track-admin + `create_protocol_template` (atómica) + `swap_template_item_order` (atómica) |
| 0016 | `v_track_visits_extend_and_kpis.sql` | amplía `v_track_visits` (offset_days, enrollment_date, treating_physician) + crea `v_protocol_kpis` (tablero del protocolo) |
| 0017 | `protocols_patients_new_columns.sql` | columnas nuevas: protocols (investigador/especialidad/código interno), patients (sex, fertility) |
| 0018 | `create_patient_with_enrollment_v2.sql` | RPC de alta v2: suma `p_sex`/`p_fertility` (drop+recreate de la de 6 params) |
| 0019 | `enrollments_update_gerencia.sql` | gerencia puede editar el enrolamiento (médico tratante); alinea la policy de UPDATE de `enrollments` con el resto (gerencia OR coordinadora asignada). **Superada por 0020** (queda inocua) |
| 0020 | `treating_physician_to_patients.sql` | mueve `treating_physician` de `enrollments` a `patients` (es atributo de la persona): columna + backfill + recrea `v_track_visits` y el RPC de alta + dropea la columna vieja |
| 0021 | `deferred_visits_optional_ivrs.sql` | visitas ancladas en `randomization_date` (generación diferida: al cargarla); `enrollments += screening_date/randomization_date`; `patients.code` opcional (IVRS se asigna en randomización); RPC de alta v4 |
| 0022 | `visitas_unificadas.sql` | modelo de visitas unificado: enum `visit_kind` + columna `kind` (`programada` vs sueltas pre-randomización); recrea las vistas en orden de dependencia |
| 0023 | `track_visita_dia.sql` | "Visitas del día": etapas operativas (marcas timestamptz) sobre `patient_visits` + flag `dispenses` en `visit_definitions` + tabla mínima `track_dispensations` |
| 0024 | `delete_patient.sql` | RPC `delete_patient` (gerencia o track leader/admin): borra el paciente en cascada (FK `ON DELETE CASCADE`), auditado |
| 0025 | `register_as_schedule.sql` | registrar una visita suelta = AGENDAR (`estimated_date`, `real_date` NULL); la atención se marca después en "Visitas del día" |
| 0026 | `protocol_schedule.sql` | gestión del cronograma del protocolo: endurece la RLS de `visit_definitions` (edición gerencia/track-admin, cierra el borrado directo) + RPCs `sync_protocol_schedule` y `delete_visit_definition` |
| 0027 | `count_deletable_visits.sql` | RPC `count_deletable_visits` (SECURITY DEFINER): impacto de borrar una definición `{deletable, attended}` con la authz del borrado (no bajo la RLS de lectura del caller) |
| 0028 | `track_admin_ve_protocolos.sql` | suma una rama `has_min_role('track','admin')` a la SELECT de `protocols` (era `leader` exacto): alinea la visibilidad de protocolos con quién gestiona el cronograma. Aditivo; no toca la visibilidad de patients/visits |
| 0029 | `visit_role_y_generacion.sql` | cuadro de actividades completo (Fase 1): `visit_definitions.role` (screening/randomizacion/comun); recrea `v_track_visits` (+`role`/`date_mode`) y `v_protocol_kpis`; `generate_patient_visits` y `sync_protocol_schedule` toman solo `date_mode='automatica'` |
| 0030 | `flujo_randomizacion.sql` | flujo operativo (Fase 2): el constraint de forma permite `programada` libre (sin ventanas); RPCs `schedule_protocol_visit` (agendar libre del cuadro), `mark_ready_with_outcome` (cierre clínico idempotente: IVRS de screening / confirma randomización), `discontinue_enrollment`; `register_visit_event` reescrito con **cutover** (cierra sueltas firma/screening/rando cuando hay cuadro); recrea `v_track_visits` (+`enrollment_randomization_date` para la salvaguarda). **Aplicada en prod (2026-06-22).** |
| 0031 | `doctor_seen.sql` | `patient_visits.doctor_seen_at` (marca "Atendido por el médico", persistente) + RPC `mark_doctor_seen`; recrea `v_track_visits` (+`doctor_seen_at`, recreando también `v_patient_visits` para que el `pv.*` re-exponga la columna). La cola "Para ver médico" deja al atendido visible (no desaparece) y suma el indicador "Médico" en Visitas del día. **Aplicada en prod (2026-06-22).** |
| 0032 | `pharma_catalogo_global.sql` | Pharma 1a: `medications` global (drop `protocol_id`, + `drug_id`→`drugs`), stock por protocolo (`medication_lots.protocol_id` + nuevo unique), `protocol_medications` (asignación), `medication_codes` (GTIN único); reescribe `apply_reception_stock`/`check_*_item_protocol`/`v_medication_stock`; RPCs `create_drug`/`create_medication`/`assign_medication_to_protocol`/`create_reception`/`verify_reception`/`adjust_stock`. Corrige de paso un bug latente de `apply_reception_stock` (0003 usaba `excluded.quantity`, columna inexistente; nunca se había ejecutado por falta de UI). **Aplicada en prod (2026-06-25).** |
| 0033 | `pharma_laboratorios.sql` | laboratorio en el catálogo: tablas `laboratorios` + `laboratorio_codes` (prefijo de empresa GS1 del GTIN → laboratorio, aprendido on-demand al escanear, espejo de `medication_codes`); `medications.laboratorio_id`; RPC `create_laboratorio` + `create_medication` re-firmado para aceptar el laboratorio. **Aplicada en prod (2026-06-28).** |
| 0034 | `pharma_dosis.sql` | `medications.dosis` como campo propio (vivía pegada al `name`; el `name` sigue compuesto para no romper los desplegables) + `create_medication` re-firmado para aceptar la dosis. **Aplicada en prod (2026-06-28).** |
| 0035 | `pharma_recepcion_tipos.sql` | recepción tipada: enum `reception_kind` (`protocolo`/`investigacion`/`ambulatoria`); `tipo` en `medication_receptions` y `medication_lots` con `protocol_id` ahora nullable + CHECK (`ambulatoria` ⇔ sin protocolo) + unique parcial de lote ambulatorio; reescribe `apply_reception_stock`, `create_reception` y `v_medication_stock` para ramificar por ámbito. **Aplicada en prod (2026-06-29).** |
| 0036 | `pharma_v_stock_fix.sql` | fix salido del review de la 0035: `v_medication_stock` agrupaba por `ml.tipo` (fan-out latente si un protocolo mezclara tipos de lote); vuelve a la invariante "una fila por (medicamento, protocolo)" con `tipo` fijo `protocolo`, rama ambulatoria intacta. **Aplicada en prod (2026-06-29).** |
| 0037 | `pharma_ip_units.sql` | Producto de Investigación (IP) por UNIDAD: tabla `ip_units` (una fila = un kit; identidad protocolo+`kit_number`; ganchos de dispensación nullable para Tajada 2) + enum `ip_unit_status` + RLS/auditoría + RPC `create_ip_reception` (por unidades) + vista `v_ip_units` + rama IP de `apply_reception_stock`. **Superada por 0038** (pivote al modelo macro; la tabla queda dormida). **Aplicada en prod (2026-06-30).** |
| 0038 | `pharma_ip_macro.sql` | PIVOTE del IP a ingreso MACRO por cantidad (definición del Director: la traza por kit la lleva el sponsor/IRT, Spira no la duplica): columnas macro en `medication_receptions` (coordinador, `temperature_ok`, `total_kits`, rango de kits, `storage_location`, doble check docs+IRT, `started_at`); RPC `create_ip_reception` re-firmado (crea la recepción directamente `verificada`: el doble check ES la verificación); vista `v_ip_stock` (kits por protocolo, excluye recepciones IP viejas sin cantidad); RPC `list_protocol_coordinators`; dropea `v_ip_units` y el RPC por-unidad; `ip_units` queda DORMIDA (no se dropea por regla de datos reales). **Aplicada en prod (2026-07-02).** |
| 0039 | `pharma_ip_storage_unificar.sql` | unifica el almacenamiento del IP a `heladera` \| `ambiente` ('estante' era operativamente lo mismo que ambiente): convierte los datos existentes, recrea el CHECK y acota la validación del RPC `create_ip_reception`. **Aplicada en prod (2026-07-02).** |
| 0040 | `pharma_recepcion_autoasocia.sql` | catálogo global + asignación como CONSECUENCIA de recibir: `create_reception` y el trigger `apply_reception_stock` hacen upsert idempotente en `protocol_medications` (asocian) en vez de rechazar. Sin migración de datos. **Aplicada en prod (2026-07-04).** |
| 0041 | `pharma_v_lotes_detail.sql` | vista `v_medication_lots_detail`: una fila por LOTE (join lots→meds→drugs→codes) con EAN13 (un código por med), lote, vencimiento y flags de estado (`vencido` / `por_vencer` <30d) en SQL; ramas protocolo (protocol_id no null) y ambulatoria (null). Backing de la vista Medicamentos rediseñada. `v_medication_stock` no se toca. **Aplicada en prod (2026-07-05).** |
| 0042 | `pharma_medicamento_unico.sql` | anti-duplicado del catálogo: índice único `(lower(btrim(name)), unit)` — un medicamento por (nombre normalizado, presentación). Pre-requisito: limpiar duplicados existentes antes de aplicar (el índice falla si hay). **Aplicada en prod (2026-07-05).** |
| 0043 | `pharma_clase.sql` | `medications.clase` (clase/indicación, opcional) + `create_medication` re-firmado para aceptar `p_clase`. **Aplicada en prod (2026-07-05).** |
| 0044 | `feedback.sql` | tabla `feedback` (sugerencia/problema/idea + contexto autoadjuntado; actor server-side vía `auth.uid()`) + RLS SELECT solo gerencia + RPC `submit_feedback` (SECURITY DEFINER, rate-limit 10s). Backend del modal "Dar feedback". **Aplicada en prod (2026-07-05).** |
| 0045 | `profile_editable.sql` | "Mi cuenta" editable: columnas `puesto`/`centro`/`name_changed_at`/`email_changed_at` en `users`; **saca la policy de UPDATE del perfil propio** (0006) y enruta todo por RPCs SECURITY DEFINER `update_my_name` (regla dura 1 cambio/30 días), `update_my_puesto` (catálogo cosmético, **no toca el acceso**) y `stamp_email_change` (guarda 30 días del correo). `centro` con default → forzado y de solo lectura. **Aplicada en prod (2026-07-06).** |
| 0046 | `profile_rpc_search_path.sql` | hardening: `create or replace` de las 3 RPCs de 0045 con `set search_path = pg_catalog, public` (estándar del repo, 0006) en vez de solo `public`. Defensa en profundidad para SECURITY DEFINER. Solo cambia el search_path. **Aplicada en prod (2026-07-06).** |
| 0047 | `visit_doctor_motivo.sql` | "Marcar para ver médico" con MOTIVO: columna `patient_visits.doctor_motivo` + RPC `mark_wants_doctor(p_visit_id, p_motivo)` (setea `wants_doctor` + motivo atómico; authz espejo de `mark_doctor_seen`). Recrea `v_patient_visits`/`v_track_visits` (patrón del `*` congelado, espejo de 0031). Base del detalle de visita unificado. **Aplicada en prod (2026-07-06).** |
| 0048 | `visit_comments.sql` | comentarios de visita (hilo plano): tabla `visit_comments` con autor **desnormalizado** (`author_name`/`author_role` snapshot en la fila — la RLS de `users` solo muestra la fila propia y un join ocultaría en silencio los comentarios ajenos; además queda registrado el puesto DE ENTONCES, correcto para app auditable) + RPC `add_visit_comment` (SECURITY DEFINER; authz de escritura espejo de la lectura: gerencia o coordinador asignado) + vista `v_visit_comments` + recrea `v_track_visits`. Backend de Comentarios en `VisitDetail` y el Drawer de la cola del médico. **Aplicada en prod (2026-07-12).** |
| 0049 | `pvm_wait_and_demographics.sql` | rediseño de "Para ver médico": columnas `patient_visits.wants_doctor_at`/`doctor_marked_by` (reloj de espera + procedencia REALES, no inventados), estampadas SOLO en la transición false→true por `mark_wants_doctor` (0047) y `toggle_wants_doctor` (0023). Expone `patients.sex`/`birth_date` en `v_track_visits`. Recrea `v_patient_visits` (el `*` congelado, patrón 0047) **y agrega el `revoke` de escritura que le faltaba desde su origen en 0023** (era *automatically updatable* por ser una vista simple sobre una sola relación, con la RLS amplia de `patient_visits` detrás — hallazgo de la revisión adversarial). **Aplicada en prod (2026-07-12).** |
| 0050 | `pharma_dispensacion.sql` | dispensación (v1): tabla `patient_medications` (medicación habilitada por paciente/enrolamiento; RLS Pharma-escribe / Track-lee scopeado / gerencia-borra; trigger de coherencia con `protocol_medications`). **Doble enforcement** extendiendo `check_request_item_protocol` (al solicitar exige `patient_medications` activa) y `check_dispensation_item_protocol` (al entregar exige que el med figure en la solicitud **y siga activo**). 4 RPCs SECURITY DEFINER: `create_dispensation_request`/`cancel_dispensation_request` (Track, authz espejo de la RLS: operator+coordinador), `reject_dispensation_request`/`resolve_dispensation` (Pharma operator; FEFO atómico con desempate determinístico `expiry,created_at,lot_number`, un lote por med, sin partición ni override en v1) + índice `idx_med_lots_fefo`. Infra base (`dispensation_requests`→`dispensations` + triggers de stock) verificada con harness antes de construir; la 0050 verificada con harness end-to-end (enforcement + FEFO + RPCs con simulación de JWT). **Aplicada en prod (2026-07-14).** |
| 0051 | `pharma_asignar_medicacion_catalogo_global.sql` | RPC `assign_patient_medication`: el desplegable de "asignar medicación al paciente" pasa a listar el catálogo global (antes, solo lo recibido en `protocol_medications`). Si el medicamento nunca se recibió para el protocolo, devuelve `needs_confirmation` en vez de rechazar; confirmando, asocia (mismo upsert de 0040) y recién ahí asigna. No toca el trigger `check_patient_med_protocol` ni la RLS existentes. Verificada con harness (4 checks + simulación de JWT). **Aplicada en prod (2026-07-15).** |
| 0052 | `pharma_historial_medicacion_paciente.sql` | RPC `historial_medicacion_paciente`: historial de movimientos de `patient_medications` de un enrolamiento (agregar/activar/desactivar/eliminar), leído de `audit_log` —que es solo-gerencia (0006)— sin abrirlo: la función SECURITY DEFINER expone solo esa tajada, ya cruzada con nombre de medicamento y actor, con candado propio (Pharma / gerencia / coordinadora asignada — espejo de la RLS de lectura de `patient_medications`, 0050). Primer lector de `audit_log` de la app. Backend del botón "Historial" de la sección Medicación de la ficha. **Aplicada en prod (2026-07-17).** |

| 0053 | `dispensacion_estado_preparando.sql` | Un solo statement: `request_status` gana el valor `preparando`. Va **sola y primero**, porque Postgres no deja usar un valor de enum recién creado dentro de la misma transacción que lo crea; pegada junto a la 0054 en el dashboard, la migración fallaría entera. **Aplicada en prod (2026-07-18).** |
| 0054 | `dispensacion_flujo_cuatro_estados.sql` | El paso único de `resolve_dispensation` se abre en cuatro estados reales (solicitada → preparando → lista → entregada). Escaneo persistido (`scanned_at`/`scanned_by` en `dispensation_request_items`: la card del tablero muestra el contador fuera del cajón y sin persistir mentiría al recargar) + `prepared_by`/`preparation_started_at` en `dispensation_requests` (evita que dos farmacéuticas preparen lo mismo). `apply_dispensation_stock` reescrita: el descuento se corre de `entregada` a `lista` y gana la rama inversa (`lista`→`en_preparacion` devuelve con movimiento `devolucion`); `entregada` ya no mueve stock. 6 RPCs nuevas (`start_dispensation_preparation`, `scan_dispensation_item`, `unscan_dispensation_item`, `mark_dispensation_ready`, `deliver_dispensation`, `cancel_dispensation_preparation`) + `reject_dispensation_request` ampliada para rechazar también desde `preparando`. **La fila `dispensations` nace recién al marcar lista** y se reusa si se cancela: `correlative_number` es un `serial`, así que crearla antes o borrarla dejaría huecos en la numeración del comprobante (nota fuente ANMAT). FEFO reusado textual de 0050. `resolve_dispensation` queda deprecada por comentario, sin borrar. **Aplicada en prod (2026-07-18).** |

| 0055 | `dispensacion_codigo.sql` | Código legible de la dispensación: `D-{n}-{ddmmyy}-{iniciales}` (ej. `D-1-180726-YM`). Hasta acá la base solo tenía el uuid y el `correlative_number` del comprobante; el tablero necesitaba un identificador dictable. Tabla `dispensation_daily_counters` (correlativo que **reinicia cada día**; upsert atómico con `returning`, que un `serial` no puede hacer) + `dispensations.daily_number`/`dispensation_code` + índice único parcial + helper `user_initials` + trigger de inmutabilidad (espeja el de 0003:270). `mark_dispensation_ready` va **drop + create** porque cambia la firma (Postgres no deja cambiar el tipo de retorno con `create or replace`) y sella el código solo la primera vez: si la fila se reusa tras cancelar, conserva código y correlativo. El código se emite al **marcar lista**, no antes: sus tres componentes (correlativo, fecha, iniciales de quien dispensa) recién existen ahí, así que en Solicitadas/Preparando la card no muestra ninguno en vez de un provisorio que después cambiaría. **Aplicada en prod (2026-07-18).** |

| 0056 | `fix_mark_ready_ambiguo.sql` | **Fix de la 0055**, encontrado en el QA logueado del 2026-07-18: "Marcar lista" fallaba con `column reference "correlative_number" is ambiguous`. En PL/pgSQL los nombres de `returns table (...)` actúan como variables de salida y compiten con las columnas sin calificar; el `returning id, correlative_number` del INSERT era ambiguo. Se califica con `dispensations.`, sin renombrar las columnas de salida (el frontend las lee por nombre). `create or replace` porque la firma no cambia. **Aplicada en prod (2026-07-18).** |

| 0057 | `liberar_codigo_al_cancelar.sql` | **Defecto de dominio encontrado en el QA logueado del 2026-07-18** (camino de cancelación): el código lleva las iniciales de quien dispensa, pero cancelar una preparación ya lista lo dejaba pegado a la solicitud — si otra farmacéutica la tomaba después, el comprobante sellado y firmado salía con las iniciales equivocadas. Ahora cancelar (y rechazar) **libera** código y `daily_number`; `mark_dispensation_ready` ya sellaba solo cuando el código es null, así que vuelve a sellarse con quien realmente dispensa. El trigger de inmutabilidad admite una única excepción: volver el código a NULL con la dispensación en `en_preparacion`. **`correlative_number` se conserva** (numeración legal sin huecos); `daily_number` sí saltea números al cancelar, y es el precio elegido a cambio de no firmar un documento con iniciales ajenas. Incluye limpieza acotada de la fila que dejó el QA. **Aplicada en prod (2026-07-18).** |

| 0058 | `fix_mark_ready_dispensation_id.sql` | **Segundo bug de la misma clase que la 0056**, en otra línea: el `delete from dispensation_items where dispensation_id = ...` chocaba con la variable de salida homónima. Solo se dispara en la rama de REUSO de la fila (cancelar y volver a marcar lista), camino que recién habilitó la 0057 — por eso el primer marcado del QA pasó y el segundo falló. Se agrega alias explícito. Incluye la **auditoría completa** de las tres variables de salida (`dispensation_id`, `correlative_number`, `dispensation_code`) contra cada uso de la función: no queda ninguna otra sin calificar. **Regla para el futuro: toda función con `returns table (...)` hereda este riesgo; calificar siempre los nombres de columna que coincidan con los de salida.** **Aplicada en prod (2026-07-18).** |

| 0059 | `dispensacion_alta_manual_pharma.sql` | **Fase 2 del tablero.** `create_dispensation_request` suma a Pharma operator+ en la authz (no es escalada: Pharma ya veía y resolvía todas las solicitudes, ahora también puede originarlas; el candado de `patient_medications` y el flag `dispenses` siguen intactos). Suma `dispensation_requests.requested_by_module` ('track'\|'pharma'): el cajón mostraba "Coordinación" **hardcodeado** y el comprobante lo derivaba de `source`, que vale `'manual'` para todas las filas desde la 0050 — en cuanto Pharma pudiera crear, esa etiqueta pasaba a ser falsa **en la nota fuente impresa**. Se resuelve con un dato registrado al crear, no con una inferencia. Backfill a `'track'` seguro por construcción (Pharma no podía crear antes). Suma RPC `visitas_dispensables(p_enrollment_id)` para el desplegable del alta manual (va como RPC porque Pharma no tiene RLS de lectura sobre `patient_visits` de todos los protocolos). **Aplicada en prod (2026-07-18).** |

| 0060 | `origen_solicitud_explicito.sql` | **Corrige el enfoque de la 0059.** Ahí el módulo de origen se *infería* de los roles; el QA logueado mostró que un alta hecha desde el botón de Pharma quedaba registrada como "Coordinación" porque el usuario tiene rol de Track además. No es un artefacto del usuario de prueba: en un centro chico es normal tener varios sombreros. **El rol dice qué podés hacer, no desde dónde lo hiciste** — la misma persona hace idéntica llamada desde el panel de Track o desde el alta de Pharma, y ninguna inferencia server-side puede distinguirlas porque la diferencia está en la pantalla, no en el usuario. El origen pasa a ser el parámetro `p_origen` que el cliente declara, **validado** contra los permisos reales (solo se acepta `'pharma'` de quien puede operar en Pharma). Default `'track'` para que la llamada de 3 argumentos del panel de Track siga andando. Va `drop` + `create` porque agregar un parámetro con `create or replace` crearía una sobrecarga en vez de reemplazar. **Aplicada en prod (2026-07-18).** |
| 0061 | `procedimientos_por_visita.sql` | Procedimientos por visita (Schedule of Assessments): catálogo global `procedures` (code/name/category/requires_dispensation) + revive `protocol_activities` (0002, latente) como join visita↔procedimiento (`procedure_id` + orden en `suggested_order` + `unique(visit_def_id, procedure_id)`; guard de tabla vacía antes de endurecer) + auditoría (`audit_row` en `procedures` y `protocol_activities`) + RPC atómico `set_visit_procedures` + seed curado de 16 procedimientos estándar. **Aplicada en prod (2026-07-21).** |
| 0062 | `enrollments_ivrs_code.sql` | Agrega `enrollments.ivrs_code`: N° de sujeto IVRS **por enrollment** (una persona en un pivotal + su extensión LTS a largo plazo tiene dos IVRS; `patients.code` guarda el del estudio madre). Columna nullable + índice único parcial `(protocol_id, ivrs_code) where ivrs_code is not null` (tolerante a legacy: no indexa las filas viejas con NULL). Base de la carga de visitas históricas. **Aplicada en prod (2026-07-21).** |
| 0063 | `checklist_reportes.sql` | ítems de checklist con reporte (`has_report`/`report_eta_hours` en plantilla+materializado) + tabla `checklist_report_ready` (estado "reporte listo" aparte del tilde, auditable) + `materialize_checklist` copia los campos + vista `v_report_alerts` (alerta persistente de reporte pendiente). **Aplicada en prod (2026-07-22).** |
| 0064 | `procedimientos_checklist.sql` | Procedimientos = checklist de la visita: `procedures.has_report`/`report_eta_hours`; tablas `visit_procedure_completions` (realizado) y `visit_procedure_reports_ready` (reporte listo) calcadas de 0063; vista `v_procedure_report_alerts`; `computed_status` (v_patient_visits/v_track_visits recreadas) suma los procedimientos (una visita no pasa a completa hasta que todos estén realizados y sus reportes listos); backfill aditivo que da por hechas las visitas ya realizadas. **Aplicada en prod (2026-07-22).** |
| 0065 | `visita_coordinador.sql` | Coordinador POR VISITA (base del rediseño de "Visitas del día"): `patient_visits.coordinator_id` (FK users, `on delete set null`) + `coordinator_name` (snapshot desnormalizado — la RLS de `users` oculta filas ajenas y v_track_visits es security_invoker, mismo motivo que author_name en 0048); RPC `set_visit_coordinator` (SECURITY DEFINER; authz espejo de `mark_wants_doctor`; valida pertenencia a `protocol_coordinators` y resuelve el nombre); recrea v_patient_visits/v_track_visits (patrón del `*` congelado) para exponer las dos columnas. Aditiva, legacy queda sin asignar. **Aplicada en prod (2026-08-04).** |
| 0066 | `visit_status_nuevos_estados.sql` | Va **sola y primero**: agrega `en_atencion` y `por_reprogramar` al enum `visit_status` (Postgres no deja usar un valor de enum recién creado en la misma transacción que lo crea, mismo motivo 0053) — `en_atencion` es el paciente en el centro hoy sin atención cerrada, `por_reprogramar` es la falta marcada por la 0067 sin fecha nueva. Inerte hasta que la 0068 los emite. **Aplicada en prod (2026-08-05).** |
| 0067 | `visita_no_show.sql` | Marca de "No vino" persistida: `patient_visits.no_show_at`/`no_show_by` (esta última solo auditoría, no se muestra en UI) + RPC `mark_no_show` (SECURITY DEFINER; gerencia o track operator+; rechaza marcar falta sobre una visita ya atendida —`real_date`— o ya recibida —`arrived_at`—, para no dejar "No vino" y "Concurrió al centro" contradictorios) + `mark_arrived` reescrita (mismo authz, create or replace) para limpiar la marca si el paciente termina concurriendo. Aditiva: filas viejas quedan con `no_show_at` null, ninguna visita cambia de estado. No toca las vistas (eso lo hace la 0068, recién después de que la 0066 esté confirmada). **Aplicada en prod (2026-08-05).** |
| 0068 | `estados_visita.sql` | Cierra la tanda 0066→0067→0068: recrea `v_patient_visits`/`v_track_visits` (patrón del `*` congelado) para emitir los dos ejes nuevos. `computed_status` (eje clínico) pasa a 7 estados: suma `en_atencion` y `por_reprogramar`, fusiona `futura` dentro de `proxima` (el valor de enum queda vivo porque Postgres no borra valores). `operational_stage` (eje operativo) pasa de 5 a 4 etapas — `por_llegar → concurrio_al_centro → inicio_atencion → fin_atencion`; `fuera` sale del recorrido porque `mark_left` siempre exigió `ready_at` (0023), así que ninguna fila con salida marcada queda huérfana. `v_track_visits` suma `no_show_at` (0067) para la fila de "Visitas del día". Sin cambio de estado para lo histórico (ninguna visita cargada retroactivamente entra en las ramas nuevas). **⚠️ Breaking para el front vigente** (`VISIT_STATES[v.computed_status]` no tiene entrada para los estados nuevos, revienta en blanco) — se aplica junto con el deploy del front que entiende los dos ejes, no antes. **Aplicada en prod (2026-08-05)** — se aplicó ANTES del deploy, con la ventana de front roto que eso implica; la próxima vez el orden es deploy primero. |
| 0069 | `retirar_checklist.sql` | Retira el checklist clínico del circuito vivo, reemplazado por el cuadro de procedimientos (0061/0064): dropea el trigger `trg_materialize_checklist` y su función (no se materializan más ítems), recrea `v_patient_visits`/`v_track_visits` sin las dos ramas de `computed_status` que miraban `checklist_items` (las de `item_vencido` y `realizada`; el resto es verbatim de la 0068) y dropea la vista `v_report_alerts` (0063), cuya alerta solo se apagaba desde la pantalla del checklist, ya borrada del front. **No borra ni una fila**: las cinco tablas `checklist_*` quedan intactas como histórico auditable. **Sin cambio de estado**: con las precondiciones en 0 (0 ítems obligatorios sin tildar, 0 alertas de reporte, 0 ítems obligatorios en plantillas — verificadas el 2026-08-06) las ramas que se van evalúan falso para todas las filas. **⚠️ El front va primero**: el vigente todavía llama a `useReportAlerts()` y sin la vista pinta error en la campana y en Alertas. **Aplicada en prod (2026-08-06).** |
| 0070 | `descartar_alerta.sql` | Descartar una alerta (archivarla, no borrarla): tabla `alert_dismissals` con motivo de **catálogo** (check, no texto libre), autor desnormalizado (`dismissed_by_name`/`_role`, mismo motivo que `author_name` en 0048) y una **huella** de la condición (`status` + `anchor`) — sin la huella, descartar "ventana vencida" escondería la visita para siempre, incluso si se reprograma y vence una ventana nueva; con ella el descarte caduca solo y la alerta vuelve. RPC `dismiss_alert` (SECURITY DEFINER) calcula la huella en el servidor y rechaza archivar lo que no está en alerta; restaurar = `delete` gobernado por RLS (queda en `audit_log`). **Sin vistas nuevas** a propósito: una vista de alertas vigentes tendría que hacer `select *` sobre `v_track_visits` y quedaría con las columnas congeladas — el filtro lo hace el front con las filas de la tabla, que la RLS ya scopea. **Aditiva y no breaking**: no toca ninguna tabla ni vista existente, así que el front desplegado sigue andando con la migración puesta. **Aplicada en prod (2026-08-06).** |
| 0071 | `dispensacion_ip.sql` | Dispensación de **producto en investigación (IP)** en una visita, en UN solo pedido y UN solo comprobante junto con la medicación concomitante. Flag propio del cronograma `visit_definitions.dispenses_ip` (`dispenses` es otro candado y lo valida el servidor desde la 0050: sin flag aparte, la visita típica de protocolo —IP y ninguna concomitante— diría "no entrega medicación", lo contrario de la verdad) expuesto en `v_track_visits` (recreada, aditiva). El pedido gana `includes_ip` (lo **sella el servidor** copiando el flag al crear, no lo declara el cliente: el cronograma puede cambiar después y el pedido tiene que recordar lo que era cierto cuando se pidió), más `off_schedule`/`off_schedule_reason` con check — no existe dispensación fuera de cronograma sin motivo. **La excepción NO implica IP** (decisión del Director, 2026-08-09): `off_schedule` saltea la validación del cronograma y nada más; fuera de cronograma `includes_ip` nace en falso y se prende recién cuando se adjunta la constancia — si no, una dispensación excepcional de pura concomitante exigiría después una constancia que no existe y descontaría kits fantasma del stock. El pedido gana además `protocol_id` **desnormalizado** (backfilleado en la misma migración, con `trg_requests_updated_at` apagado durante el update para no arrastrar el histórico a la columna "Entregadas" del tablero): `v_ip_stock` es `security_invoker` y la lee Farmacia, que **no tiene policy de select sobre `patient_visits`**, así que llegar al protocolo por la visita devolvería cero filas en silencio y la vista diría "entregados = 0, disponibles = todo" para siempre — mismo patrón que `coordinator_name` (0065) y `author_name` (0048). Tabla `dispensation_ip_documents` para la constancia del IRT: filas **inmutables y sin borrado** (nota fuente), reemplazar inserta una nueva y sella `superseded_at` en la anterior, con índice parcial que garantiza una sola vigente por pedido; se sube al bucket privado `ip-docs` (`{protocol_id}/{request_id}/{uuid}.{ext}`, el protocolo primero para que la policy lo lea del path) y entra solo por `attach_ip_document` (SECURITY DEFINER, que además valida que el path caiga en el protocolo de la visita y acepta el pedido fuera de cronograma prendiéndole `includes_ip`). Las lecturas —tabla y bucket— incluyen **gerencia**, como `ver solicitudes`/`ver dispensaciones`/`ver recepciones` de la 0006: la Dirección tiene que poder auditar la cadena de evidencia. Las policies de `storage.objects` van dentro de un bloque con manejador de `insufficient_privilege`: si el proyecto no deja crearlas por SQL (la tabla es de `supabase_storage_admin`), avisa por NOTICE y el resto de la migración entra igual — se crean a mano desde Storage → Policies. `create_dispensation_request` suma `p_off_schedule_reason` (drop+create: el parámetro nuevo con `create or replace` haría una sobrecarga y PostgREST quedaría entre dos funciones; va con default → **no hay orden de deploy que respetar**) y acepta el pedido **sin renglones** cuando la visita entrega IP. `mark_dispensation_ready` exige la constancia antes de emitir el comprobante; `apply_dispensation_stock` deja de exigir renglones cuando el pedido lleva IP (el candado de "sin renglones" vivía en el trigger, no en la función). `deliver_dispensation` pasa a `(uuid, integer)` y sella `ip_kits` en la entrega — único momento en que el IP sale, porque en el IP no hay lote ni FEFO que reservar antes y `entregada` es el paso irreversible. `v_ip_stock` suma `kits_entregados`/`kits_disponibles` **derivados** (sin tabla de movimientos ni rama de devolución); `total_kits` no cambia de significado. Tres candados más cierran el circuito: `attach_ip_document` **rechaza sumarle IP a un pedido cuyo comprobante ya se emitió** (la dispensación queda en `lista` mientras la solicitud sigue en `preparando` — el status del pedido lo mueve la entrega, no `mark_dispensation_ready` —, así que sin esta guarda el comprobante quedaría fechado ANTES de la nota fuente que lo justifica; y como no hay unique sobre `dispensations.request_id`, un segundo "marcar lista" insertaría otra dispensación con correlativo nuevo y descuento de stock repetido). Solo se frena la rama que prende `includes_ip`: reemplazar la constancia de un pedido que ya era de IP sigue funcionando mientras esté abierto. `mark_dispensation_ready` frena además el pedido **sin renglones y sin constancia** con un mensaje que nombra la constancia, en vez de dejar que lo frene el trigger de stock hablando de renglones (que manda a buscar donde no es). Y un trigger nuevo `trg_seal_request_protocol` (`before insert`, SECURITY DEFINER) **sella `protocol_id` desde la visita cuando el insert no lo trae**: la columna desnormalizada no puede depender de que todos los caminos de escritura se acuerden de llenarla — la policy de Track permite insertar sin pasar por el RPC, y una sola fila con el protocolo en null desaparece en silencio de `v_ip_stock` y sobrestima el disponible. Sigue el principio de la 0038 (Director Médico): la trazabilidad por kit la provee el sponsor/IRT, Spira no la duplica. **Requiere crear antes el bucket privado `ip-docs`** (10 MB, PDF/imagen) — las policies no fallan sin él, fallan las subidas. Aditiva y no breaking. Idempotente (las dos funciones que cambian de firma se dropean con la firma vieja **y** con la nueva). **Aplicada en prod (2026-08-10).** |
| 0072 | `agregar_renglones_a_pedido.sql` | RPC `add_dispensation_items(p_request_id, p_items)`: agrega renglones de medicación a un pedido **ya existente**. Cierra el hueco que dejaba la 0071 en la decisión de "**un solo pedido por visita**": adjuntar la constancia reusaba el pedido abierto, pero agregar medicación solo tenía `create_dispensation_request`, que siempre crea uno nuevo — así que en el orden constancia → medicación (el natural en una visita de protocolo: el IP lo manda el cronograma, la concomitante se acuerda después con el paciente ya en el centro) la visita terminaba con **dos pedidos, dos tarjetas en el tablero de Farmacia y dos comprobantes** para el mismo hecho. Authz y validación de ítems **verbatim** de `create_dispensation_request` (gerencia / track admin / track operator que coordina la visita, o pharma operator; cada ítem con medicamento y cantidad > 0; forma de `p_items` chequeada antes del cast), con dos diferencias deliberadas: la lista **vacía sí es error** (esta función existe para agregar algo, y un "agregué cero" exitoso sería una mentira), y el candado del cronograma se revalida para que no quede el desvío de crear un pedido de IP solo en una visita que no entrega concomitante y llenarlo de renglones por esta puerta. Solo con el pedido **abierto** (`solicitada`), con `for update` sobre la solicitud para cerrar la carrera con `start_dispensation_preparation`: desde `preparando` el cajón ya está armado —y en la ventana `lista` el comprobante ya salió con su correlativo—, así que sumar un renglón dejaría medicación pedida por fuera del papel impreso; el mensaje nombra la salida real (que Farmacia cancele la preparación), no el estado interno. El trigger `check_request_item_protocol` sigue validando que el medicamento esté habilitado y activo para el paciente: no se duplica. **No toca datos**: sin backfill y sin DDL sobre tablas ni vistas. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-10).** |
| 0073 | `sellar_ip_kits_entregada.sql` | Sella `ip_kits` y `delivered_at` en `guard_dispensation_immutable` (0003:268) una vez que la dispensación está **entregada**. La 0071 le dio a `ip_kits` dos trabajos —sale impreso en el comprobante y `v_ip_stock` deriva de él los kits disponibles del protocolo— y la UI le promete a la farmacéutica que el número "no se puede corregir después", al punto de interrumpirle la entrega con un pop-up para que lo declare bien la primera vez. La base no lo sostenía: la policy de `dispensations` habilita el update a todo pharma (0006:287) y el guard protegía solo `executed_by`/`request_id`/`correlative_number` y la reversión de estado, así que un PATCH directo a PostgREST —sin pasar por ninguna función— corregía los kits de una entrega cerrada y **movía el stock de IP** sin dejar rastro de que el papel archivado dice otra cosa. No rompe el camino legítimo: `deliver_dispensation` escribe `ip_kits` mientras la dispensación todavía está en `lista` y recién después la pasa a `entregada`. Corregir un error real sigue siendo posible por donde corresponde (gerencia, que ya tiene el delete). Sin DDL sobre tablas ni vistas, sin backfill, sin cambio de firma. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-11).** |
| 0074 | `track_lee_nombres_medicamentos.sql` | Suma **track** al SELECT de `medications` (0006) y `drugs` (0032). Track ya leía `patient_medications` (0050 habilita al coordinador del protocolo) y las solicitudes con sus renglones (0006), pero el embed del medicamento le volvía **NULL**: donde tiene que decir "Alvetide 92/22 mcg" la coordinadora leía "Medicamento" — en la ficha del paciente, en los renglones del pedido, en el historial y en el aviso de dispensación reciente. Agujero viejo y silencioso (se detectó al portar la dispensación como "Fase 3 pendiente") que se arrastraba porque **el usuario de QA tiene los cinco módulos y no lo reproduce**; se confirmó leyendo la policy, no la pantalla. Se abre lo MÍNIMO: solo el SELECT y solo del catálogo — ni stock, ni lotes, ni recepciones, ni códigos; las políticas de escritura quedan intactas (Track no da de alta ni edita el catálogo). El catálogo no tiene datos de paciente. No toca datos, no cambia firmas ni vistas, y el front ya degrada a "Medicamento" cuando el embed viene null: funciona igual antes y después, mejor después. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-11).** |
| 0075 | `dispensacion_unidades_y_constancia_impresa.sql` | El escaneo pasa a contar **unidades** y no renglones: `dispensation_request_items.scanned_units`, una pasada del lector = una unidad. Hasta acá un renglón de "Ibuprofeno × 3 u." se daba por confirmado con **una sola** caja pasada por el lector, y las otras dos salían del mostrador sin que nada las hubiera verificado. `scanned_at` **no se borra**: deja de ser el conteo y queda como rastro de la última pasada, con `scanned_by`. El backfill (`scanned_units = quantity where scanned_at is not null`) es fiel a lo que el dato viejo significaba y deja las preparaciones **en vuelo** con su progreso intacto. Lo sostiene una constraint que fija el invariante `scanned_units > 0 ⟺ scanned_at is not null`: sin ella, cancelar una preparación limpiaba `scanned_at` y dejaba el conteo intacto, así que el pedido volvía a la cola mostrando "6/6 escaneado" sobre medicación que nadie había vuelto a tocar — por eso `cancel_dispensation_preparation` y `reject_dispensation_request` se reescriben **enteras desde la 0057** (son las dos que limpian `scanned_at`; sin tocarlas la constraint les tiraría 23514 en el primer cancelar). `scan_dispensation_item` incrementa **sobre la columna** (`scanned_units + 1`), nunca sobre un valor leído en el cliente, y toma `for update` en el select del renglón pendiente: dos farmacéuticas escaneando el mismo pedido se serializan y a la segunda el re-chequeo de Postgres le filtra el renglón ya completo, en vez de perder pasadas en silencio. `remaining` pasa a contar unidades — el front viejo solo pregunta si es 0, y cero sigue queriendo decir "no falta nada", así que **no lo rompe**. `unscan_dispensation_item` gana `p_unidades` con default (null = volver a cero, que es el botón "Cancelar" del handoff) y **se dropea antes la firma de un argumento**: agregar un parámetro cambia la firma, `create or replace` habría dejado una sobrecarga y Postgres resolvería la llamada de un argumento con la vieja, que es justo la que no pone el conteo en cero. Además la constancia del IRT gana `printed_at`/`printed_by` + RPC `mark_ip_document_printed` (idempotente; rechaza marcar una constancia ya reemplazada, que satisfaría el bloqueo con el papel equivocado), y `mark_dispensation_ready` —reescrita desde su ÚLTIMA versión, la de la **0071**, no la de la 0054: pasó por 0056 y 0058 y partir de una vieja habría borrado esos arreglos— suma el candado de constancia impresa junto al de constancia existente. **`printed_at` NO afirma que la impresora imprimió**: ningún navegador puede saberlo (`afterprint` dispara igual si se canceló), así que la columna, su comentario y el `audit_log` dicen "marcó como impresa" — es una aserción de la persona, no una observación del sistema. **Orden invertido a propósito: esta migración va ANTES del deploy del front**, al revés de la regla general de `CLAUDE.md` §3 — es aditiva y el front viejo no pide ninguna columna nueva ni llama a las RPCs nuevas, así que el único que no funciona sin ella es el front NUEVO. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-13).** |
| 0076 | `dispensacion_sustituir_renglon.sql` | Sustituir un renglón por otra presentación del **mismo fármaco**, sin cancelar la preparación entera. **Atraviesa el choque con el doble enforcement de la 0050**: el handoff ofrece "mismo fármaco + hay stock", pero el trigger `check_request_item_protocol` exige el medicamento asignado al **protocolo** y habilitado y ACTIVO para **ese paciente** — y `patient_medications` suele tener UNA presentación por droga y paciente, así que la alternativa del handoff era justo la que la base rechazaba. Resolución (decisión del Director): **el candado no se afloja**; `substitute_dispensation_item` habilita la alternativa en `patient_medications` dentro de la MISMA transacción (upsert que también REACTIVA una dada de baja, sin pisarle la nota original), con su motivo, y las dos escrituras caen juntas en el `audit_log`. Sigue siendo imposible dispensar algo no habilitado: lo que cambia es que habilitar pasa a ser un acto explícito y auditado, hecho por quien la RLS ya autoriza (policy "pharma asigna medicación", 0050:149). Cuatro reglas de equivalencia, escritas UNA vez y leídas por las dos funciones: mismo `drug_id` (otra droga no es una sustitución, es otra receta), **misma `dosis`** (otra concentración requiere autorización del IP), asignado al protocolo (si no lo frena `check_patient_med_protocol`) y con stock no vencido (ofrecer algo que el FEFO no va a encontrar es un callejón). El renglón gana `substituted_from_medication_id` + `substitution_reason` + `substituted_at`/`_by`: sin ese rastro la sustitución sería **invisible** —la fila mostraría el medicamento nuevo como si hubiera sido el pedido desde el principio— y es lo que enciende el "· sustituido" de la UI. `substituted_from` conserva el **original** entre sustituciones sucesivas (`coalesce`), no el anterior. El conteo vuelve a **cero** junto con `scanned_at`: sin eso, las unidades ya escaneadas quedarían contadas contra un producto que ya no es ese —el dial diría 2/3 sobre medicación que nunca pasó por el lector— y además violaría el invariante de la 0075. Solo con el pedido en `preparando`: desde `lista` el comprobante ya salió con su correlativo y el stock ya se descontó del lote viejo, así que el mensaje nombra la salida real (cancelar la preparación). `alternativas_sustitucion` lista también las **bloqueadas** con su motivo en vez de esconderlas: un equivalente que está ahí y no aparece manda a buscar el problema donde no está. ⚠️ **Se declaró "aditiva y no breaking" y NO LO ERA.** La FK nueva a `medications` dejó a `dispensation_request_items` con DOS relaciones hacia esa tabla, y eso volvió **ambiguo** el embed `medication:medications(...)` que el front YA DESPLEGADO venía pidiendo: PostgREST responde `300/PGRST201` y **voltea la consulta entera**, no solo el embed. Tiró el tablero de Farmacia en producción el 2026-08-13, sin que ninguna columna vieja hubiera cambiado. Ver la **0078** para el arreglo y para la regla que queda (agregar una FK a una tabla YA EMBEBIDA en un select del front es breaking). Idempotente. **Aplicada en prod (2026-08-13).** |
| 0077 | `dispensacion_reasignar_e_historial.sql` | Las dos entradas que le faltaban al menú ⋯ del cajón: el handoff lo anuncia como "Rechazar, reasignar, historial" y solo la primera existía. **Reasignar** (`reassign_dispensation_preparation`) mueve `prepared_by` **sin tocar ni un escaneo**: hasta ahora un cambio de turno se resolvía cancelando (vuelve a Solicitadas, se pierden los escaneos y se libera el código) y volviendo a tomar, o sea tirando trabajo hecho. Solo en `preparando`: desde `lista` el comprobante ya salió sellado con las iniciales de quien lo preparó (`dispensation_code`, 0055), y cambiar de responsable después dejaría el papel impreso nombrando a una persona y la base a otra. Valida que quien recibe TENGA rol de pharma operator+ — sin ese control el pedido quedaría en manos de alguien que no lo puede ni abrir, figurando en el tablero como trabajado por quien no lo va a tocar. `farmaceuticas_disponibles` alimenta el desplegable por función y no por select: `users` y `user_module_roles` no son legibles en bloque para pharma, y abrirlos solo para dibujar un desplegable daría bastante más de lo que el desplegable necesita. **Historial** (`dispensation_audit_trail`) abre el `audit_log` acotado a ESE pedido y su cadena (renglones, dispensación, constancias) — nunca la tabla entera, que tiene datos de todos los módulos y sería una puerta lateral a lo que la RLS protege. Lo lee pharma **y gerencia**; Coordinación no (ve el estado de su pedido en Track, pero el detalle de quién preparó y con qué correcciones es operación de farmacia). Existe porque la única lectura del audit_log era la de gerencia (`historial_medicacion`, 0052) y la farmacéutica que se topa con un pedido "raro" —volvió a la cola, tiene un renglón que no pidió, el contador en cero— no tenía más forma de saber por qué que preguntar. **Ojo con los nombres de `audit_log` (0002:364): NO son los de manual** — `entity_type`/`entity_id` (no table_name/row_id), `occurred_at` (no created_at), `before_data`/`after_data` (no diff) y el autor es `actor_id` (uuid, sin nombre desnormalizado: hay que joinear `users`). Sin DDL sobre tablas, sin backfill. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-13).** |
| 0078 | `restaurar_fk_sustitucion.sql` | **Arregla el incidente del 2026-08-13.** La 0076 se declaró "aditiva y no breaking" y NO lo era: agregó `substituted_from_medication_id` con FK a `medications`, así que `dispensation_request_items` pasó a tener **DOS** claves foráneas a esa tabla y el embed `medication:medications(...)` —que el front YA DESPLEGADO venía pidiendo— quedó **ambiguo**. PostgREST responde `300 / PGRST201` y **voltea la consulta entera, no solo el embed**: el tablero de Farmacia quedó en "No pudimos cargar el tablero" apenas se aplicó, sin que ninguna columna vieja hubiera cambiado. Se restauró en el momento dropeando la constraint (una sentencia, sin deploy) y esta migración la **repone** con `on delete restrict`, para que el rastro de la sustitución no quede colgado: el medicamento sustituido deja de estar referenciado por `medication_id` —ese renglón ahora apunta al nuevo— así que sin la FK volvería a ser borrable. **⚠️ Aplicar SOLO DESPUÉS de desplegar el front que pide `medications!medication_id`**; antes de eso vuelve a tirar el tablero. Si el front nuevo ya está desplegado y la constraint nunca se dropeó, es un no-op (idempotente). **LA LECCIÓN, que vale para toda migración futura: agregar una FK a una tabla que ya está EMBEBIDA en algún `select` del front ES BREAKING**, aunque sea puramente aditiva en el schema — la base pasa a ofrecer dos caminos donde había uno y el código viejo no sabe elegir. Antes de agregar una FK, buscar la tabla en los `select(...)` del front; si aparece embebida, el front va primero. **Aplicada en prod (2026-08-13).** |
| 0079 | `visita_medico_y_fertilidad.sql` | Las dos columnas que le faltaban al encabezado de la visita (`docs/handoff-visitas-encabezado/`). **Médico a cargo POR VISITA** (`patient_visits.treating_physician`, texto libre, nullable): hasta acá el médico era un campo del PACIENTE (movido a `patients` por la 0020), así que cambiarlo desde una visita de agosto reescribía hacia atrás quién figuraba en las visitas de marzo — y el candado que el handoff le pone a la visita concretada no protegía nada, porque el mismo dato se editaba desde cualquier otra visita del paciente. `v_track_visits` devuelve ahora `coalesce(visita, paciente)`: **mientras la columna esté vacía el resultado es idéntico al de la 0071, byte a byte**, y por eso no hay backfill (inventarle a cada visita vieja el médico actual del paciente sería afirmar algo que nadie registró). El RPC `set_visit_physician` espeja la authz de `set_visit_coordinator` (0065: gerencia / track-admin / operator asignado) y suma un candado que aquella no tiene: **rechaza si la visita ya está concretada** (`ready_at` no nulo, la misma condición con la que la vista deriva `fin_atencion`), porque un dato que se congela es lo único que hace confiable la lectura hacia atrás; corregirlo después es tarea de soporte desde el dashboard. A `set_visit_coordinator` **no** se le agrega el chequeo: cambiarle el comportamiento a una función en producción excede este trabajo, así que por ahora el candado del coordinador es solo de pantalla y el del médico es de los dos lados. Segunda columna: **`pa.fertility` en `v_track_visits`** — sexo, nacimiento y edad ya llegaban en esta consulta desde la 0049 y la fertilidad era la única de las cuatro que obligaba a una segunda consulta al paciente; como el encabezado nuevo las dibuja juntas en una rejilla, esa demora recomponía el encabezado a la vista del usuario en cada apertura y en cada salto con las flechas. Las dos vistas se recrean por el **patrón del asterisco congelado** (0047/0064/0065): `treating_physician` es columna de `patient_visits`, así que `v_patient_visits` (`pv.*`) tiene que re-expandirse, y `v_track_visits` depende de ella. `v_patient_visits` se copia VERBATIM de la 0069 y `v_track_visits` VERBATIM de la 0071: ni el estado clínico ni la etapa operativa cambian acá. **Orden invertido, como en la 0075: esta migración va ANTES del deploy del front.** Es aditiva, el front desplegado hace `select('*')` e ignora columnas nuevas, y el `coalesce` no cambia ningún valor existente — el único que no funciona sin ella es el front NUEVO. **No agrega ninguna FK**, así que no puede repetir el `PGRST201` de la 0076. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-13).** |
| 0080 | `escaneo_producto_ajeno.sql` | El error del lector deja de mentir cuando la caja no es de este pedido. Reportado desde el mostrador el **2026-08-15**: un pedido con Alvetide 92/22 mcg completo y Symbicort Forte pendiente recibe la pasada de un **Alvetide 184/22 mcg** —otra concentración, ajena al pedido— y respondía *"Alvetide 184/22 mcg ya tiene sus unidades escaneadas"*. Es falso: ese producto no se escaneó nunca ahí, ni figura en el pedido, y la pregunta que deja —"¿dónde lo escaneé?"— manda a buscar un registro inexistente cuando la respuesta correcta era "agarraste la caja equivocada". La 0075 buscaba un renglón PENDIENTE de ese medicamento y, al no encontrarlo, asumía **una sola** explicación; hay **tres**, y en una farmacia de investigación son bien distintas: el renglón existe y está completo (pasaste dos veces la misma caja), el producto **no está en el pedido** (es de otro paciente o protocolo), o **estaba y se sustituyó** (0076: tenés en la mano la caja que el pedido ya no pide). Ahora cada una tiene su mensaje, y los tres nombran a continuación qué SÍ falta escanear (hasta tres nombres; con más el error se vuelve un párrafo y deja de leerse mientras se pasan cajas). Se reescribe ENTERA desde la 0075 —su última versión— y lo único que cambia es la rama del `if not found`: el incremento atómico sobre la columna, el `for update` del renglón y el `remaining` sobre unidades se conservan verbatim. **Firma idéntica `(uuid, text)`**, así que `create or replace` reemplaza de verdad y no deja la sobrecarga que documenta la 0075 §5. **Orden de despliegue indistinto**: no cambia firma, ni tipo de retorno, ni el contrato de `remaining` —el front solo pregunta si es 0—; solo redacta mejor un error que el front ya muestra tal cual viene, así que funciona igual antes y después. **No agrega ninguna FK**, así que no puede repetir el `PGRST201` de la 0076. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-15).** |
| 0081 | `escaneo_mensajes_secos.sql` | El error del lector habla **solo de la caja que se pasó**. La 0080 arregló el mensaje que mentía pero lo dejó largo — *"Alvetide 184/22 mcg no figura en este pedido. Revisá la caja. Falta Alvetide 92/22 mcg · Symbicort Forte 320/9 mcg"*—, y sobraban dos cosas: **"Revisá la caja"** es una instrucción obvia (quien tiene el lector en la mano ya sabe qué hacer cuando algo no entra; el sistema dice el hecho, no da indicaciones) y **"Falta X · Y"** ya está en pantalla, a la izquierda, en el riel del proceso y con contador por renglón — repetirlo adentro del error era decir lo mismo dos veces y encima empujaba el hecho importante ("esta caja no es de acá") a un segundo renglón. Quedan cuatro frases secas: `% no figura en este pedido`, `% fue sustituido en este pedido`, `% ya tiene sus unidades escaneadas` y `Este pedido ya tiene todas sus unidades escaneadas`. Al no depender ninguna de lo pendiente, se va también la consulta que juntaba esos nombres (`string_agg` sobre los renglones): menos SQL en el camino más caliente de la pantalla. Se reescribe ENTERA desde la 0080, con **firma idéntica `(uuid, text)`** —`create or replace` reemplaza de verdad, sin sobrecarga—. **Orden de despliegue indistinto**: solo cambia el texto de un error que el front muestra tal cual viene. Aditiva y no breaking. Idempotente. **Aplicada en prod (2026-08-15).** |
| 0082 | `reportes_enrollment_id.sql` | El enrolamiento del pedido, **desnormalizado en la fila** (`dispensation_requests.enrollment_id`) + backfill con el trigger de `updated_at` apagado + trigger `seal_request_scope` (SECURITY DEFINER) que ahora sella enrolamiento **y** protocolo y reemplaza a `seal_request_protocol` (0071). Existe porque Farmacia **no tiene policy de select sobre `patient_visits`** (0006:162): una vista `security_invoker` que joinee por ahí no da error, la RLS filtra en silencio y devuelve cero filas. Es el pre-requisito de Reportes. Aditiva (la columna es nullable y ningún front vieja la lee): va ANTES del deploy del front. **Aplicada en prod (2026-08-16).** |
| 0083 | `reportes_vistas.sql` | Las vistas de **Farmacia › Reportes**: `v_pharma_report_items` (una fila por dispensación entregada × medicamento; unidades desde `stock_movements`, el libro insert-only), `v_pharma_report_receptions`, `v_pharma_report_rejected` (cuenta PEDIDOS, no unidades: los renglones se borran al cancelar) y `v_pharma_report_expired` (corte al día de hoy), más un índice parcial sobre `stock_movements(reference_id)`. Ninguna joinea `patient_visits`. Kits y unidades viajan en columnas separadas y NUNCA se suman. Aditiva. **Aplicada en prod (2026-08-16).** |
| 0084 | `reportes_visita.sql` | El **código de la visita** en el detalle de Reportes (pedido del Director). Tercera vez que aparece el mismo muro —Farmacia no puede leer `patient_visits` (0006:162)— y se resuelve igual: `dispensation_requests.visit_code` desnormalizado, con backfill y `seal_request_scope` ampliado a sellar las tres columnas. Es un SNAPSHOT a propósito: el cuadro de actividades se puede editar y el reporte de un período tiene que seguir diciendo lo que decía entonces. Recrea `v_pharma_report_items` con la columna nueva. Aditiva. **Aplicada en prod (2026-08-16).** |

| 0085 | `recepcion_folio_y_verificador.sql` | Las dos columnas que el reskin de **Recepción** (handoff "2c") muestra y no existían: `folio` (entero correlativo con secuencia + backfill por `created_at`, porque `reception_date` se repite y es retroactivo) y `verified_by_name`. **Cuarta vez** del muro de `users` —la policy es `id = auth.uid() or has_module('gerencia')` (0006:82), así que la farmacéutica vería su nombre y NULL en lo que verificó otra persona—; se resuelve como las tres anteriores, desnormalizando. Lo escribe `verify_reception`, que ya era SECURITY DEFINER y mantiene su firma. Aditiva y **sin FK nueva** (no toca ningún embed): va ANTES del deploy del front. **Aplicada en prod (2026-08-17).** |
| 0086 | `reception_anulada_enum.sql` | Los dos valores de enum que la 0087 usa: `reception_status.anulada` y `stock_movement_type.anulacion_recepcion`. Archivo aparte **a propósito**: un valor recién creado con `ALTER TYPE ... ADD VALUE` no se puede usar en la misma transacción (trampa de la 0053). Aditiva, va ANTES del front. **Aplicada en prod (2026-08-18).** |
| 0087 | `anular_recepcion.sql` | **Anular una recepción** (`void_reception`, pharma leader+): las cuatro columnas de la anulación (`voided_at/by/by_name/reason`, el nombre desnormalizado por quinta vez del muro de `users`), el check de que una anulada siempre tiene motivo, y la RPC con sus tres ramas — pendiente sella; verificada de base valida en dos pasadas, resta los lotes y escribe el compensatorio `anulacion_recepcion`; verificada de IP sólo cambia de estado, porque su stock lo deriva `v_ip_stock` y no hay libro donde compensar. Bloquea si ya se dispensó. El lote se ubica por `(medication_id, protocol_id, lot_number)` con `is not distinct from` — la clave real desde la 0032, más la parcial de ambulatoria (`protocol_id is null`) de la 0035; buscarlo sin el protocolo podía restarle stock a OTRO protocolo con el mismo lote de fábrica. Trae además el guard `trg_guard_reception_void`, que impide marcar `anulada` por PATCH directo a PostgREST: la policy de la tabla es `for all` con `operator+`, así que sin él la RPC entera —permiso `leader+` y reversión de stock— era evitable. Aditiva, va ANTES del front. **Aplicada en prod (2026-08-18).** |
| 0088 | `anular_recepcion_guard.sql` | Cierra cuatro cosas que un review encontró sobre la 0087 (ya en prod, inmutable): el guard `guard_reception_void` era de una sola mano —bloqueaba la ENTRADA a `anulada` pero no la SALIDA, y un PATCH directo a `verificada` sobre una anulada reingresaba el stock por `trg_apply_reception_stock`—, así que suma dos cláusulas: ninguna anulada puede salir de ese estado, y con la fila ya anulada `void_reason`/`voided_at`/`voided_by` quedan sellados. Además, en `void_reception`: el caso «el lote ya no existe» pasa de `foreign_key_violation` a `check_violation`, que es el que `pharmaErrorMessage` deja pasar tal cual (23503 daba un genérico que se comía el número de lote); y los dos mensajes de bloqueo dejan de afirmar «ya se dispensaron» —la validación mide cuánto queda, no por qué falta: puede ser un `adjust_stock`, otra recepción anulada antes, o en IP, kits de otra recepción del protocolo—. `create or replace` sobre las dos funciones, firmas idénticas a la 0087. **Aplicada en prod (2026-08-18).** |
| 0089 | `procedimientos_del_estudio.sql` | **Procedimientos del estudio + definiciones de reporte** (fase 1 de `docs/plan-cronograma-reportes.md`). Un procedimiento genera VARIOS reportes, cada uno en su plataforma y con su plazo: el flag binario `procedures.has_report` (0064) no alcanza. Trae `protocol_procedures` ("este estudio usa este procedimiento" — no existía: `protocol_activities` exige `visit_def_id` NOT NULL desde la 0061, así que no había dónde anotarlo sin meterlo antes en una visita) y `report_definitions` colgando de ahí, porque el MISMO procedimiento del catálogo global lleva reportes distintos en cada protocolo. Suma `procedures.min_estimated`. `platform` es texto con check y no enum, por el precedente de la 0070 (un enum nuevo obliga a `alter type ... add value` en su propio archivo, la trampa de la 0053); `eta_hours` NO reusa el check cerrado de la 0064 (24/48/72/168/336/720) sino un rango 1..8760, porque el handoff pide chips de 1h y valor libre. Dos RPCs: `set_procedure_reports` reemplaza el set entero de una (es lo que hace que el "Cancelar" del modal cancele de verdad) y `remove_protocol_procedure` bloquea si el procedimiento está en alguna visita del cronograma — con guard en la RPC y NO con una FK compuesta, que crearía un segundo camino `protocol_activities`→`procedures` y dejaría el embed ambiguo (PGRST201, la lección de la 0076). Backfill idempotente: una definición por cada procedimiento con `has_report`, plataforma `otro` (no sabemos cuál era, e inventarla sería dato falso). **NO toca `has_report` ni las vistas**: eso es la 0091. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-23).** |
| 0090 | `estado_de_reportes.sql` | **Estado de los reportes de una visita** (fase 2 de `docs/plan-cronograma-reportes.md`). `report_status` (pendiente/descargado/evolucionado; SIN fila = pendiente, lo resuelve el coalesce de la vista) y `report_status_history`, que escribe un TRIGGER y nunca el cliente: con dos escrituras desde el front, estado e historial divergen el día que una falla, y un historial que puede divergir deja de ser prueba. Las dos tablas son de SOLO LECTURA para `authenticated` — la única puerta de escritura es `set_report_stage`, que sella el autor con `auth.uid()`; sin eso un PATCH directo a PostgREST evitaba la verificación de permiso (la lección del guard de la 0088). Suma `guard_uncomplete_with_reports`: destildar "realizado" con reportes ya avanzados borraría su historial en cascada, así que se bloquea diciendo cuántos son. La vista `v_protocol_report_status` arranca de lo ASIGNADO (`protocol_activities`) y NO de lo realizado, con las completions por LEFT JOIN: si sólo emitiera reportes de procedimientos ya hechos, una visita con un procedimiento pendiente se vería con todos sus reportes evolucionados y se cerraría sola con trabajo adentro. `updated_by_name`/`changed_by_name` van desnormalizados porque la RLS de `users` (0006:82) sólo deja ver el perfil propio y joinear esa tabla habría devuelto NULL para todos los demás, en silencio. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-23).** |
| 0091 | `set_visit_procedures_upsert_estudio.sql` | **Asignar un procedimiento a una visita lo suma al estudio.** Cierra un hueco de la 0089: había DOS caminos para meter un procedimiento en un protocolo y sólo uno escribía `protocol_procedures` — Cronograma › Visitas escribía únicamente `protocol_activities`. Como los reportes cuelgan de `protocol_procedures`, lo sumado por ese camino quedaba EN la visita pero FUERA del estudio: sin reportes posibles y sin una sola fila en `v_protocol_report_status` (0090), o sea invisible y en silencio. `create or replace` de `set_visit_procedures` con la MISMA firma (distinta habría dejado una sobrecarga viva resolviendo las llamadas viejas con la función vieja, la trampa conocida), agregando un upsert a `protocol_procedures` antes de asignar; `auth.uid()` sigue sellando al que llama aunque sea SECURITY DEFINER. Sólo AGREGA: sacar el procedimiento de todas las visitas no lo saca del estudio, para que sus definiciones de reporte sobrevivan a un cambio de cronograma. Trae backfill idempotente de los huérfanos que hayan entrado entre la 0089 y hoy. Aditiva y compatible hacia atrás → puede ir sola. **OJO con la numeración:** el plan reservaba la 0091 para la fase 3 (retirar `has_report`); se la quedó este arreglo por ser aditivo, y la fase 3 pasa a la **0092**. **Aplicada en prod (2026-08-23).** |
| 0092 | `retirar_has_report.sql` | **Retirar `has_report`** (fase 3 y última de `docs/plan-cronograma-reportes.md`, ejecutada según `docs/plan-fase3-reportes.md`). El flag binario `procedures.has_report` + `report_eta_hours` (0064) dejan de existir: qué reportes lleva un procedimiento lo dice `report_definitions` (0089) y en qué anda cada uno, `report_status` (0090). `v_patient_visits.computed_status` deriva ahora `item_vencido` y `realizada` de ese cruce, con las MISMAS reglas que las funciones puras testeadas de `src/views/track/reportes/estados.ts` (`isOverdue` / `visitClosed`); arranca de lo ASIGNADO y no de las completions, por el mismo motivo que la 0090. `v_track_visits` se recrea verbatim (cae en cascada). `v_procedure_report_alerts` pasa a emitir una fila por REPORTE vencido y no por procedimiento — antes, un procedimiento que debía dos reportes en dos portales era una sola alerta y descartarla apagaba los dos. La identidad es el par `(visit_id, report_definition_id)` y NO `report_status_id`: por el "sin fila = pendiente" de la 0090, justo los reportes en alerta son los que llegan sin fila. `alert_dismissals` suma `report_definition_id` (la vieja `completion_id` queda como histórico, no se borra) y expande los descartes previos recalculando el ancla — hoy es un no-op, había cero. `dismiss_alert` va **drop + create** y no `create or replace`, porque Postgres no deja renombrar un parámetro de entrada; misma lista de tipos, así que no queda sobrecarga viva. `visit_procedure_reports_ready` **no se dropea**: sus 19 filas son registro de lo que alguien marcó, y sus dos procedimientos no definen reportes hoy, así que no hay nada que migrar (mismo criterio que la 0069 con el checklist). **BREAKING para el front viejo → el deploy va PRIMERO y la migración inmediatamente después** (la lección de la 0068) — **se aplicó ANTES del deploy el 2026-08-23**, y eso dejó rotos el catálogo del Cronograma y el panel Procedimientos de cada visita hasta que entró el front. **Aplicada en prod (2026-08-23).** |
| 0093 | `user_preferences.sql` | **Preferencias de interfaz por usuario** (`docs/plan-ajustes-funcional.md`, PR-1). El tema se guardaba en localStorage y "funcionaba", pero eso lo hacía una preferencia de la MÁQUINA: en una clínica donde dos coordinadoras comparten computadora, la que entra segunda hereda el tema de la primera. Ahora la verdad es la cuenta y localStorage queda como CACHÉ (pinta el primer frame sin parpadeo, antes de que haya sesión). Tres columnas TIPADAS con check y no un `jsonb`: cada preferencia tiene un conjunto cerrado de valores y la base los hace cumplir — un jsonb sin esquema es texto libre disfrazado, lo contrario de la regla de desplegables del proyecto. El costo aceptado es que cada preferencia futura sea una migración. Sin RPC: no hay autorización que resolver server-side (cada quien escribe SU fila y nada más), y eso lo dice la RLS sola con un `for all using (user_id = auth.uid())`. Los defaults son el comportamiento vigente, así que aplicarla no le cambia la app a nadie. **Sin excepción de gerencia a propósito**: el tema de otra persona no es información que un administrador necesite. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0094 | `guard_cambio_correo.sql` | **Consultar la ventana de cambio de correo, sin sellarla** (`docs/plan-ajustes-funcional.md`, PR-1). Arregla un hueco de la 0045: `stamp_email_change()` valida la regla de 1 cambio/30 días y la SELLA en la misma llamada, y como el sello tiene que correr DESPUÉS de disparar el cambio en Auth (para no quemar la ventana si el correo es inválido o ya está en uso), el front terminaba llamándola al final y **descartando su error** — con lo cual el `raise` no llegaba a ningún lado y la regla no se aplicaba nunca. Lo único que frenaba era el `disabled` del input, o sea nada para quien abra la consola del navegador. `email_change_locked_until()` sólo PREGUNTA (es `stable`, no escribe), así se la llama antes sin consumir nada; `stamp_email_change` queda igual que en 0045 y sigue siendo la guarda dura ante una carrera entre dos pestañas. Función NUEVA y no `create or replace` sobre la vieja: cambiarle el tipo de retorno a una función existente falla, y cambiarle la firma dejaría una sobrecarga viva resolviendo las llamadas viejas con el cuerpo viejo, en silencio (la trampa conocida de la 0091). Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0095 | `email_en_users.sql` | **El correo, al alcance de la RLS** (`docs/plan-ajustes-funcional.md`, PR-2). La consola de accesos necesita mostrar el correo de cada persona y el correo NO está en `public.users`: vive en `auth.users`, que `authenticated` no puede leer. Se COPIA la columna en vez de joinear con una vista privilegiada, porque una vista o función SECURITY DEFINER **saltea la RLS** y el permiso habría que chequearlo a mano adentro — el día que ese `if` se caiga en un refactor, cualquier autenticado lee el correo de todo el centro. Copiándolo, la policy que ya existe desde la 0006 (`id = auth.uid() or has_module(gerencia)`) protege el dato sola: el guard pasa de recordado a estructural. Es el mismo patrón que Farmacia ya usa (desnormalizar en vez de joinear, por RLS). **SIN TRIGGER SOBRE `auth.users`**: es el único esquema que no es nuestro y un error ahí no rompe una pantalla sino el ingreso de todo el centro. Se mantiene al día por dos caminos de bajo riesgo: las cuentas nuevas nacen con él (se extiende `handle_new_user`, que YA corría ahí y ya leía `new.email` — se le agrega una columna al insert, con la misma firma, sin sobrecarga viva) y las existentes lo sincronizan solas al entrar, porque el front llama a `sync_my_email()` al abrir sesión. Esa función es SECURITY DEFINER pero no recibe id: sólo puede tocar la fila del que llama. Al final del archivo queda la sentencia de resincronización manual, idempotente, para el caso de un correo cambiado desde el dashboard. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0096 | `consola_de_accesos.sql` | **La consola de accesos** (`docs/plan-ajustes-funcional.md`, PR-2). Dos vistas y un RPC; es la superficie más delicada de la app. Las vistas van con **`security_invoker`** — sin eso una vista corre con los permisos del dueño y saltea la RLS, o sea cualquier autenticado leería el equipo entero; con él, la policy de la 0006 hace el trabajo sola y quien no es gerencia ve UNA fila (la propia) porque la RLS lo filtra, no porque la vista se lo pida. `v_team_access` devuelve los accesos ya agrupados en un `jsonb` (el front pinta una tarjeta por persona; agrupar en la vista hace imposible que alguien aparezca dos veces). `v_access_audit` lee `audit_log` —que ya se llena desde la 0003 con `trg_audit_module_roles`, así que el histórico existe desde el día uno— filtrando **primero** por `entity_type` (columna indexada en la 0005): audit_log crece sin techo y filtrar por el jsonb sin acotar antes sería un scan que empeora cada mes. Ojo que `entity_id` es el id de la FILA de rol y no el de la persona: a quién le cambiaron el acceso sale del payload, del `after` o del `before` según la acción (un DELETE no tiene after). `set_module_access` es SECURITY DEFINER y por eso chequea `has_module(gerencia)` **a mano y como primera verificación real** — la RLS no protege un SECURITY DEFINER, y sin eso cualquiera podría darse todos los módulos con una llamada a PostgREST. Lleva compare-and-swap contra `p_expected_role` (con `is distinct from`, porque los dos lados pueden ser null y con `<>` el guard no dispararía nunca) y dos guardas que impiden dejar al centro sin administración: no podés quitártela a vos mismo, y no se puede quitar la última. Se compara contra `is null` y no contra el nivel, porque `has_module` mira si EXISTE la fila sin importar el nivel. No escribe auditoría a mano: la escribe el trigger, sellando el actor con `auth.uid()`, que dentro de un SECURITY DEFINER sigue siendo la persona real porque sale del JWT. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0097 | `formato_fecha_con_mes.sql` | **Un tercer formato de fecha: "31 Ago 2026"** (pedido del Director, 2026-08-25). Ensancha el check de `user_preferences.date_format` para que acepte `dmesy` —día, mes abreviado en letras, año— junto a `dmy` e `iso`. Que agregar una opción a un desplegable sea un cambio de schema es el costo que la **0093** aceptó a propósito al tipar cada preferencia con su check en vez de meterlas en un `jsonb`: la base es la que hace cumplir el conjunto de valores válidos. **Aditiva → va ANTES del front**: ninguna fila existente deja de ser válida y ningún front desplegado escribe el valor nuevo; al revés, quien eligiera el formato nuevo se comería un `23514` al guardar, en pantalla y en el mismo momento de elegirlo (en Preferencias cada control guarda solo). El valor se llama `dmesy` y no `dmmy` porque, pegado a `dmy` dentro de un check, una letra de diferencia es una trampa para el que después escriba SQL a mano. De paso el constraint deja de llamarse como lo bautizó Postgres y pasa a tener nombre propio (`user_preferences_date_format_valido`); el barrido que lo reemplaza borra **cualquier** check de la tabla que mencione la columna, porque confiar en el nombre generado podría dejar vivo el viejo y la migración terminaría "con éxito" con el valor nuevo rebotando igual. **Aplicada en prod (2026-08-25).** |
| 0098 | `baja_y_actividad_de_cuentas.sql` | **Baja de cuentas y resumen de actividad** (`docs/plan-alta-de-cuentas.md`, PR-1). Dos funciones para la parte que vive en la base; el alta y el link de restablecimiento necesitan la Admin API de Auth y van por Edge Function en el PR-2. `user_activity_summary` responde de una pasada "¿se puede borrar esta cuenta?" y "¿por qué no?": **recorre `pg_constraint`** en vez de enumerar tablas a mano, porque con 38 claves foráneas apuntando a `users` una lista escrita a mano nace incompleta y envejece peor — la tabla 39 la agrega otra migración y nadie vuelve acá. Sólo cuenta las que BLOQUEAN (`confdeltype` 'a' y 'r'); las que cascadean o ponen null no impiden el borrado. Es SECURITY DEFINER porque tiene que contar en tablas que quien pregunta podría no poder leer por RLS, y devuelve **cantidades, nunca datos**. `dar_de_baja` revoca todos los módulos —que es lo que corta la RLS de verdad— y apaga `is_active`, que **hoy no bloquea nada** (`has_module` sólo mira si hay fila en `user_module_roles`, y el login de Auth ni pasa por la RLS): el corte del ingreso es el ban en Auth y lo hace la Edge Function DESPUÉS, para que si el ban falla la persona pueda entrar pero no vea nada. Las dos guardas son las de la 0096 (no a vos mismo, no la última gerencia), que acá pesan más porque una baja se lleva todos los módulos de una. **La auditoría de la baja se escribe a mano y es la excepción**: `public.users` no tiene trigger de auditoría, así que el `is_active` se apagaría sin rastro; no se le agrega el trigger genérico a propósito, porque auditaría también cada cambio de nombre y la sincronización de correo de cada login (0095). La fila nueva **todavía no se ve en ninguna pantalla**: `v_access_audit` filtra por `user_module_roles` y ensancharla acá la volvería breaking para el front desplegado (el caso 0068/0092) — el ensanche va en el PR-3 con el front que lo sabe leer. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0099 | `auditoria_de_altas_y_bajas.sql` | **La huella del alta y de la eliminación de una cuenta** (`docs/plan-alta-de-cuentas.md`, PR-2). Acompaña a la Edge Function `admin-usuarios`. La 0098 resolvió el rastro de la BAJA, pero quedaban dos huecos del mismo origen —`public.users` no tiene trigger de auditoría—: crear una cuenta y eliminarla tampoco dejaban huella. El alta se deducía a medias (asignarle el primer módulo sí queda registrado por `trg_audit_module_roles`), pero una cuenta creada y nunca habilitada no aparecía en ningún lado, y una eliminada desaparecía sin constancia de haber existido. **No lo escribe la Edge Function a propósito**: corre con la clave de servicio y ahí `auth.uid()` es NULL, así que la línea quedaría como "acción del sistema" y se perdería el actor, que es el dato entero — la llama el FRONT con su propia sesión y el actor se sella con el default de la columna. **Tampoco se abre una policy de INSERT sobre `audit_log`**, que convertiría el log en escribible por cualquier autenticado: acá la acción sale de un conjunto CERRADO de dos valores y el resto de los campos los pone la función. No valida que la cuenta exista, y es deliberado: en `ELIMINACION` ya no existe, y `entity_id` no es FK, así que apuntar a un id que ya no está es la única constancia de que esa cuenta estuvo. Aditiva → va ANTES del front. **Aplicada en prod (2026-08-25).** |
| 0100 | `historial_con_eventos_de_cuenta.sql` | **El historial también cuenta el alta, la baja y la eliminación** (`docs/plan-alta-de-cuentas.md`, PR-3). Reescribe `v_access_audit` (0096) para que además de los cambios de módulo muestre los eventos de cuenta que escriben la 0098 y la 0099. Tres cambios: el objetivo sale de `entity_id` cuando `entity_type = 'users'` (la entidad ES la persona) y del payload cuando es un rol; los eventos de cuenta van con `module` y los dos niveles en **null**, y el front los redacta por su `action`; y `target_name` cae a `before_data ->> 'full_name'` cuando el join no puede devolver nada — para una ELIMINACION la fila de `users` ya no existe, y sin ese respaldo el historial diría "una cuenta que ya no existe" justo en la única línea que prueba que existió. ⚠️ **ÉSTA VA DESPUÉS DEL DEPLOY, es la única de la serie que va al revés**: el front desplegado no sabe redactar filas sin módulo y `auditLine` caería en su rama de UPDATE escribiendo "volvió a guardar el acceso a un módulo, sin cambiar el nivel (—)" — o sea, aplicarla antes no rompe la pantalla, la deja MINTIENDO dentro del registro de auditoría. Es el caso 0068/0092 por tercera vez, y por eso el orden se avisó en el chat y no sólo dentro del `.sql`. **Aplicada en prod (2026-08-25).** |
| 0101 | `actor_en_los_eventos_de_cuenta.sql` | **El alta, la baja y la eliminación perdían al actor** (encontrado en el QA del 2026-08-25). `dar_de_baja` (0098) y `registrar_evento_de_cuenta` (0099) escribían en `audit_log` sin nombrar `actor_id`, confiando en un DEFAULT **que no existe**: en la 0002 `patient_timeline.actor_id` sí lleva `default auth.uid()` (línea 205) pero `audit_log.actor_id` no (línea 366) — dos columnas con el mismo nombre y distinto comportamiento, y se confundieron al escribir la 0098. Resultado: **todos** los eventos de cuenta quedaban con actor null y el historial los redactaba como "El sistema creó la cuenta de X", justo en el registro de quién dio de alta a quién. Se detectó comparando, en la misma sesión, una fila de `user_module_roles` (actor cargado, la escribe el trigger) contra una de `users` (actor null). La cura es la que `audit_row` (0003) usaba desde el día uno: resolver el actor a una variable —con el mismo `coalesce` contra la GUC `app.actor_id`, para que las dos rutas de escritura del log se comporten igual— y pasarlo explícito. **Las filas ya escritas no se tocan**: `audit_log` es inmutable, y reescribir el log para que diga algo más lindo sería peor que el bug. `create or replace` con la MISMA firma en las dos: no queda ninguna sobrecarga viva (la trampa de la 0091). |

Además, `seed_smoke_test.sql` (en `supabase/`, fuera de `migrations/`) carga datos demo
para validar el aislamiento de RLS. NO es para producción — ver instrucciones en su cabecera.
En `scripts/etapa0-preparacion.sql` hay un script idempotente para el SQL Editor que aplica
la 0012 y deja datos demo de Track (coordinaciones, esquema de visitas, plantilla global).
En `scripts/tablero-protocolo.sql` hay otro script idempotente que aplica 0016+0017+0018
(vistas ampliadas + columnas nuevas + RPC v2) para el tablero de protocolo y la ficha de paciente.

El orden respeta dependencias: enums → tablas → funciones → vistas → índices → RLS → realtime.

## Cómo aplicar

**Opción A — Supabase CLI (recomendada):**
```bash
supabase db push        # aplica las migraciones pendientes en orden
```

**Opción B — SQL Editor del dashboard:** pegar y ejecutar los archivos `0001` → `0007` en orden.

> Requiere correr como `postgres` (es el caso en el SQL Editor). Varias funciones son
> `SECURITY DEFINER` y dependen de que su owner sea `postgres` para bypassear RLS al
> generar filas del sistema (visitas, movimientos de stock, auditoría).

## Decisiones de diseño clave

1. **Timeline = log de eventos genérico.** `patient_timeline { actor, event_type, occurred_at, metadata }`. El orden vive en el log ("zona sin orden impuesto"); los hitos viejos (checkin/farmacia/coordinación/checkout) son `event_type`.
2. **Estado de visita = calculado al leer, NO almacenado.** Se deriva en `v_patient_visits` (réplica de `calcVisitState` de Spira Track). `patient_visits` no tiene columna `status`.
3. **Estado de dispensación = almacenado + realtime.** Lo actualiza la farmacéutica; se refleja en vivo vía Supabase Realtime.
4. **Stock por LOTE** (`medication_lots`), no por medicamento. `stock_movements` es inmutable (audit trail ANMAT).
5. **Aislamiento por protocolo en el lado Track** (vía `protocol_coordinators` / `auth.coordina_visita`). **Pharma es CENTRAL** (ve todos los protocolos) — decisión de negocio. Si se segrega por sponsor, crear `pharma_assignments`.
6. **Auditoría transversal** (`audit_log`) + anti-spoofing de actor + inmutabilidad de campos sensibles.

## Seguridad

El schema pasó dos rondas de revisión adversarial multi-agente. Reporte y estado en
[`schema-review.md`](./schema-review.md). Veredicto: **GO**.

## Próximos pasos (no incluidos acá)

- Seed inicial: usuarios demo + roles por módulo (`user_module_roles`).
- Migrar datos de los MVPs (localStorage de Track y Pharma).
- Edge Function de IVRS (PDF → Claude API) que pre-llena `dispensation_requests`.
- Verificar contra `App.jsx` de Track la elección de plantilla al materializar checklist.
